# MA-020 — Delete data layer: soft-delete an account, keep its history as Deleted Account
base: 93c0e34dc59be06345562f7ab3ce0396d476cc1d · verify: none · flags: money path · expected diff: ~150 lines

Invariants every step keeps: a deleted row has `is_deleted = 1`, `is_archived = 1` (untouched), `name = ''` (`name` is `NOT NULL`, `001_create_accounts.ts`); `current_balance` and `revolving_balance` of every account are never written by the delete; the label "Deleted Account" is chosen on `is_deleted`, never on the scrubbed name.

## Steps

### 1. Migration 019 adds `accounts.is_deleted`, and the real runner has a suite
- File: `src/database/migrations/019_add_account_is_deleted.ts` (new), `src/database/migrations/index.ts` (`MIGRATIONS`), `__tests__/schema.test.ts:70-100`
- Change: `ALTER TABLE accounts ADD COLUMN is_deleted INTEGER NOT NULL DEFAULT 0 CHECK(is_deleted IN (0, 1));`, shape of `017_add_account_balance_review.ts`; append `migration019` to `MIGRATIONS`. `schema.test.ts` column list gains `is_deleted` after `balance_review_required`, `toHaveLength(19)`.
- Test: `__tests__/database/migrations/019_add_account_is_deleted.test.ts`, written first. Two parts. (a) DDL, through `registerOpenDbsDrain` and `MIGRATIONS.filter(version <= 18)` as in the 017 suite: a pre-existing row reads `is_deleted = 0` after the migration, `UPDATE ... SET is_deleted = 2` throws, `opening_balance`/`current_balance`/`revolving_balance`/`is_archived` byte-equal before and after. (b) The runner, the first suite in the repo to drive `runMigrations(db)` from `@/database/client` (`grep -rln runMigrations __tests__/` returns only the mocked `use_layout_init.test.ts`): bridge `execAsync`, `runAsync`, `getAllAsync` through `getExpoSQLiteTestDatabase` + `getSQLiteParams` (`runMigrations` passes `runAsync` params variadically, `client.ts:33-37`; `getSQLiteParams` handles both shapes), map `withTransactionAsync` to real `BEGIN`/`COMMIT`/`ROLLBACK`; fresh db → `schema_migrations` has 19 rows, `PRAGMA table_info('accounts')` has `is_deleted`; second run → still 19 rows and `runAsync` not called again.

### 2. `Account.is_deleted` exists on the entity and every literal
- File: `src/modules/accounts/entities/account.entity.ts` (`Account`), `src/modules/accounts/repositories/account.repository.ts:19-22` (`NewAccountInput`), `:62-77` (`add`), `src/modules/accounts/database/accounts.ts:70-99` (`addAccount`), `src/test_helpers/transaction.ts:17-39` (`makeTestAccount`)
- Change: `is_deleted: 0 | 1` after `is_archived`; `NewAccountInput` omits it; `add()` sets `is_deleted: 0`; `addAccount` inserts the column. Test fixtures that spell an `Account` literal gain `is_deleted: 0`: `__tests__/account.store.test.ts:10`, `commitment.repository.test.ts:84`, `screens/transactions/transaction_row.helpers.test.ts`, `screens/transactions/detail/detail_helpers.test.ts`, `screens/dashboard/dashboard_presentation.test.ts`, `dashboard_helpers.test.ts`, `dashboard_hook.test.ts` (two), `account_carousel.helpers.test.ts`, `screens/commitments.hook.test.ts`, `screens/transactions/transaction_form/transaction_form_prerequisites.test.ts`, `transaction_form_host_state.test.ts`, `transaction_form_body.test.tsx`, `screens/transactions/transaction_row.test.tsx` (two), `screens/accounts/balance_review_alert.test.tsx`; `grep -rln "balance_review_required: [01]" __tests__/ src/test_helpers/` is the complete list, `npm run typecheck` confirms none is missed.
- Test: `__tests__/account.repository.test.ts` `AccountRepository.add`: the inserted row reads `is_deleted = 0`.

### 3. Deleted accounts leave both lists
- File: `src/modules/accounts/database/accounts.ts:7-18` (`getAccounts`, `getArchivedAccountCount`)
- Change: `getAccounts` filters `is_archived = 0 AND is_deleted = 0`; `getArchivedAccountCount` counts `is_archived = 1 AND is_deleted = 0`. `getAccountByIdIncludingArchived` and `getAccountsByIdsIncludingArchived` (`:20-40`) do not change: the lookup at `account.store.ts:67-84` must keep returning deleted rows for the label. There is no archived-list query at this checkout; the count is the archived list's only read, and MA-017's list joins this filter when it lands.
- Test: `__tests__/account.repository.test.ts`: a row inserted raw with `is_archived = 1, is_deleted = 1` appears in neither `getAll()` nor `countArchived()`, and `getByIdsIncludingArchived([id])` still returns it.

### 4. One resolver decides an account's display name
- File: `src/constants/strings.ts:840` (`Strings`), `src/utils/account_name.ts` (new)
- Change: `deletedAccount: 'Deleted Account'` beside `unknownAccount`. `resolveAccountName(account: Account | undefined): string` returns `Strings.unknownAccount` for `undefined`, `Strings.deletedAccount` when `is_deleted === 1`, else `account.name`. Undefined keeps its current meaning (audit L27, a transient lookup miss).
- Test: `__tests__/account_name.test.ts`, written first: the three branches, and a deleted account whose `name` is `''` still reads `Strings.deletedAccount`.

### 5. Every place that prints a transaction's account name goes through the resolver
- File: `src/utils/format_transaction_title.ts:25-26`, `src/modules/transactions/screens/transactions/components/transaction_row.helpers.ts:71,76`, `src/modules/transactions/screens/transactions/detail/detail.helpers.ts:158-160` (`accountLabel`), `src/modules/transactions/screens/transactions/detail/components/transfer_flow_card.tsx:56,73` (`Cell`)
- Change: replace each `x?.name ?? Strings.unknownAccount` with `resolveAccountName(x)`; `detail.helpers.ts:159` also replaces the bare `toAccount.name`; `transfer_flow_card.tsx` prints `resolveAccountName(account)` in the cell text and in `Strings.detailOpenAccountAccessibility`. `accountTypeLabel`, `accountIcon` and `transferFlow`'s account objects keep reading the deleted row's `type`; `git grep -n "unknownAccount" src/` returns only `strings.ts` and `account_name.ts` when this step is done.
- Test: `__tests__/format_transaction_title.test.ts`: expense on a deleted account → subtitle `Strings.deletedAccount`; transfer between two deleted accounts → `Deleted Account → Deleted Account`. `__tests__/screens/transactions/transaction_row.helpers.test.ts`: same two cases on `contextFor` output, plus the card-credit branch on a deleted card. `__tests__/screens/transactions/detail/detail_helpers.test.ts`: `accountLabel` for a transfer with a deleted destination, and for a deleted source with a live destination. No render test for `transfer_flow_card.tsx` (`tests.md`: keep, don't add); its two lines are a call into the tested resolver.

### 6. A commitment query clears every reference to one account
- File: `src/modules/commitments/database/commitments.ts` (after `updateCommitment`, `:91-121`)
- Change: `clearCommitmentAccount(db, accountId, updatedAt): Promise<void>` runs `UPDATE commitments SET account_id = NULL, updated_at = ? WHERE account_id = ?`. `commitment_payments` is not touched. Verb precedent: `clearAccountBalanceReview`, `accounts.ts:142`.
- Test: `__tests__/commitment.query.test.ts`, new `describe`: two commitments on the account and one on another; after the call the two read `account_id = NULL` with the given `updated_at`, the third is byte-identical; a `commitment_payments` row on the account keeps `account_id` and `transaction_id`.

### 7. `AccountRepository.delete` soft-deletes an archived account in one transaction
- File: `src/modules/accounts/repositories/account.errors.ts` (new), `src/modules/accounts/database/accounts.ts` (after `archiveAccount`, `:114-123`), `src/modules/accounts/repositories/account.repository.ts:29-39` (`IAccountRepository`), `:41-99` (`AccountRepository`), `__tests__/account.store.test.ts:46-59` (`makeRepo`)
- Change: `AccountNotFoundError` and `AccountNotArchivedError`, shape of `transaction.errors.ts:3-8`. `setAccountDeleted(db, id, updatedAt): Promise<number>` runs `UPDATE accounts SET is_deleted = 1, name = '', updated_at = ? WHERE id = ? AND is_archived = 1 AND is_deleted = 0` and returns `changes`; the predicate is the guard, so the write itself cannot land on a live or already-deleted row. `delete(id): Promise<void>` on the interface and class: load through `getAccountByIdIncludingArchived`; `undefined` or `is_deleted === 1` → `AccountNotFoundError`; `is_archived !== 1` → `AccountNotArchivedError`; then `db.withTransactionAsync` (precedent `transaction.repository.ts:363-367`) runs `setAccountDeleted` (`changes !== 1` → `AccountNotArchivedError`, which rolls back) then `clearCommitmentAccount` imported from `@/modules/commitments/database/commitments` (cross-module query import precedent: `commitment.repository.ts:211` imports `getAccountByIdIncludingArchived`; `commitments.ts` imports only enums and its entity, no cycle). `applyAccountDelta`, `setAccountBalance` and `revolving_balance` are not called or written. `makeRepo` gains `delete: jest.fn()`. No store action (Out of scope).
- Test: `__tests__/account.repository.delete.test.ts`, written first, bridge of `transaction.repository.test.ts:56-79` with `realDb.pragma('foreign_keys = ON')` and `withTransactionAsync` mapped to real `BEGIN`/`COMMIT`/`ROLLBACK`. Seed: `target` (archived), `survivor` (live), `live_unarchived`; an expense on `target`; a transfer `target → survivor`; a transfer `survivor → target`; two commitments, one on `target`, one on `survivor`; one paid `commitment_payments` row on `target` referencing the expense. Cases: (a) `delete(live_unarchived)` rejects with `AccountNotArchivedError` and the row is unchanged; (b) unknown id → `AccountNotFoundError`; (c) a second `delete(target)` → `AccountNotFoundError`; (d) after `delete(target)`: `getAll()` and `countArchived()` exclude it, `getByIdsIncludingArchived([target])` returns it with `is_deleted = 1`, `is_archived = 1`, `name = ''`; (e) every transaction row's `id, amount, egp_amount, to_amount, account_id, to_account_id` is byte-identical before and after; (f) `survivor` and `live_unarchived` `current_balance`/`revolving_balance` unchanged; (g) the target commitment reads `account_id = NULL`, the survivor's is unchanged, the payment row keeps `account_id` and `transaction_id`; (h) `repo.add({ ...input, name: <old name> })` succeeds and `getAll()` lists exactly one row with that name; (i) atomicity: `jest.spyOn(commitmentsModule, 'clearCommitmentAccount').mockRejectedValueOnce(...)` → `delete` rejects, `target` still reads `is_deleted = 0` with its name; (j) `new TransactionRepository().delete(expenseId)` on the deleted account succeeds, the expense row is gone, `survivor` balance unchanged, `target.current_balance` absorbed the reversal.

### 8. Decision record
- File: `docs/adr/2026-09-08-account-soft-delete-keeps-history.md` (new), shape of `docs/adr/2026-09-01-empty-error-state-stay-separate.md` (Date, Status, Ticket, Applies to, then numbered sections)
- Change: records the four rulings and their reasons: soft delete over hard delete (five `ON DELETE`-less foreign keys, `004:11-12`, `006:22`, `007:21-22`, and the inner joins at `transactions.ts:51,321`, `budget_stats.ts:20,76`, `dashboard_snapshot.ts:79`); balances never replayed (three writers of `current_balance`, none recomputes from history); name scrubbed to `''` and the label decided on `is_deleted`; commitments `SET NULL` app-side while payments keep their rows. Names the rejected alternative: `ON DELETE` clauses, deferred to backlog item 2.
- Test: none, a document.

## Non-goals
- The delete screen, warning, commitment replacement prompt, the store action and any UI wiring: MA-021 (#387).
- Archive gate, unarchive, an archived-accounts list query: MA-017 (#383). "Cannot be unarchived" is met at this checkout by the archived count excluding deleted rows; no unarchive function exists to guard.
- `ON DELETE` clauses on the five foreign keys; the categories equivalent.
- Changing `TransactionRepository.delete` or `update` (`transaction.repository.ts:342-435`): both keep loading accounts through `getAccountByIdIncludingArchived` and applying reversals to the hidden row.
- Transaction search (`transactions.ts:148-160`) matching the text "Deleted Account": the scrubbed name is `''`, so a search for the old name or the label finds nothing; no ticket asks otherwise.
- Opening a deleted account from the transfer card (`detail/index.tsx:73-74`) or editing a transaction on one; `commitments/detail/components/details_card.tsx:38` showing "None".
- `commitment.repository.ts:212` (`markAsPaid` refuses archived accounts) already refuses deleted ones since `is_archived` stays `1`; no change.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- Gate check (`review.md`): `npx jest __tests__/account.repository.delete.test.ts __tests__/database/migrations/019_add_account_is_deleted.test.ts` fails at base (files absent) and passes at head; `test -f` both paths before citing them.

## Risks
- Acceptance "no live account's balance changes when [a transaction on a deleted account] is deleted" is met for single-account transactions; a transfer between a deleted and a live account still reverses onto the live side, which is today's `TransactionRepository.delete` and which Context says stays unchanged. Case (j) tests the expense; if the reviewer reads the acceptance as covering transfers, that is MA-021's rule, not a data-layer change here.
- Step 2's fixture sweep: a missed `Account` literal fails `typecheck`, never a test; run it after the sweep.
- Step 1(b) is the repo's first real-runner suite; if `runMigrations`'s variadic `runAsync` call does not round-trip through `getSQLiteParams`, fix the bridge in the test, not `client.ts`.
- `name = ''` in `add_account.schema.ts:34` and `account_detail.hook.ts:77` compares against `accounts` from `getAll()`, which excludes deleted rows, so an empty-string name never reaches the duplicate check; if a later list includes deleted rows, this assumption breaks.
- PR #407 merged; this checkout already carries it. No rebase needed at base `93c0e34d`.

## Self-assessment
Step 7's case (i) is the one I am least sure of: the atomicity proof spies on `clearCommitmentAccount` through the module namespace, which works for the `getTransactions` spy in `transaction.repository.test.ts` because the repository imports the function by name from the same module object; if `account.repository.ts` imports it the same way the spy lands, but if the implementer inlines the SQL into the accounts query file instead of importing from `commitments.ts`, the spy has nothing to hit and the test must fail the second statement another way (a `runAsync` mock that rejects on the `UPDATE commitments` text). Everything else in the plan is an additive column, three filters and one resolver over code I opened at this checkout.
