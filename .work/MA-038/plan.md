# MA-038 — Delete: the warning, the busy and failure states, the toast and the return
base: 82c465d637d387852132838278bba3b6a69592e6 · verify: emulator · flags: user copy · expected diff: ~230 lines

Context at HEAD, where it differs from the ticket: `IAccountRepository.delete(id)` ships (`src/modules/accounts/repositories/account.repository.ts:123-134`, MA-020); the transaction count on either leg ships (`getTransactionCountByAccount`, `src/modules/transactions/database/transactions.ts:267-276`); the archived detail with Unarchive ships and reads both counts into a slot (`archived_account_detail.repository.ts:32-36`, `archived_detail_body.tsx`); `ConfirmDialog` already takes `busy` and `errorMessage` (`src/components/ui/confirm_dialog.tsx:17-19`) and the danger confirm is flat at `Radius.cta` (`:78-85`). Hardware back is already handled: HeroUI's `Dialog.Content` primitive registers `hardwareBackPress` while mounted and calls `onOpenChange(false)`, returning true (`node_modules/heroui-native/lib/module/primitives/dialog/dialog.js:147-155`), and the portal mounts it only while open (`:86-89`); `ConfirmDialog.handleOpenChange` drops that call while `busy` (`confirm_dialog.tsx:37-39`). No `BackHandler` is added here. What is new: the by-account commitment rows (the single-commitment sentence needs a name, the count cannot supply it), the ghost danger tone, the busy label on the confirm, the store action, the detail state, the hook flow, the dialog and the wiring.

## Steps

### 1. The copy exists, one string shape per count case
- File: `src/constants/strings.ts` (Account Detail block, after `accountDetailUnarchive` at `:305`)
- Change: add `accountDetailDelete: 'Delete account'`, `accountDetailDeleteTitle: (name) => \`Delete ${name}?\``, `accountDetailDeleteTransactionsNone: 'Nothing is recorded on this account.'`, `accountDetailDeleteTransactionsOne: 'Its 1 transaction stays, labelled Deleted Account.'`, `accountDetailDeleteTransactionsMany: (count: string) => \`Its ${count} transactions stay, labelled Deleted Account.\``, `accountDetailDeleteCommitmentOne: (name) => \`${name} will need another account.\``, `accountDetailDeleteCommitmentsMany: (count: string) => \`${count} commitments will need another account.\``, `accountDetailDeleteClose: 'Keeping it archived keeps its name on all of them.'`, `accountDetailDeleteKeep: 'Keep archived'`, `accountDetailDeleteConfirm: 'Delete'`, `accountDetailDeleting: 'Deleting…'`, `accountDetailDeleteError: "Couldn't delete this account. Nothing was changed."`, `accountDetailDeleted: (name) => \`${name} deleted.\``. Verbatim from Acceptance; the count parameters take an already formatted string.
- Test: `__tests__/screens/accounts/account_detail_delete.strings.test.ts`, written first, in the shape of `account_detail_archive.strings.test.ts`: every key above against its Acceptance literal, byte-exact, the functions called with `'CIB'`, `'Gym'`, `'1,204'` and `'2'` as the shapes need; the title with `Strings.unnamedAccount`; the failure line `not.toMatch(/sorry|apolog/i)`. Step 3 then asserts composition through the same functions.

### 2. The archived slot carries the active commitments, not only their number
- File: `src/modules/commitments/database/commitments.ts` (`getActiveCommitmentCountByAccount`, `:134-143`); `src/modules/accounts/repositories/archived_account_detail.repository.ts` (`ArchivedAccountDetailSnapshot`, `:8-14`; `getSnapshot`, `:32-43`); `src/modules/accounts/screens/accounts/detail/account_detail.hook.ts` (`archived` memo, `:114-124`)
- Change: replace the count query with `getActiveCommitmentsByAccount(db, accountId): Promise<AccountCommitmentRef[]>`, `export type AccountCommitmentRef = Pick<Commitment, 'id' | 'name'>`, `SELECT id, name FROM commitments WHERE account_id = ? AND is_active = 1 ORDER BY name COLLATE NOCASE, id` (same predicate as today, index-friendly). The snapshot field `activeCommitmentCount: number` becomes `activeCommitments: AccountCommitmentRef[]`; the repository reads the rows in the same `Promise.all`. The hook's `archived` object keeps `activeCommitmentCount` as `activeCommitments.length` and adds `activeCommitments`, so `ArchivedDetailBody`, `buildArchivedAccountFacts` and `index.tsx` are untouched. Delete the count query; no other caller (`grep -rn getActiveCommitmentCountByAccount src __tests__` lists the repository and its test only).
- Test: `__tests__/archived_account_detail.repository.test.ts`, replace the `getActiveCommitmentCountByAccount` describe with `getActiveCommitmentsByAccount` rows on the existing seed: `ARCH` → `[{ id: 'com-arch-on', name: 'Gym' }]` (the inactive `com-arch-off` absent), `LIVE` → `[{ id: 'com-live-on', name: 'Netflix' }]`, `ARCH_EMPTY` → `[]`, `UNKNOWN` → `[]`; the `getSnapshot` and store cases assert `activeCommitments` instead of the count, the `toEqual` at `:140-146` included. `__tests__/screens/accounts/account_detail.hook.test.ts`: `archivedSnapshot()` (`:606-616`) carries `activeCommitments: [{ id: 'com-1', name: 'Gym' }]`; the expectation at `:626-630` adds `activeCommitments` and keeps `activeCommitmentCount: 1`. `__tests__/screens/accounts/archived_account_detail.store.test.ts`: `snapshot()` (`:26-28`) builds `activeCommitments: []` in place of `activeCommitmentCount: 0`, or `npm run typecheck` fails after this step. `account_facts.helpers.test.ts` is untouched: `ArchivedAccountCounts` keeps its count.

### 3. The warning body resolves from the two counts
- File: `src/modules/accounts/screens/accounts/detail/components/delete_confirmation.helpers.ts` (new, beside `archive_confirmation.helpers.ts`); `src/modules/accounts/screens/accounts/detail/components/account_facts.helpers.ts` (`COUNT_DISPLAY_DECIMALS`, `:20`, gains `export`)
- Change: `resolveDeleteWarningBody({ transactionCount, activeCommitments }): string` returns the opening sentence (none / one / many, the many count through `formatAmount(count, COUNT_DISPLAY_DECIMALS)` so 1,204 groups like the fact row does), then the commitment sentence when `activeCommitments.length > 0` (`activeCommitments[0].name` for one, the formatted count for several), then the closing sentence; sentences joined by a single space. Pure, no React.
- Test: `__tests__/screens/accounts/delete_confirmation.helpers.test.ts` (beside `archive_confirmation.helpers.test.ts`), written first: rows for (0 tx, no commitments) → none + close only; (1 tx, none) → singular; (2 tx, none) → plural; (3 tx, one `Gym`) → "Gym will need another account." in the middle; (3 tx, two) → "2 commitments will need another account."; (1204 tx, none) → "Its 1,204 transactions stay, …". Assert the whole string against the `Strings` functions, so a copy edit in step 1 moves the test with it.

### 4. The shared button paints a transparent danger label at `Radius.cta`
- File: `src/components/ui/button.content.ts` (`resolveFlatButtonStyle`, `:17-39`); `src/components/ui/button.tsx` (`ButtonProps` union, `:35-39`)
- Change: add a `variant === 'ghost'` branch ahead of the shared `variant !== 'secondary'` return at `button.content.ts:27` (that return stays as it is for primary and danger), returning `{ style: { borderRadius: Radius.cta }, labelClass: tone === 'danger' ? 'text-danger' : undefined }`; an undefined `labelClass` keeps the existing ghost row's `toEqual` green (`button_content.test.ts:121-125`). The union gains `{ variant: 'ghost'; flat: true; tone?: 'danger' }`. Ghost without a tone stays byte-identical (HeroUI's ghost label is already the foreground, `node_modules/heroui-native/src/styles/components/button.css:116-118`), so `confirm_sheet.tsx:107` and `adjust_balance_sheet.tsx:144` do not change; the icon colour branch at `button.tsx:104-109` already maps `danger` to `SemanticTokens.negative`.
- Test: `__tests__/components/ui/button_content.test.ts`, one `flatRows` entry `{ variant: 'ghost', flat: true, tone: 'danger' }` → `{ style: { borderRadius: Radius.cta }, labelClass: 'text-danger' }`; the existing ghost row without a tone keeps its resolved value, which is the "no shipped screen changes shape" gate.

### 5. The confirm button says "Deleting…" while busy, and the dismiss gate is a tested function
- File: `src/components/ui/confirm_dialog.tsx` (`ConfirmDialogProps`, `:8-22`; `handleOpenChange`, `:37-39`; confirm `Button`, `:78-85`); `src/components/ui/confirm_dialog.helpers.ts` (new)
- Change: add `confirmLoadingLabel?: string`, passed as `loadingLabel` to the confirm `Button` (`resolveButtonContent` already prefers it over `Strings.loading`, `button.content.ts:65`). Move the gate into `shouldDismissOnOpenChange(open: boolean, busy: boolean): boolean` (true only for `!open && !busy`) and call it from `handleOpenChange`; a one-line comment on the helper names the callers, the overlay press and the HeroUI primitive's hardware-back listener. Cancel keeps `isDisabled={busy}`; the overlay keeps `isCloseOnPress={!busy}`; swipe stays off.
- Test: `__tests__/components/ui/confirm_dialog.helpers.test.ts`, written first: `(false, true)` → false, `(false, false)` → true, `(true, false)` → false, `(true, true)` → false. This is the "back handler while busy" gate the ticket asks for; the primitive's listener and the overlay both land on it. Plus one assertion in `__tests__/components/ui/confirm_surfaces_shape.test.ts` (the settled source-text suite for this file, `:38-46`, the `{errorMessage}` precedent): the dialog source contains `shouldDismissOnOpenChange(`, so an inlined `if (!open) onCancel()` that bypasses the gate fails. No render test.

### 6. The detail state holds the delete dialog, its busy flag and its failure
- File: `src/modules/accounts/screens/accounts/detail/account_detail.state.ts` (`AccountDetailStateShape`, `:5-17`; setters `:19-32`; `INITIAL_STATE` `:34-46`)
- Change: add `isDeleteVisible: boolean`, `isDeleting: boolean`, `deleteError: string | undefined` with `setDeleteVisible`, `setDeleting`, `setDeleteError`; `reset` covers them through `INITIAL_STATE`.
- Test: `__tests__/account_detail.state.test.ts`: three setter cases in the shape of `setUnarchiving toggles` and `sets and clears the unarchive error` (`:88-108`); the initial-state and reset cases (`:8-19`, `:110`) assert the three new fields.

### 7. The store deletes, reloads, and hands the deleted row to the lookup
- File: `src/modules/accounts/store/account.store.ts` (`AccountStore`, `:31-41`; after `unarchiveAccount`, `:142-153`)
- Change: `deleteAccount(id)`: `await repo.delete(id)` inside the try that logs and rethrows; then `await get().loadAccounts().catch(() => undefined)` (the row is gone, a failed reload publishes `loadError`, as archive and unarchive do); then drop `id` from `accountLookupById` with a functional `set` (a cached copy carries the old name and `is_deleted: 0`, and `findMissingAccountIds` would treat it as known, `account_lookup.helpers.ts:26-31`); then `await get().loadAccountLookup([id]).catch(() => undefined)`. The lookup call is what makes Acceptance line 22 true without a refetch: after the reload and the drop neither list nor the cache holds the id, so `loadAccountLookup` fetches it through `getAccountsByIdsIncludingArchived` (no `is_deleted` filter, `accounts.ts:31-43`) and the transactions screen's `mergeAccountsById` map (`transactions.hook.ts:313`) resolves the row with `is_deleted: 1`, which `resolveAccountName` renders as "Deleted Account" (`src/utils/account_name.ts:7`).
- Test: `__tests__/account.store.test.ts`, a `describe('accountStore.deleteAccount')` in the shape of the archive block (`:522-570`): delegates to `repo.delete`; reloads both lists once; propagates a `repo.delete` rejection and calls neither `getAll` nor `getByIdsIncludingArchived`; resolves with `loadError` true when only the reload fails; and, with `getByIdsIncludingArchived` resolving `[{ ...mockAccount, is_deleted: 1, name: '' }]` and both lists resolving `[]`, publishes `accountLookupById['test-id'].is_deleted === 1`; the same with the id pre-seeded in `accountLookupById` (a prior `loadAccountLookup(['test-id'])` resolving `[mockAccount]`) still calls `getByIdsIncludingArchived(['test-id'])` a second time and ends with `is_deleted === 1`, not the cached row.

### 8. The hook runs the delete: name read first, busy while it runs, failure keeps the dialog, success pops and toasts
- File: `src/modules/accounts/screens/accounts/detail/account_detail.hook.ts` (store reads `:39-44`; state selector `:45-83`; after `handleUnarchive`, `:265-294`; return `:356-390`)
- Change: read `deleteAccount` from the account store and the three new fields and setters from the detail state. `handleDelete`: return when `!archived` or `useAccountDetailState.getState().isDeleting`; `const name = resolveAccountName(archived.account)` before any await (MA-020 scrubs the name inside the write); `setDeleteError(undefined)`, `setDeleting(true)`; `await deleteAccount(id)` in a try whose catch logs, sets `Strings.accountDetailDeleteError` and returns, with `setDeleting(false)` in `finally`; outside the try, `setDeleteVisible(false)`, `router.back()`, then `toast.show({ label: Strings.accountDetailDeleted(name), variant: 'success' })`. The order matters: the pop before the toast, so the toast lands over the screen the detail came from, and nothing after the write can be read as a failed write (the archive handler's shape, `:241-257`). `closeDelete` mirrors `closeArchive` (`:260-263`). Expose `isDeleteVisible`, `isDeleting`, `deleteError` in `state` and `setDeleteVisible`, `closeDelete`, `handleDelete` as actions. No route is forced; a failed reload after the write shows on the list through its own `loadError` content (`accounts_list.hook.ts:162`).
- Test: `__tests__/screens/accounts/account_detail.hook.test.ts`, written first, in the "an id outside the active list" block: `mockAccounts` gains `deleteAccount: mockDeleteAccount`; `DetailStateMock`/`createDetailStore` gain the three fields and setters. Cases: success with `archivedSnapshot()` → `mockDeleteAccount('acc-1')`, `setDeleteError` called only with `undefined`, `setDeleting` first `true` then last `false`, `setDeleteVisible(false)`, `mockBack` once, `toast.show({ label: 'CIB deleted.', variant: 'success' })` after `mockBack` by `invocationCallOrder`; the name survives the scrub, `mockDeleteAccount` mutates the mocked slot's account to `{ name: '', is_deleted: 1 }` and the toast still reads `'CIB deleted.'`; a blank-named account toasts `${Strings.unnamedAccount} deleted.`; rejection → `setDeleteError` last called with `Strings.accountDetailDeleteError`, `setDeleteVisible` never called with `false`, no `mockBack`, no toast, `setDeleting` last `false`; a second call while `isDeleting: true` writes nothing; no archived row (`mockSlot(undefined, 'idle')`) writes nothing; `closeDelete` hides and clears the error without a write.

### 9. The archived detail shows Delete account and the dialog
- File: `src/modules/accounts/screens/accounts/detail/components/delete_confirmation_dialog.tsx` (new, beside `archive_confirmation_dialog.tsx`); `src/modules/accounts/screens/accounts/detail/components/archived_detail_body.tsx` (props `:18-25`, button `:67-79`); `src/modules/accounts/screens/accounts/detail/index.tsx` (archived branch `:108-124`, hook destructure `:37-71`)
- Change: `DeleteConfirmationDialog({ visible, account, transactionCount, activeCommitments, onClose, onConfirm, isLoading, errorMessage })` renders `ConfirmDialog` with `destructive`, `busy={isLoading}`, `title={Strings.accountDetailDeleteTitle(resolveAccountName(account))}`, `body={resolveDeleteWarningBody(...)}`, `confirmLabel={Strings.accountDetailDeleteConfirm}`, `confirmLoadingLabel={Strings.accountDetailDeleting}`, `cancelLabel={Strings.accountDetailDeleteKeep}`, `errorMessage` through the shipped prop (it renders under the body, `confirm_dialog.tsx:59-66`); no children. `ArchivedDetailBody` takes `onDelete: () => void` and renders, under the Unarchive `Box`, a `Button variant="ghost" flat tone="danger" icon="trash-can-outline" label={Strings.accountDetailDelete}` in its own `mx-4 mt-2` Box, before `FormErrorText`. `index.tsx` passes `onDelete={() => setDeleteVisible(true)}` and mounts `DeleteConfirmationDialog` inside the `archived ?` branch with the slot's account, counts and the state fields. The active branch (`:131-287`) does not change; an active account never sees Delete because the dialog and the button live only in the archived body.
- Test: none; render layer, forbidden by `.claude/rules/tests.md`. Steps 3, 5, 6 and 8 hold every branch this file only wires.

## Screens
Seed: one archived account with ≥2 transactions on either leg and exactly one active commitment paid from it; a second archived account with nothing on it is not needed (its copy is step 3's test). `mqa claim` first; `mqa needs-build` says JS-only.
- Archived detail (C4): Delete account under Unarchive, transparent, danger label and glyph, 48 high at `Radius.cta` (HeroUI md, Rule 33). Unarchive is the compact accent arm at `Size.compactCtaTrack` = 36 (`button.content.ts:30`, `theme.ts:147`), so Delete reads 12 taller; that is the design, not a divergence.
- Delete dialog (E1): the filled body with the commitment sentence, Keep archived default fill left, Delete danger fill right; one shot for layout and wrap, the copy per count is step 3's test.
- Delete dialog busy (E3): spinner and "Deleting…", Keep archived dimmed; force with a temporary `await new Promise((r) => setTimeout(r, 4000))` at the top of `AccountRepository.delete`, reverted before commit.
- Delete dialog failure (F5): the danger caption under the body, both buttons live; force with a temporary `throw new Error('forced')` at the top of `AccountRepository.delete`, reverted before commit.
- Accounts list after a real delete (F4): the archived card without the account and the toast "<name> deleted."

## Non-goals
- The replacement-account sheet, the move, and the toast naming the new account (MA-039). The by-account rows query from step 2 is the read MA-039 reuses.
- The dashboard: an archived account is already outside the carousel and net worth (`dashboard.helpers.ts:28,132,174,283`), so a deleted one, which stays archived, needs no change; Acceptance line 23 holds at base and gets no test.
- The list: the no-accounts state falls out of `resolveAccountsListEmptyState` (`accounts_list.presentation.ts:37`) once the reload drops the archived count; no change, no new test.
- A `BackHandler` in `ConfirmDialog` or the sheet. The HeroUI primitive owns hardware back for the open dialog; step 5's helper is the gate it lands on.
- The active detail's action row (MA-035); Delete on an active account in any form.
- The archive dialog and the categories delete dialog keep their behaviour; step 5 changes nothing they render.
- The design canvas's older body copy ("Its 42 transactions and 1 commitment break without it. Keeping it archived preserves your history.") is superseded by Acceptance, which the Rules make verbatim.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- Every jest path above exists after its step; `test -f` each before citing it in the PR.

## Risks
- MA-035 (#424) touches `screens/accounts/detail/index.tsx`; step 9 edits only the archived branch, so a rebase is a merge of neighbours, not a conflict of lines.
- The ghost `tone` widening in `button.tsx` is a type-union change; if `oxlint-tsgolint` or the `FlatButtonTone` import elsewhere narrows `tone` to `secondary`, step 4 needs the union split into two members rather than one widened one.
- The hardware-back behaviour rests on `heroui-native@1.0.8`'s primitive (`primitives/dialog/dialog.js:147-155`); a HeroUI bump that drops that listener reopens Acceptance line 20 and would need a `BackHandler` in `ConfirmDialog` then, not now.
- Step 7's `loadAccountLookup([id])` after the reload is the only path that repaints an already-mounted transactions list; if that list is refreshed on focus anyway (`transactions.hook.ts:275-284`), the call is redundant but harmless, one query of one row.
- Amended after review round 2: step 5 adds a source-text assertion in `confirm_surfaces_shape.test.ts` that `handleOpenChange` calls the helper, step 4 names a new `variant === 'ghost'` branch ahead of the shared return, step 7's anchor corrected to `unarchiveAccount` at `:142-153`.
- Amended after review round 1: step 2 adds the store test's snapshot builder (typecheck), step 1 gains a byte-exact strings test, step 3's test moves beside its sibling, step 7 drops the cached row before the lookup with a pre-seeded case, and the C4 line expects 48 over Unarchive's 36.
- The toast after `router.back()` relies on the app-level `AppToastProvider` (`src/components/ui/toast.tsx:10`); the unarchive flow already toasts across a `dismissTo` on the same provider (`account_detail.hook.ts:285-286`).

## Self-assessment
Step 8's success order is the step I am least sure of: the hook test asserts `toast.show` after `router.back()` by invocation order, which the archive flow never needed and the unarchive flow does the other way round (toast, then `dismissTo`). If the pop unmounts the detail before the toast call runs, `useToast` from the app-level provider still shows it, but a reviewer may prefer the unarchive order for symmetry; either passes Acceptance, and the test should be written for whichever the implementer keeps rather than pinning both.
