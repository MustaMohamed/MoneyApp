# MA-028 — Detail: recent activity, month figures, empty, loading and failed states, See all
base: 13f5c7fc7daa9a7c9d5f6fbf721f88cba3bb2c4b · verify: emulator · flags: none · expected diff: ~540 lines

## Steps
### 1. The approved copy is in `Strings`, and the load-error family test knows the new pair
- File: `src/constants/strings.ts` (the `// Account Detail (U3)` block, `:282-303`); `__tests__/screens/load_error_copy.test.ts`
- Change: add `accountDetailMonthInLabel: 'This month in'`, `accountDetailMonthOutLabel: 'This month out'`, `accountActivityTitle: 'Recent activity'`, `accountActivitySeeAll: 'See all'`, `accountActivityEmptyTitle: 'Nothing recorded yet'`, `accountActivityEmptyBody: 'Transactions on this account show up here.'`, `accountActivityEmptyCta: 'Add a transaction'`, `accountActivityLoadError: "Couldn't load recent activity."`, `accountActivityLoadRetry: 'Try again'`, `accountActivityToday: (time: string) => \`Today, ${time}\``, `accountActivityYesterday: 'Yesterday'`. `Strings.dashSeeAll` (`:252`) and `Strings.todayLabel` (`:852`, upper-case) stay untouched. The `*LoadError` + `*LoadRetry` pair lands in the family `load_error_copy.test.ts` derives by suffix (`:6-13`, `:28-34`).
- Test: `__tests__/screens/load_error_copy.test.ts`: `accountActivityLoadError` joins the title list (`:47-63`, length 14) and `accountActivityLoadRetry` the retry list (`:78-88`, length 8). Fails at base, passes at head.

### 2. A bank's month figures are the dashboard card's rows, selected, with their colour
- File: `src/modules/dashboard/screens/dashboard/components/account_card.helpers.ts` (`buildInfoRows`, the `monthRows` literal at `:200-213`); `src/modules/accounts/screens/accounts/detail/components/account_facts.helpers.ts` (`AccountFact` `:9-12`, `buildAccountFacts` `:36`); `src/modules/transactions/screens/transactions/detail/components/detail_row.tsx` (`BaseProps` `:35-44`, `ItemTitle` `:84-90`)
- Change: lift the `monthRows` literal into an exported `buildMonthRows(stats: AccountStats, currency: Currency): [InfoRow, InfoRow]` that `buildInfoRows` calls in place; output byte-identical (the existing `account_card.helpers.test.ts` rows guard it). `AccountFact` gains `valueColor?: string`. New `buildMonthFacts(account: Account, stats: AccountStats | undefined): AccountFact[]`: `[]` unless `account.type === AccountType.Bank`; with `stats`, the two `buildMonthRows` rows mapped to `{ label: Strings.accountDetailMonthInLabel | accountDetailMonthOutLabel, value: row.value, valueColor: row.valueColor }`; without `stats`, both values `Strings.accountDetailFactUnset` and no colour. `buildAccountFacts` is unchanged (its 2-row invariant at `account_facts.helpers.test.ts:63` stands). `DetailRow` takes `valueColor?: string` and merges `{ color: valueColor }` into the `ItemTitle` style; nothing else on the row changes, so `DETAIL_ROW_HEIGHT` holds through the placeholder-to-figure swap.
- Test: `__tests__/screens/accounts/account_facts.helpers.test.ts`: bank + stats `{ month_in: 1250.75, month_out: 640.25, … }` EGP gives `'1,251 EGP'` positive-coloured and `'640 EGP'` negative-coloured (`Colors.dark.positive` / `Colors.dark.negative`); USD gives `'1,250.75 USD'`; zero figures take `Colors.dark.text1`; `stats` undefined gives two `'—'` rows with no `valueColor`; card, wallet, savings, smart wallet give `[]`. `__tests__/screens/dashboard/account_card.helpers.test.ts`: `buildMonthRows(stats, cur)` equals `buildInfoRows(...)` rows with kinds `monthIn`, `monthOut` for a bank.

### 3. One read returns the last ten rows and the month stats together
- File: new `src/modules/accounts/repositories/account_activity.repository.ts`
- Change: `RECENT_ACTIVITY_LIMIT = 10`; `interface AccountActivitySnapshot { accountId: string; rows: Transaction[]; stats: AccountStats; loadedAt: number }`; `interface AccountActivityLoadInput { accountId: string; mutationVersion: number; now: Date }` (`mutationVersion` is the store's freshness stamp, step 4; the repository does not read it); `interface IAccountActivityRepository { getSnapshot(input): Promise<AccountActivitySnapshot> }`; class `AccountActivityRepository` opens `getDb()` once and runs `getTransactionsByAccount(db, accountId, RECENT_ACTIVITY_LIMIT)` (`src/modules/transactions/database/transactions.ts:252`) and `getAccountsStats(db, [accountId], now)` (`src/modules/accounts/database/account_stats.ts:21`) under `Promise.all`, returning `stats[accountId]` and `loadedAt: input.now.getTime()`; export `accountActivityRepository`. No new SQL. Cross-module query import mirrors `dashboard.repository.ts:2-6`.
- Test: new `__tests__/account_activity.repository.test.ts`, better-sqlite3 bridge per `__tests__/transaction.repository.test.ts`: twelve transactions on `acc1` dated inside a fixed month plus one transfer whose `to_account_id` is `acc1`, `now` fixed at the 15th; `rows` has ten entries, newest first by `transaction_date DESC, transaction_time DESC, created_at DESC, id DESC`, and includes the transfer's to-leg; `stats.month_in` and `stats.month_out` equal the same `getAccountsStats` call's figures; `loadedAt` equals `now.getTime()`; an account with no rows returns `rows: []` and zero stats.

### 4. The activity has its own data store with the dashboard's ownership rules
- File: new `src/modules/accounts/screens/accounts/detail/account_activity.store.ts`
- Change: `export type AccountActivityStatus = 'idle' | 'initialLoading' | 'ready' | 'initialError'`; `createAccountActivityStore(repository: IAccountActivityRepository)`, shape `{ snapshot: AccountActivitySnapshot | undefined; status: AccountActivityStatus; requestedKey: string | undefined; requestGeneration: number }`, actions `ensure(input)`, `retry(input)`, `reset()`. Copy `dashboard.store.ts:43-143` without the `sameKeySnapshot` branch (`:59-67`, `:99`): the key `freshKeyOf(input) = \`${input.accountId}:${input.mutationVersion}\`` drives `freshKey`, `inFlight.key` and `requestedKey`; `ensure` returns early when `freshKey === freshKeyOf(input)` and `snapshot?.accountId === input.accountId`; every other start publishes `snapshot: undefined, status: 'initialLoading'` (`:68-74`); the generation is re-checked after the await before the `ready` publication; a failure sets `initialError`; `reset` bumps the generation so an in-flight result is dropped. Export `useAccountActivityStore = createAccountActivityStore(accountActivityRepository)`. No refresh in place: step 10 resets the store on unmount, and `router.navigate` into `(tabs)` pops the detail (`src/app/(app)/_layout.tsx:20`, a Stack with `(tabs)` at index 0 and `accounts/[id]` above it), so every re-entry is `initialLoading`; `mutationVersion` stays in the key (from `transaction.store.ts:124-127`) so a detail that stays mounted through a write reloads instead of serving the stale rows.
- Test: new `__tests__/screens/accounts/account_activity.store.test.ts`, injected repository with `deferred()` as `__tests__/dashboard.store.test.ts:9-21`: one publication carries rows and stats together (no intermediate state between `initialLoading` and `ready`); a slower first response never overwrites a newer stamp; `ensure` with the fresh stamp resolves without calling the repository; a bumped `mutationVersion` on the same account and a different `accountId` each call it under `initialLoading` with `snapshot` undefined; rejection sets `initialError`, `retry` then reaches `ready`; `reset` drops an in-flight result.

### 5. A row's time slot reads Today, Yesterday or `d Mon`
- File: new `src/modules/accounts/screens/accounts/detail/components/account_activity.helpers.ts`
- Change: `formatActivityDayLabel(transactionDate: string, transactionTime: string, now: Date): string`: equal to `now`'s local `YYYY-MM-DD` gives `Strings.accountActivityToday(formatTime12h(transactionTime))`; equal to the day before gives `Strings.accountActivityYesterday`; else `\`${day} ${MONTHS_SHORT[month - 1]}\`` with `MONTHS_SHORT` from `@/utils/year_month`. Date arithmetic on the `YYYY-MM-DD` parts, `now` an input, no `new Date()` inside. `buildActivityRowPresentation(input: TransactionRowPresentationInput, now: Date): TransactionRowPresentation` spreads `buildTransactionRowPresentation(input)` (`transaction_row.helpers.ts:125`) and replaces `timeText` only. `labelFor` in `group_transactions_by_date.ts:34` stays private and unused here.
- Test: new `__tests__/screens/accounts/account_activity.helpers.test.ts`: today 14:30 gives `'Today, 2:30 PM'`; yesterday across a month end (`now` 2026-09-01, date 2026-08-31) gives `'Yesterday'`; across a year end (`now` 2027-01-01, date 2026-12-31) gives `'Yesterday'`; a leap day (2028-02-29, `now` 2028-03-15) gives `'29 Feb'`; two days ago gives `'6 Sep'`; `buildActivityRowPresentation` equals `buildTransactionRowPresentation` on every field but `timeText`.

### 6. The shared pieces take the options the card needs, with every other render site unchanged
- File: `src/modules/transactions/screens/transactions/components/transaction_row.tsx` (`:36-203`); `transaction_rows_skeleton.tsx` (`ROWS` `:13`, header bar `:19-21`); `src/components/ui/load_error_alert.tsx` (`LoadErrorAlertCommonProps` `:9-15`, retry `Button` `:54-60`); `src/components/ui/section_header.tsx` (`:7-38`)
- Change: split `TransactionRowComponent` at the `PressableFeedback` (`:88`): an exported `TransactionRowBody({ presentation, category, onPress }: { presentation: TransactionRowPresentation; category?: Category; onPress: () => void })` owns `useRowPressScale`, the `Animated.View` at `TRANSACTION_ROW_HEIGHT` and everything inside it; `TransactionRow` keeps its props, builds the presentation, and wraps the body in `SwipeableRow` exactly as now (`__tests__/screens/transactions/transaction_row.test.tsx` and `transactions.screen.test.tsx` must stay green untouched). `TransactionRowsSkeleton` takes `rows?: number` (default 5) and `showDateHeader?: boolean` (default true); the row `testID`s and track heights do not move (`transaction_rows_skeleton.test.tsx:29-41` holds). `LoadErrorAlert` takes `flatRetry?: boolean` on the common props; when true the retry is `<Button variant="secondary" size="sm" flat …>` (the `{ variant: 'secondary'; flat: true }` arm of `ButtonProps`, `button.tsx:31-35`); the other 7 consumer files pass nothing and render as before. `SectionHeader` takes `action?: { label: string; onPress: () => void }`, rendered in the right slot as the `LinkButton` at `total_balance_strip.tsx:103-118` (size `sm`, `text-accent font-inter-semibold` label, `Type.caption` with `lineHeightFor`, `accessibilityRole="button"`); `count` and `action` are never passed together by any caller.
- Test: none. Render suites are frozen (`tests.md:19-21`); the row split, the skeleton props and the alert option have no logic to assert, and the existing `transaction_row.test.tsx` (4 cases) and `transaction_rows_skeleton.test.tsx` (2 cases) are the regression guard for the split and the defaults.

### 7. The activity card renders one of four bodies under one header
- File: new `src/modules/accounts/screens/accounts/detail/components/activity_empty_block.tsx`; new `src/modules/accounts/screens/accounts/detail/components/account_activity_card.tsx`
- Change: `ActivityEmptyBlock({ onAdd })`: a flat `View` (`px-4 py-5`, `bg-surface` inherited from the card) with `Strings.accountActivityEmptyTitle` (`font-sora-semibold`, `Type.meta`), `Strings.accountActivityEmptyBody` (`font-inter text-foreground/55`, `Type.overline`) and a `LinkButton` (`size="sm"`, label `Strings.accountActivityEmptyCta`, `text-accent font-inter-semibold`) calling `onAdd`; no icon, no `Button`, not an `EmptyState` variant (`empty_state.tsx` is icon + `h3` + flat `Button` at screen geometry, ADR 2026-09-01). `AccountActivityCard({ status, rows, onRowPress, onSeeAll, onAdd, onRetry })` with `rows: Array<{ id: string; presentation: TransactionRowPresentation; category?: Category }>` and `status: AccountActivityStatus`: `SectionHeader title={Strings.accountActivityTitle} action={{ label: Strings.accountActivitySeeAll, onPress: onSeeAll }}` above a `DetailRowsCard` (`detail_rows_card.tsx:8`, the same bordered flat container as the facts) whose body is, in order of precedence: `initialLoading` → `<TransactionRowsSkeleton rows={3} showDateHeader={false} />`; `initialError` → `<LoadErrorAlert mode="inline" flatRetry title={Strings.accountActivityLoadError} retryLabel={Strings.accountActivityLoadRetry} onRetry={onRetry} testID="account-activity-error" />`; zero rows → `ActivityEmptyBlock`; else one `TransactionRowBody` per row with `onPress={() => onRowPress(row.id)}`. The header renders in every status. No gradient, no shadow (`style={{ boxShadow: 'none' }}` comes with `DetailRowsCard`).
- Test: none, render layer; the body choice is `status` plus `rows.length`, both asserted at the store in step 4.

### 8. See all seeds the transactions screen in one publication
- File: `src/modules/transactions/screens/transactions/transactions.store.ts` (`:27-38`, `:52-86`)
- Change: action `seedAccountFilter(accountId: string, yearMonth: string)`: one `set({ searchQuery: '', activeFilter: 'all', period: { type: 'month', yearMonth }, appliedFilters: { ...EMPTY_FILTERS, accountIds: [accountId] } })`. Totals fields untouched; the screen's query effect (`transactions.hook.ts:151-157`) and totals effect key off these fields already. Nothing calls `reset`, so the seed persists until the user clears it, as any applied filter does.
- Test: `__tests__/screens/transactions/transactions_store.test.ts`: after `seedAccountFilter('acc1', '2026-09')`, `searchQuery` is `''`, `activeFilter` is `'all'`, `period` is `{ type: 'month', yearMonth: '2026-09' }`, `appliedFilters.accountIds` is `['acc1']`, `countActiveFilters(appliedFilters)` is 1, `formatAppliedFilterSummary(appliedFilters, accountsById, categoriesById)` is the account's name; a seed over a typed search and a `type` chip clears both; `totals` and `totalsYearMonth` are unchanged by the seed.

### 9. The add form can open with an account preselected, and the two existing openers still open it bare
- File: `src/modules/transactions/screens/transactions/transaction_form/add_transaction.store.ts` (`:7-41`); `transaction_form_host.state.ts` (`openAdd` `:37`, `:123-137`); `add_transaction.hook.ts` (`defaultValues` `:281-290`)
- Change: `AddTransactionStoreShape` gains `initialAccountId: string | undefined` (initial `undefined`) and `setInitialAccountId`; `reset` clears it. `openAdd(options?: { accountId?: string })`: after `resetFormSessions()` (`:124`) call `useAddTransactionStore.getState().setInitialAccountId(options?.accountId)`; the rest of the session state is unchanged. The add hook's `defaultValues.accountId` reads `useAddTransactionStore.getState().initialAccountId ?? ''` once at mount; `selectedAccount` (`:301-304`) and `semantics` (`:318-320`) already derive from the watched `accountId`, so the picker, the category type and the rate demand follow. `tabs.hook.ts:28` stays: `fab.tsx:131` calls `onAddTransaction()` and `:191` calls `item.onPress()`, both bare, so no event reaches `options`. `transactions.hook.ts:418-420` already calls `openAdd()` bare.
- Test: `__tests__/screens/transactions/transaction_form/transaction_form_host_state.test.ts`: `openAdd({ accountId: 'a1' })` sets `useAddTransactionStore.getState().initialAccountId` to `'a1'` and the session to `mode: 'add', phase: 'open'`; a following `openAdd()` clears it to `undefined`. `__tests__/screens/transactions/transaction_form/add_transaction.hook.test.ts`: after `openAdd({ accountId: 'a2' })`, `renderHook(useAddTransaction)` reports `state.accountId === 'a2'` and `state.selectedAccount?.id === 'a2'`; after `openAdd()`, `state.accountId === ''`. `__tests__/screens/transactions/transactions_hook.test.ts`: `openAddTransaction()` calls `mockOpenAdd` with no arguments.

### 10. The detail hook owns the load, the retry, and the three navigations
- File: `src/modules/accounts/screens/accounts/detail/account_detail.hook.ts` (`:1-12` imports, `:52-63` unmount and `beforeRemove`, `:122-132` `handleArchive`, `:157-178` return)
- Change: imports `useFocusEffect`, `useAccountActivityStore`, `useTransactionStore`, `useTransactionsScreenStore`, `useTransactionFormState`, `useCategoryStore`, `runAfterInteractions`, `currentYearMonth` (`@/utils/year_month`), `buildActivityRowPresentation`, `buildMonthFacts`. Focus loader, the `spending_plan_detail.hook.ts:64-74` shape: `useFocusEffect(useCallback(() => { const task = runAfterInteractions(() => { if (!useAccountStore.getState().accounts.some((a) => a.id === id)) return undefined; return ensure({ accountId: id, mutationVersion: useTransactionStore.getState().mutationVersion, now: new Date() }); }, { onError: (error) => console.error('[accountDetail] activity load failed:', error) }); return () => task.cancel(); }, [ensure, id]))`; the store owns the error status, so `onError` only logs. The unmount effect at `:53` also calls `useAccountActivityStore.getState().reset()`, which bumps the generation and drops a result racing an archive (M14). `retryActivity` calls `retry` with the same input composition. Reactive reads: `snapshot`, `status` from the activity store through `useShallow`; `categories` from `useCategoryStore`; `accountsById` and `categoriesById` as `Map`s (`transactions/index.tsx:93-95` lookups, from `accounts` and `categories`). `activityRows = useMemo(() => (snapshot?.accountId === id ? snapshot.rows : []).map((tx) => ({ id: tx.id, category, presentation: buildActivityRowPresentation({ tx, account: accountsById.get(tx.account_id), toAccount, category }, now) })))` with `now` captured once per snapshot (`snapshot.loadedAt`). `monthFacts = useMemo(() => account ? buildMonthFacts(account, snapshot?.accountId === id ? snapshot.stats : undefined) : [])`. Navigations: `goToTransaction(txId)` is `router.navigate(\`/(app)/(tabs)/transactions/detail/${txId}\`)`; `goToAllTransactions` is `useTransactionsScreenStore.getState().seedAccountFilter(id, currentYearMonth())` then `router.navigate('/(app)/(tabs)/transactions')`; `addTransactionForAccount` is `useTransactionFormState.getState().openAdd({ accountId: id })` then the same `navigate`. `navigate`, not `push`: the detail sits on the `(app)` Stack above `(tabs)` (`src/app/(app)/_layout.tsx`), and a `push` of a route already in that stack would mount a second tab navigator; `dashboard.hook.ts:181` pushes from inside the tabs, which is a different case. Return adds `state.activity: { status, rows: activityRows, monthFacts }` and the four handlers.
- Test: `__tests__/screens/accounts/account_detail.hook.test.ts`: the `expo-router` mock (`:14-18`) gains `useFocusEffect: (effect) => mockFocusEffect(effect)` and `navigate: mockNavigate`; `@/utils/run_after_interactions` is mocked to run the task at once (`spending_plan_detail_hook.test.ts:21-26`); the activity store, `useTransactionStore` (`getState: () => ({ mutationVersion: 3 })`), `useTransactionsScreenStore`, `useTransactionFormState` and `useCategoryStore` are jest-mocked with `attachMockSelectorStore` or bare `getState` stubs. Cases: the focus effect calls `ensure` with `{ accountId: 'acc-1', mutationVersion: 3 }` when `accounts` holds the id, and never when it does not; unmount calls the activity `reset`; `goToAllTransactions` calls `seedAccountFilter('acc-1', <current month>)` before `navigate('/(app)/(tabs)/transactions')`; `addTransactionForAccount` calls `openAdd({ accountId: 'acc-1' })` then the same `navigate`; `goToTransaction('tx-9')` navigates to `/(app)/(tabs)/transactions/detail/tx-9`; `state.activity.monthFacts` is two `'—'` rows for a bank with no snapshot and `[]` for a card; the existing 12 cases pass unchanged.

### 11. The screen composes the card after the actions and the month rows after the facts
- File: `src/modules/accounts/screens/accounts/detail/index.tsx` (`:58-60`, `:146-174`)
- Change: `const facts = [...buildAccountFacts(account), ...activity.monthFacts]`; each `DetailRow` passes `valueColor={fact.valueColor}`. Inside the existing `{!isEditing && (…)}` block (`:158-174`), wrap the `Box className="mx-4 mt-4 gap-2"` (`:159`) in a fragment and render the card as the `Box`'s sibling after it, never as its child (`SectionHeader` and `DetailRowsCard` carry their own `mx-4`, `section_header.tsx:15`, `detail_rows_card.tsx:12`): `<AccountActivityCard status={activity.status} rows={activity.rows} onRowPress={goToTransaction} onSeeAll={goToAllTransactions} onAdd={addTransactionForAccount} onRetry={retryActivity} />`, so the card hides with the actions while the inline edit form is open. Hero, review alert, form, sheet and dialog are byte-identical.
- Test: none (render layer); the composition inputs are the hook's, covered in step 10.

## Screens
- Account detail, bank with 12+ transactions including a transfer into it: first entry (skeleton, three rows, no date bar, hero and facts already painted); loaded (ten rows, newest first, no swipe, `Today, <time>` / `Yesterday` / `d Mon` in the time slot, month rows filled and coloured after Currency and Opening balance).
- Account detail, bank with no transactions: the empty block, the two month rows at `—`.
- Account detail, credit card: no month rows, the activity card present.
- Account detail while editing: hero and form only, no actions, no card.
- Row tap: the transaction detail opens; note where back lands.
- See all: transactions tab, date header context label is the account name, filter badge shows 1, month is the current one, search empty, type chip on All; back to Dashboard and into Transactions again, the filter still applied.
- Add link: transactions tab with the add form open and the account already selected; then the tab bar's add button, the form opens with no account selected.
- Return path: from the seeded transactions tab, add an expense on the account; the See all navigate popped the detail, so open it again from the accounts list: the skeleton, then the new row first and the month out figure moved. If the detail is still on the stack (the first Risks line), the same walk must show the skeleton and the fresh rows, never the stale ten.
- Failed read: a temporary `throw` at the top of `AccountActivityRepository.getSnapshot`, uncommitted and reverted before the render commit; the card shows the inline alert with the flat `Try again` inside the bordered `DetailRowsCard`, the hero, the facts and the two actions painted above it; revert, tap `Try again`, the rows replace the alert. The `emulator-verify` skill names no forced-failure method; an unreadable id does not reach the load (the step 10 guard returns before `ensure`).

## Non-goals
- The hero, the facts other than the month rows, the actions, the review alert, the Adjust balance sheet (MA-027, #401).
- The accounts list (MA-015, #381), the archived detail (MA-017, #383), the edit screen (MA-019, #385), deleting (MA-020, #386; MA-021, #387).
- An all-time period on the transactions screen; See all lands on the current month.
- Editing, deleting or swiping a row on the detail; the rows are read-only.
- A month figure on any account type but a bank, or a `thisWeek` / `inBase` row on the detail.
- An `EmptyState` variant for the empty block, or a change to `EmptyState` or `ErrorState`.
- Archived-account names in a transfer row's context (the transactions list has the same `unknownAccount` fallback); the `accountLookup` path stays the transaction detail's.
- A route param on the transactions screen; the seed is the store action.
- Pagination or a `limit` prop; ten rows is the card.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- Gate check for step 1: `npx jest __tests__/screens/load_error_copy.test.ts` fails at base once the expected lists carry the new keys and passes at head.

## Risks
- `router.navigate` from the `(app)` Stack into `(tabs)` and into `transactions/detail/[id]` is unverified in this repo; if it pushes a second tab navigator or lands on the wrong tab, the fix is `router.dismissTo` for See all and a nested `Href` object for the row; the emulator pass is the gate.
- `openAdd({ accountId })` fires while the tabs subtree is frozen (`freezeOnBlur: true`); if the `Sheet` does not open on unfreeze, swap the order to navigate first and open the form in a `runAfterInteractions` task.
- `transactions.hook.ts:151-157` re-queries when the seeded store fields change; if the tab was never mounted it mounts on the seed, and if it was mounted and frozen it re-runs on unfreeze; a stale list would mean the effect deps miss `period`.
- The expected diff is ~540 lines, above the ~400 mark; the ticket records the one-PR decision (issue-review, 2026-09-07) and names the cut as See all plus the Add link (steps 8, 9 and their hook rows), which the user can take later.
- The pre-load value of the month rows is `—` (`Strings.accountDetailFactUnset`), the unset glyph the facts already use; the canvas may show a blank or a skeleton bar instead.
- The `SectionHeader` header and its See all render in the empty and error states; the canvas may hide the link on the empty frame.
- `buildMonthFacts` gates on `AccountType.Bank` only, per the ticket's "a bank"; `buildInfoRows` gives a smart wallet the same rows, so widening it later is a one-line change.
- Amended 2026-09-08 on review: step 4 keys the snapshot on `accountId` with `mutationVersion` as the freshness stamp, so a same-account return refreshes in place and `refreshing` / `refreshErrorWithData` have a caller (step 3's interfaces and step 10's input follow); step 9 drops the `tabs.hook.ts` rewrite and its test, `fab.tsx:131,191` call bare; step 11 places the card as the `Box`'s sibling; Screens shoots the failed read; the expected diff is recounted at ~570.
- Amended 2026-09-08, second review: reverses the refresh-in-place part of the line above. `router.navigate` from `(app)/accounts/[id]` into `(tabs)` pops the detail and unmount resets the store, and no screen stacked above the detail writes a transaction (`tabs/index.tsx:86` hosts the form inside the tabs), so `refreshing` / `refreshErrorWithData` had no caller: step 4 drops them and their store cases and exports `AccountActivityStatus` for step 7; step 3 stamps `loadedAt` from `input.now`; Screens re-enters from the accounts list. `mutationVersion` stays in the key for the mounted-detail case the first Risks line doubts. Expected diff recounted at ~540.
- The hook test is fully mocked; each new store mock must expose `getState` plus the reactive selector shape, or `useShallow` reads return `undefined` and the memoised rows stay empty.

## Self-assessment
Step 10's navigation is the row I am least sure of. Every existing tab jump in the repo starts inside the tabs, where `push` is a sibling switch; this is the first jump from a screen stacked above `(tabs)`, and I chose `navigate` from React Navigation's stack semantics, not from a case I could run. If it misbehaves the store seed and the form preselect still hold, so the failure is a wrong landing, not a wrong state, and the emulator walk names both landings. The second uncertainty is step 6's row split: `TransactionRowBody` must keep the `testID`s and the `TRANSACTION_ROW_HEIGHT` style on the same node so the four frozen render cases keep passing without edits; the implementer should run `transaction_row.test.tsx` before touching anything else in that step.
