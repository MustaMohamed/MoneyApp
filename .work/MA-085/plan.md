# MA-085 — List: lift, drag and drop within the visible rows
base: f33475bdbdcf538e949c1a9f83e18c23c65e9560 · verify: emulator · flags: none · expected diff: ~390 lines (10 files outside tests: reorder 25, state 8, hook 40, theme 3, geometry 30, anim 140, row tsx 40, lifted row 60, index 40, feature 2)

## Steps
### 1. The drop index and the row shift are pure arithmetic
- File: `src/modules/accounts/screens/accounts/list/accounts_list.reorder.ts`
- Change: add two worklet-marked pure functions (a `'worklet'` directive compiles to plain property assignments under `babel.config.js`, no runtime import, so the reorder test needs no mock). `resolveDropIndex(input: { fromIndex: number; translationY: number; cellHeight: number; count: number }): number` returns `fromIndex + Math.round(translationY / cellHeight)` clamped to `[0, count - 1]`; `count <= 0` or `cellHeight <= 0` returns `fromIndex`. `resolveRowShift(input: { index: number; fromIndex: number; toIndex: number }): -1 | 0 | 1` returns `-1` when `fromIndex < index <= toIndex`, `1` when `toIndex <= index < fromIndex`, else `0` (the lifted row itself is `0`).
- Test: `first` · `__tests__/screens/accounts/accounts_list.reorder.test.ts`: a half-cell translation rounds to the neighbour, above the first row clamps to 0, below the last clamps to `count - 1`, zero translation returns `fromIndex`, `cellHeight` 0 returns `fromIndex`; `resolveRowShift` for a move down (rows between shift `-1`), a move up (`1`), the lifted row, and rows outside the span.

### 2. The state knows which row is lifted
- File: `src/modules/accounts/screens/accounts/list/accounts_list.state.ts`
- Change: `liftedId: string | undefined` on the shape, `INITIAL_STATE.liftedId = undefined`, `setLiftedId: (id: string | undefined) => void`; `reset` clears it through `INITIAL_STATE`; `resetArchivedCard` leaves it alone.
- Test: `first` · `__tests__/screens/accounts/accounts_list.state.test.ts`: starts undefined, round-trips, `reset` clears it, `resetArchivedCard` leaves it.

### 3. The hook lifts, releases and guards the row press
- File: `src/modules/accounts/screens/accounts/list/accounts_list.hook.ts` (`useAccountsList`, `dropRow` at `:169`, `goToAccount` at `:121`, the unmount effect at `:77`)
- Change: `liftRow(id: string): void` returns when `liftedId` is set, `isReordering` is true or `isAccountsListReorderable(selectedType)` is false; otherwise `setLiftedId(id)` then `void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` (`expo-haptics`, the `swipeable_row.tsx:87` call). `releaseRow(id: string, fromIndex: number, toIndex: number): Promise<void>` returns when `liftedId !== id`; otherwise `setLiftedId(undefined)` then `await dropRow(fromIndex, toIndex)` (equal indices already write nothing; a failed write already toasts `Strings.accountsReorderError`). `goToAccount` returns without pushing while `liftedId` is set. The unmount cleanup also calls `setLiftedId(undefined)`. `state` gains `liftedId` and `canLift: boolean` (`isReorderable && !isReordering`). Both actions are returned flat beside `dropRow` and `moveRow`.
- Test: `first` · `__tests__/screens/accounts/accounts_list.hook.test.ts` (adds `jest.mock('expo-haptics', ...)` with `impactAsync` and `ImpactFeedbackStyle.Light`): `liftRow` sets `liftedId` and fires the light impact once; a second `liftRow` while lifted does nothing; `liftRow` under a type filter does nothing; `releaseRow` to a new index clears `liftedId` and calls `reorderAccounts` once with the reordered ids; `releaseRow` to the same index clears `liftedId` and writes nothing; `releaseRow` with another id changes nothing; `goToAccount` pushes nothing while lifted and pushes again after release; unmount clears `liftedId`; `canLift` is false during an in-flight write and under a filter; no haptic on release.

### 4. B6 values as tokens and geometry
- File: `src/constants/theme.ts` (`AlphaHex` at `:64`, `Size` at `:142`); `src/modules/accounts/screens/accounts/list/accounts_list.geometry.ts`
- Change: `AlphaHex` gains `'73'` (45%) and `'80'` (50%); `Size.liftedRowInset: ms(6)` with a B6 comment beside `reorderGripSlot`. Geometry adds two frozen styles: `ACCOUNTS_LIST_LIFTED_ROW_STYLE` (`borderWidth: Size.hairline`, `borderColor: withAlpha(Colors.dark.gold, '80')`, `borderRadius: Radius.md`, `marginVertical: Spacing.xxs`, `marginHorizontal: Size.liftedRowInset`, `overflow: 'hidden'`) and `ACCOUNTS_LIST_DROP_SLOT_STYLE` (`minHeight: Size.accountListRowMinHeight`, `borderWidth: Size.hairline`, `borderStyle: 'dashed'`, `borderColor: withAlpha(Colors.dark.gold, '73')`, `borderRadius: Radius.md`, `backgroundColor: 'transparent'`). No shadow key on either. The lifted fill is the `bg-surface-secondary` class on the inner `View` (step 7), not a style key.
- Test: `first` · `__tests__/screens/accounts/accounts_list.geometry.test.ts`: each style's values, exact key sets (no `shadow*`, no `elevation`), frozen.

### 5. One drag context per screen, on the UI thread
- File: `src/modules/accounts/screens/accounts/list/accounts_list.anim.ts` (new)
- Change: export `LIFT_LONG_PRESS_MS = 500`, `LIFT_SCALE = 1.02`, `LIFT_DURATION_MS`, `SHIFT_DURATION_MS` (the implementer picks the two durations; one-shot `withTiming`, no spring, no shadow). Export `interface ListDrag { liftedIndex: SharedValue<number>; targetIndex: SharedValue<number>; translationY: SharedValue<number>; cellHeight: SharedValue<number>; reducedMotion: boolean }` with `-1` meaning no lift. `useAccountsListDragAnim(input: { isLifted: boolean }): { drag: ListDrag; slotStyle: AnimatedStyle; liftedStyle: AnimatedStyle }` reads `useReducedMotion()` once; `isLifted` is the React-side `liftedId !== undefined`, passed into every `useAnimatedStyle` as a closure dependency, and a `useEffect` on `isLifted` turning false writes `-1`/`-1`/`0` into `liftedIndex`/`targetIndex`/`translationY` for the next lift; the gesture never resets them. `slotStyle`: `translateY = targetIndex * cellHeight`, `height = cellHeight`, `opacity` 1 while lifted else 0. `liftedStyle`: `translateY = liftedIndex * cellHeight + translationY`, `scale` `LIFT_SCALE` while lifted else 1, `opacity` 1 while lifted else 0; when `isLifted` is false both styles return the plain `0` opacity / `1` scale / `0` translate with no `withTiming`, whatever the shared values hold; while lifted, scale and opacity go through `withTiming` unless `reducedMotion`, and translate is never animated (it follows the finger). Invariant: the shared values change only on the UI thread during the gesture and only from the `useEffect` after it, and the React commit that clears `liftedId` is the one that applies `pendingOrder` (`setLiftedId(undefined)`, `setReordering(true)`, `setPendingOrder(next)` are synchronous on one store before `dropRow`'s first `await`, `accounts_list.hook.ts:177-180`), so the new order paints unshifted in one commit and no UI frame shows the old order between the release and the write.
- Test: `after` · `__tests__/screens/accounts/accounts_list.anim.test.ts` (new, on the `account_type_tile_anim.test.ts` shape: `react-native-reanimated` mocked with `useSharedValue`, `useAnimatedStyle` running the factory, `useReducedMotion`, `withTiming` recording its arguments; `react-native-worklets` mocked with `scheduleOnRN` calling synchronously; `react-native-gesture-handler` mocked with a `Gesture.Pan()` builder that records every chain call and its argument and exposes the `onStart`/`onUpdate`/`onFinalize` callbacks): with a lift at index 1, cell 65 and translation 130, `slotStyle` puts the slot at index 3 and `liftedStyle` at 195 with `LIFT_SCALE`; under reduced motion `withTiming` is never called and the same values land.

### 6. Every row can be lifted and shifts out of the way
- File: `src/modules/accounts/screens/accounts/list/accounts_list.anim.ts` (the two per-row hooks below join step 5's file; one anim file for the screen, no `components/*.anim.ts`); `src/modules/accounts/screens/accounts/list/components/account_list_row.tsx`; `src/modules/accounts/screens/accounts/list/index.tsx` (the `rows.map` at `:121`)
- Change: `useLiftGesture(input: { drag: ListDrag; id: string; index: number; count: number; enabled: boolean; onLift: (id: string) => void; onRelease: (id: string, fromIndex: number, toIndex: number) => void }): { gesture: PanGesture; onLayout: (e: LayoutChangeEvent) => void }`. The gesture is `Gesture.Pan().activateAfterLongPress(LIFT_LONG_PRESS_MS).enabled(enabled).shouldCancelWhenOutside(false)`; `onStart` writes `liftedIndex = targetIndex = index`, `translationY = 0`, `cellHeight` from the row's last `onLayout` height, then `scheduleOnRN(onLift, id)`; `onUpdate` writes `translationY` and `targetIndex = resolveDropIndex(...)`; `onFinalize(_, success)` computes `to = success ? targetIndex : index`, writes `targetIndex = to` and snaps `translationY = (to - index) * cellHeight` so the copy sits in the slot until React commits, leaves `liftedIndex` alone, then `scheduleOnRN(onRelease, id, index, to)`. `onFinalize` is the one exit: a cancel and a drop run the same path, so `liftedId` always clears, and the `useEffect` in step 5 is the only reset of the indices. `useRowShiftStyle(input: { drag: ListDrag; index: number; isLifted: boolean })` returns, when `isLifted` is false, the plain `translateY` 0 and opacity 1 with no `withTiming`, whatever the shared values hold; when true, `translateY = resolveRowShift(...) * cellHeight` and opacity 0 while `liftedIndex === index` else 1, through `withTiming(SHIFT_DURATION_MS)` unless `reducedMotion`. `isLifted` is a closure dependency of the `useAnimatedStyle`, so the commit that clears `liftedId` and applies `pendingOrder` paints every row at 0 in the new order (Acceptance 2, no gap, no flash of the old order). `AccountListRow` gains props `index: number`, `count: number`, `drag: ListDrag`, `canLift: boolean`, `onLift`, `onRelease`, `showSeparator: boolean`; it wraps the optional `Separator` and the `ListGroup.Item` in one `Animated.View` carrying the shift style and the `onLayout` (the cell pitch is this wrapper's measured height), and wraps the grip `PressableFeedback` in `GestureDetector` with the gesture; the row body (prefix, content, suffix) moves into an exported `AccountListRowBody({ account, caption })` in the same file for step 7 to reuse. The item stays mounted for the whole gesture (the `dropHandlers` trap); nothing here conditionally unmounts on lift. `index.tsx` mounts `useAccountsListDragAnim()` once, drops its inline `Separator`, and passes the new props; nothing else in the tree changes yet.
- Test: `after` · `__tests__/screens/accounts/accounts_list.anim.test.ts`: the builder received `activateAfterLongPress(500)`, `enabled(false)` when `enabled` is false, `shouldCancelWhenOutside(false)`; `onStart` then `onFinalize(success)` calls `onLift(id)` once and `onRelease(id, index, targetIndex)` once with the shared values back at `-1`/`0`; `onFinalize(failure)` calls `onRelease(id, index, index)`; `useRowShiftStyle` gives `-cellHeight` to a row between a downward lift and its target, `0` outside, and opacity 0 on the lifted row; reduced motion skips `withTiming`; `onFinalize(success)` leaves `liftedIndex` at `index` and puts `translationY` at `(targetIndex - index) * cellHeight`; with `isLifted` false and stale shared values at 1/3, every row's shift is 0, opacity 1, `liftedStyle` opacity 0, and no `withTiming` is called; rendering `useAccountsListDragAnim` with `isLifted` true then false writes `-1`/`-1`/`0` into the shared values.

### 7. The floating copy and the slot, and the scroll lock
- File: `src/modules/accounts/screens/accounts/list/components/lifted_account_row.tsx` (new); `src/modules/accounts/screens/accounts/list/index.tsx`
- Change: `LiftedAccountRow({ account, caption, style }: { account: Account; caption: string; style: AnimatedStyle })` is an absolute `Animated.View` (`position: 'absolute', top: 0, left: 0, right: 0`, plus `style`) with `pointerEvents="none"` and `importantForAccessibility="no-hide-descendants"`, holding a plain `View` with `className="bg-surface-secondary"` and `ACCOUNTS_LIST_LIFTED_ROW_STYLE`, inside it a `View` with `ACCOUNTS_LIST_ROW_STYLE` (the row's own padding and gap, or the copy packs left), `AccountListRowBody`, and the `drag-vertical` glyph at `Size.reorderGripSlot` in `Colors.dark.gold`. Colour classes stay on the inner `View`, never on the `Animated.View` (`hero_shell.tsx:50`). `index.tsx`: inside `ListCard`, after the rows, render `<Animated.View pointerEvents="none" style={[ACCOUNTS_LIST_DROP_SLOT_STYLE, absolute, slotStyle]} />` and `<LiftedAccountRow style={liftedStyle} />` for the row whose `account.id === liftedId`, both only while `liftedId` is defined; `ScreenScroll` gets `scrollEnabled={liftedId === undefined}`. `AccountListRow` receives `canLift`, `onLift={liftRow}`, `onRelease={(id, from, to) => void releaseRow(id, from, to)}`.
- Test: `none` · render behaviour; the repo forbids component render tests. Pixels are step 8's walk.

### 8. The feature file carries the new state
- File: `.claude/skills/emulator-verify/features/accounts_list.md` (States table)
- Change: append the row `after a drop | no frame, MA-085 | All selected, three or more active rows; `adb shell settings put secure long_press_timeout 1000`; grip bounds by `mqa find 'Reorder <first name>'`; `adb shell input draganddrop <grip x> <grip y> <grip x> <third row centre y> 1500`; then `adb shell settings put secure long_press_timeout 400` | `mqa db "select name, sort_order from accounts where is_archived = 0 order by sort_order"` shows the first name at index 2; `am force-stop` and relaunch, same query and `mqa ui` row order unchanged; no toast. If the timeout setting does not take, this row is device QA and the walk proves the rest`. Leave `row lifted mid-drag` as device QA only.
- Test: `none` · a skill file.

## Screens
- `emulator-verify/features/accounts_list.md`: grip on every active row, filtered reorder off, after a drop
- `after a drop`: lacking today, `no frame`; step 8 appends it. `row lifted mid-drag` stays device QA only, as the file says.

## Non-goals
- Scrolling while lifted, auto-scroll near the card's edges, a drag past the rows on screen (MA-083).
- Any change to the grip's label, actions, the note, the toast, `dropRow`, `moveRow`, `reorderAccounts` or the repository (MA-084, MA-081).
- A new string, a new store action, a new dependency, a shared UI component, an edit to `swipeable_row.tsx` or `fab.tsx`.
- Reordering under a filter, in the archived card, in the dashboard carousel or any picker.
- A shadow or elevation on the lifted row in either mode; a spring; a haptic on drop.
- Migrating the FAB's `LongPressGestureHandler`.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.

## Risks
- `GestureDetector` around the grip's `PressableFeedback`: `fab.tsx:206` records a conflict between a `GestureDetector` and a nested `Pressable` `onPress`. If the grip's tap absorption or the pan breaks, the fallback is a plain `View` grip with the a11y props and `onStartShouldSetResponder={() => true}`; the `grip tap absorbed` state in the feature file is the check.
- `ListGroup` clipping: if HeroUI's root sets `overflow: hidden`, the 1.02 scale clips at the card's edges; the fix is a wrapper `View` around the card holding the two absolute siblings, not a HeroUI change.
- Two fingers lifting two rows share one drag context; the id guards in `liftRow`/`releaseRow` keep the write correct, the pixels are device QA.
- The estimate sits near the ~400 cap and no seam exists: steps 5 to 7 are Acceptance lines 1 to 4 and step 3 is lines 5 to 7, so nothing ships without the rest. The review's count was ~420 across the same files before the anim fold; if the implementer's diff outside tests passes 400, that is a discrepancy for `/prep --amend`, not a cut.
- Amended after review round 2: the release no longer resets the shared values on the UI thread (that painted the old order for a frame before `pendingOrder` landed); `onFinalize` parks the copy in the slot, the styles take the React-side `isLifted` and return plain values when it is false, and a `useEffect` resets the shared values after the commit. Anim estimate 130 to 140, header recounted.
- Amended after review round 1: step 5 and step 6 now snap at `liftedIndex === -1` instead of running `withTiming` through the release (the drop settled one cell off under `pendingOrder`); step 6's per-row hooks moved from a second anim file into `accounts_list.anim.ts` (one file fewer, the header recounted).
- `Math.round` against the wrapper's measured height treats every cell as the lifted one's pitch; a row taller than 64 (a scaled font) drifts the target by a fraction of a cell per row, never past the clamp.

## Self-assessment
Step 6 is the one I am least sure of: it puts RNGH's `Gesture.Pan().activateAfterLongPress` around a HeroUI `PressableFeedback` inside a `ListGroup.Item` that is itself a `Pressable`, and no file in this repo has that nesting today; the FAB's note says a `GestureDetector` fought a nested `Pressable`, and I cannot tell from the code whether that was the long-press handler or any handler. The interfaces around it (`resolveDropIndex`, `liftRow`/`releaseRow`, `onFinalize` as the single exit) hold whichever grip element the implementer ends up with, so a change there amends one step.
