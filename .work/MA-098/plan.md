# MA-098 — List hero: Out this month (M25)
base: 78cc257f2a2633128bd23afd45403d04b3684f86 · verify: emulator · flags: money path, user copy · expected diff: ~470 lines outside tests, per file against `78cc257f` without `__tests__`, `.work` and the strip: `transactions_hero.tsx` ~206 (211 at `5a4b93f5`, the rail's `accessibilityValue` onto one line), `transactions.helpers.ts` ~128 (119 + the net-credit branch, the dashes label and the three-line month name), `transactions.hook.ts` 31, the ADR ~35, `index.tsx` 28 (12 added, 16 removed once the rail is back), `strings.ts` 15, the features file ~16, `search_filter_row.tsx` 4, `transactions.presentation.ts` 4, `search_row.tsx` 3; 11 files, plus the 359-line deletion of `totals_strip.tsx`; over the ~400 gate by the user's ruling of 2026-09-26

## Steps
### 1. The hero's copy and the two alert flips exist under `Strings`
- File: `src/constants/strings.ts` (`§6 Transactions: Totals strip` block at `:1237-1252`)
- Change: add a `§6 Transactions: Hero` block: `transactionsHeroTitle: 'Out this month'`, `transactionsHeroTitleScoped: (account: string) => \`Out this month · ${account}\``, `transactionsHeroIn: 'In'`, `transactionsHeroNet: 'Net'`, `transactionsHeroLeftOfIncome: 'Left of income'`, `transactionsHeroShareSpent: (pct: number) => \`${pct}% of income spent\``, `transactionsHeroDaysLeft: (days: number) => (days === 1 ? '1 day left' : \`${days} days left\`)` (the `budget5030DaysLeft` form, `strings.ts:744`; user ruling 2026-09-25, 0 stays `0 days left`), `transactionsHeroLastMonthOut: (month: string, amount: string) => \`${month} ${amount}\``, `transactionsHeroUnavailable: '—'`. Flip `transactionsTotalsLoadError` to `"Couldn't load this month's figures."` and `transactionsLoadRetry` to `'Try again'`; the keys keep their names, `__tests__/screens/load_error_copy.test.ts:45-110` derives them by name. `totalsIncome`, `totalsExpense`, `totalsNet`, `totalsVsPrev`, `totalsExpenseShareA11y` and the four `totals*` captions stay: the dashboard card and `buildTotalsPresentation` read them.
- Test: `none` · copy constants; `load_error_copy.test.ts` already pins the keys and that the four transactions titles stay distinct; step 2's `1 day left` case pins the singular.
- Amend 1 (on c0621e2f): `transactionsHeroDaysLeft` (`strings.ts:1261`) prints `` `${days} days left` `` for every count; make it the ternary above, no other key moves. Its test is step 2's `1 day left` case, `first`.

### 2. A pure model turns the scoped totals into every string and number the hero prints
- File: `src/modules/transactions/screens/transactions/transactions.helpers.ts` (below `deltaDisplay`, `:130`); `docs/adr/2026-09-24-transactions-hero-figures.md`
- Change: add, with `computeDeltaPct`, `deltaDisplay`, `expenseSharePct`, `formatSignedAmount`, `buildTotalsPresentation` and their types byte-identical (the dashboard card imports them, `transactions_card.tsx:11-19`):
  ```ts
  export type TransactionsHeroMode = 'skeleton' | 'dashes' | 'figures';
  export interface TransactionsHeroInput {
    mode: TransactionsHeroMode;
    current: PeriodTotals | null;
    previous: PeriodTotals | null;
    yearMonth: string;        // the selected month, YYYY-MM
    today: string;            // YYYY-MM-DD, an input, never new Date() here
    accountLabel?: string;    // set only when the sheet's account filter holds exactly one account
  }
  export interface TransactionsHeroModel {
    mode: TransactionsHeroMode;
    title: string;            // transactionsHeroTitle, or transactionsHeroTitleScoped(accountLabel)
    monthLabel: string;       // new Date(`${yearMonth}-01T12:00:00`).toLocaleDateString('en-US', { month: 'long' }), the budget.helpers.ts:301-303 form, the month's full name with no year: frame A1 draws `September` right of `Out this month`, A16 `August`
    out: string; in: string; net: string;   // EGP through formatDisplayMagnitude + signAmountText, MINUS_SIGN only when the figure is negative; transactionsHeroUnavailable in 'dashes' mode
    leftOfIncome: string;     // `${n}%`, n = Math.round((in − out) / in × 100), MINUS_SIGN when n < 0 (−0 is not); transactionsHeroUnavailable when in ≤ 0 or 'dashes'
    railPct: number;          // share clamped to 0..100; 0 when in ≤ 0. share = Math.round(out / in × 100), rounded once and read by the three fields below, as buildTotalsPresentation does at :101
    railDanger: boolean;      // share > 100
    railAccessibilityLabel: string;   // Strings.totalsNetCredit when out < 0, checked first as buildTotalsPresentation does at :78; else Strings.totalsExpenseShareA11y(share); Strings.totalsNoIncome when in ≤ 0 or 'skeleton'; Strings.transactionsTotalsLoadError in 'dashes'
    shareCaption: string | null;   // Strings.totalsNetCredit when out < 0, whatever in is; else transactionsHeroShareSpent(share), null when in ≤ 0 or 'dashes'
    caption: string;          // `${transactionsHeroDaysLeft(d)} · ${last}` in the current month, `${last}` otherwise
  }
  export function resolveTransactionsHeroMode(status: TransactionTotalsStatus, hasTotals: boolean): TransactionsHeroMode;
  export function buildTransactionsHeroModel(input: TransactionsHeroInput): TransactionsHeroModel;
  ```
  `resolveTransactionsHeroMode`: `hasTotals` → `'figures'` (ready, refreshing and refreshErrorWithData all keep the figures); `'firstLoadError'` → `'dashes'`; anything else → `'skeleton'`. `last` = `transactionsHeroLastMonthOut(MONTHS_SHORT[month − 1] of previousPeriod(yearMonth), amount)` (`MONTHS_SHORT` is exported from `src/utils/year_month.ts:3`, the import `month_filter.hook.ts:5-11` already makes; `format_date.ts` keeps its own private long-name table and is not touched), amount the previous month's Out formatted as `out`, or `transactionsHeroUnavailable` when `previous` is null or its In and Out are both 0 (ruled at the /prep review 2026-09-24: the aggregate carries no row count and is MA-088's, so In 0 and Out 0 is the dash, a month whose rows net to zero included; the ADR records it). `d` = last day of `yearMonth` minus `Number(today.slice(8, 10))`, floored at 0, present only when `today.slice(0, 7) === yearMonth`. `'figures'` with zero totals prints `0`, `0`, `0` and a dash (A5). The ADR records: the hero derives every figure from `TransactionMonthAggregate.scoped` unchanged, the two percentages round once with `Math.round` as `computeDeltaPct` does, no money value rounds in this layer, EGP only because the aggregate sums `egp_amount`, last month's dash is In 0 and Out 0, and a month whose Out is below 0 prints no share: the caption and the rail's label read `Credits exceed expenses` and the rail stays empty.
- Test: `first` · `__tests__/screens/transactions/transactions_helpers.test.ts`, a `buildTransactionsHeroModel` block: In 22,300 Out 9,400 on `2026-09-24` (30-day month) → `monthLabel 'September'`, `leftOfIncome '58%'`, `railPct 42`, `railDanger false`, `shareCaption '42% of income spent'`, `railAccessibilityLabel Strings.totalsExpenseShareA11y(42)`, caption `'6 days left · Aug <prev out>'`; In 1,000 Out 1,200 → `'−20%'`, `railPct 100`, `railDanger true`, `railAccessibilityLabel Strings.totalsExpenseShareA11y(120)`; In 1,000 Out 1,004 → `leftOfIncome '0%'`, `railPct 100`, `railDanger false`, `shareCaption '100% of income spent'` (one rounded share drives all three); In 0 Out 500 → dash, `railPct 0`, `shareCaption null`, `railAccessibilityLabel Strings.totalsNoIncome`; In 1,000 Out −50 Net 1,050 (card credit) → `out '−50'`, `leftOfIncome '105%'`, `railPct 0`, `railDanger false`, `shareCaption` and `railAccessibilityLabel` both `Strings.totalsNetCredit`, and no string field of the model contains U+002D; In 0 Out −50 → `leftOfIncome '—'`, `shareCaption Strings.totalsNetCredit`, `railAccessibilityLabel Strings.totalsNetCredit`, `railPct 0`, `railDanger false`; `yearMonth '2026-08'` with `today '2026-09-24'` → `monthLabel 'August'`, caption `'Jul 16,900'`-shaped with no days segment; `today` on the 30th → `'0 days left'`; `today '2026-09-29'` → `'1 day left · Aug 16,900'`; `previous null` and `previous {0,0,0}` → `'Aug —'`; `'dashes'` mode → four dashes, `railPct 0`, `railAccessibilityLabel Strings.transactionsTotalsLoadError`, caption intact; `resolveTransactionsHeroMode` over all six statuses × hasTotals. A `31st` and a leap-February case for the day arithmetic.
- Amend 1 (on c0621e2f): `buildTransactionsHeroModel`'s figures return (`transactions.helpers.ts:236-256`) reads a negative share when Out < 0 and In > 0, so it prints `-5% of income spent` and `Expenses are -5% of income` with ASCII hyphens. Check `current.expenseEgp < 0` first and give `shareCaption` and `railAccessibilityLabel` `Strings.totalsNetCredit` (`strings.ts:1246`); `out`, `in`, `net`, `leftOfIncome`, `railPct` and `railDanger` keep their HEAD values, no new string. The ADR's share row (`2026-09-24-transactions-hero-figures.md:19`) gains the net-credit clause above. The two card-credit cases and the `1 day left` case are new, `first`; the card-credit case at `transactions_helpers.test.ts:309-313` extends in place.
- Amend 2 (on 5a4b93f5): `monthLabel` (`transactions.helpers.ts:217`) reads `new Date(\`${input.yearMonth}-01T12:00:00\`).toLocaleDateString('en-US', { month: 'long' })`, the `budget.helpers.ts:301-303` form, and the now-unused `formatMonthYear` import at `:10` goes. The null-current branch's `railAccessibilityLabel` (`:231`) reads `Strings.transactionsTotalsLoadError` when `input.mode === 'dashes'`, `Strings.totalsNoIncome` otherwise (the skeleton renders no rail, `transactions_hero.tsx:84-90`); no new string. The In 0 caption line needs no code past Amend 1: with the Out < 0 check first, In 0 and Out ≥ 0 keep `shareCaption null` (the cases at `:296` and `:315`), In 0 and Out < 0 give `totalsNetCredit`. Tests `first`: the A1 case (`:252`) expects `monthLabel: 'September'` and the unused `formatMonthYear` import (`:17`) goes; the A16 case (`:326-328`) adds `monthLabel: 'August'`; the dashes case (`:368`) expects `Strings.transactionsTotalsLoadError`.

### 3. Both totals failures raise the figures alert; a list refresh failure keeps its own and wins
- File: `src/modules/transactions/screens/transactions/transactions.presentation.ts` (`loadErrorVariant`, `:37-45`)
- Change: `showFirstLoadError` → `'none'`; `listStatus === 'refreshErrorWithData'` → `'refresh'`; `totalsStatus` in `{'firstLoadError', 'refreshErrorWithData'}` → `'totals'`; `accountLookupError` → `'accounts'`; else `'none'`. Nothing else in the file moves.
- Test: `first` · `__tests__/screens/transactions/transactions_presentation.test.ts:46-47` flips to `'totals'`; add a row with both `listStatus` and `totalsStatus` at `refreshErrorWithData` → `'refresh'`.

### 4. The hero component renders the model inside the screen's one gradient shell
- File: `src/modules/transactions/screens/transactions/components/transactions_hero.tsx` (new)
- Change: `export function TransactionsHero({ model }: { model: TransactionsHeroModel }): React.ReactElement`, props-only, no store reads, no hooks. `HeroShell` (`src/components/ui/hero_shell.tsx:61`) with `style={{ marginTop: 8 }}` as the balance hero does; inside, in order: header row, `title` left and `monthLabel` right, both Sora semibold (`font-sora-semibold`) at the frame's overline size, per A1's `<span class="fl-over">Out this month</span>` beside `September` in the display face at weight 600, and A16's `August`; the main number `out` Sora bold at `Type.hero` with the `EGP` code beside it at `Type.subhead` (the `balance_hero.tsx:78-106` row: `alignItems: 'baseline'`, a container gap, `lineHeightFor` on every `fontSize`); three columns `in` / `net` / `leftOfIncome` under `transactionsHeroIn` / `transactionsHeroNet` / `transactionsHeroLeftOfIncome` labels, Sora numbers, Inter labels; a rail of `Size.progressThin` height, `railPct` wide, `bg-accent`, `bg-danger` when `railDanger`, `accessibilityRole="progressbar"` with `accessibilityLabel={model.railAccessibilityLabel}` and `accessibilityValue={{ min: 0, max: 100, now: model.railPct }}`, no `text`: RN Android appends `accessibilityValue.text` to the content-desc after the label (`node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/BaseViewManager.java:439-446`), so a screen reader would hear the sentence twice, and the percentage comes from the range info `min`/`max`/`now` set (`ReactAccessibilityDelegate.kt:146-166`); a caption row, `shareCaption` left (empty when null) and `caption` right, one line each, `numberOfLines={1}`. Money colour follows review.md item 3: the figures render `text-foreground` or `text-accent`, never by sign; danger is the rail alone. The fonts fix the geometry: header texts at `Type.overline`, the main number at `Type.hero` with the code at `Type.subhead`, column labels Inter at `Type.micro` over Sora numbers at `Type.body` with `Spacing.xxxs` between, caption texts at `Type.chip`, every `fontSize` paired with `lineHeightFor` of itself. Export `TRANSACTIONS_HERO_GEOMETRY = { header: lineHeightFor(Type.overline), amount: lineHeightFor(Type.hero), columns: lineHeightFor(Type.micro) + Spacing.xxxs + lineHeightFor(Type.body), rail: Size.progressThin, caption: lineHeightFor(Type.chip) }` and give the five loaded rows those heights, so the `'skeleton'` branch, five `Skeleton` bars of the same five heights in the same shell (A6), matches the loaded height to the dp, and the caption row does not move when its text changes length. Where a row's height constant doubles as its text's `lineHeight`, the `oxlint-disable-next-line moneyapp/font-size-pairs-line-height` form at `totals_strip.tsx:98` names why. In `'dashes'` mode the same loaded layout prints the model's dashes. `testID="transactions-hero"` on the shell's child, `transactions-hero-skeleton` on the skeleton branch.
- Test: `none` · a render suite is not added for a new component (`tests.md`); the strings and numbers are step 2's, the pixels are the Screens section's.
- Amend 2 (on 5a4b93f5): drop `text: model.railAccessibilityLabel` from the rail's `accessibilityValue` (`transactions_hero.tsx:170-175`); role, label, `min`, `max` and `now` stay. Test `none`; the content-desc read once is the Screens section's `hero, current month` and `hero, figures failed`.

### 5. The hook publishes one hero model whose identity survives a keystroke
- File: `src/modules/transactions/screens/transactions/transactions.hook.ts` (`displayTotals`/`displayTotalsStatus` `:426-428`, the `state` return `:521-549`)
- Change: `scopedAccountLabel = appliedFilters.accountIds?.length === 1 ? accountLabelsById.get(appliedFilters.accountIds[0])?.name : undefined` (`labelAccountsById`, `filter/filter.helpers.ts:115`, already computed at `:383`). `today = toLocalDateString(new Date())` (`src/utils/format_date.ts:46`), a value the hero memo compares by string. `heroMode = resolveTransactionsHeroMode(displayTotalsStatus, displayTotals !== null)`. `hero = useMemo(() => buildTransactionsHeroModel({ mode: heroMode, current: displayTotals?.current ?? null, previous: displayTotals?.previous ?? null, yearMonth: period.yearMonth, today, accountLabel: scopedAccountLabel }), [...those six])`. Return `state.hero: TransactionsHeroModel` and `state.searchDisabled: boolean` (`heroMode === 'skeleton'`). `state.totals`, `totalsStatus`, `previousLabel` stay (MA-092 reads `totals.days`). In the same commit, add `hero` and `searchDisabled` to `baseTransactionsState` in `__tests__/screens/transactions.screen.test.tsx:101-126`: it is typed `TransactionsScreenHook['state']` and `tsconfig.json:10` includes `__tests__`, so `npm run typecheck` reads the two new fields at this step, not at step 6. Invariant: a debounced keystroke runs `beginTotalsRequest` with `preserveData` true and `resolveTotals` keeps `current`/`previous` identity on equal figures (`transactions.store.ts:95-105`), and `'refreshing'` maps to `'figures'`, so none of the six deps change and `hero` keeps its identity.
- Test: `first` · `__tests__/screens/transactions/transactions_hook.test.ts`, in `useTransactions monthly totals`: after `ready`, hold `result.current.state.hero`, `setSearchQuery('coffee')`, wait for `matchCount` 2 (the `:299` recipe), `expect(result.current.state.hero).toBe(held)`; a second case: `setActiveFilter(Expense)` leaves `hero` identical too; a third: `useAccountStore` mocked with `acc-1` named `Wallet` and `setAppliedFilters({ ...EMPTY_FILTERS, accountIds: ['acc-1'] })` → `hero.title` is `Strings.transactionsHeroTitleScoped('Wallet')`, two accounts → `Strings.transactionsHeroTitle`; a fourth: `mockGetMonthAggregate` rejecting on first load → `hero.mode 'dashes'`, `state.loadErrorVariant 'totals'`, `state.searchDisabled false`; rejecting on a refresh after `ready` → `hero.mode 'figures'` and `loadErrorVariant 'totals'`.

### 6. The screen draws the hero under the shipped filter rail and retires the strip
- File: `src/modules/transactions/screens/transactions/index.tsx`; `src/components/ui/search_filter_row.tsx` (`SearchFilterRowProps` `:13-22`, `SearchField` `:71`); `src/modules/transactions/screens/transactions/components/search_row.tsx`; `src/modules/transactions/screens/transactions/components/totals_strip.tsx` (deleted); `__tests__/screens/transactions/totals_strip.test.tsx` and `totals_strip_skeleton.test.tsx` (deleted); `__tests__/screens/transactions.screen.test.tsx`
- Change: `SearchFilterRowProps.isDisabled?: boolean` forwarded to `SearchField isDisabled` (`search-field.types.d.ts:42`); `SearchRow` gains `disabled?: boolean` and forwards it. The commitments `search_row.tsx:31`, the other `SearchFilterRow` caller, sets neither and renders unchanged. `tabs.tsx`, `segment_filter.hook.ts`, `segment_filter.tsx` and `month_filter.tsx` stay byte-identical to base: trigger and pill hit slop are MA-109's. Keep the h3 `Strings.transactions` header, `Separator` and `FilterRail` (`:177-193`) as shipped; `TotalsStrip`'s import goes. `hero = useMemo(() => <TransactionsHero model={state.hero} />, [state.hero])`; the list header memo (`:122-135`) renders `{hero}` then `SearchRow` with `disabled={state.searchDisabled}`, its deps the same plus `hero` and `state.searchDisabled` and minus `totalsStrip`; the memoised element is what lets React skip the hero when the header re-renders on a keystroke. The floating alert (`:224-229`) is unchanged. Delete `totals_strip.tsx` and its two suites: `index.tsx` was its only importer. `__tests__/screens/filter_rail_usage.test.ts` stays byte-identical to base; its transactions case (`:39-56`) holds against the shipped rail. `transactions.screen.test.tsx`: the `filter_rail` mock stays; the `totals_strip` mock becomes a `transactions_hero` mock that increments a module counter per render and prints `model.mode` (`hero` and `searchDisabled` are already on `baseTransactionsState` from step 5); `:181-187` asserts the rail mounts outside the list and the header holds the hero; the M25 case: render with `hero: H`, `rerender` with `searchQuery: 'c'` and the same `H`, the counter does not move; a second `rerender` with a new `hero` object raises it by one.
- Test: `after` · the screen suite cases above; the render count is the acceptance's own assertion and lives in the suite this step rewrites. It fails if the hero element is built inside the header memo, which is the gate. The `disabled` passthrough is `none`: the disabled field is proved on the emulator (Screens, `hero, skeleton`), never by mock.
- Amend 2 (on 5a4b93f5): (a0) `git checkout 78cc257f -- src/components/ui/tabs.tsx src/components/ui/segment_filter.hook.ts src/components/ui/segment_filter.tsx src/components/ui/month_filter.tsx`, in one commit with (a) and (b), because `index.tsx:191` passes `triggerHitSlop` until (a) lands and `typecheck` fails in between; `search_filter_row.tsx` and `search_row.tsx` stay as at HEAD, so `git diff 78cc257f -- src/components/ui/` then lists `search_filter_row.tsx` alone, and the unapplied Amend 1 of the old step 3 (`resolveScrollSlopInset`, `__tests__/components/ui/tabs_slop_inset.test.ts`, the pill slop 6 → 8) is dropped. (a) `index.tsx` back to the rail: the imports at `:7`, `:9` and `:13` return to base's `FilterRail, type FilterRailOption` and `Colors, Size`, `TRANSACTION_FILTERS` (`:30`) takes `FilterRailOption<TransactionFilter>[]`, `TYPE_TAB_HIT_SLOP` (`:59`) goes, and the `px-4 pt-1 pb-1` column (`:184-194`) is replaced by base's `<FilterRail … />` block verbatim (`git show 78cc257f:src/modules/transactions/screens/transactions/index.tsx`, `:186-193`); afterwards `git diff 78cc257f` on the file shows only the hero lines, 12 added and 16 removed. (b) `git checkout 78cc257f -- __tests__/screens/filter_rail_usage.test.ts`; in the screen suite the `month_filter` and `segment_filter` mocks (`:44-63`) become base's `filter_rail` mock (base `:44-53`, testID `transactions-filter-rail`), and the scope-controls case asserts that testID where `:214-215` read the two. (c) Owed from Amend 1, `after` (`git grep heroRenders __tests__` hits only the mock, `:72-77`): the M25 case reads the counter through `jest.requireMock('@/modules/transactions/screens/transactions/components/transactions_hero').heroRenders` and zeroes `count` in the `beforeEach` (`:188-191`), because `jest.clearAllMocks` leaves a module counter alone; render with `hero: H`, `mockUseTransactions({ hero: H, searchQuery: 'c' })` and `rerender`, the count is unchanged; `rerender` with a new `hero` object, it rises by one. (d) Owed from Amend 1: the scope-controls case (`:211-218`) gains `transactions-hero-mock` found `within` `getByTestId('transactions-list-header')`. The fixture's `monthLabel` (`:140`) reads `'August'`. The unapplied Amend 1 spacing (`gap: Spacing.sm`, `pb-2`) is dropped.

### 7. The features file names the hero's states
- File: `.claude/skills/emulator-verify/features/transactions.md` (header `:3`, the three `totals strip` rows `:29-31`, Gotchas `:44`)
- Change: the header adds the hero `components/transactions_hero.tsx` and frames A1, A5, A6 and A16 beside A2, B1 and B2; the month pill stays `filter_rail.tsx`'s, and the `month filter pill` and `compact tab label` rows keep base's rail wording. Replace the three `totals strip` rows with the rows the Screens section lists, same seeds, the proof reading the hero's `mqa ui` nodes (`Out this month`, the month's full name, the `EGP` code node, `In`, `Net`, `Left of income`, the caption, the rail's content-desc) and `mqa db` for the sums. Two Gotchas: `mqa ui` returns nothing while the skeleton shimmers, so the search field is read through `adb shell dumpsys activity top`; a source-forced totals failure's LogBox toast is not the screen's alert. No tab-row or hit-slop row (MA-109).
- Test: `none` · a skill document.
- Amend 2 (on 5a4b93f5): in the file at HEAD, the header (`:3`) returns to base's `month pill from src/components/ui/filter_rail.tsx` and keeps `hero components/transactions_hero.tsx` and the frames `A1, A2, A5, A6, A16, B1 and B2`; the `month filter pill` (`:15`) and `compact tab label` (`:17`) rows return to base byte for byte (`the rail as it mounts`, `one shot of the rail`); the `type tabs, form corners with icons` (`:37`) and `hit slop` (`:38`) rows and the `hitSlop` Gotcha (`:52`) go. `hero, current month` (`:29`) reads the month's full name with no year (A1 `September`) where it reads `<month> <year>`, and its Proof gains the rail node's content-desc `Expenses are <n>% of income`, once. A `hero, credits exceed expenses` row follows `hero, over income` (`:33`), Force and Proof as the Screens section gives them. `hero, past month` (`:34`) Proof: the month label reads that month's full name (A16 `August`) and the caption is `<Mmm> <amount>` alone, no days segment, though A16 draws `0 days left`. `hero, figures failed` (`:36`): the account filter stays as it is across both loads, the first-load Proof gains the rail empty with content-desc `Couldn't load this month's figures.` once, never `No income this month`, and the row asserts nothing across a scope change (MA-107). The unapplied Amend 1 for this step is dropped.

## Screens
- `emulator-verify/features/transactions.md`: `hero, current month` (A1: the walk's seed, `Out this month` with the month's full name and no year right of it, figures, rail, `<n> days left · <Mmm> <amount>`, the rail's content-desc `Expenses are <n>% of income` once), `hero, scoped to one account` (A1 via `See all`, C5 on `account_detail.md`: the title reads `Out this month · <account>`, Out equals the one-account `mqa db` sum), `hero, unchanged by tabs and search` (no frame: `mqa ui` reads identical hero nodes before and after a tab switch and a typed search), `hero, empty month` (A5: `0`, `0`, `0`, a dash, `<Mmm> —`), `hero, over income` (no frame: seed In 1,000 Out 1,200 this month; one shot, the rail full in danger, Left of income `−20%`, the caption `120% of income spent`), `hero, credits exceed expenses` (no frame: the `row, card credit` seed, an income on a credit card with no expenses on that card this month, then `See all` on the card, In 0 and Out below 0 under the ledger rule, `REPORTING_SIGN_SQL.out` at `reporting_sign.ts:3-7`; the caption node reads `Credits exceed expenses`, the rail's content-desc reads it once, the rail is empty, Left of income reads `—` and Out `−<amount>` with U+2212; one shot), `hero, past month` (A16: the month label `August`-shaped, the caption `<Mmm> <amount>` with no days segment where A16 draws `0 days left`), `hero, skeleton` (A6: five bars, the filter rail's month and type controls answer taps, the search field has no `E` flag in `dumpsys activity top`; force by a source-forced slow `getMonthAggregate`), `hero, figures failed` (no frame: source-force a throw in `getMonthAggregate` with the account filter unchanged between the loads; the alert reads `Couldn't load this month's figures.` with `Try again`, the rows stay, the hero prints dashes on a first load with the rail empty and its content-desc `Couldn't load this month's figures.` once, and keeps its figures on a refresh; nothing asserted across a scope change, MA-107).
- Every state above is added to the file by step 7; the three `totals strip` rows it replaces are retired with the strip.

## Decision record
- `docs/adr/2026-09-24-transactions-hero-figures.md`: the hero prints `TransactionMonthAggregate.scoped` unchanged, rounds only its two percentages, once, with `Math.round`, formats through `formatDisplayMagnitude` at EGP 0 dp with U+2212, and prints last month's Out as a dash when that month's In and Out are both 0, the aggregate carrying no row count (ruled 2026-09-24), and prints no share on a month whose Out is below 0; step 2 adds the file.

## Non-goals
- `flatRetry` on `TransactionLoadError`: the Acceptance asks for the label only; the alert's button shape is untouched.
- The search field's look, the tally line and the funnel count (MA-099); the sheet's footer (MA-100); the aggregate query (MA-088); day cards and the date header's context label (MA-092); empty, error and delete states beyond the hero's own (MA-093); page retention (MA-089).
- `getPeriodTotals` removal, `src/database/transactions.ts` compat, the dashboard card, the commitments rail, the accounts-list rail and the form's type tabs: unchanged.
- Any figure in USD: the aggregate sums `egp_amount`, so the hero is EGP.
- The month row above the type tabs, their form corners, the trigger and pill hit slop and the spacing between the two rows (MA-109): `filter_rail.tsx`, `month_filter.tsx`, `segment_filter.*` and `tabs.tsx` stay as at base.
- The hero while a new account scope loads or after its load fails (MA-107).

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.

## Risks
- `today` from `new Date()` in the hook: a test that pins the caption at hook level needs `jest.useFakeTimers().setSystemTime`; the plan pins the caption in the helpers suite instead.
- `SearchField isDisabled` may not grey the input in HeroUI 1.0.8 the way the frame expects; the `enabled=false` node is the proof either way.
- The hero memo also breaks on a real figures change from a write, focus refresh or pull, by design; the M25 assertion covers keystrokes and tab switches only.
- Amended after round 1 (2026-09-24): the model carries `railAccessibilityLabel` (finding 1); step 6 owns the `baseTransactionsState` fields so each commit typechecks (finding 2); last month's dash is a recorded ruling, not an open risk (finding 3); `hero, over income` proves the danger rail (finding 4); the month pill takes a 6 dp slop in step 3, a 13th code file of one line (finding 5); `monthLabel` stays, cited to A1 and A16 (finding 6).
- Amended after round 2 (2026-09-24): the hero geometry derives from `lineHeightFor` of the named `Type.*` sizes, so the skeleton matches the loaded height to the dp (finding 1); one rounded share drives the rail, its label and the caption, with the In 1,000 Out 1,004 case (finding 3); the `filter_rail_usage.test.ts` transactions case is deleted, not rewritten (finding 4); step 8 retitles the `:15` and `:17` rows to the month row and the tab row (finding 5); the pill's tap-above proof joins the `hit slop` row (finding 6). Finding 2 disputed: `MONTHS_SHORT` is exported from `src/utils/year_month.ts:3` and imported by `month_filter.hook.ts:5-11`; the reviewer read `format_date.ts`'s private long-name table; no `formatMonthShort` and no 16th file.
- Amend 1 (2026-09-25), on `c0621e2f`: steps 1, 2, 3, 7 and 8 and the Screens `hit slop` bullet keep their contracts, edited in place, and each step carries an `Amend 1` line with its delta on that commit; steps 4, 5 and 6 stand as committed. The render pass found the tab slop dead outside the scroll view's box and the 4 dp row spacing unable to hold both 44 targets (steps 3, 7, 8); the self-review found `-5% of income spent` on a net-credit month (step 2); the user ruled `1 day left` (step 1).
- Amend 1, round 2 (2026-09-25): step 7's Amend 1 line owes the M25 render-count case and the hero-in-header assertion, both `after`, where it read "No new test" (finding 1). Finding 2, the plan's size, is with the user; the header estimate stands.
- Amend 2 (2026-09-26), on `5a4b93f5`: the ticket was cut to the hero and the month row, form corners, hit slop and row spacing went to MA-109 (#574), so steps 3, 7 and 8 take that work back out, their unapplied Amend 1 parts deleted rather than applied, and the Screens `type tabs` and `hit slop` states go; steps 2, 5, 7 and 8 meet the /issue-review Acceptance of 2026-09-26 (the month's name without the year, the rail's sentence once, the dashes label, the same-scope refresh, A16's caption); the Amend 1 lines of steps 1 and 2 stand, still owed; steps 4 and 6 stand as committed. The A1 4 dp spacing risk and the step 3 self-assessment left with the rail work.
- Amend 2, round 2 (2026-09-26): `monthLabel` takes the `budget.helpers.ts:301-303` `toLocaleDateString` form, so `format_date.ts` and its test block leave the plan and the count is 11 files, the ticket's Size line (finding 1); the old step 3's search-row prop and primitive revert fold into step 6, and steps 4 to 8 renumber to 3 to 7, 7 steps; the Risks lines above keep the numbering they were written with (finding 2); the `hero, credits exceed expenses` row proves the Out below 0 Acceptance line on the emulator (finding 3). Round 2's review added `railAccessibilityLabel Strings.totalsNetCredit`, `railPct 0` and `railDanger false` to step 2's In 0 Out −50 case, so a net-credit check nested after `share === null` (`transactions.helpers.ts:253-254`) fails in Jest, not at the render pass.

## Self-assessment
Step 4's rail is the one I am least sure of. Dropping `text` removes the second copy of the sentence from the content-desc (`BaseViewManager.java:439-446`), and `mqa ui` shows that. The "0 to 100 percentage" half of the Acceptance comes from the range info `ReactAccessibilityDelegate.kt:146-166` builds out of `min`, `max` and `now`, and `uiautomator dump` does not print range info. TalkBack is the only thing that proves the spoken percentage, so it falls to device QA on real hardware, not to the render lens.
