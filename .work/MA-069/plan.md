# MA-069 — Account writes: a failed reload after a committed add, edit, adjustment or review no longer reads as a failed write
base: 82c465d637d387852132838278bba3b6a69592e6 · verify: none · flags: none · expected diff: ~80 lines

## Steps
### 1. Every store write rejects only when the write rejects; a failed reload after it publishes `loadError` and resolves
- File: `src/modules/accounts/store/account.store.ts` (`createAccountStore`, `:43-182`; `addAccount` `:108`, `updateAccount` `:119`, `archiveAccount` `:129`, `unarchiveAccount` `:142`, `adjustBalance` `:155`, `confirmBalanceReviewed` `:165`)
- Change: one closure inside the `create((set, get) => ...)` body, `writeThenReload<T>(tag: string, write: () => Promise<T>): Promise<T>`: `try { await write() } catch { console.error('[accountStore] <tag> failed:', err); throw err }`, then `await get().loadAccounts().catch(() => undefined)`, then return the write's value. The six actions become one-line calls to it (`addAccount` returns the repo's `Account`; the other five return `void`). The create callback turns from an expression body into a block that declares the closure and returns the object. `loadAccounts` (`:53-75`) is untouched; it already sets `loadError` and logs. No repo call, log tag or `AccountNameTakenError` path changes.
- Test: `__tests__/account.store.test.ts`, one case per action in its existing `describe`, shaped on `archiveAccount`'s `'resolves when the write lands and only the reload after it fails'` (`:562-570`): `getAll` rejects; `addAccount(baseInput)` resolves to `mockAccount`, `updateAccount`, `adjustBalance`, `confirmBalanceReviewed` resolve to `undefined`; the repo method was called with its arguments; `loadError` is `true`. All four fail at base (each rejects today). Add `expect(repo.getAll).not.toHaveBeenCalled()` to the three write-failed cases that lack it (`'propagates errors thrown by repo.add'`, `'propagates errors from repo.update'`, `'propagates errors from repo.adjustBalance'`; `confirmBalanceReviewed` has it at `:673`). The archive and restore cases at `:562` and `:614` are the regression guard that those two keep their behaviour. In-app add needs no caller change: `use_account_form.hook.ts:53-54` runs `markInserted` once `addAccount` resolves. Its caller case, `__tests__/screens/accounts/add_account.hook.test.ts`: add `let mockLoadError = false` to the mocked store in `setup()` (`:27-34`, `loadError: mockLoadError`), cleared in `beforeEach`; one case, `'a committed add whose reload failed pops once with no save error and inserts once'`: `mockAddAccount` resolves without republishing and sets `mockLoadError = true`; two `handleSave` taps; expect `mockAddAccount` once, `mockBack` once, `state.errorMessage` undefined, `useAccountFormState.getState().inserted` true. A guard, not a gate: it passes at base and fails only if the form or `useAddAccountApp` starts reading `loadError` into `failSave`.

### 2. Onboarding N2 retries the reload once after a committed add, and a second failure shows the accounts list's failed-load line without a second insert
- File: `src/modules/onboarding/screens/onboarding/add_account/add_account.hook.ts` (`onSaved`, `:32-47`)
- Change: after the existing `busy` decline (`:34`) and before the step resolves (`:37`): `if (useAccountStore.getState().loadError)`, take `const session = useAddAccountTransitionState.getState().begin()` (non-null, `busy` was just false), `await useAccountStore.getState().loadAccounts()` inside a `try`; on rejection `fail(session, Strings.accountsReadErrorTitle)` and `return false`; on success `settle(session)` and fall through. Invariants: the retry runs before `resolveOnboardingStep` reads `accounts.length` (`:39`), or a stale empty list resolves N2, writes it, and `finishSave` locks the form on N2; both exits of the retry clear `busy` (`fail` and `settle` do, `onboarding_transition.state.ts:42-50`), or the `busy` guard at `:34` and `:52` swallows every later tap; the decline returns `false`, so `declineSave` (`account_form.state.ts:50`) keeps `inserted` and the next tap enters `onValid` past the insert (`use_account_form.hook.ts:49,73`). `Strings` is already imported; `accountsReadErrorTitle` (`strings.ts:339`) is the title the accounts list renders in its failed-load state (`list/index.tsx:69`); no new copy. `handleSave`'s `reset()` (`:54`) clears the line on the next tap.
- Test: `__tests__/screens/onboarding_add_account.hook.test.ts`. Add `let mockLoadError = false` to the mocked store in `setup()` (the factory's fields at `:38-40`, `loadError: mockLoadError` after `:40`), cleared in `beforeEach`. Three cases, all failing at base since `loadAccounts` is never called there and no line is set:
  - `'a committed add whose reload failed retries the reload once and moves to N3 when it succeeds'`: `mockAddAccount` resolves and sets `mockLoadError = true` without republishing; `mockLoadAccounts` resolves, sets `mockLoadError = false` and republishes the row. Expect `mockAddAccount` once, `mockLoadAccounts` once, `mockSetStep(N3)`, `mockReplace('/(onboarding)/more_accounts')`, `statusMessage` falsy.
  - `'a committed add whose reload failed twice stays on N2 with the failed-load line and no step write'`: `mockLoadAccounts` rejects and leaves `mockLoadError` true. Expect `mockAddAccount` once, `mockSetStep` not called, `mockReplace` not called, `statusMessage === Strings.accountsReadErrorTitle`, `saving` false, `useAddAccountTransitionState.getState().busy` false.
  - `'the tap after a failed reload retry reloads again and never inserts again'`: continue the previous case with `mockLoadAccounts` resolving on the second call; second `handleSave`; expect `mockAddAccount` once, `mockLoadAccounts` twice, `mockSetStep(N3)`, `mockReplace` once.

### 3. The active detail closes its edit, its adjust sheet or its review and returns to the accounts list when the write landed and the reload failed
- File: `src/modules/accounts/screens/accounts/detail/account_detail.hook.ts` (`handleSave` `:218-227`, `handleAdjustBalance` `:229-239`, `handleConfirmBalanceReviewed` `:296-309`; `ACCOUNTS_LIST` `:31`; the restore precedent `:283-288`)
- Change: in each handler the write stays inside its `try`/`finally` and everything after it moves outside, in archive's shape (`:254-256`). `handleSave`: `try { await updateAccount(...) } finally { setSaving(false) }`, then `setEditing(false)`, then `if (useAccountStore.getState().loadError) router.dismissTo(ACCOUNTS_LIST)`. `handleAdjustBalance`: the same with `setAdjustVisible(false)`. `handleConfirmBalanceReviewed`: add `return` to its `catch` (`:303-305`), then after the `finally` the same `loadError` pop. Invariants: `setEditing(false)` runs before `dismissTo`, since the `beforeRemove` listener (`:103-108`) reads `isEditing` live and cancels the removal while it is true; a rejected write still propagates out of `handleSave` and `handleAdjustBalance` past the `finally`, so the adjust sheet's own `catch` (`adjust_balance_sheet.tsx:134-136`) and the H5 test keep their behaviour; the review's failure line is set only in the `catch`. No toast: none of the three has a success toast today and no copy changes.
- Test: `__tests__/screens/accounts/account_detail.hook.test.ts`. Replace `updateAccount: jest.fn()` (`:114`) with a module-level `mockUpdateAccount` next to `mockAdjustBalance` (`:175`); `accountLoadError` (`:108,113`) is already the reload-failure switch. Three cases, each `mockX.mockImplementationOnce(async () => { accountLoadError = true; })` with `mockAccounts([mkAccount()])`, all failing at base since nothing there calls `dismissTo` on these paths:
  - `'closes the edit and pops to the list when the update landed and the reload failed'`: `await act(() => result.current.handleSave())` (the form's defaults come from the account, so the edit schema passes). Expect `mockSetEditing(false)` with an `invocationCallOrder` below `mockDismissTo`'s, `mockDismissTo.mock.calls` equal to `[['/accounts']]`, `mockSetSaving` last `false`, `mockBack` not called.
  - `'closes the adjust sheet and pops to the list when the adjustment landed and the reload failed'`: `handleAdjustBalance(1500)` resolves; `mockSetAdjustVisible(false)` before `mockDismissTo('/accounts')`; `mockSetAdjusting` last `false`.
  - `'pops to the list with no review failure line when the review landed and the reload failed'`: `handleConfirmBalanceReviewed()`; `mockSetBalanceReviewError.mock.calls` equal to `[[undefined]]`; `mockDismissTo.mock.calls` equal to `[['/accounts']]`; `mockSetConfirmingBalanceReview` last `false`.
  - `'keeps the edit open and stays on the screen when the update rejects'`: `mockUpdateAccount.mockRejectedValue(failure)`; `await expect(result.current.handleSave()).rejects.toBe(failure)` (RHF 7.87.0 rethrows from `handleSubmit`, `node_modules/react-hook-form/dist/index.esm.mjs:3259-3260`); `mockSetEditing` not called with `false`, `mockDismissTo` not called, `mockSetSaving` last `false`. A guard, not a gate: it passes at base and fails only if the move out of the `try` runs the close or the pop on a rejected write.
  - Add `expect(mockDismissTo).not.toHaveBeenCalled()` to `'closes the adjust sheet on a successful balance adjust'` (`:323`) and `'confirms the legacy card balance without changing its amount'` (`:288`), the no-pop pair for a reload that succeeded.

## Non-goals
- Archive and restore: MA-065 (#479), MA-046 (#447); their store cases and hook cases stay as they are.
- Retry UI on N2 or the detail beyond the list's Try again: MA-026 (#400). N2 shows the line and the CTA retries the reload.
- An edit failure line, or anything in edit beyond the close on a failed reload: MA-019 (#385). `handleSave`'s rejection keeps propagating to `void handleSave()` at `detail/index.tsx:142`.
- A retry-once in the in-app add form or in `use_account_form.hook.ts`: the list's failed-load state is the in-app recovery.
- N3 and N4 reading `loadError`: the N2 retry is what keeps them from mounting over an empty stale list.
- New strings, a toast for add, edit, adjust or review, a `CategoryReloadError`-style typed error for the account store.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- Gate check at base: `npx jest __tests__/account.store.test.ts __tests__/screens/onboarding_add_account.hook.test.ts __tests__/screens/accounts/account_detail.hook.test.ts` with the new cases on `82c465d6` fails the ten named cases; `test -f` each path first.

## Risks
- MA-019 (#385) edits `account_detail.hook.ts`; whichever lands second rebases `handleSave` and its test.
- Step 2 latches the transition state's `busy` for the reload retry; a throw between `begin()` and `fail`/`settle` (none today: the only awaited call is inside the `try`) would dead-lock the CTA through the guards at `:34` and `:52`.
- `dismissTo('/accounts')` on a detail reached without the list in the stack (dashboard) relies on expo-router pushing the route; the restore precedent at `:286` already does.
- The N2 line is `accountsReadErrorTitle`; if the ticket meant `accountsReadErrorDescription`, only the constant in step 2 and its test assertion change.
- Amended after review round 1: step 3 gained the edit's write-failed guard case (Acceptance 5, fourth action) and step 1's `confirmBalanceReviewed` cite moved from `:648` to `:673`.
- Amended after review round 2: step 1 gained the in-app add caller's resolved-write-failed-reload guard case; the restore cite moved from `:610` to `:614`; the onboarding factory cite reads `:38-40`.

## Self-assessment
Step 2 is the one I am least sure about. The retry and the decline are settled by the acceptance line, but the line itself has to reach N2's footer through a state that exists for step transitions, so the plan takes a `begin()`/`fail()`/`settle()` session on `useAddAccountTransitionState` for a reload that is not a step write. The alternative, a decline-with-message on `account_form.state.ts`, changes the shared form contract for a case only onboarding has; and the ticket's copy rule leaves only existing strings, so the title of the list's failed-load state is the closest fit. If the reviewer prefers the form-state route, steps 1 and 3 stand unchanged.
