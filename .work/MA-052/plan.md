# MA-052 — Commitment detail: no spinner flash on a store publish and an error state (audit M31)
base: 662bb98ac4bab1bcbd66ad30a887dc829fbba134 · verify: emulator · flags: none · expected diff: ~220 lines

## Steps
### 1. The view-state fold and the status overlay are pure functions
- File: `src/modules/commitments/screens/commitments/detail/detail.helpers.ts` (new; same anatomy slot as `src/modules/transactions/screens/transactions/detail/detail.helpers.ts:40-52`)
- Change: export `type CommitmentDetailViewState = 'loading' | 'notFound' | 'firstLoadError' | 'refreshErrorWithData' | 'ready'` and `resolveCommitmentDetailViewState({ hasCommitment, status, hasRows, refreshError })`: `!hasCommitment` → `notFound` first, whatever `status` is; `status === 'loading'` → `hasRows ? 'ready' : 'loading'`; `status === 'firstLoadError'` → `hasRows ? 'refreshErrorWithData' : 'firstLoadError'`; `refreshError && hasRows` → `refreshErrorWithData`; else `ready`. Export `overlayStorePayments(rows, storePayments)`: returns `rows` with every row whose `id` the store's month array also holds replaced by the store's copy, order and length unchanged, and returns `rows` itself when nothing matched. The store's array is the optimistic one (`commitment.store.ts:259-275`, `:291-302`), so the history row and `CurrentCycleCard` read one status while the re-query is in flight.
- Test: `__tests__/screens/commitments_detail.helpers.test.ts` (new): a table over the fold (notFound beats loading and error; warm loading is ready; cold error is firstLoadError; warm error is refreshErrorWithData; zero-row success is ready); overlay replaces a Due history row with the store's Paid row, leaves unmatched rows untouched, keeps order, returns the same reference when nothing matches.

### 2. The two error titles exist
- File: `src/constants/strings.ts` (the C4 Detail block, `:1027-1047`)
- Change: add `commitmentsDetailLoadErrorTitle: 'Could not load this commitment.'` and `commitmentsDetailRefreshErrorTitle: 'Could not refresh this commitment.'`, the transaction detail's form (`:1225-1226`). Retry reuses `commitmentsLoadRetry` (`:1046`).
- Test: none, a string table.

### 3. The data slot knows which commitment its rows belong to
- File: `src/modules/commitments/screens/commitments/detail/detail.store.ts`, and the one call site `detail.hook.ts:116`
- Change: entry becomes `{ commitmentId: string | undefined; allPayments: CommitmentPayment[] }`, `INITIAL_DATA_ENTRY` has `commitmentId: undefined` and the same frozen `EMPTY_PAYMENTS`; `setAllPayments(owner, commitmentId, payments)` writes both. It stays the data slot's begin action, no released-owner guard. The hook call passes `commitment.id` so the branch compiles.
- Test: `__tests__/screens/commitments_detail.store.test.ts`: all nine `setAllPayments` calls take the new signature (`:43, :51, :53, :62, :65, :74, :75, :85, :94`); `:45` pins the new entry shape `{ commitmentId, allPayments }`, `:68` keeps its `.allPayments` read; add "rows written for one commitment read as not owned by another id" (a pure read of `entryOf(owner).commitmentId`); `:103-106` unchanged.

### 4. The UI slot carries a load status, a refresh error, and the skip outcome
- File: `src/modules/commitments/screens/commitments/detail/detail.state.ts`
- Change: entry becomes `{ status: 'loading' | 'ready' | 'firstLoadError'; refreshError: boolean; reloadKey: number; skipConfirmVisible: boolean; skipBusy: boolean; skipError: boolean }`, `INITIAL_UI_ENTRY` = `loading, false, 0, false, false, false`. Actions: `beginLoad(owner, preserveData)` creates the entry when absent (the begin action, no guard, as `setViewState` today) and sets `status: preserveData ? 'ready' : 'loading', refreshError: false`; `resolve(owner)` sets `status: 'ready', refreshError: false`; `failLoad(owner, preserveData)` sets `refreshError: true` when warm, `status: 'firstLoadError'` when cold; `bumpReload(owner)` increments `reloadKey`; `setSkipBusy`, `setSkipError` alongside `setSkipConfirmVisible`. `resolve`, `failLoad`, `bumpReload` and the three skip setters return `state` unchanged when `owner` is not in `entries`, the `:38-42` shape. Keep `setViewState` and `DetailViewState` in this step so `detail.hook.ts` still compiles; step 5 deletes them.
- Test: `__tests__/screens/commitments_detail.state.test.ts`: rewrite `:16-20` and `:64-72` to `beginLoad` and the new entry shape; add: warm `beginLoad` keeps `ready` and clears `refreshError`; cold `failLoad` reads `firstLoadError`; warm `failLoad` keeps `ready` with `refreshError` true; `resolve` after `failLoad` clears it; `resolve`, `failLoad`, `bumpReload`, `setSkipError` on a released owner leave `entries` the same reference; sibling isolation tests (`:22-42`, `:74-83`) keep their assertions on the new actions.

### 5. A publish re-queries the history in place; a failure sets a state; skip failure stays in the sheet
- File: `src/modules/commitments/screens/commitments/detail/detail.hook.ts` (`useCommitmentDetail`, load effect `:105-128`, `viewState` `:99-103`, `skipPayment` `:164-172`)
- Change: the effect keeps its deps (`commitment?.id`, `payments`, `owner`, the actions) and adds `reloadKey`. When `!commitment` it writes nothing and returns; the fold puts `notFound` first. Otherwise: read the data entry off `useCommitmentDetailStore.getState()`, `preserveData = entry.commitmentId === commitment.id && entry.allPayments.length > 0`, `beginLoad(owner, preserveData)`, query, and under `!cancelled`: `setAllPayments(owner, commitment.id, rows)` then `resolve(owner)`; on rejection keep the `console.error('[commitmentDetail] getPaymentsByCommitment failed', err)` line and `failLoad(owner, preserveData)`. Rows are never cleared. In render: `ownsRows = dataEntry.commitmentId === commitment?.id`, `rows = ownsRows ? allPayments : INITIAL_DATA_ENTRY.allPayments`, `historyRows = overlayStorePayments(rows, payments)` in a `useMemo`; `viewState = resolveCommitmentDetailViewState({ hasCommitment: !!commitment, status, hasRows: rows.length > 0, refreshError })`. Delete `setViewState`, `DetailViewState`, `screenViewState` and the `:99-103` memo; `state.allPayments` now returns `historyRows`. `skipPayment`: `setSkipError(owner, false)`, `setSkipBusy(owner, true)`, await the store skip, on success `setSkipConfirmVisible(owner, false)`, on rejection keep the log line and `setSkipError(owner, true)`, `finally setSkipBusy(owner, false)`; `confirmSkip` clears `skipError` before opening. New returns: `state.refreshError`, `state.skipBusy`, `state.skipError`, `state.floatingOffset: stackedPrefix ? 'edge' : 'tabBar'` (the tabbed copy sits under the tab bar, `src/modules/navigation/screens/tabs/index.tsx:45`; the stacked twin does not), and `reload = () => bumpReload(owner)`. `currentPayment` and `findCurrentPayment` stay as they are.
- Test: `__tests__/screens/commitments_detail.hook.test.ts`: `:164-182` stands. Rewrite `:229-266`: after the skip and the rejected re-query the copy reads `refreshErrorWithData`, `useCommitmentDetailStore.getState().entries[owner].allPayments` is `rentHistory` by reference (the data slot kept its rows), `state.allPayments[0].status` is Skipped (the overlay from step 1 reads the store's `:255` publish), `skipConfirmVisible` false. Add: a `payments` identity change with rows on screen never reads `loading` between the publish and the resolve (deferred query, assert `ready` while pending, rows unchanged, then the new rows); the first query rejects → `firstLoadError`, `reload()` with a resolving mock → `ready` with rows, `reload()` with a rejecting mock → still `firstLoadError`; a warm failure then `reload()` success → `ready`, `refreshError` false; the route param moves to `pay-2` (`otherCommitment`) → `loading` with `allPayments` `[]` until the second query resolves, and the first commitment's late result writes nothing; while the deferred re-query is pending after `paymentsState` carries `pay-1` as Paid, `state.allPayments[0].status` is Paid; skip rejects → `skipConfirmVisible` true, `skipError` true, `skipBusy` false, `allPayments` unchanged, `mockGetPaymentsByCommitment` not called again; a second `skipPayment()` that resolves closes the sheet. `:268-395` stand, including `:355-373` and `:375-394`.

### 6. The screen renders the two error states and stays mounted on a refresh
- Files: `src/modules/commitments/screens/commitments/detail/components/detail_load_error.tsx` (new), `src/modules/commitments/screens/commitments/detail/index.tsx`
- Change: `DetailLoadError({ floating?, floatingOffset, onRetry })` wraps `LoadErrorAlert` (`src/components/ui/load_error_alert.tsx`): `mode` `'floating'` with `floatingOffset` or `'fill'`, title from step 2 by `floating`, `retryLabel={Strings.commitmentsLoadRetry}`, `testID="commitment-detail-load-error"`. `index.tsx`: header `right` renders Edit when `state.commitment` is set and `viewState` is `ready`, `refreshErrorWithData` or `firstLoadError`, not `loading` (`:28` hides it under the cold spinner today and Acceptance keeps that); a warm refresh reads `ready`, so it never drops; `loading` renders `LoadingCenter` (`src/components/ui/loading_center.tsx`) in place of the raw `ActivityIndicator` (`:42-46`); `firstLoadError` renders `<DetailLoadError onRetry={reload} />` under the header; the `ScreenScroll` tree renders for `ready` and `refreshErrorWithData`, with `<DetailLoadError floating floatingOffset={state.floatingOffset} onRetry={reload} />` as a sibling after it when `refreshErrorWithData`; `PaymentHistory` keeps `payments={state.allPayments}`; `SkipConfirmSheet` gets `busy={state.skipBusy}` and `errorMessage={state.skipError ? Strings.commitmentsSkipError : undefined}` (`commitments/index.tsx:281` shape). Not-found block `:48-54` unchanged.
- Test: none, `.claude/rules/tests.md:19` forbids a new render suite; the fold is covered in step 1 and the hook in step 5.

## Screens
- Commitment detail, tabbed copy (commitments tab → a commitment), recorded through the whole action: Mark as paid → confirm, then Skip → confirm on another cycle. Assert no spinner frame, the header Edit action present in every frame, the history showing the paid or skipped row and then the next cycle's row, scroll position unchanged.
- Both error walks use one source force on `getPaymentsByCommitment` (`commitment.repository.ts:168-171`) that counts its calls and decides by count, applied before launch and never edited while a detail is mounted: a Fast Refresh of the repository re-executes `commitment.store.ts` and `detail.hook.ts` up to the component boundary, both Zustand stores re-create empty, and the mounted copy reads `notFound` before Retry is tapped.
- Commitment detail, cold error: force rejects call 1, passes after. Open a detail: shoot the fill alert under the header with Edit present; tap Retry, the history renders. For "another failure keeps the alert", a second launch with the force rejecting calls 1 and 2: Retry once keeps the alert, Retry again renders.
- Commitment detail, warm error: force passes call 1, rejects calls 2 and 3, passes from 4. Open a detail with rows, then Mark as paid (or Skip) on that mounted detail: the optimistic publish is call 2 and the snapshot publish after housekeeping (`commitment.store.ts:166-196`) is call 3, both rejected, so the alert survives until Retry. Leaving the screen is not a trigger: back pops the Stack (`src/app/(app)/(tabs)/commitments/_layout.tsx:7`), the release effect (`detail.hook.ts:130-135`) drops both entries, and the return is a cold mount. Confirm two `[commitmentDetail] getPaymentsByCommitment failed` lines in the console, shoot the floating alert over the rows above the tab bar with the paid row showing, tap Retry (call 4, passes): it clears, no third console line.
- First mount, no rows: the spinner (now `LoadingCenter`) once, then the history.
- Skip failure in the sheet, the per-copy holds, the stale drop and the status overlay: unit tests, not emulator.

## Non-goals
- The pay sheet's slot (MA-050, #452) and the edit screen's slots (MA-051, #453); the account lookup slot (MA-043, #441).
- `commitment.store.ts` and `__tests__/commitment.store.test.ts`: untouched. No `generation` dependency, no focus effect, no store field.
- The redundant second query per action and a staleness gate on the list's focus reload (audit M32); the unbounded history render.
- Removing or repurposing `currentPayment` / `findCurrentPayment` (`detail.hook.ts:47-59`, `:148`): dead, left alone.
- A refreshing indicator or `revalidating` flag: the ticket wants nothing to move on a refresh.
- Any change to `SkipConfirmSheet`, `ConfirmSheet`, `PaymentHistory`, `PaymentRow`, `CurrentCycleCard`, `LoadErrorAlert`, `ErrorState`.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- Gate that fails at base: `npx jest __tests__/screens/commitments_detail.hook.test.ts __tests__/screens/commitments_detail.state.test.ts __tests__/screens/commitments_detail.store.test.ts __tests__/screens/commitments_detail.helpers.test.ts` (the helpers path does not exist at base, so `test -f` it before citing the command anywhere else).

## Risks
- `reloadKey` in the UI entry is the retry trigger, read as the transaction detail's `bumpReload` (`detail.state.ts:33`, `detail.hook.ts:224`). If the reviewer reads the ticket's "no counter dependency" as covering it too, retry has to call an extracted `runQuery` directly with a request stamp in a ref; step 5 changes, nothing else.
- The status overlay assumes the store's month array is never staler than the history query for a row both hold. `loadMonthSnapshot` publishes after housekeeping and the detail re-queries on that publish, so both read the same rows; a future writer that mutates `commitment_payments` without republishing `payments` breaks the assumption silently.
- MA-050 (#452) edits `detail.hook.ts` and `index.tsx` on adjacent lines; the second to merge rebases and re-runs the render pass.
- The `notFound`-first fold changes precedence over today's `:100-101` (loading beat notFound); the hook test `:183-186` passes either way, so nothing pins the old order.
- A commitment whose history query legitimately returns zero rows re-shows the spinner on every publish (`hasRows` false). The route's own payment is always a row of that commitment, so the case does not occur in the app.
- Amended on review: step 6 keeps Edit hidden under the cold spinner (finding 1); the Screens error walks use one call-counting force applied before launch, since a Fast Refresh of the repository resets both stores on a mounted copy (finding 2). Round 2: the warm walk triggers the publish on the mounted detail and rejects calls 2 and 3, since back pops the Stack and the snapshot publish would clear a single rejection (finding 1); the `:229-266` rewrite asserts the data slot by reference and the overlaid status separately (finding 2); step 3's test row names all nine `setAllPayments` calls (finding 3).

## Self-assessment
Step 5's effect is the step I am least sure about, on two points. First, `preserveData` reads the data entry off `getState()` inside the effect while `rows` in render reads the subscribed entry; both key on `commitmentId`, but the hook test that moves the route param to another commitment is the only thing proving the copy shows `loading` with no rows rather than the old commitment's rows, and that test drives the param through the `mockParamsContext` wrapper, which no existing test re-renders with a changed value. Second, the overlay: `overlayStorePayments` runs on every render against the whole month array; the array is small, but if the reviewer wants it memoised on `[rows, payments]` only, that is a `useMemo` dep choice the implementer makes, not a plan change.
