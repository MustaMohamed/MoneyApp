# MA-074 — Edit data path: update writes the credit columns, one credit validation shared by add and edit, archived names refused
base: 36e32dc01e91609c4a59d8cb9533215bc9bd898d · verify: none · flags: money path · expected diff: ~220 lines

## Steps

### 1. Add and edit validate the credit fields through one function
- File: `src/modules/accounts/utils/credit_fields.schema.ts` (new); `src/modules/accounts/utils/add_account.schema.ts` (`createAddAccountSchema`, the `superRefine` body from the `selected_type` gate at `:48` to the end)
- Change: the new file exports `CreditFieldValues = { interest_tracking: boolean; credit_limit?: string; apr?: string; min_payment?: string; due_day?: string }` and `addCreditFieldIssues(data: CreditFieldValues, ctx: z.core.$RefinementCtx<CreditFieldValues>, comparand: { isCreditCard: boolean; owed: number | undefined })`; zod 4.5.4 has no `z.RefinementCtx`, `superRefine`'s context is `core.$RefinementCtx<output>` (`node_modules/zod/v4/classic/schemas.d.ts:40`), and the add schema's `$RefinementCtx<AddAccountFormData>` passes because `value` is covariant and `AddAccountFormData` is structurally a `CreditFieldValues`. If that assignment fails, type `ctx` as `{ addIssue: (issue: z.core.$ZodRawIssue) => void }` instead. The body is the add schema's credit block moved verbatim, with two substitutions: `data.selected_type !== AccountType.CreditCard` becomes `!comparand.isCreditCard`, and `parseNonNegativeDecimal(data.balance)` becomes `comparand.owed` (the minimum payment is compared only when `owed !== undefined`, as today). Issue paths, messages and order stay as at `:51-122`. `createAddAccountSchema` keeps the name issue and then calls `addCreditFieldIssues(data, ctx, { isCreditCard: data.selected_type === AccountType.CreditCard, owed: parseNonNegativeDecimal(data.balance) })`. `AddAccountFormData` is unchanged.
- Test: `__tests__/add_account.schema.test.ts`, unchanged, is the gate: 53 cases green before and after, `grep -c "^\s*it(" __tests__/add_account.schema.test.ts` reads 53 on both.

### 2. The widened edit schema knows every credit fault and every archived name
- File: `src/modules/accounts/utils/edit_account.schema.ts`
- Change: a second export beside `createEditAccountSchema`, which stays byte-identical. `createEditAccountFormSchema(accounts: Account[], archivedAccounts: Account[], account: Pick<Account, 'id' | 'type' | 'current_balance'>)` returns `z.object({ name, color: z.string(), interest_tracking: z.boolean(), credit_limit, apr, min_payment, due_day })` with the four credit strings `z.string().optional()` as the add schema has them, `name` trimmed with `errNameRequired` and `errNameTooLong` as at `:11-15`, then one `superRefine`: the duplicate issue on `name` with `Strings.errNameDuplicateNamed(data.name)` when `!isBlankName(data.name) && isAccountNameTaken([...accounts, ...archivedAccounts], data.name, account.id)`, then `addCreditFieldIssues(data, ctx, { isCreditCard: account.type === AccountType.CreditCard, owed: account.current_balance })`. Export `EditAccountFormData = z.infer<ReturnType<typeof createEditAccountFormSchema>>`. The two lists are separate parameters so a caller cannot forget the archived one.
- Test: `__tests__/screens/accounts/edit_account_schema.test.ts`, a new `describe('createEditAccountFormSchema')` written first, the old describe untouched. Cases: an archived account's name (fixture `is_archived: 1`, diff case, surrounding spaces) refuses with `errNameDuplicateNamed` carrying the typed trimmed name; the edited account's own name passes; an active duplicate refuses with the same line; on a card with `current_balance: 8450`: min `9,000` → `errMinPaymentExceedsOwed`, `8,450` passes, limit blank → `errCreditLimitRequired`, limit `0` → `errCreditLimitPositive`, limit `abc` / min `5abc` / apr `abc` (tracking on) → `errAmountInvalid`, day `0`, `32`, `15.5` → `errDueDayRange`, apr blank with tracking on → `errAprRequired`, apr `100.01` → `errAprRange`, apr `abc` with tracking off passes; a card at `current_balance: 0` refuses min `1` and accepts min `0`; a Bank account with a leftover credit draft (`credit_limit: ''`, `min_payment: '9,000'`, `interest_tracking: true`, `apr: ''`) raises no issue at all. The fixture is `makeTestAccount` from `@/test_helpers/transaction`, not a hand literal.

### 3. The update writes name, colour, the five credit columns and the timestamp
- File: `src/modules/accounts/database/accounts.ts` (`updateAccount`, `:105-116`); `src/modules/accounts/repositories/account.repository.ts` (`UpdateAccountInput`, `:44-47`; `update`, `:102-105`)
- Change: `updateAccount(db, id, data: Pick<Account, 'name' | 'color' | 'credit_limit' | 'minimum_payment' | 'statement_due_day' | 'interest_tracking' | 'apr'> & { updated_at: string })`, `Account` from `../entities/account.entity`, which the query file already imports at `:5`; it imports nothing from the repository, which imports it. `UpdateAccountInput` in the repository is the same `Pick` without `updated_at`. The query runs one `UPDATE accounts SET name, color, credit_limit, minimum_payment, statement_due_day, interest_tracking, apr, updated_at WHERE id = ?`. No other column is named. `AccountRepository.update` stays a stamp-and-write with no rounding and no guard, as `add` is: rounding is the mapper's (step 5, ADR 2026-08-22 §5), refusal is the schema's (step 2). `src/database/accounts.ts`, `src/repositories/account.repository.ts`, `src/store/account.store.ts` and `src/modules/accounts/index.ts` re-export the names and need no edit.
- Test: `__tests__/account.repository.test.ts`, the `AccountRepository.update` describe, written first. The suite's `beforeEach` at `:27` becomes `DELETE FROM transactions` then `DELETE FROM accounts`: better-sqlite3 enables `foreign_keys` by default, and `transactions.account_id` / `to_account_id` reference `accounts(id)` (`004_create_transactions.ts:11-12`), so once this describe seeds a payment row the account-only delete throws for every describe after it. Seed a card through `repo.add` (`credit_limit: 5000`, `minimum_payment: 500`, `statement_due_day: 15`, `interest_tracking: 1`, `apr: 24.99`, `revolving_balance: 1200`, `opening_balance: 8450`) and a bank account, then `insertTransactionRow(db, makeTestTransaction({ type: TransactionType.CCPayment, account_id: bank, to_account_id: card, category_id: null, minimum_payment_snapshot: 500 }))` through the bridged `sqlite` handle. Read the card row, `repo.update(card, { name: 'After', color: '#3D7A5F', credit_limit: 7000, minimum_payment: 700, statement_due_day: 20, interest_tracking: 0, apr: null })`, read it again. Assert the row after equals the row before with exactly those seven fields replaced and `updated_at` different, so `opening_balance`, `current_balance`, `revolving_balance`, `is_archived`, `sort_order` are proven untouched by the equality, not by a list; assert the transaction's `minimum_payment_snapshot` still reads 500. Second case: tracking on with an APR writes `interest_tracking = 1, apr = 24.99`. The existing name/colour and timestamp cases gain the five fields in their payloads and stay.
- Note: this step and step 4 land in one commit; `tsc` fails between them because the hook and the store test still build the two-field payload.

### 4. The detail's inline save passes the stored credit values through the widened update
- File: `src/modules/accounts/screens/accounts/detail/account_detail.hook.ts` (`handleSave`, `:240-250`)
- Change: the payload becomes `{ name: data.name.trim(), color: data.color, credit_limit: account.credit_limit, minimum_payment: account.minimum_payment, statement_due_day: account.statement_due_day, interest_tracking: account.interest_tracking, apr: account.apr }`, read from the `account` the hook already resolves at `:125`; when `account` is undefined the save returns before the write, as it does for a missing `id`. `createEditAccountSchema(accounts, id)` at `:219` and the `errors.name` render in `detail/index.tsx:230-234` stay as they are.
- Test: `__tests__/screens/accounts/account_detail.hook.test.ts:396`, the `toHaveBeenCalledWith` payload gains the five stored values from `mkAccount` (`null, null, null, 0, null`), plus one case with `mkAccount({ type: AccountType.CreditCard, credit_limit: 5000, minimum_payment: 500, statement_due_day: 15, interest_tracking: 1, apr: 24.99 })` asserting they pass through unchanged. `__tests__/account.store.test.ts:508-540`, the four `updateAccount` payloads carry the seven fields and the first case asserts `repo.update` received exactly that object.

### 5. The edit form maps to the update through the add form's rounding layer
- File: `src/modules/accounts/components/account_form/account_form.helpers.ts`
- Change: `toUpdateAccountInput(data: EditAccountFormData, account: Pick<Account, 'type'>): UpdateAccountInput`, beside `toNewAccountInput`, reusing the module-private `optionalAmount`, `optionalDay`, `optionalPercent` so no `roundMoney` call site is added (ADR 2026-08-22 §6 check 1). `isCC = account.type === AccountType.CreditCard`; `credit_limit`, `minimum_payment`, `statement_due_day` are `isCC ? optional*(…) : null`; `interest_tracking: isCC && data.interest_tracking ? 1 : 0`; `apr: isCC && data.interest_tracking ? optionalPercent(data.apr) : null`, so tracking off and APR empty land in one object. `name: data.name.trim()`, `color: data.color`. `AccountFormMappingError` is not thrown here: the edit has no required amount, the credit limit's presence is the schema's refusal.
- Test: `__tests__/account_form.helpers.test.ts`, a `describe('toUpdateAccountInput')` written first: on a card, `credit_limit '5,000.005'` → `5000` (half-even at 2dp, `'5,000.015'` → `5000.02`), `min_payment '  7  '` → `7`, `due_day '15'` → `15`, tracking on + `apr '24.995'` → `25` (`'24.985'` → `24.98`); tracking off with `apr '24.99'` typed → `interest_tracking: 0, apr: null`; on a Bank account with every credit field filled and tracking on → the five columns `null, null, null, 0, null`; the returned object has exactly the seven `UpdateAccountInput` keys (`Object.keys(...).sort()`), never a balance.

### 6. The credit rows and the message rail accept either form's values
- File: `src/modules/accounts/components/account_form/credit_card_fields.tsx` (`CreditCardFieldsProps`, `:14-16`); `src/modules/accounts/components/account_form/field_message_rail.tsx` (`FieldMessageRailProps`, `:17-21`)
- Change: type-only. `FieldMessageRail<T extends FieldValues>({ control: Control<T>; name: FieldPath<T>; helper?: string })`; if `errors[name]` stops indexing on the generic, read the message through `control.getFieldState(name, formState).error?.message` from the same `useFormState` call. `CreditCardFields<T extends CreditFieldValues>({ form: UseFormReturn<T> })` (or the props typed `UseFormReturn<AddAccountFormData> | UseFormReturn<EditAccountFormData>`), with exactly one assertion inside the component, replacing `const { control } = form` at `:19`: `const control = form.control as unknown as Control<CreditFieldValues>` under one `oxlint-disable-next-line typescript/no-unsafe-type-assertion` line citing that `T extends CreditFieldValues` holds every name the file uses. Nothing else in the file moves: the nine `name=` literals (five on `Controller`, four on `FieldMessageRail`), the `useWatch({ control, name: 'interest_tracking' })` at `:20`, and every render prop's `value` keep their concrete types because `control` is `Control<CreditFieldValues>`; a generic `T` would leave `value` as `FieldPathValue<T, FieldPath<T>>` (`react-hook-form/dist/types/controller.d.ts:14`), which never narrows to the `string` `Input` at `:32` or the `boolean` `ControlField` at `:100` takes. The JSX is untouched; `credit_card_slot.tsx`, `account_form.tsx`, `use_account_form.hook.ts`, `account_form.state.ts` and `onboarding/add_account/index.tsx` are not edited and still pass `UseFormReturn<AddAccountFormData>`.
- Test: none at runtime, render tests are barred by `.claude/rules/tests.md`. The gate is `npm run typecheck` plus one type-level line at the top of the step 2 describe: `type _Assert = EditAccountFormData extends CreditFieldValues ? true : never; const _ok: _Assert = true;`.

### 7. Decision record
- File: `docs/adr/2026-09-16-account-edit-credit-columns.md` (new); `docs/adr/2026-09-09-account-unarchive-sort-position.md` (§3, `:36`)
- Change: the new ADR records, in the house shape (Date, Status, Ticket, Applies to, numbered sections): (1) one `UPDATE` names name, colour, the five credit columns and `updated_at` and nothing else, and `revolving_balance` keeps the three writers ADR 2026-09-09 §4 lists; (2) the credit-field check is one function with two comparands, typed balance and selected type on add, stored `current_balance` and stored type on edit, and why the card gate is the stored type (type is never editable, epic rule); (3) the edit path rounds in `account_form.helpers.ts` under ADR 2026-08-22 §5's grandfathered exception, adding no `roundMoney` site, and the named follow-up there now covers `toUpdateAccountInput` too; (4) a payment's `minimum_payment_snapshot` is read at the payment's write (`transaction.repository.ts:299-302`) and never rewritten, so an edit changes later payments only. In the 2026-09-09 ADR, §3's last sentence, "Widening the add and edit checks to archived accounts is off this milestone.", becomes "Widened for the edit check by MA-074 (#503), which passes the archived list beside the active one; the add check is widened by MA-076 (#505)."
- Test: none, documentation.

## Non-goals
- The edit screen, its route, footer, strings, `Strings.errNameDuplicate`'s retirement and the detail's inline edit: MA-075.
- The add form's duplicate check against archived names: MA-076 (#505). `createAddAccountSchema` keeps `isAccountNameTaken(accounts, …)` over the active list.
- `CreditCardSlot` stays typed to `AddAccountFormData`; MA-075 widens it if the edit screen renders the slot rather than the rows.
- No repository guard for tracking on with a null APR, and no rounding in `AccountRepository.update`: both would be a second rule beside the schema and the mapper, and `add` has neither.
- Adjusting the current balance (MA-027), any write to `current_balance`, `opening_balance` or `revolving_balance`.
- No migration, no string, no route, no native config.

## Verification
- Per commit: `npm run format:check && npm run lint && npm run typecheck && npm test -- --ci`
- Once, before hand-off: the full CI parity chain from `CLAUDE.md`.
- ADR 2026-08-22 §6 check 1: `git diff main...HEAD -U0 -- src __tests__ | grep -nE 'roundMoney\('` adds no call site in `src/`.

## Risks
- `Control<AddAccountFormData>` is not assignable to `Control<CreditFieldValues>` (function-typed properties, contravariant), so step 6 cannot avoid one type assertion; if the lint rule refuses even the disabled line at error severity, the union-of-two-forms shape is the fallback and MA-075 adds its form to the union.
- The hook test's mocked `useAccountStore` returns `accounts` only; step 4 reads the credit values from `account`, found in `accounts`, so the mock needs no `archivedAccounts`. If MA-075 moves the inline caller first, step 4 is already dead code and can be dropped.
- ADR 2026-08-22 §5 names the mapper rounding temporary; a reviewer may ask the update to round in the repository instead. The ticket's Goal fixes the mapper ("the rounding layer the add form uses"), and the ADR step records the extension of the follow-up.
- Amended after review: step 3's test moves the `DELETE FROM transactions` into the suite's `beforeEach` (FK on by default in better-sqlite3, the per-test delete protected one test only); step 3 types the query on the entity `Pick`, not the repository type (import direction); step 6 counts nine `name=` sites, not five; line cites fixed (`account` at `:125`, react-hook-form 7.87.0).
- Amended after round 2: step 6 drops the `field()` helper and the `FieldPath<T>` rewrite, the one assertion is on `form.control` itself so `useWatch` and every render prop's `value` keep their concrete types; step 1 names `z.core.$RefinementCtx<CreditFieldValues>`, `z.RefinementCtx` does not exist in zod 4.5.4.

## Self-assessment
Step 6 is the one I am least sure of. The ticket asks for a type-only change that lets the credit rows and the rail take the edit form's values while the add form, its hook and onboarding N2 stay untouched, and react-hook-form's `Control<T>` does not widen or narrow across two concrete form types without an assertion somewhere. The plan now puts the single assertion on `form.control` so the ten name sites and every render prop keep their concrete types, but I have not compiled it against `react-hook-form@7.87.0` in this tree, and `typescript/no-unsafe-type-assertion` is at error severity in `src/`, so the implementer may need the union prop shape rather than the generic one, and the rail's generic `getFieldState` read is likewise untested here. Everything else is a move (step 1), a new export beside an old one (steps 2, 5), or a column list (step 3), and the tests named for them can fail at base.
