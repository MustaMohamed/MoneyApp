// Stub — full content lands in Task 28.
import { Text, View } from 'react-native';

export default function TransactionDetailScreen() {
  return (
    <View>
      <Text>Transaction Detail (stub)</Text>
    </View>
  );
}
