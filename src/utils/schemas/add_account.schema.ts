// Backward-compat re-export; remove when all consumers are migrated to `@/modules/accounts`.
export { createAddAccountSchema } from '@/modules/accounts/utils/add_account.schema';
export type { AddAccountFormData } from '@/modules/accounts/utils/add_account.schema';
