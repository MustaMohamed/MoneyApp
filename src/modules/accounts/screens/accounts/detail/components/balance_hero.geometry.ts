import { ms } from '@/utils/responsive';

/** Canvas `.id-bal .cur`: the currency code sits 6pt off the magnitude at 80% opacity (C1, C2b). */
export const HERO_CURRENCY_GAP = ms(6);
export const HERO_CURRENCY_OPACITY = 0.8;
