// backward-compat re-export — remove when all consumers are migrated to @/modules/currency
export {
  CurrencyStore,
  createCurrencyStore,
  currencyStore,
  useCurrencyStore,
} from '@/modules/currency/store/currency.store';
