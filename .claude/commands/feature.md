---
description: Kick off a new feature through the superpowers flow with the team
---

@sarah Plan and ship the following feature through the full superpowers flow:
brainstorm → design doc (`docs/superpowers/specs/YYYY-MM-DD-{feature}-design.md`)
→ plan (`docs/superpowers/plans/YYYY-MM-DD-{feature}.md`)
→ 🛑 Gate 1 (plan approval) → execute → 🛑 Gate 2 (code review).

Consult `[marcus]` and `[layla]` inline during brainstorming.
Dispatch `@marcus`, `@layla`, `@tariq`, `@dev` for file-producing work.
Stop at each gate and wait for human approval.

$ARGUMENTS
