---
description: Kick off a new feature through the superpowers flow with the team
---

@sarah Plan and ship the following feature through the full superpowers flow (autonomous team mode — see CLAUDE.md):
brainstorm → design doc (`docs/superpowers/specs/YYYY-MM-DD-{feature}-design.md`)
→ 🛑 spec sign-off (user) → plan (`docs/superpowers/plans/YYYY-MM-DD-{feature}.md`, you approve)
→ execute in a worktree → code review (@tariq returns verdict + merge recommendation)
→ 🛑 device QA (user).

Consult `[marcus]` and `[layla]` inline during brainstorming.
Dispatch `@marcus`, `@layla`, `@tariq`, `@dev` for file-producing work.
Stop only at the two user gates and on critical triggers. Merging or pushing requires an explicit user request.

$ARGUMENTS
