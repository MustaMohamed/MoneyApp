---
id: MA-004
scope: MA-onboarding-redesign
milestone: M1
issue: 206
verify: emulator
branch: claude/ma-004-task-wkjda9
pr: https://github.com/MustaMohamed/MoneyApp/pull/217
---

# MA-004 — Onboarding shell: four fixed tracks and an honest busy CTA

## Summary

Give all four onboarding routes one shared chrome — header, progress rail, content viewport, and a footer whose status track and CTA never move — and make a busy button say what it is actually doing.

## Details

Today the four onboarding routes each assemble their own chrome, share a row of progress dots, and put their CTA in a bar that differs between screens. The approved design freezes one shell for all four: a fixed-height header, a fixed-height progress rail, a flexible scrolling content viewport, and a footer built from a permanently mounted status track above the primary action. Exact heights and behaviour are in `spec.md` § *Product & UX — Shell*; the reference frames are mockup A1, A2 and A3.

**The status track is the load-bearing part.** It is always mounted and never empty. When nothing is wrong it carries the one footnote that screen genuinely wants — a different line per screen, listed in the spec — and when a step fails, the error replaces that footnote **in the identical box**. The footer does not grow, the button does not move, and retry is the primary action itself, so a second button never appears. It is announced as a live region so a screen reader hears a failure without focus jumping. It is plain text, not a bordered alert component: an alert brings its own padding and would make the footer taller in exactly the state the track exists to keep still.

**The status track is delivered here and fed by later tasks.** Nothing in this task produces a real failure message — the four routes still hold their current bodies and their current save paths. This task proves the track can hold and swap a message; MA-005, MA-008, MA-009, MA-010, MA-011 and MA-012 are the tasks that put real failures into it. Force a message through it in the emulator run rather than waiting for a real one.

**The progress rail** shows four segments and a label reading which step of four this is plus that step's name. Advancing changes a segment's colour, never its width. The segments themselves are hidden from assistive technology; the label carries the meaning.

**The welcome route's header is a variant, not a different header.** It swaps the back affordance for the app mark and wordmark plus a small right-aligned setup label, at the same track height as every other route. It consumes the in-app mark delivered by MA-003.

**This task changes no navigation.** Which route the user lands on, when the step is persisted, and whether a swipe-back can desynchronise it are MA-005's, deliberately kept out of this diff so that a chrome review is a chrome review. Walking the flow here should reach the dashboard exactly as it does today, by exactly the route it does today.

### A busy button must stop lying

Every primary action in the app currently reads "Loading..." while it works, because the shared button hardcodes that string. The design needs "Saving…" on the account form and "Opening…" on the finish button. One optional label, supplied by the caller, fixes it everywhere; callers that supply nothing keep exactly today's behaviour.

The same change adds the spinner the design shows on a busy button. The shared button accepts a loading flag today and does nothing with it beyond swapping the label — the flag never reaches the underlying primitive, which has no such concept.

**Understand the blast radius before planning this.** Roughly a dozen call sites across the app pass that loading flag today — confirmation dialogs and sheets, the commitment form, the pay sheet, the transaction form, the category sheet, account detail, adjust-balance, balance-review, the startup error screen. **Every one of them gains a spinner in this task**, and none of them is an onboarding screen. That is why this task runs on the emulator rather than resting on a unit test, and why its device QA at gate 3 has to leave onboarding and press a saving button somewhere else in the app.

**The gold gradient on the primary button stays.** The mockup draws a flat accent fill; the shipped wrapper paints a two-stop gold gradient underneath. Keeping the wrapper is a recorded, deliberate deviation — flattening it would re-skin every CTA in the app, which `scope.md` excludes. See `spec.md` § *Known design/codebase disagreements*, item 1. Do not "fix" it here.

**All four routes adopt the shell in this task, with their current content unchanged.** They will look transitional — new chrome, old bodies — and that is fine; each body is rebuilt in its own task. Merging this alone leaves a working app. The progress-dots component the four routes share today has no other consumer and goes with them.

**Done when:** all four onboarding routes render the same four tracks at the same offsets; the status track holds its per-screen footnote and swaps to a forced error message without the button moving a pixel; a test asserts the shell's track heights against the named geometry values from MA-001; a busy button renders a spinner and a caller-supplied label, and a test covers both the supplied and the omitted case; a busy button **outside onboarding** has been watched on the emulator with its new spinner; and walking the whole flow reaches the dashboard exactly as it does today.

## Plan

### Summary

- Add one geometry token (`Size.progressRail`) and the shell's copy to `strings.ts`. Everything else the shell measures itself against already exists: `Size.headerHeight`, `Size.statusTrack`, `Size.ctaHeight` (`src/constants/theme.ts:136-209`).
- Split the busy-button decision out of `src/components/ui/button.tsx` into a pure `button.content.ts` (label ↔ `loadingLabel` ↔ `Strings.loading`, spinner on/off, spinner colour per variant) so both Done-when cases are provable in a logic-only `.ts` test, then wire `Spinner` and the new `loadingLabel?: string` prop into the wrapper. Sixteen existing `isLoading` call sites gain a spinner; none gains a label except the two onboarding CTAs.
- Build `OnboardingShell` under `src/modules/onboarding/components/onboarding_shell/`, with the checkable parts (track heights, progress-rail model, status-track text + live-region mapping) in a pure `onboarding_shell.geometry.ts` sibling — the same shape MA-002 shipped as `display_headline.geometry.ts` and MA-003 as `logo_mark.a11y.ts`.
- Adopt the shell in all four routes with their bodies unchanged, wire `Saving…` on N2 and `Opening…` on N4, and delete `src/components/progress_dots/` with its four consumers.
- Two deviations from the mockup are taken deliberately and recorded below: the CTA keeps its shipped gold gradient (already recorded in `spec.md` § *Known design/codebase disagreements* item 1) and the CTA **slot** is 52 while HeroUI's own `md` button is 48 (new, verified against the packaged CSS, see § *Decisions*; added to `spec.md` § *Known design/codebase disagreements* as item 6 so MA-009 and MA-012 meet it where they actually read). A third, smaller one is noted and not escalated: the mockup dims the busy CTA to `--color-gold-700` (`mockup.html:349`), which the shipped gradient wrapper does not do — subsumed by item 1, which is the decision to keep that wrapper in every state.
- No new dependency, no native change, no migration, no navigation change. No critical trigger fires — see § *Critical triggers* at the end.

---

### Decisions, and the evidence for each

Everything here was read in the tree or in the packaged `heroui-native@1.0.8` source. `node_modules/` is absent from this checkout, so HeroUI facts were verified against the published tarball (`registry.npmjs.org/heroui-native/-/heroui-native-1.0.8.tgz`, `package/src/...`), which is the same code `package.json:49` resolves.

| # | Decision | Evidence |
|---|---|---|
| 1 | **HeroUI `Button` has no `isLoading` and never will here — the spinner is a child.** `package/src/components/button/button.types.ts:41-66` has no loading prop; the library's own recipe (`button.md`, § *Loading State with Spinner*) is `<Button>{isDownloading ? <Spinner color={…}/> : 'Download now'}</Button>`. Confirms `spec.md` § *Known design/codebase disagreements* item 2. | `button.types.ts`, `button.md` |
| 2 | **A `Spinner` sibling of `Button.Label` lays out inline for free.** `.button__root { flex-direction: row; align-items: center; justify-content: center }` and `.button__root--size-md { gap: calc(var(--spacing) * 2) }` = 8dp — the same 8 the mockup's `.cta { gap: var(--space-xs) }` draws (`mockup.html:341-348`). No wrapper `View`, no `flexDirection` override. **And the children survive:** `ButtonRoot` replaces its children with a single `<ButtonLabel>` only when `childrenToString(children)` is non-null, and that helper returns `null` the moment any child is a React element (`children-to-string.ts:64-68`). A `[Spinner, HButton.Label]` array is rendered as authored. | `package/src/styles/components/button.css:1-6,50-56`; `button.tsx:98,261-265`; `helpers/internal/utils/children-to-string.ts` |
| 3 | **Spinner's default colour is the theme accent, i.e. gold — invisible on our gold CTA.** `colorMap.default = themeColorAccent` (`package/src/components/spinner/spinner.tsx:132-139`), and `color` accepts an arbitrary hex (`SpinnerColor \| (string & {})`, `spinner.types.ts:52`). `--accent` is `#d4a44c` (`global.css:18`), which is the exact fill of the primary CTA. So the primary branch must pass a colour explicitly. | `spinner.tsx`, `spinner.types.ts`, `global.css:18` |
| 4 | **Spinner colour by variant:** `primary` → `Colors.dark.bg` (#0F1923), which is exactly `--accent-foreground` (`global.css:19`), matches the label's existing `text-accent-foreground` (`button.tsx:49`), **and is the colour the mockup itself draws the spinner in** — `.spin { border-top-color: var(--accent-foreground) }` (`mockup.html:356-359`). `danger` → `Colors.dark.text1` (#F0EBE3) = `--danger-foreground` (`global.css:31`). Every other variant → leave undefined, which resolves to the accent gold on a dark surface; that is the same hex `LoadingCenter` already renders, though it reaches it by passing `GoldTokens[500]` explicitly rather than by omission (`src/components/ui/loading_center.tsx:11-14`; `GoldTokens[500] === '#D4A44C' === --accent`). | `global.css`, `theme_tokens.ts:19-24`, `mockup.html:356-359` |
| 5 | **The CTA slot is `Size.ctaHeight` (52); the button inside stays HeroUI's 48. Not overridden.** `.button__root--size-md { height: calc(var(--spacing) * 12) }` = 48 (`button.css:50-51`; `--spacing` is Tailwind v4's default `0.25rem` and Uniwind resolves `rem` as `× 16` — the identical derivation `spec.md` § *Geometry tokens* already ratified for `Input`'s 48), and `Size.ctaHeight` has no rendering consumer today: the whole tree references it twice, at `theme.ts:137` and `sheet.tsx:85`, and the second is `SHEET_FOOTER_CLEARANCE` — clearance, not a height. So *every* CTA in the shipped app is already 48, not the mockup's 52. Forcing 52 in onboarding only would make onboarding's CTA taller than every other CTA in the app and would rest on a `style`-vs-Uniwind merge order I cannot verify without a device. The footer track stays 52 and the button is centred in it, so the zero-shift contract — the reason the number exists — is fully served. Cost: the gold button is drawn 4dp shorter than the mockup, with 2dp extra breathing room above and below. Reversible in one line. **MA-009 and MA-012 inherit this, and it is recorded in `spec.md` § *Known design/codebase disagreements* item 6** — a note living only in this plan file is not somewhere their planners read (`TEMPLATES.md` reading order is spec → mockup → task file). | `button.css:50-51`, `theme.ts:137`, `sheet.tsx:85`, `spec.md` item 6 |
| 6 | **`Size.progressRail = ms(55)` is a sixth geometry token, added here.** MA-001 shipped five (`theme.ts:199-208`); the rail's 55 was left unnamed, and the Done-when requires the rail height to be asserted against a named value like the other three tracks. One line plus one assertion. | `theme.ts:199-208`, `spec.md` § *Shell* |
| 7 | **The status track's line height is derived, not authored: `Math.floor(Size.statusTrack / 2)`.** Authoring the mockup's 17 as `ms(17)` clips on large phones — at scale 1.15 (the jest-expo Dimensions mock, and a real 448pt phone) `ms(34) = 39` but `2 × ms(17) = 40`. Deriving it from the track makes two lines fit at every scale by construction, and it still equals the mockup's 17 at the 390pt reference. | `src/utils/responsive.ts:16-22`, `__tests__/geometry_tokens.test.ts:13-17` |
| 8 | **The rail's inner content is not a rigid sum.** The mockup composes it as `2 × Spacing.sm + Size.progressThin + Spacing.xs + Size.compactBodyLineHeight` (`mockup.html:296-309`: `.rail` padding `--space-sm`, `.seg` height `--size-progress-thin`, `.rail-l` `margin-top: --space-xs`). Each `ms()` rounds independently, so that sum is **not** monotonically under `ms(55)`. At both clamp ends it lands exactly on the track and leaves zero slack (0.85 → 47 vs 47; 1.15 → 63 vs 63); in between it **overshoots** — most importantly at scale 0.9615, i.e. a **375 pt phone** (iPhone SE2 / 8 / X): `2×12 + 3 + 8 + 19 = 54` against a 53 track. It also overshoots by 2 at 0.875. So the label row takes `flex: 1` with `justifyContent: 'center'` inside the fixed track instead of stacking fixed offsets, and the fit assertion drops the gap term entirely: what has to hold is that padding + bar leave at least one label line, which is true at every scale (0.85 → 23 used of 47, 1.15 → 31 used of 63). | `mockup.html:296-309`, same rounding argument as #7 |
| 9 | **The shell does not own a `ScrollView`.** `more_accounts/index.tsx:55` renders a `FlashList`; wrapping the shell's viewport in `ScreenScroll` would nest a virtualised list in a scroll view. The viewport is a plain `View style={{ flex: 1 }}`; each route keeps the scroller and the horizontal padding it has today. | `more_accounts/index.tsx:55-85` |
| 10 | **The header is onboarding's own component, not a change to `StackHeader`.** `src/components/ui/stack_header.tsx:26-35` renders its back button unconditionally and has no brand variant. Onboarding needs both a brand variant (N1) and an *absent* back affordance (N3/N4 in this task — their back semantics are MA-005's). Teaching the shared header two onboarding-only shapes would push them onto every other screen that uses it. The onboarding header reuses its primitives verbatim: `PressableFeedback`, `Typography`, `chevron-left` at `Size.iconBack`, `Radius.sm`, `hitSlop={8}`, `Strings.goBackAccessibility`. | `stack_header.tsx:17-46` |
| 11 | **N3 and N4 get a header track with no back button in this task.** Rendering one would mean deciding what it does, and per `spec.md` § *Async and navigation ownership* + `locked-design-2026-07-23.md:307-308` the answer is "the N3 back action persists N1 and replaces the route with Welcome" — MA-005's diff. The header takes `onBack?`; when absent it renders the same 36dp spacer `stack_header.tsx:43` uses, so the title stays centred and the track height is identical on all four routes. **Hand-off flagged, because it is not currently owned:** mockup frames E1 (`mockup.html:2004`) and F1 (`mockup.html:2319`) both draw a back chevron on N3 and N4, and MA-005's Done-when (`MA-005.md:45`) does **not** require a back affordance to appear — only its body prose (`MA-005.md:39`) mentions "the header's back affordance". If MA-004 ships without one and MA-005 is reviewed against its Done-when, N3 and N4 lose their back button silently. @sarah: MA-005's plan must add it, or its Done-when must gain the line. | task § *This task changes no navigation*, `MA-005.md:39,45` |
| 12 | **N3's footnote follows `spec.md`. The mockup is stale in exactly one frame, and it is not an N3 frame.** Spec § *Shell*: `One account is enough to start. You can add more any time.` The three frames that actually draw N3 — E1, E2, E4 at `mockup.html:2042,2078,2145` — already draw that sentence verbatim. Only the A2 *reference* frame (`mockup.html:960`) and the A2 prose summary (`mockup.html:975-976`) carry the older `…You can add more later.` / truncated forms. So there is no live spec-vs-mockup disagreement here, just one un-refreshed anatomy frame; taking the spec's wording costs nothing and no escalation is owed. | `spec.md:56`, `mockup.html:960` vs `2042,2078,2145` |

---

### Files

**Create**
- `src/modules/onboarding/components/onboarding_shell/index.tsx` — `OnboardingShell`
- `src/modules/onboarding/components/onboarding_shell/onboarding_shell.geometry.ts` — pure: track heights, progress-rail model, status-track model + live-region mapping
- `src/modules/onboarding/components/onboarding_shell/onboarding_header.tsx` — titled variant + brand variant
- `src/modules/onboarding/components/onboarding_shell/onboarding_progress_rail.tsx`
- `src/modules/onboarding/components/onboarding_shell/onboarding_status_track.tsx`
- `src/modules/onboarding/components/onboarding_shell/onboarding_footer.tsx` — status track + CTA slot
- `src/components/ui/button.content.ts` — pure busy-button resolver
- `__tests__/components/ui/button_content.test.ts`
- `__tests__/screens/onboarding_shell_geometry.test.ts`

**Modify**
- `src/constants/theme.ts` — one token in the `Size` block
- `src/constants/strings.ts` — shell copy
- `src/components/ui/button.tsx` — `loadingLabel` prop, `Spinner`, delegate to `button.content.ts`
- `src/modules/onboarding/screens/onboarding/welcome/index.tsx`
- `src/modules/onboarding/screens/onboarding/add_account/index.tsx`
- `src/modules/onboarding/screens/onboarding/more_accounts/index.tsx`
- `src/modules/onboarding/screens/onboarding/ready/index.tsx`
- `__tests__/geometry_tokens.test.ts` — the rail token and its fit assertion

**Delete**
- `src/components/progress_dots/index.tsx`
- `src/components/progress_dots/progress_dots.anim.ts`

No file under `src/app/` is touched. No hook, store or repository is touched — `welcome.hook.ts`, `add_account.hook.ts`, `more_accounts.hook.ts` and `ready.hook.ts` keep the exact behaviour they have today.

---

### Step 1 — Tokens and copy

**Files:** `src/constants/theme.ts`, `src/constants/strings.ts`, `__tests__/geometry_tokens.test.ts`

- [ ] **1.1** Add to the `Size` block in `src/constants/theme.ts`, immediately after `statusTrack` (line 204), inside the existing zero-shift comment group:

```ts
progressRail: ms(55), // onboarding progress rail — bar + step label, fixed
```

- [ ] **1.2** Extend `__tests__/geometry_tokens.test.ts`. Add to the value-lock test:

```ts
expect(Size.progressRail).toBe(ms(55)); // onboarding progress rail
```

and add one relationship test — this is the assertion that can catch a real mistake, in the style of the two already in that file:

```ts
it('the progress rail holds its bar and one label line', () => {
  // Composed content is padding + bar + one label line box. Each ms() rounds
  // independently, so this is a fit check, never an equality. The mockup's
  // literal composition also carries a Spacing.xs gap between bar and label
  // (mockup.html:305); including it makes the sum land exactly on the track at
  // both clamp ends and OVERSHOOT in between — 54 against a 53 track at scale
  // 0.9615, i.e. any 375pt phone. That gap is therefore not a fixed offset in
  // the component: the label row takes flex: 1 (plan decision 8), so the only
  // thing that must fit is padding + bar + one line.
  //
  // Under jest-expo, Dimensions is mocked at 750pt, so responsiveScale is
  // clamped to 1.15 and this assertion evaluates at ONE scale. It is a
  // change-detector, not a proof across the range; the range holds because
  // the label row is flexed rather than offset.
  expect(Spacing.sm * 2 + Size.progressThin + Size.compactBodyLineHeight).toBeLessThan(
    Size.progressRail,
  );
});
```

- [ ] **1.3** Add to `src/constants/strings.ts`, in the onboarding block after `n3AddMoreSubtitle` (line 59). Exact values — every one is from `spec.md` § *Shell* or the frame markup cited beside it:

```ts
// N-shell — MA-004. Mockup § A (A1-A3) and the per-route frames B1/C1/E1/F1.
onboardingStepOf: (step: number) => `Step ${step} of 4`,
onboardingProgressA11y: (step: number, name: string) => `Step ${step} of 4, ${name}`,
n1StepName: 'Choose your currency',
n2StepName: 'Add your first account',
n3StepName: 'Add more accounts',
n4StepName: 'Review and finish',
n1HeaderWordmark: 'MoneyApp',
n1HeaderSetup: 'Setup',
n2HeaderTitle: 'Add your first account',   // mockup.html:1334
n3HeaderTitle: 'Your accounts',            // mockup.html:2004
n4HeaderTitle: 'Ready to start',           // mockup.html:2319
n1Footnote: 'You can change this later in Settings.',
n2Footnote: 'Saved on this phone. Nothing is uploaded.',
n3Footnote: 'One account is enough to start. You can add more any time.',
n4Footnote: 'This is your starting point, not your net worth.',
n2CtaBusy: 'Saving…',
n4CtaBusy: 'Opening…',
```

The ellipses in `Saving…` / `Opening…` are U+2026, as drawn (`mockup.html:1196,2146`) — not three dots. `Strings.loading` stays `'Loading...'` untouched; changing it is an app-wide copy change this task does not make.

- [ ] **1.4** Run: `npm test -- __tests__/geometry_tokens.test.ts`. Expected: PASS, 4 tests.

---

### Step 2 — The busy-button resolver, test first

**Files:** `src/components/ui/button.content.ts`, `__tests__/components/ui/button_content.test.ts`

- [ ] **2.1** Write `__tests__/components/ui/button_content.test.ts` **before** the module. It is a `test.each` over `resolveButtonContent`, and it is what discharges "a test covers both the supplied and the omitted case". Rows, exactly:

| `variant` | `isLoading` | `label` | `loadingLabel` | expected `text` | `showSpinner` | `spinnerColor` |
|---|---|---|---|---|---|---|
| `primary` | `undefined` | `'Save'` | `undefined` | `'Save'` | `false` | `undefined` |
| `primary` | `false` | `'Save'` | `'Saving…'` | `'Save'` | `false` | `undefined` |
| `primary` | `true` | `'Save'` | `undefined` | `Strings.loading` | `true` | `Colors.dark.bg` |
| `primary` | `true` | `'Save'` | `'Saving…'` | `'Saving…'` | `true` | `Colors.dark.bg` |
| `danger` | `true` | `'Delete'` | `undefined` | `Strings.loading` | `true` | `Colors.dark.text1` |
| `secondary` | `true` | `'Fetch'` | `'Fetching…'` | `'Fetching…'` | `true` | `undefined` |
| `ghost` | `true` | `'Cancel'` | `undefined` | `Strings.loading` | `true` | `undefined` |

Row 3 is the one that proves omission preserves today's behaviour byte for byte (`button.tsx:31`). Row 5 is the one that catches a gold spinner on a red destructive button.

- [ ] **2.2** Run: `npm test -- __tests__/components/ui/button_content.test.ts`. Expected: FAIL — cannot resolve `@/components/ui/button.content`.

- [ ] **2.3** Write `src/components/ui/button.content.ts`. Type-only import of `ButtonVariant` from `heroui-native`; runtime imports are `Strings` and `Colors` only, so the module stays render-free and logic-only.

```ts
export interface ButtonContentInput {
  variant: ButtonVariant;
  label: string;
  isLoading?: boolean;
  loadingLabel?: string;
}

export interface ButtonContent {
  text: string;
  showSpinner: boolean;
  /** undefined = Spinner's own 'default', which resolves to the theme accent. */
  spinnerColor?: string;
}

export function resolveButtonContent(input: ButtonContentInput): ButtonContent;
```

Behaviour: not loading → `{ text: label, showSpinner: false }`. Loading → `text: loadingLabel ?? Strings.loading`, `showSpinner: true`, and `spinnerColor` per decision 4. Document decision 3 in a comment at the `primary` branch — a future reader deleting "the redundant colour prop" is exactly how the spinner goes invisible.

- [ ] **2.4** Run: `npm test -- __tests__/components/ui/button_content.test.ts`. Expected: PASS, 7 rows.

---

### Step 3 — Wire the wrapper

**Files:** `src/components/ui/button.tsx`

- [ ] **3.1** Add `loadingLabel?: string` to `ButtonProps` (`button.tsx:9-18`) with a doc comment: *when `isLoading`, replaces `Strings.loading`; omitted keeps today's default.*

- [ ] **3.2** Replace `const content = isLoading ? Strings.loading : label;` (`button.tsx:31`) with a call to `resolveButtonContent({ variant, label, isLoading, loadingLabel })`.

- [ ] **3.3** In **both** return branches, render `{showSpinner ? <Spinner size="sm" color={spinnerColor} /> : null}` as the sibling **before** `<HButton.Label>` — spinner left of the label, as drawn (`mockup.html:1196`). No wrapper `View` and no `flexDirection` (decision 2). In the primary branch it goes after the `LinearGradient`, which is `absoluteFill` and takes no part in the row.

- [ ] **3.4** Do **not** touch `isDisabled`. `isLoading` does not imply disabled today and 16 call sites depend on that; three of them pass both explicitly (`reassign_category_sheet.tsx:69-70`, `add_edit_category_sheet.tsx:179-180`, `adjust_balance_sheet.tsx:75-76`).

- [ ] **3.5** Run: `npm run typecheck && npm test -- --ci`. Expected: PASS. Two suites reference the loading label and neither should move: `__tests__/screens/navigation/startup_error.test.ts:14` asserts a *helper's* output (`startup_error.helpers.ts:5` already sets `label: Strings.loading` itself, so that button has always rendered the string twice-resolved and still will), and `__tests__/screens/accounts/balance_review_alert.test.tsx:22` mocks this wrapper with its own hardcoded `'Loading...'`, so it never sees the real component. Leave both alone.

#### Blast radius — the sixteen call sites that gain a spinner

All verified by `grep -rn "isLoading" src` cross-checked against imports of `@/components/ui/button`. **None gets a `loadingLabel` in this task** except the two onboarding CTAs in step 6; every other one keeps `Strings.loading` and changes only by gaining a spinner.

| # | Call site | Variant | Spinner colour |
|---|---|---|---|
| 1 | `src/modules/transactions/screens/transactions/transaction_form/index.tsx:35` | primary | `Colors.dark.bg` |
| 2 | `src/modules/navigation/components/startup_error.tsx:41` | primary (default) | `Colors.dark.bg` |
| 3 | `src/modules/currency/screens/currency/index.tsx:63` | secondary | default (gold) |
| 4 | `src/modules/currency/screens/currency/index.tsx:111` | primary | `Colors.dark.bg` |
| 5 | `src/modules/categories/screens/settings/categories/components/reassign_category_sheet.tsx:69` | primary | `Colors.dark.bg` |
| 6 | `src/modules/categories/screens/settings/categories/components/add_edit_category_sheet.tsx:179` | primary | `Colors.dark.bg` |
| 7 | `src/modules/budget/screens/budget/components/set_budget_sheet.tsx:58` | primary | `Colors.dark.bg` |
| 8 | `src/modules/budget/screens/budget/components/budget_copy_sheet.tsx:88` | primary (default) | `Colors.dark.bg` |
| 9 | `src/modules/budget/screens/budget/components/income_sheet.tsx:30` | primary | `Colors.dark.bg` |
| 10 | `src/modules/budget/screens/budget/spending_plan_sheet/index.tsx:37` | primary | `Colors.dark.bg` |
| 11 | `src/modules/commitments/screens/commitments/detail/components/pay_sheet.tsx:97` | primary | `Colors.dark.bg` |
| 12 | `src/modules/commitments/screens/commitments/components/commitment_form_body.tsx:479` | primary | `Colors.dark.bg` |
| 13 | `src/modules/accounts/screens/accounts/detail/components/adjust_balance_sheet.tsx:76` | primary | `Colors.dark.bg` |
| 14 | `src/modules/accounts/screens/accounts/detail/components/balance_review_alert.tsx:38` | secondary | default (gold) |
| 15 | `src/components/ui/confirm_dialog.tsx:91` | `danger` when destructive, else primary | `Colors.dark.text1` / `Colors.dark.bg` |
| 16 | `src/components/ui/confirm_sheet.tsx:133` | `danger` when destructive, else primary | `Colors.dark.text1` / `Colors.dark.bg` |

15 and 16 are the shared wrappers; their `busy` prop fans out to **seven distinct user-visible dialogs** — transaction delete (`transactions/index.tsx:231`, `detail/index.tsx:157` via `tx_delete_confirm_sheet.tsx:27`), budget delete (`budget/index.tsx:295` via `budget_delete_confirm_sheet.tsx:35`), spending-plan delete (`budget/index.tsx:305` via `spending_plan_delete_confirm_sheet.tsx:27`), commitment delete (`commitments/index.tsx:286` via `commitment_delete_confirm_sheet.tsx:30`), commitment skip (`commitments/index.tsx:296` via `skip_confirm_sheet.tsx:19`), commitment deactivate (`edit_commitment/index.tsx:38` via `deactivate_sheet.tsx:18`), account archive (`archive_confirmation_dialog.tsx:29`). The archive dialog is the cheapest place to see the `danger` spinner colour on the emulator.

---

### Step 4 — Shell geometry and a11y, test first

**Files:** `src/modules/onboarding/components/onboarding_shell/onboarding_shell.geometry.ts`, `__tests__/screens/onboarding_shell_geometry.test.ts`

- [ ] **4.1** Write `__tests__/screens/onboarding_shell_geometry.test.ts` first. It carries the Done-when's "a test asserts the shell's track heights against the named geometry values from MA-001", plus the rail and status models.

**What this test does and does not prove, stated so nobody reads it as more:** it binds `ONBOARDING_SHELL_TRACKS` to the tokens. It does **not** prove the four `.tsx` files consume that constant — that link is closed by the emulator offset comparison in § A, and by `@impl-reviewer` reading the diff for a literal where a track height belongs. A render suite would close it in CI, but `.claude/rules/tests.md:19` is "keep the files, don't add to them" for `.tsx` render suites, so adding one here is against policy, not merely unfashionable.

```ts
it('binds every track to its named token', () => {
  expect(ONBOARDING_SHELL_TRACKS.header).toBe(Size.headerHeight);
  expect(ONBOARDING_SHELL_TRACKS.progressRail).toBe(Size.progressRail);
  expect(ONBOARDING_SHELL_TRACKS.statusTrack).toBe(Size.statusTrack);
  expect(ONBOARDING_SHELL_TRACKS.cta).toBe(Size.ctaHeight);
});

it('holds exactly two status lines at every scale', () => {
  expect(STATUS_TRACK_LINE_HEIGHT * 2).toBeLessThanOrEqual(Size.statusTrack);
  expect(STATUS_TRACK_LINE_HEIGHT * 2).toBeGreaterThan(Size.statusTrack - 2);
});
```

plus `test.each` over `resolveProgressRail(1..4)` asserting `filled` is `[t,f,f,f] … [t,t,t,t]` (4 entries always), `stepLabel` is `Step N of 4`, `stepName` matches the four `Strings.n*StepName`, and `accessibilityLabel` is `Step N of 4, <name>`; and over `resolveStatusTrack`:

| input | expected |
|---|---|
| `(Strings.n2Footnote, undefined)` | `{ text: n2Footnote, tone: 'idle', a11y: { accessibilityLiveRegion: 'polite' } }`, no `accessibilityRole` |
| `(Strings.n2Footnote, 'Could not save that.')` | `{ text: 'Could not save that.', tone: 'error', a11y: { accessibilityLiveRegion: 'assertive', accessibilityRole: 'alert' } }` |
| `(Strings.n2Footnote, '')` | idle — an empty string is not a failure, and the track is never empty |

- [ ] **4.2** Run: `npm test -- __tests__/screens/onboarding_shell_geometry.test.ts`. Expected: FAIL, module not found.

- [ ] **4.3** Write `onboarding_shell.geometry.ts`. Exports:

```ts
export type OnboardingStepIndex = 1 | 2 | 3 | 4;
export const ONBOARDING_TOTAL_STEPS = 4;
export const ONBOARDING_SHELL_TRACKS: {
  header: number; progressRail: number; statusTrack: number; cta: number;
};
/** Derived from the track, never authored — see plan decision 7. */
export const STATUS_TRACK_LINE_HEIGHT: number; // Math.floor(Size.statusTrack / 2)
/** Status-track glyph column — mockup.html:330-332. Same width in both states. */
export const STATUS_GLYPH_BOX: number; // ms(14)
export const STATUS_IDLE_DOT: number; // ms(5)

export interface ProgressRailModel {
  filled: boolean[]; stepLabel: string; stepName: string; accessibilityLabel: string;
}
export function resolveProgressRail(step: OnboardingStepIndex): ProgressRailModel;

export type StatusTone = 'idle' | 'error';
export interface StatusTrackModel {
  text: string;
  tone: StatusTone;
  a11y: { accessibilityLiveRegion: 'polite' | 'assertive'; accessibilityRole?: 'alert' };
}
export function resolveStatusTrack(footnote: string, message?: string): StatusTrackModel;
```

The live-region mapping mirrors the only error precedent in the tree — `income_sheet.tsx:81-82` pairs `accessibilityRole="alert"` with `accessibilityLiveRegion="assertive"`; idle stays `polite` so a route change (which remounts the shell, and a live region does not announce on mount) never speaks a footnote over the screen title.

- [ ] **4.4** Run: `npm test -- __tests__/screens/onboarding_shell_geometry.test.ts`. Expected: PASS.

---

### Step 5 — The shell components

**Files:** the five `.tsx` files under `src/modules/onboarding/components/onboarding_shell/`

No `useState` and no `useSharedValue` anywhere in these files — they are presentation over props, per CLAUDE.md § *module screen anatomy*. Layout-critical dimensions go in `style` with tokens; `className` carries colour, border and typography only (`.claude/rules/ui.md` § *Screen layout*).

- [ ] **5.1** `onboarding_header.tsx` — `OnboardingHeaderProps { title?: string; onBack?: () => void }`.
  - Root `View`, `style={{ flexDirection: 'row', alignItems: 'center', height: ONBOARDING_SHELL_TRACKS.header }}`, `className="border-separator justify-between border-b px-2"` — the same shape as `stack_header.tsx:22-25`.
  - `title` present → left `PressableFeedback` back button when `onBack` is given (copy `stack_header.tsx:26-35` verbatim: `h-9 w-9`, `Radius.sm`, `hitSlop={8}`, `chevron-left` at `Size.iconBack` in `CoreTokens.text2`, `accessibilityRole="button"`, `accessibilityLabel={Strings.goBackAccessibility}`), else a `View className="w-9"` spacer; centred `Typography` title (`font-sora-semibold text-foreground flex-1 text-center`, `fontSize: Type.title`, `numberOfLines={1}`); right `View className="w-9"` spacer.
  - `title` absent → brand variant, **two children, not three**. The root's `justify-between` (inherited from `stack_header.tsx:24`) spreads its direct children evenly, so the mark, the wordmark and `Setup` as three flat siblings would push the wordmark into the centre of the header instead of leaving it beside the mark. The mockup groups the first two: `.brand { display: flex; gap: var(--space-xs) }` holds mark + wordmark, and `Setup` is a separate right-hand node (`mockup.html:292-294,1027-1028`). So:
    - child 1 — `View style={{ flexDirection: 'row', alignItems: 'center', gap: Spacing.xs }}` containing `<LogoMark />` from `@/components/ui/logo_mark` with **no** `accessibilityLabel` (its `resolveLogoMarkA11y()` no-arg overload returns the decorative branch — `logo_mark.a11y.ts:27-37` says exactly why: the wordmark sits beside it) and `Typography` `{Strings.n1HeaderWordmark}` (`font-sora-bold`, `fontSize: Type.subhead`).
    - child 2 — `Typography` `{Strings.n1HeaderSetup}` (`fontSize: Type.micro`, `className="text-content-secondary"`).
    - Same root, so the track height is identical.

- [ ] **5.2** `onboarding_progress_rail.tsx` — `{ step: OnboardingStepIndex }`.
  - Root `View style={{ height: ONBOARDING_SHELL_TRACKS.progressRail, paddingVertical: Spacing.sm, paddingHorizontal: Spacing.md }}`.
  - Bar `View style={{ flexDirection: 'row', gap: Spacing.xxs }}` marked `importantForAccessibility="no-hide-descendants"` + `accessibilityElementsHidden`; four segments `style={{ flex: 1, height: Size.progressThin, borderRadius: Radius.xs }}` with `className={filled ? 'bg-accent' : 'bg-muted'}`. `--muted` is decorative-only (`spec.md` § *Accessibility*) and this track is the one legitimate use of it.
  - Label row `View style={{ flex: 1, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }}`, `accessible` with `accessibilityLabel={model.accessibilityLabel}`; two `Typography` at `fontSize: Type.caption`, `lineHeight: Size.compactBodyLineHeight`, `numberOfLines={1}` — left `stepLabel` (`text-foreground font-inter-semibold`), right `stepName` (`text-content-secondary`). `flex: 1` on the row, not a fixed offset (decision 8).

- [ ] **5.3** `onboarding_status_track.tsx` — `{ footnote: string; message?: string }`.
  - Root `View style={{ height: ONBOARDING_SHELL_TRACKS.statusTrack, flexDirection: 'row', alignItems: 'flex-start', gap: Spacing.xs, overflow: 'hidden' }}` spread with `model.a11y`.
  - Glyph column, fixed width so the text's left edge does not shift between states: idle → a small round dot in `--content-secondary`; error → `alert-circle-outline` from MaterialCommunityIcons in `Colors.dark.negative` (`mockup.html:985`). The mockup draws the dot at 5 and the glyph box at 14 (`mockup.html:330-332`). **Both sizes are named constants in `onboarding_shell.geometry.ts`, not expressions over unrelated tokens** — `Size.hairline * 5` would look scaled and is not (`ms(1)` is 1 at every scale inside the 0.85–1.15 clamp, so it is a literal 5 wearing a token), and `Size.iconMicro` is 12, which is a silent 2dp shrink of a glyph the mockup sized deliberately. Export `STATUS_GLYPH_BOX = ms(14)` and `STATUS_IDLE_DOT = ms(5)` from the geometry module and use those; the column width is `STATUS_GLYPH_BOX` in both states, which is what actually keeps the left edge still.
  - `Typography` `numberOfLines={2}`, `style={{ fontSize: Type.caption, lineHeight: STATUS_TRACK_LINE_HEIGHT, flex: 1 }}`, `className={tone === 'error' ? 'text-danger' : 'text-content-secondary'}`.
  - `overflow: 'hidden'` and `numberOfLines={2}` together are what make the box unmoveable; at large OS font scales the second line clips rather than growing the footer, which is the fixed-track reading of `spec.md` § *The zero-shift contract* (the "grows into the viewport" sentence governs the *field message rail*, MA-009's, not this track). Say so in a comment so @impl-reviewer does not have to re-derive it.

- [ ] **5.4** `onboarding_footer.tsx` — `{ footnote: string; message?: string; cta: ReactNode }`.
  - Root `View className="border-separator border-t"` with `style={{ paddingTop: Spacing.sm, paddingHorizontal: Spacing.md, paddingBottom: Spacing.lg }}`. `Screen` already adds the bottom safe-area inset (`screen.tsx:41-44`), so no extra allowance.
  - `<OnboardingStatusTrack />`, then a spacer of `Spacing.sm`, then the CTA slot: `View style={{ height: ONBOARDING_SHELL_TRACKS.cta, justifyContent: 'center' }}` wrapping `{cta}` (decision 5).

- [ ] **5.5** `index.tsx` — `OnboardingShell`:

```tsx
export interface OnboardingShellProps {
  step: OnboardingStepIndex;
  /** Omit for the N1 brand header. */
  title?: string;
  onBack?: () => void;
  /** Idle copy. Required — the track is never empty. */
  footnote: string;
  /** When set, replaces the footnote in the identical box. */
  statusMessage?: string;
  cta: React.ReactNode;
  children: React.ReactNode;
}
```

Renders `<Screen>` → header → rail → `<View style={{ flex: 1 }}>{children}</View>` → footer. `cta` is a node rather than a prop bundle so each route keeps the `Animated.View` it wraps its button in today (`welcome/index.tsx:67`, `add_account/index.tsx:347`, `ready/index.tsx:75`) and MA-010/MA-012 can change motion without touching the shell.

- [ ] **5.6** Run: `npm run typecheck && npm run lint`. Expected: clean.

---

### Step 6 — Adopt the shell in all four routes, and delete the dots

**Files:** the four route `index.tsx` files under `src/modules/onboarding/screens/onboarding/`, then `src/components/progress_dots/`

Each route loses its `<Screen>`, its `<ProgressDots>`, its hand-rolled header (N2 only) and its footer `Box`. **Nothing else in these files changes** — same body markup, same hooks, same animations, same handlers.

- [ ] **6.1** `welcome/index.tsx` — `<OnboardingShell step={1} footnote={Strings.n1Footnote} cta={<Animated.View entering={ctaEntering}><Button …/></Animated.View>}>` wrapping the existing `<ScreenScroll>` block (lines 25-64). No `title`, no `onBack` → brand header. CTA keeps `label={Strings.o1Cta}` and gains nothing else: `onContinue` has no busy state today and giving it one is MA-005's persist work.
- [ ] **6.2** `add_account/index.tsx` — `step={2}`, `title={Strings.n2HeaderTitle}`, `onBack={onBack}` (already returned by `useAddAccount`), `footnote={Strings.n2Footnote}`. Delete the header `Box` (lines 48-57) and the CTA bar `Box` (lines 346-358); the CTA moves into the `cta` prop keeping its `Animated.View style={btnAnim}` wrapper and `triggerBtnPress()`, and gains `isLoading={isSubmitting}` + `loadingLabel={Strings.n2CtaBusy}` alongside the existing `isDisabled={isSubmitting}`. Body — the whole `<ScreenScroll>` at lines 61-343 — moves across unchanged, `contentContainerStyle` included. Drop the now-unused `Strings.o4Title` from `strings.ts:24` **only if** `grep -rn "o4Title" src __tests__` comes back empty.
- [ ] **6.3** `more_accounts/index.tsx` — `step={3}`, `title={Strings.n3HeaderTitle}`, **no** `onBack` (decision 11), `footnote={Strings.n3Footnote}`. Body is the existing `<Box style={{ flex: 1 }} className="px-4">` (lines 28-90) including the `FlashList` — the shell adds no scroller (decision 9). CTA `label={Strings.o5Cta}`, unchanged.
- [ ] **6.4** `ready/index.tsx` — `step={4}`, `title={Strings.n4HeaderTitle}`, no `onBack`, `footnote={Strings.n4Footnote}`. CTA keeps `Animated.View entering={ctaEntering}` and `isDisabled={completing}`, gains `isLoading={completing}` + `loadingLabel={Strings.n4CtaBusy}`.
- [ ] **6.5** `git rm -r src/components/progress_dots`. Consumers were the four imports just removed; `grep -rn "progress_dots\|ProgressDots" src __tests__` must return nothing. No test covers it.
- [ ] **6.6** Run the full CI-parity chain from CLAUDE.md § *Commands*. Expected: green through `npx expo prebuild --no-install --platform android`.

---

### Step 7 — Emulator verification

`verify: emulator`. Follow the `emulator-verify` skill; give the worktree its own Metro port (`adb reverse` is global per device). Both @dev at step 6 and @impl-reviewer at step 7 run **all** of this, not just the walk.

**Setup**

- [ ] `mqa reset` (`pm clear`) so the next launch starts at N1 with an empty database, then `mqa boot` / `install` / `launch`.

**A. The four tracks, on all four routes**

- [ ] `mqa shot n1` … `n4` walking N1 → N2 → N3 → N4. On each: header at the top (N1 = Cross Fan mark + `MoneyApp` + right-aligned `Setup`; N2 = back + `Add your first account`; N3 = `Your accounts`, no back; N4 = `Ready to start`, no back), a four-segment rail with N segments gold, the correct `Step N of 4` + step name, the footnote for that route, and the CTA.
- [ ] The offsets are the assertion: compare the four screenshots and confirm the footnote's top edge and the CTA's top edge are at the same y on all four. A route that differs means a body is still owning chrome.
- [ ] `mqa ui` on N1 must show `Step 1 of 4, Choose your currency` as one node (the rail's accessible label) and must **not** list four segment nodes.

**B. Force a message through the status track** — nothing in this task produces a real failure, so force one:

- [ ] Temporarily edit `more_accounts/index.tsx` to pass `statusMessage="Couldn't save that. Your account is safe — tap Review setup to try again."` on the shell. Reload.
- [ ] `mqa shot n3-error`. The line must be danger-coloured with the alert glyph, two lines, in the same box: diff `n3` against `n3-error` and confirm the CTA's y is **identical**, not merely similar. That single comparison is what the status track exists for.
- [ ] `git checkout -- src/modules/onboarding/screens/onboarding/more_accounts/index.tsx`, reload, confirm the footnote is back, and confirm `git status` is clean of it before any commit. A forced-error edit reaching a PR is the one way this step causes harm.

**C. A busy button outside onboarding, which is why this task is `verify: emulator`**

- [ ] Finish onboarding to reach the dashboard, then Budget → Income → Save (`income_sheet.tsx:27-30`): the CTA must show a **dark** spinner on the gold fill next to `Loading...` — dark, not gold, is the whole point of decision 3.
- [ ] Accounts → an account → Archive (`archive_confirmation_dialog.tsx:29` → `confirm_dialog.tsx:91`): the destructive confirm is the `danger` variant, so its spinner must be bone-white, not gold.
- [ ] Local writes are fast; if the spinner is gone before `mqa shot` lands, say so plainly in the emulator note rather than claiming a pass — `__tests__/components/ui/button_content.test.ts` covers the mapping, and the frame that matters most is the N2 `Saving…` in D below.

**D. The flow reaches the dashboard exactly as today**

- [ ] `mqa reset`, then walk N1 (EGP) → N2 (save an account, watch for `Saving…` + spinner) → N3 → N4 (`Opening…` + spinner) → dashboard.
- [ ] `mqa db "SELECT id, name, type, currency, opening_balance, current_balance FROM accounts;"` — one row, `current_balance = opening_balance` (business rule 6).
- [ ] `mqa db "SELECT key, value FROM app_settings WHERE key IN ('onboarding_complete','base_currency');"` — `onboarding_complete = 'true'`, `base_currency = 'EGP'`. (The step itself lives in SecureStore, `onboarding.repository.ts:28-30`, and is not queryable here; it is MA-005's surface anyway.)
- [ ] `mqa logs` — no JS errors, no `[runtime not ready]`.

---

### Non-goals

- **All navigation.** Route resolver, persist-before-navigate, `router.replace`, gesture lock, the N3/N4 back handlers, the stale-completion guard: MA-005. `welcome.hook.ts:17`, `add_account.hook.ts:85` and `more_accounts.hook.ts:34` keep their `router.push` calls in this diff.
- **Flattening the CTA gradient.** `spec.md` § *Known design/codebase disagreements* item 1 — keeping the wrapper is the recorded decision.
- **Any route body.** N1's composition (MA-010), the account form (MA-007/008/009), N3's list (MA-011), N4's summary and the `computeTotalBalance` bug (MA-012).
- **`Strings.loading`**, the 14 non-onboarding busy labels, `isDisabled` semantics, and `src/components/ui/stack_header.tsx`.
- **`scripts/design-tokens.js`.** `Size.progressRail` is written as `ms(55)` so it emits without the regex fix that `spec.md` § *Geometry tokens* flags as a known gap; nothing to do here.

### Risks

| Risk | Mitigation |
|---|---|
| Every busy button in the app changes at once — 16 call sites, 7 of them dialogs nobody in this scope opens. | The resolver is pure and table-tested; the emulator run leaves onboarding for two of them (§ C), and gate-3 QA does the same. Recorded in `spec.md` as the reason this task is `verify: emulator`. |
| Fixed tracks clip at large OS font scales. | Line heights are derived from the tracks (decisions 7, 8) and both fixed texts carry `numberOfLines`. **The assertions do not sweep scales** — jest-expo pins `Dimensions` at 750pt, so `responsiveScale` is clamped to 1.15 and every `ms()` assertion in this repo evaluates at exactly that one scale. What makes the fit hold across the range is construction (`Math.floor` on the track; `flex: 1` on the label row), not the assertion; the assertion is a change-detector on top. Actual TalkBack/large-font behaviour is a gate-3 observation, not a CI one. |
| The live-region announcement itself is not testable in CI — only the mapping is. | Mapping is unit-tested against the `income_sheet.tsx:81-82` precedent; the announcement is a gate-3 TalkBack check. Named here so nobody reads the green test as proof a screen reader spoke. |
| The forced-error edit (§ B) gets committed. | Explicit `git checkout --` and a `git status` gate in the step, before the commit. |
| Cold start — N1 is first paint. | The shell adds four `View`s and four segment `View`s; the only new drawing on N1 is `LogoMark`, one 40×40 `Svg` already measured and merged in MA-003. No image, no measurement pass, no dependency. |
| `Size.progressRail` is a sixth token where the spec named five. | One line, one assertion, same rationale MA-001 recorded. Flagged rather than slipped in — if @plan-reviewer would rather the rail be unnamed, the Done-when's "named geometry values" loses one of its four tracks, and that is the trade. |

### Critical triggers

**None fires.** Stated explicitly because three of the four triggers that would fire live near this diff:

- **New dependency — no.** `Spinner` is already imported from `heroui-native` in four files (`loading_center.tsx:1`, `detail_header.tsx:2`, `transaction_form_body.tsx:4`, `categories/index.tsx:2`). `package.json` is untouched.
- **Native change — no.** `app.json`, `android/`, `ios/` and the plugin list are untouched. `expo prebuild --no-install` runs in the CI-parity chain as a check only.
- **Migration — no.** No file under `src/database/migrations/`.
- **Cross-scope binding — no, but one in-scope binding is recorded:** decision 5 (CTA slot 52, button 48) is inherited by MA-009 and MA-012 and now lives in `spec.md` § *Known design/codebase disagreements* item 6, which is where their planners read.

---

## Plan review

### Round 1 — approved with edits

Verdict: **approved with edits**. The approach is right and the three HeroUI facts the plan turns on are all true — I re-verified every one against the `heroui-native@1.0.8` tarball myself, because `node_modules/` is absent from this checkout too. Nine claims were wrong or overstated; all nine were detail-level and are corrected in place. No critical trigger fires that @tariq missed.

#### The three HeroUI claims @tariq flagged as tarball-only

All three hold.

- **(a) The button primitive has no loading concept — confirmed, stronger than stated.** `button.types.ts` contains **zero** occurrences of `loading` across all 203 lines; the library's own recipe (`button.md:191-211`) passes `<Spinner color={themeColorAccentForeground}/>` as a child. I also checked the thing the plan did not: `ButtonRoot` calls `childrenToString(children)` and, when that returns non-null, **discards the authored children** in favour of one `<ButtonLabel>` (`button.tsx:98,261-265`). That would have silently eaten the spinner. It does not, because `childrenToString` returns `null` the moment any child is a React element (`children-to-string.ts:64-68`), so a `[Spinner, HButton.Label]` array renders as written. Added to decision 2 — it is the kind of thing that looks fine in review and renders wrong.
- **(b) `.button__root` is already `row` with an 8dp `md` gap — confirmed.** `button.css:1-6` and `:50-56`. Same 8 the mockup's own `.cta { gap: var(--space-xs) }` draws. No wrapper needed.
- **(c) Spinner's default colour is the theme accent — confirmed, and it is the most valuable thing in the plan.** `colorMap.default = themeColorAccent` (`spinner.tsx:132-139`); `--accent` is `#d4a44c` (`global.css:18`); `Colors.dark.gold` is `#D4A44C`; the primary CTA's gradient runs `GoldTokens[400] → [600]` = `#E0B968 → #C9973A`. A default-coloured spinner is gold on gold. Not dead complexity — **and the mockup independently agrees**: `.spin { border-top-color: var(--accent-foreground) }` (`mockup.html:356-359`) is exactly the `Colors.dark.bg` the resolver passes. The resolver earns its place.

#### Claims that were false, with the correction

1. **Decision 12's citation was wrong and the disagreement it describes barely exists.** A2's `…You can add more later.` is at `mockup.html:960`; `975-977` is the prose paragraph. More to the point, the three frames that actually draw N3 — E1/E2/E4 at `2042`, `2078`, `2145` — already carry the spec's `…any time.` verbatim. There is no live spec-vs-mockup split, only one un-refreshed anatomy frame. Decision rewritten; the conclusion is unchanged and now costs no escalation argument.
2. **Decision 11 asserted an inheritance MA-005 does not carry.** MA-005's Done-when (`MA-005.md:45`) never requires a back affordance to appear on N3 or N4 — only its body prose at `:39` mentions "the header's back affordance" in passing. The mockup draws a back chevron on both (`mockup.html:2004`, `:2319`). Ship MA-004 with no back button and review MA-005 against its Done-when, and N3/N4 lose their back button with nobody at fault. The deferral itself is correct — deciding what that button does is genuinely MA-005's — so I did not change it; I made the hand-off explicit and addressed it to @sarah.
3. **Step 5.1's brand header would have laid out wrong.** The root copies `stack_header.tsx:24`'s `justify-between`, and the plan gave it three flat children (mark, wordmark, `Setup`) — which spreads them evenly and floats `MoneyApp` into the centre of the header. The mockup groups the first two in one `gap: --space-xs` row (`mockup.html:292-294,1027-1028`). Rewritten as two children.
4. **The Risks table claimed "unit assertions at every `ms()` scale". There is no such thing in this repo.** jest-expo mocks `Dimensions` at 750pt, so `responsiveScale` is clamped to 1.15 and every `ms()` assertion — the new ones and the three already in `geometry_tokens.test.ts` — evaluates at exactly one scale. What makes the fit hold across the range is construction (`Math.floor`, `flex: 1`), not the assertion. Corrected in the Risks table and in the new test's comment.
5. **Decision 8's "at 0.875" is not a scale extreme and not a phone.** I recomputed the naive sum (`2×Spacing.sm + Size.progressThin + Spacing.xs + Size.compactBodyLineHeight`, which is what `mockup.html:296-309` actually composes) across the clamp. At **both** ends it lands exactly on the track with zero slack — 0.85 → 47 vs 47, 1.15 → 63 vs 63 — and it overshoots *in the middle*, including at scale 0.9615, i.e. **any 375 pt phone (iPhone SE2 / 8 / X)**: 54 against a 53 track. The defect is real and the `flex: 1` mitigation is right; the citation now points at a device someone can pick up. The mitigated form holds at both ends (0.85: 23 used of 47; 1.15: 31 of 63).
6. **Decision 4 mis-cited `LoadingCenter`.** `loading_center.tsx:11` passes `GoldTokens[500]` explicitly; it does not omit the colour. The hex is identical to `--accent`, so the conclusion survives — wording corrected so nobody reads it as precedent for omission.
7. **Three line-number drifts in the HeroUI citations,** all corrected: `button.css:1-5,55-60` → `:1-6,50-56`; `button.css:55` → `:50-51`; `spinner.types.ts:51` → `:52`.
8. **Decision 5's "MA-009 and MA-012 inherit this, written down here rather than rediscovered there" was not discharged.** `TEMPLATES.md`'s reading order sends a planner to `spec.md`, then the mockup, then their own task file — never to a sibling task's plan. **I added the deviation to `spec.md` § *Known design/codebase disagreements* as item 6**, the list the spec already tells reviewers to expect deviations in, and pointed decision 5 at it. That is the only edit I made outside this task file.
9. **The status-track glyph sizes were tokens in name only.** `Size.hairline * 5` evaluates to a literal 5 at every scale inside the 0.85–1.15 clamp (`ms(1)` is always 1) — a hardcoded number wearing a token, which is worse than an honest constant. And `Size.iconMicro` is 12 where the mockup sizes the glyph box 14 (`mockup.html:330-332`), a silent 2dp shrink. Both are now named exports of the geometry module (`STATUS_GLYPH_BOX = ms(14)`, `STATUS_IDLE_DOT = ms(5)`), which also makes the fixed column width — the thing that actually keeps the text's left edge still — a checkable value.

#### Claims I checked that were true, and are worth not re-deriving

- **The sixteen `isLoading` call sites are exactly sixteen, and every variant in the table is right.** I opened all sixteen. The task text's "roughly a dozen" was the undercount, not @tariq's sixteen. `grep -rn isLoading src` returns far more lines; the rest are `SkeletonGroup`, dashboard/summary card props, `.state.ts` fields and prop declarations, none of which reach `@/components/ui/button`. `budget_copy_sheet.tsx:88` and `startup_error.tsx:41` do pass no `variant` and default to primary, as the table says. `confirm_dialog.tsx:88` / `confirm_sheet.tsx:131` do switch on `destructive`, so both spinner colours are live.
- **`Size.ctaHeight` has no rendering consumer.** Whole-tree grep: `theme.ts:137` and `sheet.tsx:85` only, and the second is clearance. The 48 measurement is real and rests on the same `--spacing × 16` derivation the spec already ratified for `Input`. **The deviation is the right call** — forcing 52 in onboarding alone would make onboarding's CTA the only 52 in the app, which is a worse inconsistency than 4 pt against a drawing.
- **The rail was genuinely left unnamed.** `theme.ts:201-208` carries MA-001's five and no rail. 55 is `spec.md:51`, `mockup.html:296` and A1's `<span class="measure">55</span>`. Worth adding: `spec.md:31` says a number given in the Product & UX section "is load-bearing and appears in a token" — so `Size.progressRail` **executes** the spec rather than exceeding it. The Risks row offering to drop it can be ignored.
- **Decision 7's arithmetic.** At the jest scale 1.15, `ms(34) = 39` but `2 × ms(17) = 40` — the clip is real and would have shipped. `Math.floor(statusTrack / 2)` holds at both ends (1.15: 38 ≤ 39; 0.85: 28 ≤ 29) and still equals the mockup's 17 at the 390 pt reference.
- **Both precedents exist and are genuinely pure.** `src/components/ui/display_headline.geometry.ts` and `src/components/ui/logo_mark.a11y.ts`, no React import in either.
- **Step 3.5's two test claims.** `startup_error.test.ts` asserts `resolveStartupRetryPresentation` output only, and `startup_error.helpers.ts:5` really does already set `label: Strings.loading` itself — that button has always resolved the string twice. `balance_review_alert.test.tsx:8-25` mocks `@/components/ui/button` with its own `'Loading...'` literal and never sees the real component. Both correctly left alone.
- **Decision 9.** `more_accounts/index.tsx:55` is a `FlashList` and all four routes already carry their own 16 pt horizontal padding, matching `.body`'s `--space-md` (`mockup.html:311`). A shell-owned scroller would nest a virtualised list.
- **Boundaries.** No `src/app/` file needs touching — all four routes are one-line re-exports and `(onboarding)/_layout.tsx` is untouched. `progress_dots` has exactly the four consumers named and no test. The gradient is preserved. All four bodies move across intact, and the line ranges cited for each (`welcome:25-64`, `add_account:48-57`/`61-343`/`346-358`, `more_accounts:28-90`, `ready` CTA at `:75`) are accurate.

#### Left alone deliberately

- **No render test for the shell.** `.claude/rules/tests.md:19` is "keep the files, don't add to them" for `.tsx` render suites, so the pure-module + emulator split is the honest maximum here, not a shortcut. I added a paragraph at step 4.1 saying exactly what the test proves and what it does not, so the green tick is not later mistaken for proof that the components consume the constants.
- **The mockup dims the busy CTA to `--color-gold-700` (`mockup.html:349`); the plan's gradient wrapper does not.** Subsumed by `spec.md` disagreement item 1 — noted in the plan summary, not made a fourth decision.
- **The whole test strategy for Done-when's "the button does not move a pixel".** That is not reachable from a `.ts` file and the plan does not pretend it is: it is § B's `n3` vs `n3-error` screenshot diff, with the CTA's y compared for identity. Correct allocation.

#### Critical triggers

**None, confirmed independently.** No `package.json` change — `Spinner` is already imported from `heroui-native` in four files (`loading_center.tsx:1`, `detail_header.tsx:2`, `transaction_form_body.tsx:4`, `categories/index.tsx:2`). No `app.json`, no `android/`, no `ios/`, no plugin list. No file under `src/database/migrations/`. No auth or secure-store surface. The one cross-task binding (CTA 52/48) is now recorded in the spec rather than asserted in a plan, which is what stops it becoming one.

Ready for @dev.

---

## Implementation review

### Round 1 — approved

Verdict: **approved**, with one escalation that is not @dev's to fix (§ *Unverified* below). The diff is exactly the nineteen files the plan named — nine created, eight modified, two deleted — and nothing else. I checked the three places the two prior reviews said the risk sits and all three hold. No defect found in any of the five recurring classes. Local CI parity is green except `expo-doctor`, which fails on two network checks in this container and not on anything in the diff.

**The one thing that did not happen: there is no emulator in this container** (no `adb`, no `emulator`, no `ANDROID_HOME`, no Android SDK). My independent step-7 run could not be performed, and neither the task file nor commit `96e0e15` records a step-6 emulator note, so the task's three device-only Done-when items are undischarged going into gate 3. See § *Unverified* — that list is now gate 3's, and it is longer than usual.

#### The three risk areas, checked

- **The spinner is a sibling of `HButton.Label`, not a wrapper.** `src/components/ui/button.tsx:58` and `:72` render `{showSpinner ? <Spinner size="sm" color={spinnerColor}/> : null}` immediately before `<HButton.Label>` in both branches, with no `View` and no `flexDirection`. `ButtonRoot` calls `childrenToString(children)` (`heroui-native/src/components/button/button.tsx:98`) and only substitutes a stringified `<ButtonLabel>` when that returns non-null; `children-to-string.ts:63-68` returns `null` as soon as any array member is a React element, and both branches always carry at least one (`LinearGradient` + `Label`, or `Label`). Authored children survive. Layout comes from `.button__root` (`row`/`center`) — correct, and one correction to plan decision 2 for the record: the gap on the primary CTA is **6dp, not 8** — `.button__root--variant-primary { gap: calc(var(--spacing) * 1.5) }` (`button.css:9`) sits alongside `.button__root--size-md { gap: … * 2 }` (`:50-52`), so whichever wins the merge the value is 6 or 8. Cosmetic, no action.
- **Spinner colour reaches the render.** `button.content.ts:44` returns `Colors.dark.bg` (`#0F1923`) on `primary` and `:47` `Colors.dark.text1` (`#F0EBE3`) on `danger`; `button.tsx:24` defaults `variant = 'primary'`, so the two call sites that pass no variant (`startup_error.tsx:41`, `budget_copy_sheet.tsx:88`) resolve dark, not gold. `Spinner` declares `color = 'default'` as a **default parameter** (`spinner.tsx:79`), so the `undefined` the non-primary branches return degrades to the accent rather than to a crash, and `colorMap[color] || color` (`spinner.tsx:132-140`) passes an arbitrary hex straight through. The comment at `button.content.ts:37-41` warning a future reader off deleting the prop is present.
- **The rail rounding mitigation is implemented as corrected.** `onboarding_shell.geometry.ts:26` is `Math.floor(Size.statusTrack / 2)`, not `ms(17)`. `onboarding_progress_rail.tsx:52-58` gives the label row `flex: 1` with `justifyContent: 'space-between'` and `alignItems: 'center'`, and carries **no** `Spacing.xs` offset between bar and label — which is the whole point of decision 8, since that fixed offset is what overshoots a 53pt track at 54 on a 375pt phone. **No test in the diff claims multi-scale coverage.** `geometry_tokens.test.ts:46-59` states in its own comment that jest pins `Dimensions` at 750pt and that it evaluates at one scale.

#### Test honesty — what is actually proven

I mutated the two load-bearing values and re-ran, then reverted (`git status` clean afterwards):

- `STATUS_TRACK_LINE_HEIGHT` → `ms(17)` ⇒ `onboarding shell geometry › holds exactly two status lines at every scale` **fails**. The assertion catches precisely the clip the plan predicted.
- `spinnerColor: Colors.dark.bg` → `undefined` ⇒ two `resolveButtonContent` rows **fail**. The gold-on-gold regression is caught.

Both are real assertions over real module output — no mock in either file. Judged against Done-when, item by item:

| Done-when | Proven by | Standing |
|---|---|---|
| Four routes render the same four tracks at the same offsets | **Diff reading only.** All four routes render `<OnboardingShell>` as root; `grep` for `height:` / `h-[` across the four route files returns nothing, so no route re-authors a track. Offsets are identical by construction. The pixel comparison is emulator-only. | Structurally sound, visually unverified |
| Status track holds a footnote and swaps to an error in the identical box | `resolveStatusTrack` table (3 rows, incl. `''` → idle) proves the **swap logic**; the box is a fixed-height `View` whose height and glyph-column width do not depend on tone (`onboarding_status_track.tsx:36-49`), so "identical box" holds by construction. Nothing in this diff passes `statusMessage`, so **the error branch renders nowhere in this build**. | Logic proven, pixels unverified |
| A test asserts track heights against MA-001's named values | `onboarding_shell_geometry.test.ts:19-24`, and the four `.tsx` files all consume `ONBOARDING_SHELL_TRACKS` rather than literals — I checked each. Genuinely discharged. | ✅ |
| Busy button renders a spinner + caller-supplied label, tests for supplied and omitted | The **resolver** is table-tested for both (rows 3 and 4). That the resolved values reach a rendered `Spinner` is diff-read, not tested — correct under `.claude/rules/tests.md:19`, which forbids adding `.tsx` render suites. | Mapping proven, wiring diff-read |
| A busy button outside onboarding watched on the emulator | Not possible here. | ❌ gate 3 |
| The flow reaches the dashboard exactly as today | No hook, store or repository file is in the diff; all three `router.push` calls are untouched. | Statically safe, unwalked |

#### Boundaries and defect classes

- **No navigation change.** The diff touches no `*.hook.ts`, `*.store.ts` or repository. `welcome.hook.ts`, `add_account.hook.ts`, `more_accounts.hook.ts`, `ready.hook.ts` are byte-identical.
- **Gold gradient preserved** — `button.tsx:51-57` untouched.
- **Route bodies unchanged.** N2's `<ScreenScroll>` moves across with its `contentContainerStyle` and `keyboardShouldPersistTaps` intact; N3's `FlashList` container and N1/N4's `Box` bodies are unmodified. Every level of the new nesting is `flex: 1`, so the `FlashList` stays bounded.
- **`progress_dots` fully removed** — `grep -rni "progress_dots\|ProgressDots" src __tests__` is empty; `BackButton` still has three consumers elsewhere, so nothing else was orphaned by the deletion.
- **`Strings.o4Title` removal is safe** — `grep -rn "o4Title" src __tests__` is empty (only this task file mentions it).
- **`src/app/` untouched**; no route `index.tsx` changed, no non-route file placed beside a `_layout.tsx`.
- **N3/N4 have no back affordance**, as decision 11 intends.
- **Five defect classes:** none present. I specifically expected class 1 (silent async failure) to bite here, because this task delivers the status track that later tasks feed — it does not, because the diff adds no async path at all and leaves the existing `void handleSave()` / `void handleContinue()` calls exactly as they were. Classes 3, 4 and 5 have no surface in this diff (no money, no SQL, no durable state). Class 2 has no `useFocusEffect`. Worth one line so nobody re-derives it.

#### Suggestions

1. `__tests__/screens/onboarding_shell_geometry.test.ts:47-49` re-derives `filled` with the same `Array.from({length}, (_, i) => i < step)` expression the implementation uses, where the plan asked for literal `[t,f,f,f] … [t,t,t,t]`. It still catches an off-by-one (the expected value is computed in the test, not read from the model), so it is not vacuous — but a literal array is a strictly stronger change-detector and costs four lines. Worth doing next time this file is opened; not worth a round.
2. `onboarding shell geometry › holds exactly two status lines at every scale` is **algebraically tautological** given `STATUS_TRACK_LINE_HEIGHT = Math.floor(track / 2)` — `2·⌊x/2⌋ ≤ x` and `> x−2` hold for every integer. Its real value is as a change-detector against re-authoring the constant, which the mutation above confirms it delivers. The name is defensible (the property genuinely is scale-invariant), but a one-line comment saying "this is a guard on the *formula*, not a measurement" would keep a future reader from over-reading the green tick.
3. `geometry_tokens.test.ts:57` has ~9dp of slack at the only scale jest evaluates (54 against 63), so as a fit check it is loose; the value-lock `expect(Size.progressRail).toBe(ms(55))` two lines up is what would actually catch a token change. Keep both — the plan asked for exactly this and the honesty comment is the important part.

#### Nits

- `cn(...)` wrapping a single ternary at `onboarding_progress_rail.tsx:43` and `onboarding_status_track.tsx:71` does nothing a bare ternary would not. Harmless.
- `Size.progressDot` (`theme.ts:193`) is now unconsumed — it was the deleted `ProgressDots`' only reader. `Size.progressTrack` was already unconsumed before this diff, as was `Size.ctaHeight` until now, so an orphan token is not new here. Deleting it is out of this plan's scope; flagging it so it can ride along with a future theme sweep.
- The N1 brand wordmark (`onboarding_header.tsx:66`) takes no colour class and relies on `Typography`'s default. Fine unless the default is not `text-foreground` on a dark surface — a gate-3 glance, not a change.

#### Unverified — this all falls to gate 3 device QA

No emulator exists in this container, so **neither** the step-6 nor the step-7 run happened. @sarah: this is the sharpened list, and it is longer than the task's own § C because nothing in § A–D was walked by anyone.

1. **Track offsets across N1–N4.** Screenshot the four routes and confirm the footnote's top edge and the CTA's top edge sit at the same y on all four. A route that differs means a body still owns chrome.
2. **The error swap does not move the CTA.** Nothing in this build passes `statusMessage`, so the error branch of the status track has never rendered anywhere. Force one (the task's § B recipe) and diff the CTA's y for *identity*, not similarity. Also confirm the glyph swap (dot → `alert-circle-outline`) does not shift the text's left edge.
3. **The status track at accessibility font sizes.** It is a fixed 34 with `overflow: 'hidden'` and `numberOfLines={2}`, i.e. it clips rather than grows. That is the deliberate reading of `spec.md` § *The zero-shift contract* (the spec's table marks only the field message rail as a *minimum*), and it is right — growing it would move the CTA, which is the defect the track exists to prevent. But it means a two-line error at 200% font **will** truncate. Someone has to look at that at the largest OS font scale and decide it is acceptable.
4. **The rail announces as one node.** `mqa ui` on N1 must show `Step 1 of 4, Choose your currency` as a single accessible node and must **not** list four segment nodes. The `accessible` + `accessibilityElementsHidden` + `importantForAccessibility` combination is unverifiable in CI.
5. **A busy spinner outside onboarding** — the reason this task is `verify: emulator`. Budget → Income → Save must show a **dark** spinner on the gold fill; Accounts → account → Archive (`danger`) must show a **bone-white** one. Gold-on-gold is the failure mode and it is invisible in a screenshot only if you are not looking for it.
6. **Narrow busy buttons.** `confirm_dialog.tsx:91` and `confirm_sheet.tsx:133` render two `flex: 1` buttons side by side; each now has to hold a 16dp spinner + 6–8dp gap + `Loading...` in roughly half a dialog width. Arithmetic says it fits; check for truncation on the smallest device in the matrix.
7. **The full flow to the dashboard**, with the `mqa db` assertions from the task's § D — one account row with `current_balance = opening_balance`, and `onboarding_complete = 'true'` / `base_currency = 'EGP'` in `app_settings`.

#### Hand-off still open (not this diff's defect)

Plan decision 11's flag is unresolved and now shipped: **N3 and N4 have no back affordance**, mockup frames E1 (`mockup.html:2004`) and F1 (`:2319`) draw one, and MA-005's Done-when (`MA-005.md:45`) does not require one to appear. If MA-005 is reviewed against its Done-when, both screens lose their back button with nobody at fault. @sarah: MA-005's plan must add it, or its Done-when must gain the line.

#### CI parity evidence

Run in order on `96e0e15`, actual output:

| Step | Result |
|---|---|
| `npm run format:check` | exit 0 — "All matched files use the correct format", 740 files |
| `npm run lint` | exit 0 — warnings only, all in `jest.setup.js` and `scripts/`; **zero** on any file in this diff |
| `npm run typecheck` | exit 0 |
| `npm test -- --ci` | exit 0 — **230 suites, 2121 tests passed**, 13.7s |
| `npx --yes expo-doctor@1.20.1` | **exit 1 — environmental.** 18/20 passed. The two failures are *Check Expo config schema* (`SyntaxError: Unexpected token 'H', "Host not i"… is not valid JSON` — the agent proxy's 403 body where `api.expo.dev` JSON was expected) and *Validate packages against React Native Directory* ("Directory check failed with unexpected server response"). Confirmed not diff-caused: `git diff 40c4262..96e0e15 -- app.json package.json package-lock.json` is **empty**, `git diff app.json` is empty, and `npx expo config --type public` exits **0** locally. |
| `npx expo prebuild --no-install --platform android` | exit 0 — "Finished prebuild"; `test -d android` passes |

`git status --porcelain` is clean after the run (the forced-error edit risk from the plan's § B never materialised — no such edit exists in the diff, and my two mutation probes were reverted and verified).

@sarah: the branch is ready to push. Push it with the gate-3 list above carried into the PR body — device QA is doing double duty on this one.

---

## PR review

### Round 1 — approved with edits

Verdict: **approved with edits**. Nothing in the code changes. The five things step 7 could not see produced four findings: a PR title that would land a non-conventional subject on a history that is 100 % conventional, empty `branch:`/`pr:` frontmatter, and two gaps in the gate-3 list — one of which is the only place this shell can plausibly hurt a user. Real-runner CI is fully green, including the `expo-doctor` job that fails locally, and there is zero drift against `main`.

**Environment note, because it bounds this verdict.** `gh` is not installed in this container and the GitHub REST API is blocked at the session policy layer (`api.github.com` → *"GitHub access is not enabled for this session"*). Everything below was read from authenticated `github.com` HTML and `git ls-remote`, which do work. **I could not read job logs** — GitHub serves *"Sign in for the full log view"* anonymously. Job conclusions, names and durations are first-hand; the contents of the jest run are inferred, and I say so where it matters.

#### 1. Real-runner CI — green, and the local `expo-doctor` failure is adjudicated as environmental

Workflow run [31189631327](https://github.com/MustaMohamed/MoneyApp/actions/runs/31189631327) on `e54e7fa`, all seven jobs, rollup **success**:

| Job | Result |
|---|---|
| Install | succeeded, 20s |
| Typecheck | succeeded, 21s |
| Lint | succeeded, 18s |
| Format check | succeeded, 16s |
| **Expo doctor** | **succeeded, 26s** |
| Unit tests (Jest) | succeeded, 45s |
| Prebuild dry-run (Android) | succeeded, 33s |

- **`expo-doctor` is not a finding.** It ran on the real runner, where `api.expo.dev` and `cdp.expo.dev` are reachable, and passed in 26s. The local exit 1 that @impl-reviewer recorded is this container's proxy 403 on the two network checks, exactly as diagnosed. Expo's live requirement table has not moved against us either — the job is green with the pinned `1.20.1`.
- **`prebuild-check` genuinely ran.** 33s with `test -d android` passing, on a checkout that has no `android/`. No config-plugin regression; `app.json`, `package.json` and `package-lock.json` are untouched by this branch, so the `@expo/config-plugins` class of escape has no surface here.
- **CI ran against the merge result, not the branch tip alone.** `refs/pull/217/merge` resolves to `b850218`, so GitHub computed a conflict-free merge and the `pull_request` event tested it.
- **Caveat worth recording, not blocking.** `.github/actions/setup` restores a `node_modules` cache keyed on `hashFiles('package-lock.json', 'patches/**')` and only runs `npm ci` on a miss. The Install job's 20s is a cache hit, so **CI did not perform a fresh resolve on this PR**. That is fine here — neither the lockfile nor `patches/` changed, so the tree is byte-for-byte the one that shipped MA-003 green — but it means a green CI on this repo is not, by itself, evidence that a clean `npm ci` produces a flat tree. Anyone bumping a dependency should not read this run as covering that.
- **Not verifiable from here:** that the jest job's 2121 tests include the two new suites. `jest.config.js` discovers `__tests__/**` with no path filter that excludes either new file, and the command CI runs is the same `npm test -- --ci` @impl-reviewer ran locally to 230 suites / 2121 tests. Inference, not observation, because the logs are behind a sign-in.
- One pre-existing warning annotation, unrelated to this diff: `actions/cache@v4` targets Node 20 and is being forced onto Node 24.

#### 2. Merge-base drift — none

`origin/main` is at `c66545f`, which is exactly the commit this branch forked from. `git rev-list --left-right --count origin/main...HEAD` → `0 4`: zero behind. No task from this scope has landed since MA-003, so the four files the brief named as drift candidates (`theme.ts`, `strings.ts`, `button.tsx`, onboarding) have no competing edits. Nothing to find, and I checked rather than assumed.

#### 3. The commit that will actually land — two problems, both at the button, not in the code

**3a. The PR title is not a conventional commit, and the squash subject is the PR title.** `main` is a pure linear squash history and its last fourteen subjects are all `type(scope): subject (#N)`, with the task ID inside the subject where the change belongs to one:

```
refactor(workflow): task status moves from the repo to GitHub issues (#216)
feat(design-system): Cross Fan launcher, adaptive, splash and in-app mark (MA-003) (#202)
feat(design-system): display headline primitive, gradient treatment (MA-002) (#198)
feat(design-system): colour families, geometry tokens and the 32-colour palette (MA-001) (#194)
```

PR #217 is titled `MA-004 — Onboarding shell: four fixed tracks and an honest busy CTA`. Squashed, that lands as `MA-004 — Onboarding shell: four fixed tracks and an honest busy CTA (#217)` — the first subject in this scope with no type and no scope. The correct subject already exists as commit `96e0e15`'s: **`feat(onboarding): shell with four fixed tracks and an honest busy CTA (MA-004)`**. Retitle the PR to that string before merging. Nothing else about the squash needs touching — the body is accurate and honest about what lands, and `Closes #206` will close the issue.

**3b. The repository's default merge method is `MERGE`, not `SQUASH`.** Read off the PR page payload (`defaultMergeMethod: "MERGE"`). `main` has zero merge commits, so the green button's default would put a merge bubble and four commits — including two docs-only ones — onto a history that has never had one. @sarah, this is the user's button to press: tell them explicitly to pick **Squash and merge**.

#### 4. Diff membership — clean

21 files against `origin/main`, and every one belongs:

- **No generated or ignored output.** `git ls-files` matches nothing under `android/` or `ios/`. No `.env`, no stray patch, no snapshot file, no `package.json`/`package-lock.json` change, so no unrelated dependency bump rode along.
- **The docs commits are docs-only.** `509b857` touches one file (`tasks/MA-004.md`); `40c4262` touches two (`spec.md`, `tasks/MA-004.md`) — both under `docs/scopes/MA-onboarding-redesign/`. No source file is in either.
- **No file from another task's scope.** Every `src/` path is either the shell, the shared button, the two constants files, or one of the four onboarding routes.
- **The forced-error edit from plan § B did not survive.** `statusMessage` is passed by nothing in the diff; `more_accounts/index.tsx` carries no hardcoded error string. That was the plan's own named risk and it is discharged.
- **No leftover debug logging.** No `console.` in any added file.
- **Both deliberate deviations are recorded where MA-009 and MA-012 will read them**, as the brief required me to confirm: `spec.md` § *Known design/codebase disagreements* item 1 (gold gradient CTA stays) and item 6 (`The CTA slot is 52; the button drawn inside it is 48. Recorded at MA-004 step 5, binds MA-009 and MA-012.`). Item 6 is the one added during this task, and it is in the spec, not only in this plan file.

#### 5. Step 7 escapes and gate-3 list completeness

**5a. Escape — the task frontmatter was never updated with the branch and the PR.** `branch:` and `pr:` were both empty on this branch. MA-002 and MA-003 both carry them filled (`pr: https://github.com/MustaMohamed/MoneyApp/pull/198` / `/202`), and `TEMPLATES.md` defines both fields. Step 7 owns the push and the PR open, so filling them is step 7's, and neither the diff review nor the local parity chain can see a field that only becomes knowable once the PR exists. **Fixed in this commit** — recorded here as an escape because it is the second artifact-hygiene item to reach this gate rather than being caught upstream, and if it recurs the step-7 checklist needs the line, not the reviewer.

**5b. Gate-3 list gap, and this is the substantive one: nothing in the seven items looks at the N2 form with the keyboard open.** The shell adds a large amount of *permanent* chrome to the one onboarding screen that has to coexist with a keyboard. Measured from the tokens, at scale 1:

| | before | after |
|---|---|---|
| header | 56 | 56 |
| rail / dots | 20 (`Size.progressDot × 5`) | 55 (`Size.progressRail`) |
| footer | 80 (`pt-2` 8 + button 48 + `pb-6` 24) | 130 (`Spacing.sm` 12 + `statusTrack` 34 + `Spacing.sm` 12 + `ctaHeight` 52 + `Spacing.lg` 20) |
| **total fixed chrome** | **156** | **241** |

That is **+85 dp taken out of the scrolling viewport on N2**, which is the account form. On a 375 × 667 device with a ~270 dp keyboard raised, the form's visible area goes from roughly 240 dp to roughly 155 dp — a third of it gone. The structure is not wrong (the footer sits outside `ScreenScroll` exactly as it did before, and `keyboardShouldPersistTaps="handled"` survives), so nothing here is a code defect and I am not asking for a change. But it is device-only, it is the single place this shell can plausibly make the app worse, and **item 7 walks the flow without naming it**. Add as an eighth item:

> 8. **N2 with the keyboard up, on the smallest device in the matrix.** The shell takes ~85 dp more permanent chrome than the old N2 chrome did. Focus *Account name*, then *Opening balance*, and confirm the focused field is still visible above the keyboard, that the form scrolls to reach the credit-card block when Credit Card is selected, and that the footer's status track and CTA are not squeezed against the keyboard.

**5c. Gate-3 list gap, minor.** @impl-reviewer's own nit 3 — the N1 brand wordmark at `onboarding_header.tsx:66` takes no colour class and relies on `Typography`'s default — was described there as "a gate-3 glance" and then not carried into the seven items. It costs one line on the N1 screenshot:

> 9. **The N1 wordmark renders in the foreground colour.** `MoneyApp` beside the Cross Fan mark takes no explicit colour class; confirm it is not a muted or default-grey on the dark surface.

Everything else in the seven-item list checks out against the diff. Item 2's premise is right (`statusMessage` is passed nowhere, so the error branch has genuinely never rendered), item 3's numbers are right (`Size.statusTrack` is `ms(34)`, `overflow: 'hidden'`, `numberOfLines={2}`), item 5 names the two correct call sites and the correct expected colours, and item 6's two `flex: 1` buttons are really there in `confirm_dialog.tsx` and `confirm_sheet.tsx`.

**The open hand-off is not a defect in this diff.** N3 and N4 render `OnboardingShell` with no `onBack`, and `onboarding_header.tsx` renders a `w-9` spacer in that case, so the title stays centred and all four header tracks are the same height. Omitting the affordance is plan decision 11 executed as written, and this diff contains no navigation code that could own it. The risk is entirely in MA-005's Done-when, and it is recorded on #207.

#### Edits required before merge

None touch code. All three are @sarah's:

1. **Retitle PR #217** to `feat(onboarding): shell with four fixed tracks and an honest busy CTA (MA-004)`.
2. **Add gate-3 items 8 and 9** above to the PR body's device-QA list.
3. **Tell the user to use Squash and merge**, not the repository's `MERGE` default.

Frontmatter `branch:`/`pr:` is already fixed in this commit. Re-review is not needed for any of the three; this is `approved with edits`, and the code is done.
