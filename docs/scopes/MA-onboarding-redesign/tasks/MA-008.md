---
id: MA-008
scope: MA-onboarding-redesign
milestone: M2
issue: 210
verify: emulator
branch:
pr:
---

# MA-008 — Onboarding adopts the shared account form

## Summary

Delete onboarding's copy of the account form, host the shared one instead, and fix the two onboarding-side defects that only stop being duplicated once there is one form.

## Details

This is the second half of the de-duplication MA-007 began, and it is the one that touches the live first-run path. When it merges, the second onboarding step should look and behave as it does today — same fields, same order, same copy — but rendered by the Accounts module's form, with onboarding keeping only what is genuinely its own: the starting currency, its own copy, the back behaviour, the step it persists after a save, and where it goes next.

**Everything onboarding duplicated goes in this task**: its copy of the form body, its copy of the submission mapping, its own twelve-colour list, and its hand-rolled swatch row. The swatch row is replaced by the colour sheet MA-006 built — onboarding picks that up as a side effect of adopting the shared form, and it is the first visible change on this screen.

**The starting currency is onboarding's, not the form's.** The form takes a starting currency from its host; onboarding passes the base currency the user chose on the first step. Settings passes its own default. Neither knows about the other.

### Two live defects fixed here

- **The "adding another account" flag is a string tested for truthiness.** An explicit `false` therefore takes the add-more branch, and the stack's own parameter type claims the value is a boolean when it is not. It must be compared exactly, and the type must stop lying. MA-011 depends on this working — its whole add-another round trip runs through this flag.
- **The screen reloads the account list on mount** even though startup already loaded it and saving publishes its own refresh. The load goes.

### The post-save checkpoint

This is behaviour the mockup draws and no task owned. Frame A3's caption states it and C4/C6 are where a user meets it: **when the account insert succeeds but the step write fails, the retry repeats only the step write and never re-inserts the account.**

The failure the user sees is about the step, not the data. Their account is on disk. Tapping the primary action again must not produce a second account — and it must not be prevented from retrying either, because the step really does need writing. The error renders in the shell's status track, which MA-004 delivered; the form reports its failure and the host renders it.

This is the interaction between the guarded save path from MA-007 and the persist-before-navigate ordering from MA-005, and it is the single most valuable thing to force on the emulator in this task: make the step write fail with an account already inserted, tap retry, and count the rows.

**Behaviour that must be preserved exactly:** duplicate names still rejected case-insensitively after trimming; current balance still equals opening balance; credit-only values still absent rather than zero on non-credit accounts; and a successful save still advances to the more-accounts step, persisting before it navigates and replacing rather than pushing, per MA-005.

**Done when:** both entry points render from the same form, and the duplicated form body, submission mapping and colour list exist in exactly zero places; the add-more flag is compared exactly, its declared type matches what is actually passed, and a test covers the explicit-`false` case that is broken today; no onboarding route loads the account list itself; a forced step-write failure after a successful insert leaves exactly one account row, shows the error in the status track, and retries into the next step without inserting again; and creating an account from onboarding and from Settings on the emulator both produce a correct row, checked against the database.

---

## Plan

### Summary

- **Onboarding's N2 becomes a host, nothing more.** `add_account.hook.ts` drops its own RHF/Zod setup, its own `ACCOUNT_COLORS`, and its own `parseFloat` submission mapping, and calls `useAccountForm` with three things onboarding actually owns: the base currency, `Strings.n2SaveError`, and an `onSaved` that writes the step and replaces the route. `index.tsx` drops ~280 lines of duplicated field markup and renders `<AccountForm form={form} ownerId="onboarding/add_account" />` inside the shell it already has.
- **One re-entry guard and one busy flag on the save path — the form's.** `runOnboardingTransition` is *not* nested inside `onSaved`; the tail is written inline from `resolveOnboardingStep` + `ONBOARDING_STEP_HREF`, and it throws rather than swallowing, so `useAccountForm`'s `inserted` latch and `failSave` are what implement the post-save checkpoint. The transition state survives on this screen for `onBack` only, and the two are interlocked explicitly in both directions.
- **`useAccountForm.submit` must skip re-validation once `inserted` is latched** — **D9, added at plan review.** Without it the post-save checkpoint is unreachable on a real store: `addAccount` awaits its own `loadAccounts()` (`account.store.ts:85`), the memoised schema rebuilds from an `accounts` array that now contains the row just written (`use_account_form.hook.ts:50-51`), and the retry tap fails `errNameDuplicate` inside `handleSubmit` before `onValid` ever runs. This is a change to `use_account_form.hook.ts`, which the rest of this plan treats as untouched.
- **`isAddingMore` is compared with `=== 'true'`**, once, at the top of the hook, and `OnboardingStackParams` stops declaring it a boolean.
- **All three onboarding `loadAccounts()` call sites go** — N2, N3 and N4 — because `spec.md:502` names exactly those three and the Done-when is the plural one.
- **`CurrencySelector` gains the accessible name** onboarding is otherwise about to lose (flagged for this task at MA-007 step 5, `MA-007.md:108`).
- **Tests:** `__tests__/screens/onboarding_add_account.hook.test.ts` is rewritten to **seventeen** cases against the new hook shape, including the explicit-`false` case that is broken today (both on save and on back), the retry-writes-only-the-step case, both halves of the interlock, both directions of the status-track precedence, and the back-inside-validation race. `__tests__/account_form.hook.test.ts` gains one case for D9. Four other suites are cited rather than duplicated.
- **Emulator:** four scoped scenarios, one of which forces `setStep` to reject exactly once with an account already inserted and counts rows in SQLite.

### Detail

#### Step 0 — decisions made before any code, with the evidence

**D1 — how the two guarded paths compose. The form owns the save; the transition state owns the back.**

The two candidate designs, and why the second one loses:

| | A: `onSaved` calls `runOnboardingTransition` | **B (chosen): `onSaved` is the tail and it throws** |
|---|---|---|
| Re-entry guard on the CTA | two — `beginSave()` (`account_form.state.ts:45`) *and* `begin()` (`onboarding_transition.state.ts:41`) | one — `beginSave()` |
| Busy flag the CTA renders | two — `saving` and `busy` | one — `saving` |
| Error channel | `runOnboardingTransition` catches and calls `api.fail` (`onboarding_transition.ts:64`), so `onSaved` never rejects, `failSave` never runs, and `state.errorMessage` is permanently `undefined` | `failSave(Strings.n2SaveError)` — exactly the contract `use_account_form.hook.ts:26-34` documents *for this task* |
| Back-in-flight + Save | **silent orphan insert.** `beginSave()` passes, `addAccount` writes the row, then `begin()` returns `null` (`busy` is true from `onBack`), `runOnboardingTransition` never runs, `onSaved` resolves normally, `finishSave()` clears the flag. An account on disk, no step written, no error shown. Today this cannot happen because `begin()` runs *before* `addAccount` (`add_account.hook.ts:75`) | blocked at the CTA by the D3 interlock, before validation |

B is chosen. A is not merely redundant; its third row is a live data defect it would introduce.

**What B gives up, and what replaces it.** `runOnboardingTransition` contributes five things to today's save path. Four are kept:

| Contribution | Under B |
|---|---|
| `resolveOnboardingStep(desiredStep, count)` called **after** the insert (MA-005 Decision 2 row 2 — the first-account hard loop) | called directly in `onSaved`, which by construction runs after `markInserted()` (`use_account_form.hook.ts:67-72`) |
| persist before navigate | two statements in `onSaved`, in that order, with no `try` between them — a rejecting `setStep` cannot reach the `router.replace` line |
| `invalidate()` immediately before every navigate (MA-005 Decision 3) | `useAddAccountTransitionState.getState().invalidate()` on the line before `router.replace` |
| `ONBOARDING_STEP_HREF[resolved]`, `replace` not `push` | same map, same call |
| `isCurrent(session)` between the insert and the step write | **dropped — see D4** |

**D2 — one status track, two writers, stated precedence. *Corrected at plan review.*** `OnboardingShell` takes a single `statusMessage` (`onboarding_shell/index.tsx:19`). After D1 the save path writes `useAccountFormState.errorMessage` and the back path writes `useAddAccountTransitionState.statusMessage`. The hook merges them as:

```ts
statusMessage: transitionStatusMessage || formErrorMessage
```

Transition wins because it is the later event in the only sequence where both are set: a failed save leaves `errorMessage` set, and if the user then taps back and *that* fails, the message the user needs is the one about the chevron. The reverse precedence would pin the stale save error.

**The original argument for why nothing can go stale was wrong, and the fix is mandatory.** It said "`begin()` clears `statusMessage` on every accepted back attempt and `beginSave()` clears `errorMessage` on every accepted save attempt, so neither channel can hold a message from an attempt two operations ago." Each of those clears only *its own* channel — `beginSave` (`account_form.state.ts:47`) sets `{ saving: true, errorMessage: undefined }` and never touches the transition store; `begin` (`onboarding_transition.state.ts:44`) sets `{ busy: true, statusMessage: '' }` and never touches the form store. With a fixed precedence the higher-precedence channel therefore pins: **back fails → user taps Save → save fails → the track still reads `onboardingBackSaveError`**, naming a chevron the user did not touch.

`ready.hook.ts` already hit this and solved it — `handleComplete` calls `useReadyTransitionState.getState().reset()` at the start of every completion attempt, with a comment (`ready.hook.ts:57-61`) saying exactly this. Mirror it: `handleSave` clears the transition channel before it submits (code in step 2.3). The early return on `busy` above it guarantees `reset()` cannot clobber a live back transition. Consequence to accept deliberately: a save that fails *validation* now also clears a standing back error, where today it would leave it up. That is the right way round — the user's latest intent is "save".

Pinned by tests 15 **and 16** (both directions; 16 fails without the `reset()`).

**D3 — the interlock, in both directions, because there are still two guards on this screen.**

- `onBack` returns early when `useAccountFormState.getState().saving` is true. This also makes `spec.md:81` ("Back disabled during the write") true for the first time, and it restores exactly the coverage today's single shared `busy` gave.
- `handleSave` returns early when `useAddAccountTransitionState.getState().busy` is true, **before** calling `submit()`, so nothing is validated and no state moves. Silent, matching `spec.md:492` ("CTA re-entry is ignored while an operation is active") and matching today's behaviour, where `begin()` returns `null` with no message.

Residual, **corrected at plan review**: both checks read a flag that is set *inside* an async callback, so there is a window after a Save tap — the whole of Zod validation — where `saving` is not yet set. The original claim that this window "is identical to `main`'s, so this is not a regression" is **false**. On `main` there is one shared gate and it sits *after* validation (`add_account.hook.ts:75`, inside `onSubmit`): if a Back lands during a Save's validation, Back's `begin()` wins and the Save's own `begin()` then returns `null`, so **`main` inserts nothing**. Under D1/D3 the two guards are separate and `handleSave`'s check runs *before* `submit()`, so the same interleaving inserts a row, writes N3 and fires a second `replace` while the back transition is navigating to welcome. That is a new race, and it is exactly what the dropped `isCurrent()` used to catch. It is closed in D4.

**D4 — what replaces `isCurrent()` on the save path. *Corrected at plan review.*** The only producer of a competing session on this screen is `onBack`; nothing else calls `begin()` on `useAddAccountTransitionState` (grep confirmed: `add_account.hook.ts` is the file's sole consumer, and `add_account.state.ts` creates a per-screen instance, `onboarding_transition.state.ts:34`). D3 stops that producer at the source *for taps that arrive after `saving` is set*, and the `invalidate()` in D1 bumps the session before every navigate, so any back transition that somehow got in flight cannot navigate afterwards (`onboarding_transition.ts:68` re-checks `isCurrent` after `persist`).

Prevention at the source does **not** cover the validation window described in D3, so the mid-flight check is not deleted, it is relocated: **`onSaved` re-reads `useAddAccountTransitionState.getState().busy` as its first statement and returns early if a back transition is live** (code in step 2.3). Returning — not throwing — reproduces today's `isCurrent()` → `return undefined` exactly: the row stays on disk, no step is written, nothing navigates, no error is shown, and `resolveOnboardingStep`'s row 5 heals the step on next launch. The one behaviour this does not restore is `main`'s "no row at all": the insert happens before the host gets control, and preventing it would mean moving the interlock inside `useAccountForm`. Accepted, stated, and pinned by test 17.

Checked by tests 14 and 17 and emulator scenario S4.

**D5 — the flag and its type.**

- Runtime: `const isAddingMore = useLocalSearchParams<{ isAddingMore?: string }>().isAddingMore === 'true';` computed once at the top of the hook and used at all three sites (today `add_account.hook.ts:106`, `:131`, `:136`).
- Declared type: `src/app/(onboarding)/_layout.tsx:10` becomes `add_account: { isAddingMore?: 'true' }`. `'true'` and not `string`, because `more_accounts.hook.ts:52-55` passes that literal and nothing else ever does.
- **Honest about what this buys:** nothing at runtime. `OnboardingStackParams` has exactly one occurrence in the tree — its own declaration (grep over `src/` and `__tests__/`) — and `useLocalSearchParams<T>` does not validate. The runtime fix is the `=== 'true'`. The type fix exists so MA-011 plans its add-another round trip against what is actually on the wire. The *reader* keeps `string | undefined`, because a deep link can put anything there and the exact comparison is what makes that total.

**D6 — the mount load, and the widening from one call site to three.** The Details paragraph names N2's; the Done-when says "no onboarding route loads the account list itself", and `spec.md:502` names all three explicitly: `add_account.hook.ts:47`, `ready.hook.ts:27`, `more_accounts.hook.ts:24-28`. Doing all three here. Verified, not assumed:

- Startup loads it: `use_layout_init.hook.ts:38` awaits `loadAccounts()` inside `start`, and `src/app/_layout.tsx:78` renders the `Stack` **only** when `status === 'ready'`, which `resolveReady` sets after that await (`use_layout_init.hook.ts:38-41`). No onboarding route can mount before the snapshot exists.
- Saving republishes: `account.store.ts:85` — `addAccount` awaits its own `loadAccounts()` before resolving.

Three lines and two test assertions, all inside `src/modules/onboarding/`. No module boundary is crossed and `main` works after this alone.

**D7 — `CurrencySelector` loses onboarding's two extra props.** `MA-007.md:108` flagged this for this task. Onboarding's inline control passes `listClassName="w-full"` and `accessibilityLabel="Account currency"` (`onboarding/.../index.tsx:133-134`); `currency_selector.tsx:14-21` passes neither.

- **The accessible name is a real regression on the first-run path and is fixed here**, in `CurrencySelector` (its only consumer is `account_form.tsx:89` — grep), not with a form prop.
- **`w-full` is not added.** It is very likely a no-op: `Tabs.List` sits in a column parent, and React Native's default `alignItems: 'stretch'` already makes a width-less child fill it; Settings has rendered `CurrencySelector` without it since before this scope with no narrowing reported. I could not confirm HeroUI's `Tabs.List` base classes statically — `node_modules` is not installed in this checkout — so this is an observation, not a proof. Settled on the emulator in S2/S3, with the fallback decided in advance: if N2's currency row is visibly narrower than the pre-merge build, add `listClassName="w-full"` inside `CurrencySelector` and record it in the emulator section. Adding it speculatively would widen Settings' row, on a screen this task otherwise does not touch, for a row MA-009 rebuilds.

**D8 — every visible change, named. "No visible change" would be false.**

| # | Surface | Today (onboarding) | After | Verdict |
|---|---|---|---|---|
| 1 | Colour control | 12 `PressableFeedback` swatches, label `COLOR — 12 PRESETS` | `AccountColorField` trigger row + MA-006's 32-colour sheet, label `ACCOUNT COLOUR` | **Expected and task-stated.** Default colour is unchanged: `DEFAULT_ACCOUNT_COLOR` is `AcctTokens.midnight.rich` (`account_palette.ts:127`), which is what `add_account.hook.ts:63` uses today |
| 2 | Section labels | `Text variant="hint"` (`text.tsx:17` → `font-inter text-[12px] text-muted`) + `font-sora-bold text-gold-500 pt-2 pb-2 tracking-widest` | `FormSectionLabel` → `Typography` + `font-sora-bold text-gold-500 pt-2 pb-2 text-xs tracking-widest uppercase` | Same 12 px, same Sora bold, same gold, same padding and tracking. `uppercase` is a no-op — every label string is already upper (`strings.ts:25-35`). The line-height unknown is narrower than stated: `text-xs` sets a line height, the arbitrary `text-[12px]` sets none, so the labels may gain a point or two of leading regardless of `Typography`. Screenshot in S2 |
| 3 | Field errors | `Animated.Text`, `text-negative font-inter text-[12px] mt-1`, FadeInDown 150 / FadeOutUp 100 | `FormErrorText` → HeroUI `FieldError` with its own colour token, size and animation | **Real delta.** Already shipped to Settings at MA-007; onboarding now matches it |
| 4 | Interest switch | React Native `Switch` with gold `trackColor` (`index.tsx:303-311`) | HeroUI `Switch` (`credit_card_fields.tsx:115-119`) | **Real delta**, credit-card path only. Already in Settings |
| 5 | Credit hints, interest label | `Text variant="caption"` / `variant="body"` + classes | `Typography` with `text-[11px]` / `text-[15px]` and the same colour classes | Same size and colour |
| 6 | Currency control | inline `SegmentedTabs`, `variant="solid-gold"`, `w-full`, a11y label | `CurrencySelector`, same primitive and variant, a11y label restored (D7) | Width: see D7 |
| 7 | CTA busy source | `formState.isSubmitting` (`index.tsx:64-65`) | `state.saving` | The busy label no longer flashes while an **invalid** draft is being validated. Matches Settings (`accounts/.../index.tsx:41-42`) |
| 8 | Credit block content and order | revolving · limit · min+hint · due day · switch · APR+hint | identical, from `credit_card_fields.tsx:35-145` | Field-for-field the same, read off both files |

Nothing else on this screen moves: `OnboardingShell` props, `n2HeaderTitle`, `n2Footnote`, `o4Cta`, `n2CtaBusy`, the `ScreenScroll` padding and `keyboardShouldPersistTaps`, and the button's press-scale animation all stay as they are.

**D9 — the post-save checkpoint does not work without a change to `useAccountForm`. *Added at plan review; this is the task's central Done-when clause.***

`submit` is `form.handleSubmit(onValid)` (`use_account_form.hook.ts:80`), so **every** tap re-validates before `onValid` runs. Trace the retry on a real store:

1. Tap 1 validates against a schema built from `accounts = []`, passes, `beginSave()`, `addAccount(...)`.
2. `addAccount` awaits its own `loadAccounts()` (`account.store.ts:82-91`) → the store publishes a new `accounts` array **containing the row just written**.
3. `markInserted()`, then `onSaved()` rejects on the step write → `failSave(n2SaveError)`. So far so good.
4. That publication re-renders the hook: `accounts` is a new reference → `useMemo` rebuilds the schema (`use_account_form.hook.ts:50-51`) → `useZodForm`'s `useEffect` writes it to `schemaRef.current` (`use_zod_form.hook.ts:14-17`), and the resolver reads that ref at validation time.
5. Tap 2 validates against the new schema. `createAddAccountSchema`'s `superRefine` (`add_account.schema.ts:26-29`) compares the draft name case-insensitively after trimming against every loaded account — **including the one this very form just created**. Validation fails with `errNameDuplicate`, `onValid` never runs, no step is written, and the status track keeps the stale `n2SaveError` with `This name is already used` under the name field.

So the retry is impossible except by renaming the account. The `inserted` latch is fine and survives; nothing ever reaches it. The hook's own JSDoc (`use_account_form.hook.ts:27-34`) already promises "the next `submit()` re-runs only this callback" — this makes the code deliver what it documents, so it is this task's, not a new scope.

**The change**, in `use_account_form.hook.ts`:

```ts
const handleSubmit = form.handleSubmit(onValid);

const submit = async () => {
  // Post-save checkpoint (MA-008). Once the row is on disk the draft has
  // already passed validation once, and the schema has since been rebuilt
  // from an accounts array that CONTAINS that row — re-validating fails
  // errNameDuplicate against the account this form itself created, and the
  // retry can never reach onSaved. Re-enter the guarded tail directly;
  // onValid's `inserted` check is what keeps addAccount from running twice.
  if (useAccountFormState.getState().inserted) return onValid(form.getValues());
  return handleSubmit();
};

return { form, submit, state: { saving, errorMessage } };
```

**Blast radius on Settings: none behavioural.** `useAddAccountApp`'s `onSaved` is `router.back()` (`accounts/add_account/add_account.hook.ts:13`), which cannot reject, so `inserted` is only ever true there after a save that already succeeded and unmounted the screen. The existing case `account_form.hook.test.ts:65` ("a sequential re-tap after success inserts once but re-runs onSaved") takes the new branch and still asserts `addAccount` once / `onSaved` twice.

**A test must be added to `__tests__/account_form.hook.test.ts`**, because the suite's existing rejecting-`onSaved` case (`:81`) passes today only because `mockAddAccount` never grows `mockAccounts` — it mocks away the exact republication that breaks the retry, which `.claude/rules/tests.md` calls vacuous. New case, which **fails on `main`'s hook**:

> *the retry does not re-validate against the row it just inserted* — `mockAddAccount.mockImplementation(async () => { mockAccounts = [...mockAccounts, { id: 'new', name: 'New Account' }]; })` (the same name `fillValidDraft` types), `onSaved` rejects once then resolves, two sequential `submit()`s → `addAccount` 1 · `onSaved` 2 · `state.errorMessage` undefined.

---

#### Step 1 — the declared param type

**Files:** modify `src/app/(onboarding)/_layout.tsx:10`.

- [ ] **1.1** Replace the lying member and say why, so the next reader does not "fix" it back:

```ts
  // Expo Router params are strings on the wire. more_accounts.hook.ts:52-55
  // passes the literal 'true' and nothing else ever navigates here with a
  // param — so this is the type, and the reader compares with === 'true'.
  add_account: { isAddingMore?: 'true' };
```

- [ ] **1.2** `npm run typecheck` — expected green; `OnboardingStackParams` has no consumer (grep over `src/` and `__tests__/` returns only its own declaration), so nothing depends on the old shape.
- [ ] Commit: `fix(onboarding): the add-more param type matches what is passed (MA-008)`

---

#### Step 2 — the N2 host (test first)

**Files:**
- Test: rewrite `__tests__/screens/onboarding_add_account.hook.test.ts`
- Test: **add one case to** `__tests__/account_form.hook.test.ts` (D9)
- Modify: `src/modules/accounts/components/account_form/use_account_form.hook.ts` (D9 — the `submit` bypass, and only that)
- Modify: `src/modules/onboarding/screens/onboarding/add_account/add_account.hook.ts`
- Modify: `src/modules/onboarding/screens/onboarding/add_account/index.tsx`
- Modify: `src/modules/onboarding/screens/onboarding/add_account/add_account.anim.ts`
- Modify: `src/constants/strings.ts` (remove one orphaned key)
- Unchanged: `add_account.state.ts` (still needed — `onBack` uses it)

- [ ] **2.1 Rewrite the suite.** Keep the existing mock scaffolding shape (`__tests__/screens/onboarding_add_account.hook.test.ts:11-48`): `jest.mock` for `expo-router`, `@/modules/accounts/store/account.store` and `@/modules/onboarding/store/onboarding.store`, with `attachMockSelectorStore` from `@/test_helpers/mock_zustand_selectors`. Two changes to the scaffolding:
  - `beforeEach` resets **both** stores: `useAddAccountTransitionState.getState().reset()` and `useAccountFormState.getState().reset()`. Missing the second leaks `inserted` between cases and turns case 7 green for the wrong reason.
  - `setup()` takes the raw param string rather than a boolean, so `'true'`, `'false'` and absent are all expressible: `setup(isAddingMore?: string)` → `useLocalSearchParams` returns `isAddingMore === undefined ? {} : { isAddingMore }`.
  - `attachMockSelectorStore` must supply `accounts`, `addAccount` **and** `loadAccounts` — the last only so case 4 can assert it was never called.

  - **`mockAddAccount` must republish `mockAccounts`** in every case that asserts anything after an insert: `mockAddAccount.mockImplementation(async () => { mockAccounts = [...mockAccounts, { id: 'new', name: 'CIB Savings' }]; })` — same name `fillAndSubmit` types, and `name` present, not just `id`. The real `addAccount` awaits `loadAccounts()` before resolving (`account.store.ts:85`); a mock that leaves `accounts` empty deletes the schema rebuild that D9 exists for and turns case 7 green with the production defect still in place. Today's suite already half-does this (`:83-85`, `:104-106`) but with `{ id: 'new' }` and no `name`, which would throw inside `superRefine` the moment a second validation ran.

  The seventeen cases:

  | # | Name | Asserts |
  |---|---|---|
  | 1 | renders without throwing | `renderHook(() => useAddAccount())` resolves |
  | 2 | the draft starts on the onboarding base currency | store `baseCurrency: Currency.USD` → `form.getValues('currency') === Currency.USD`. The form never reads the onboarding store itself — no onboarding mock is needed by `use_account_form.hook.ts`, which is asserted structurally by `__tests__/account_form.hook.test.ts` having no such mock |
  | 3 | the draft's defaults are unchanged by the adoption | `selected_color === DEFAULT_ACCOUNT_COLOR`, `selected_type === AccountType.Bank` |
  | 4 | does not load the account list on mount | the `loadAccounts` mock has zero calls after `renderHook` |
  | 5 | a successful save writes the step **before** it navigates, and replaces | `addAccount` once · `setStep(OnboardingStep.N3)` · `replace('/(onboarding)/more_accounts')` · and the ordering explicitly: `mockSetStep.mock.invocationCallOrder[0] < mockReplace.mock.invocationCallOrder[0]` |
  | 6 | a rejecting insert writes no step, does not navigate, and shows `n2SaveError` | `setStep` not called · `replace` not called · `state.statusMessage === Strings.n2SaveError` · `state.saving === false` |
  | 7 | **a rejecting step write after a resolved insert retries into the next step without inserting again** | `mockSetStep.mockRejectedValueOnce`, **with `mockAddAccount` republishing `{ id: 'new', name: 'CIB Savings' }` into `mockAccounts`** per the scaffolding note above. After submit 1: `addAccount` 1 · `replace` 0 · `statusMessage === Strings.n2SaveError` · `saving === false`. After submit 2: `addAccount` still **1** · `setStep` 2 · `replace` 1, with `/(onboarding)/more_accounts` · `statusMessage` falsy · `form.formState.errors.name` **undefined** (the last assertion is what distinguishes D9 landing from the retry being eaten by `errNameDuplicate`) |
  | 8 | `?isAddingMore=true` saves without writing a step | `addAccount` 1 · `setStep` not called · `replace('/(onboarding)/more_accounts')` |
  | 9 | **`?isAddingMore=false` saves like a first account** | `setStep(OnboardingStep.N3)` **was** called — this fails on `main`, where the truthy check takes the add-more branch and skips the step write |
  | 10 | onBack with no param writes N1 and replaces to welcome | as today (`:115`) |
  | 11 | onBack with `?isAddingMore=true` writes no step and replaces to more_accounts | as today (`:124`) |
  | 12 | **onBack with `?isAddingMore=false` writes N1 and replaces to welcome** | the second half of the same defect; fails on `main` |
  | 13 | onBack is ignored while a save is in flight | `useAccountFormState.setState({ saving: true })`, then `await onBack()` → `setStep` not called, `replace` not called |
  | 14 | the CTA is ignored while a back transition is in flight | `useAddAccountTransitionState.getState().begin()`, then fill a valid draft and `await handleSave()` → `addAccount` not called |
  | 15 | a back failure replaces a stale save-failure message | make the save's `setStep` reject once (leaving `errorMessage` set), then make the back's `setStep` reject → `state.statusMessage === Strings.onboardingBackSaveError`. Pins D2's precedence |
  | 16 | **a save failure replaces a stale back-failure message** | the reverse of 15, and the direction the merge alone gets wrong: make the back's `setStep` reject (leaving `transitionStatusMessage` set), then make `addAccount` reject → `state.statusMessage === Strings.n2SaveError`. **Fails without `handleSave`'s `reset()`** — D2 |
  | 17 | **a back that starts inside the save's validation window does not navigate twice** | after `renderHook`, fill a valid draft; start `handleSave()` without awaiting, and while it is pending call `useAddAccountTransitionState.getState().begin()` to simulate the back that slipped through, then await both. Assert `addAccount` 1 (the row is written — stated in D4) · `mockSetStep` **not** called with `OnboardingStep.N3` · `replace` never called with `/(onboarding)/more_accounts`. **Fails without `onSaved`'s `busy` re-check** — D4 |

  Deliberately **not** re-asserted here, because they are already covered and re-testing them through this hook buys nothing:
  - case-insensitive trimmed duplicate rejection → `__tests__/add_account.schema.test.ts:69` and `:74`
  - `current_balance = opening_balance` → `__tests__/account.repository.test.ts:72`
  - credit-only values absent (`null`) rather than `0` on non-credit types → `__tests__/account_form.helpers.test.ts:205`, plus `:254`/`:263` for blank vs explicit zero
  - the guarded-save latch in the abstract → `__tests__/account_form.hook.test.ts:81` and `:154`, `__tests__/account_form.state.test.ts:53`

- [ ] **2.2 Run it and watch it fail.** `npm test -- onboarding_add_account` — expect failures across the board (the hook still returns `state.busy`, still exposes `ACCOUNT_COLORS`, still truthy-checks the flag). Cases 9 and 12 must fail *for the right reason*: on `main` case 9 fails because `setStep` was not called, not because the module is missing. If either passes before the change, stop — the premise is wrong.

- [ ] **2.3 Rewrite `add_account.hook.ts`.** Whole file, in this order:

  - imports: `useLocalSearchParams`, `useRouter` · `OnboardingStep` · `Strings` · `useAccountStore` · `useAccountForm` and `useAccountFormState` from `@/modules/accounts/components/account_form/…` · `ONBOARDING_STEP_HREF`, `resolveOnboardingStep` from `@/modules/onboarding/domain/onboarding_route` · `runOnboardingTransition` · `useOnboardingStore` · `useInit` · `useAddAccountTransitionState`.
  - **Deleted:** the `ACCOUNT_COLORS` array (`:19-34`), the `AcctTokens` import, `createAddAccountSchema`/`AddAccountFormData` from `@/utils/schemas/add_account.schema` (`:10-13` — a compatibility surface, `spec.md:465`), `useZodForm`, `useMemo`, `AccountType`, the `accounts`/`addAccount`/`loadAccounts` reads, `useInit(loadAccounts)` (`:47`), the whole `useZodForm` block (`:54-72`) and the whole `onSubmit` body (`:74-121`).
  - **Kept:** `useInit(() => useAddAccountTransitionState.getState().reset())` with its comment (`:48-52`) — it is `onBack`'s channel now, and the add-more re-entry still arrives by `replace`.
  - the flag, once: `const isAddingMore = useLocalSearchParams<{ isAddingMore?: string }>().isAddingMore === 'true';`
  - the form:

```ts
const { form, submit, state } = useAccountForm({
  initialCurrency: baseCurrency,
  saveErrorMessage: Strings.n2SaveError,
  onSaved: async () => {
    // D4: a back that started inside the CTA's validation window is not
    // covered by handleSave's pre-submit check. Return, do not throw —
    // this is exactly today's isCurrent() -> `return undefined`: the row
    // stays on disk, no step is written, nothing navigates, no error is
    // shown, and resolveOnboardingStep heals the step on next launch.
    if (useAddAccountTransitionState.getState().busy) return;

    // resolve AFTER the insert — useAccountForm calls onSaved only past
    // markInserted(), so accounts.length already includes the new row.
    // Reading it earlier reproduces MA-005 Decision 2 row 2's hard loop.
    const resolved = isAddingMore
      ? OnboardingStep.N3
      : resolveOnboardingStep(OnboardingStep.N3, useAccountStore.getState().accounts.length);

    // Add-more: the persisted step never moved off N3, so there is nothing
    // to write and only the route changes — identical to add_account.hook.ts:106
    // before this task.
    if (!isAddingMore) await setStep(resolved);

    // No catch: a rejecting setStep propagates to useAccountForm's catch,
    // which calls failSave(Strings.n2SaveError) and leaves `inserted` latched.
    // Persist-before-navigate is structural — the replace below is unreachable
    // from a failed write.
    useAddAccountTransitionState.getState().invalidate(); // MA-005 Decision 3
    router.replace(ONBOARDING_STEP_HREF[resolved]);
  },
});
```

  - `handleSave` and `onBack` with the D3 interlock:

```ts
const handleSave = async () => {
  // D3: the back path's guard, checked before validation so nothing moves.
  if (useAddAccountTransitionState.getState().busy) return;
  // D2: mirror of ready.hook.ts:57-61 — every writer of the single status
  // track clears it when its own attempt starts. beginSave() clears only
  // the form's channel, and the merge below always prefers the transition
  // channel, so without this a failed back stays pinned over a failed save.
  // Safe: the early return above means no back transition is live.
  useAddAccountTransitionState.getState().reset();
  await submit();
};

const onBack = async () => {
  // D3: spec.md:81 — back is inert during the write.
  if (useAccountFormState.getState().saving) return;
  const session = useAddAccountTransitionState.getState().begin();
  if (session === null) return;
  await runOnboardingTransition({ /* unchanged from :127-142, with the
    exact isAddingMore above in both places */ });
};
```

  - the return: `{ form, handleSave, onBack, state: { statusMessage: transitionStatusMessage || state.errorMessage, saving: state.saving } }`. `busy` is **not** returned — nothing renders it, and leaving it would be the second busy flag D1 exists to prevent.

- [ ] **2.4 Rewrite `index.tsx`.** The shell call is unchanged (`step`, `n2HeaderTitle`, `onBack`, `n2Footnote`, `statusMessage`, `cta`) and so is the `ScreenScroll`. Everything between them collapses to `<AccountForm form={form} ownerId="onboarding/add_account" />` — the owner id per `AccountFormProps.ownerId`'s JSDoc (`account_form.tsx:21-28`) and `account_color_sheet.state.ts:5-16`; Settings keeps `'accounts/add_account'`. The CTA's `isDisabled`/`isLoading` switch from `isSubmitting` to `state.saving`; `label`, `loadingLabel` and the `Animated.View` wrapper are untouched. Deleted imports: `cn`, `PressableFeedback`, `Controller`, `useWatch`, `Switch`, `TYPE_OPTIONS`/`TypePill` (this removes the last onboarding consumer of the `src/components/account_type_pill` compat shim), `Input`, `SegmentedTabs`, `Text`, `AccountType`, `Currency`, `CoreTokens`, `GoldTokens`, `ACCOUNT_COLORS`.

- [ ] **2.5 `add_account.anim.ts` keeps only `btnAnim` + `triggerBtnPress`.** The six other values (`ccEntering`/`ccExiting`/`aprEntering`/`aprExiting`/`errorEntering`/`errorExiting`, `:28-33`) are deleted: the first four now live in `account_form.anim.ts`, and the last two are `FieldError`'s. The file ends up identical in shape to `src/modules/accounts/screens/accounts/add_account/add_account.anim.ts`.

- [ ] **2.6 Remove the orphaned string.** `Strings.o4SectionColor` (`strings.ts:29`, `'COLOR — 12 PRESETS'`) has exactly one consumer today, the swatch row this step deletes. Grep to confirm zero references before removing it. Leave `o4ColorLabel` alone — it was already unreferenced before this task and is not this task's to clean.

- [ ] **2.7 Delete the other two `loadAccounts` call sites (D6).**
  - `src/modules/onboarding/screens/onboarding/ready/ready.hook.ts:23,27` — the `loadAccounts` read and `useInit(loadAccounts)`.
  - `src/modules/onboarding/screens/onboarding/more_accounts/more_accounts.hook.ts:16,24-28` — the `loadAccounts` read and the whole `useFocusEffect` block, plus the now-unused `useFocusEffect` and `useCallback` imports.
  - Add one assertion to each suite: `expect(mockLoadAccounts).not.toHaveBeenCalled()` in `__tests__/screens/onboarding_ready.hook.test.ts` (the mock already exists at `:73`) and the same in `__tests__/screens/onboarding_more_accounts.hook.test.ts` (promote the inline `jest.fn()` at `:50` to a named mock first). Leave the `useFocusEffect: jest.fn()` entry in the more-accounts `expo-router` mock — harmless, and removing it is churn.

- [ ] **2.8 Run.** `npm test -- --ci` — the seventeen new cases green, `account_form.hook.test.ts` green **with its one added D9 case**, and these four suites green **untouched**: `account_form.state.test.ts`, `account_form.helpers.test.ts`, `add_account.schema.test.ts`, `screens/accounts/add_account.hook.test.ts`. If `screens/accounts/add_account.hook.test.ts` moves, something crossed into Settings that should not have.
- [ ] **2.9** `npm run typecheck`.
- [ ] Commit: `refactor(onboarding): N2 renders the shared account form (MA-008)`

---

#### Step 3 — the currency control's accessible name

**Files:** modify `src/constants/strings.ts`, `src/modules/currency/components/currency_selector.tsx`.

- [ ] **3.1** Add `accountCurrencyA11y: 'Account currency'` to `strings.ts`, beside the other onboarding form copy. Same wording onboarding uses today (`onboarding/.../index.tsx:134`), so this restores rather than invents.
- [ ] **3.2** Pass it: `accessibilityLabel={Strings.accountCurrencyA11y}` on the `SegmentedTabs` inside `CurrencySelector`. `SegmentedTabsProps.accessibilityLabel` already forwards to `Tabs.List` (`tabs.tsx:88-91`, `:237`). One consumer only — `account_form.tsx:89` — so both hosts gain it and nothing else in the app is touched.
- [ ] **3.3** `npm run typecheck && npm test -- --ci`.
- [ ] Commit: `fix(currency): the currency selector announces itself (MA-008)`

---

#### Step 4 — CI parity, then the emulator

- [ ] **4.1** Run the chain from `CLAUDE.md` § Commands, top to bottom, fixing and restarting from the top until green. It ends in `expo prebuild --no-install`, which regenerates `android/` — so it runs **before** any build, per the emulator skill.
- [ ] **4.2** `git status --porcelain` must be clean apart from the intended files. This matters at 5.1, which temporarily edits a source file.

---

#### Step 5 — emulator verification (workflow step 6)

`verify: emulator`. Setup per the `emulator-verify` skill: `npm install` in the worktree, `mqa needs-build` (**expect exit 1 — this diff is JS-only**: no `package*.json`, `app.json`, `eas.json`, `patches/`, `android/` or `ios/` change), a private Metro on 8082, `MQA_PORT=8082 mqa launch`, then `mqa park`. Confirm the branch's bundle is what loaded before trusting anything — the colour **trigger row** in place of the twelve swatches is visible on the first screen under test, which makes that check free.

Four scenarios, scripted through `mqa walk`.

- [ ] **5.1 — S1: the post-save checkpoint.** The scenario the task calls the single most valuable thing to force here.

  **Forcing the failure. *Corrected at plan review — a bare fail-once flag never reaches N2.*** `setStep` writes to SecureStore (`onboarding.repository.ts:28-30`), so it cannot be broken from outside the app. Temporarily edit `src/modules/onboarding/repositories/onboarding.repository.ts`, and no other file:

```ts
// module scope, above the class — MA-008 S1, TEMPORARY, revert before commit
let forcedN3FailureUsed = false;

  // ...inside OnboardingRepository:
  async setStep(step: OnboardingStep): Promise<void> {
    // Keyed on N3, not on "the first call": N1's Continue writes N2 first
    // (welcome.hook.ts:37-43), so an unkeyed fail-once flag is spent on the
    // welcome step and the walk never gets to N2 at all.
    if (step === OnboardingStep.N3 && !forcedN3FailureUsed) {
      forcedN3FailureUsed = true;
      throw new Error('forced MA-008 S1');
    }
    await SecureStore.setItemAsync(SecureStoreKeys.OnboardingStep, step);
  }
```

  The `let` is at module scope, outside the class body — a `let` between class members is a syntax error. Fail-once rather than fail-always is deliberate: reverting the file between the two taps would fast-refresh the route, remount the hook, and `useInit` would `reset()` the latch (`use_account_form.hook.ts:60`) — which is precisely the state the retry is supposed to be testing. The module-level flag keeps the bundle still across both taps.

  Walk: `mqa reset` → launch → N1 Continue → N2 → type a name and a balance → **Save Account**. Then assert:
  - `mqa ui | grep -c "Couldn't save that"` ≥ 1 — the error is in the status track, red glyph, in the footer box.
  - `mqa ui | grep -c "Add your first account"` ≥ 1 — still on N2, no navigation.
  - `mqa db "select count(*) as c from accounts"` → **1**.

  Tap **Save Account** again (same bundle, no reload) and assert:
  - `mqa db "select count(*) as c from accounts"` → still **1**. This is the Done-when.
  - `mqa ui | grep -c "This name is already used"` → **0**. Without D9 this is what tap 2 actually produces (`Strings.errNameDuplicate`, `strings.ts:101`), and it is the cheapest on-device signal that the retry bypass landed.
  - `mqa ui | grep -c "Anything else"` ≥ 1 — landed on N3.
  - `mqa db "select count(*) as c from accounts"` once more after the navigation settles, in case a second insert lands late.

  Then revert the repository file, `git diff --stat src/modules/onboarding/repositories/` must be empty, and re-run `npm test -- --ci` before committing.

- [ ] **5.2 — S2: onboarding writes a correct row, and the colour sheet is wired to the right owner.** `mqa reset` → N1 → N2. Fill name `MA008 Bank`, balance `5000`. Tap the colour trigger row (`testID="account-color-trigger"`, `account_color_field.tsx:37`) — the sheet must open, pick a non-default swatch, **Use this colour**, and the trigger row's family label must change. Save. Then:

```
mqa db "select name,type,currency,opening_balance,current_balance,color,credit_limit,minimum_payment,statement_due_day,apr,revolving_balance,sort_order from accounts"
```
  Assert `opening_balance = current_balance = 5000` (business rule 6, on the live path), all five credit columns **NULL and not 0** (the "absent, not zero" preservation), `color` equal to the hex picked in the sheet, `sort_order = 0`.

  One screenshot of N2 before saving, Read and compared against `main`'s: the section labels (D8 row 2), the field error styling with a deliberately blank name (D8 row 3), and the currency row's width (D7). Report any delta rather than assuming parity.

- [ ] **5.3 — S3: Settings still writes a correct row, on a credit card.** Finish onboarding through N3/N4, then Settings → Accounts → Add Account. Create a Credit Card with a limit, a minimum payment, a due day and interest tracking on with an APR. Save, then `mqa db` the same column list: credit columns **populated**, `current_balance = opening_balance`. This is the other half of the Done-when and the regression check on Step 3's shared-component edit. Screenshot the currency row here too, for the D7 comparison against S2's.

- [ ] **5.4 — S4: the add-more re-entry, which is what MA-011 depends on.** From N3, tap **Add another account** (this is the only navigation in the app that sets `isAddingMore`, `more_accounts.hook.ts:52-55`). Create a second account. Assert:
  - `mqa db "select count(*) as c from accounts"` → **2** — the latch reset on the fresh mount, so the second insert is not swallowed.
  - back on N3 (`mqa ui | grep -c "Anything else"`).
  - `mqa db` shows `sort_order` 0 and 1, distinct.
  - then the add-more **back** chevron from a re-entered N2 returns to N3, not to N1.

  The explicit-`false` case is **not** walked: no UI path passes `isAddingMore=false`, so it is only reachable from a deep link and is pinned by unit tests 9 and 12 instead. Saying so here so the omission is a decision, not a gap.

- [ ] **5.5** Record the walk in `## Emulator verification` on this file: the queried values (not screen readings), the screenshots Read, and what could not be checked here — typography weight, shadows, gesture feel and the sheet's spring, all of which stay gate 3's.

---

### Non-goals

- **The redesign.** MA-009 owns the type grid, the message rails, the credit slot, the balance+currency row, and the `Save and continue` CTA label. This task keeps `Strings.o4Cta` (`Save Account`) and every other string on the screen exactly as they are.
- **The `n2SaveError` copy.** It reads "Tap Save and continue to try again" while the button says `Save Account` — a mismatch that arrived with MA-004 and that MA-009 resolves by changing the label. Not fixed here: it is user-facing copy with a CTA rename attached, and fixing it in this task would put the two out of sync in the other direction. Flagged to @sarah.
- **`src/utils/schemas/add_account.schema.ts`.** Onboarding was its last consumer; after Step 2 it has zero. `spec.md:465` says leave it. Left, unimported. A later cleanup can delete it.
- **`runOnboardingTransition`, `resolveOnboardingStep`, `ONBOARDING_STEP_HREF`, `onboarding_transition.state.ts`** — read and reused, not modified. The other three onboarding screens' use of them is untouched except for the `loadAccounts` deletions in D6.
- **`listClassName="w-full"` on `CurrencySelector`** unless S2/S3 shows a narrowing (D7).
- **No new dependency, no native-surface change, no migration, no feature-flag flip.** `mqa needs-build` is expected to say so.

### Risks accepted, with the check that would catch each

| # | Risk | Check |
|---|---|---|
| R1 | Two guards remain on one screen, interlocked rather than unified. `handleSave`'s check runs *before* validation and `saving` is only set after it, so a Back landing inside the validation window passes both gates — **wider than `main`, which aborts the save outright at its single post-validation `begin()`** (D3, corrected) | Tests 13, 14 and 17. D4's `busy` re-check in `onSaved` stops the step write and the navigation; the row is still written, which `main` would not have done. Stated, not hidden |
| R2 | The `isCurrent()` stale guard is relocated, not deleted: `onSaved` re-reads `busy` instead (D4, corrected) | Tests 14 and 17 and S4. If a future task adds a third async control to N2, a `busy` read is no longer sufficient and a real session token comes back — say so in that task |
| R3 | `useAccountFormState` is a module singleton shared with Settings; a second mounted host would share `inserted` | Only one add-account host is mounted at a time, and `useInit(reset)` clears on every mount (`use_account_form.hook.ts:60`, pinned by `account_form.hook.test.ts:154`). S4 proves the add-more remount on device |
| R4 | `Tabs.List`'s default width is unverified — `node_modules` is not installed in this checkout, so D7's "`w-full` is a no-op" is an inference from RN's default `alignItems: 'stretch'`, not a reading of HeroUI's source | S2/S3 screenshots, with the fallback decided in advance |
| R5 | `Typography`'s default line height may differ from RN `Text`'s on the section labels, shifting the form vertically by a few points | S2 screenshot compared against `main` |
| R6 | S1's forced failure is a temporary source edit that could be committed | `git diff --stat src/modules/onboarding/repositories/` empty and `npm test -- --ci` re-run after the revert, before the commit (4.2, 5.1) |
| R8 | D9 edits `use_account_form.hook.ts`, which Settings shares — a bad bypass would let Settings skip validation | The bypass is gated on `inserted`, which on Settings is only ever true after a save that already succeeded and popped the screen. `account_form.hook.test.ts` runs untouched apart from the one added case, and `screens/accounts/add_account.hook.test.ts` must not move (2.8). S3 re-walks Settings on device |
| R7 | D6 widens the task from one `loadAccounts` call site to three, touching two screens MA-011 and MA-012 own | `spec.md:502` names all three; each is one line, all inside `src/modules/onboarding/`, and each has a `not.toHaveBeenCalled()` assertion in the suite that already exists for that screen |

---

## Sarah's rulings on the plan (step 4 → 5)

Both of @tariq's escalations resolved without waking the user; neither is a critical trigger.

**D6's widening stands — all three `loadAccounts()` call sites.** Not a scope balloon. The task's own Done-when is plural ("no onboarding route loads the account list itself"), `spec.md:502` names exactly those three, and the change is three lines plus two assertions entirely inside `src/modules/onboarding/`. Handing two of them to MA-011/MA-012 would leave this task's Done-when unmet by its own PR, which is worse than touching two lines in screens those tasks will rewrite anyway.

**The `n2SaveError` copy is not fixed here, and it is not the user's call.** CLAUDE.md's critical trigger 5 excludes error messages explicitly — they stay team-decided. @tariq's reasoning is accepted as written: the sentence names the CTA, MA-009 renames that CTA per `spec.md:81`, and correcting the string here would put the pair out of sync in the other direction for exactly one merge. It stays a Non-goal. **MA-009 must reconcile both halves in the same commit** — recorded here so the pairing is not rediscovered.

R4's unverified `Tabs.List` width is acceptable at plan stage precisely because D7 decides the fallback in advance rather than deferring it to discovery.

---

## Plan review

### Round 1 — changes made

Plan edited in place. Design B stands, D6 and the `n2SaveError` ruling untouched. Four defects found, all of which would have surfaced as rework at step 6 or 7; the plan now carries the corrections and the tests that pin them. **@dev implements the plan as amended — the amended parts are not optional and none of them needs another planning round.**

#### Claims checked that turned out false, with the correction

**1. The post-save checkpoint cannot work as planned — the retry is eaten by validation, not by the latch. (`use_account_form.hook.ts:80`, `:50-51`; `account.store.ts:82-91`; `add_account.schema.ts:26-29`; `use_zod_form.hook.ts:14-17`)**

The plan asserts the `inserted` latch is what implements the checkpoint and that `use_account_form.hook.ts` needs no change. The latch is fine; nothing reaches it. `submit` is `form.handleSubmit(onValid)`, so tap 2 re-validates first, and by then `addAccount` has awaited its own `loadAccounts()`, the store has republished `accounts` **including the row just written**, the memoised schema has rebuilt, and `useZodForm`'s `schemaRef` has been updated. `superRefine`'s duplicate-name check then rejects the draft against the account this very form created. Result on device: `This name is already used` under the name field, a stale `n2SaveError` in the status track, no step write, no navigation — the retry is only escapable by renaming the account.

The task's Done-when clause "retries into the next step without inserting again" is therefore unproduceable by the plan as written. Added as **D9**, with the exact bypass, the Settings blast-radius argument, and the required test.

**2. That defect was invisible to the plan's own test 7 — the mock deletes the mechanism.** Test 7 asserts the retry through a mocked `addAccount` that need not grow `mockAccounts`. `.claude/rules/tests.md`: *a test that mocks the thing it claims to verify is vacuous*. The existing suite has the same hole in both directions — `__tests__/account_form.hook.test.ts:81` passes only because `mockAddAccount` never republishes, and `__tests__/screens/onboarding_add_account.hook.test.ts:104-106` republishes `{ id: 'new' }` with **no `name`**, which would throw inside `superRefine` the instant a second validation ran. Scaffolding note added to 2.1, test 7 rewritten with the republication and a `form.formState.errors.name` assertion, one case added to `account_form.hook.test.ts`, and an on-device `grep -c "This name is already used" → 0` added to S1.

**3. D2's precedence merge does pin a stale message — in the direction D2 did not consider. (`account_form.state.ts:47`, `onboarding_transition.state.ts:44`, `ready.hook.ts:57-61`)**

D2 argued neither channel can go stale because each `begin` clears on every accepted attempt. Each clears *only its own* channel. So: back fails → user taps Save → save fails → the track reads `onboardingBackSaveError`, naming a chevron the user did not touch. `ready.hook.ts` already hit this exact bug and fixed it by resetting the transition state at the start of every completion attempt, with a comment saying so; the plan reproduced the bug rather than the fix. `handleSave` now calls `useAddAccountTransitionState.getState().reset()` before `submit()`, and **test 16** (the reverse of 15) fails without it. The plan's line citations for the two clears were off by one and are corrected in D2's text.

**4. D3/D4's "the residual window is identical to `main`'s" is false, and the `isCurrent()` deletion is not safe on that argument. (`add_account.hook.ts:75`)**

On `main` there is one gate and it sits *after* validation, inside `onSubmit`. A Back landing during a Save's validation takes `begin()` first, and the Save's own `begin()` then returns `null` — **`main` inserts nothing**. Under D1/D3 the guards are separate and `handleSave` checks `busy` *before* `submit()`, with nothing re-checking afterwards, so the same interleaving writes a row, writes N3 and fires a second `replace` against a back transition already navigating to welcome. That is a new race, and it is precisely what the dropped `isCurrent()` covered. D4 now relocates the check rather than deleting it: `onSaved` re-reads `busy` as its first statement and returns early, reproducing today's `isCurrent()` → `return undefined` exactly. The one thing it does not restore — `main`'s "no row at all" — is stated in D4 and R1 rather than papered over. **Test 17** pins it.

**5. Step 5.1's forced failure never reaches N2. (`welcome.hook.ts:37-43`)**

`setStep` is not first called by N2's save. N1's **Continue** writes N2 through the same repository method, so after `mqa reset` the unkeyed fail-once flag is spent on the welcome step and the walk stops there with a status message on N1. The flag is now keyed on `step === OnboardingStep.N3`. Separately, the snippet placed `let forcedFailureUsed` between class members, which is a syntax error; it is now shown at module scope.

#### Claims checked that held

- **D1's rejection of design A is correct.** `beginSave()` (`account_form.state.ts:45`) and `begin()` (`onboarding_transition.state.ts:41`) are genuinely independent, and under A a Back-in-flight would let `beginSave` pass, `addAccount` write, `begin()` return `null`, `runOnboardingTransition` never run and `onSaved` resolve normally — an orphan row with no step, no navigation, no error. And it really is impossible on `main`, because `begin()` runs at `add_account.hook.ts:75`, before `addAccount`. Design B is the right call, and it survives every correction above.
- **D5.** `OnboardingStackParams` has exactly one occurrence in `src/` and `__tests__/` — its own declaration at `(onboarding)/_layout.tsx:8-13`, with `isAddingMore?: boolean` at `:10`. `more_accounts.hook.ts:52-55` passes the literal `'true'` and is the only navigator that passes anything.
- **D6.** All three call sites exist where named (`add_account.hook.ts:41,47`, `ready.hook.ts:23,27`, `more_accounts.hook.ts:16,24-28`), `spec.md:502` names exactly those three, `use_layout_init.hook.ts:38` awaits `loadAccounts()` inside `start`, `src/app/_layout.tsx:78` gates the `Stack` on `status === 'ready'`, and `account.store.ts:85` republishes after every insert. `main` works with this change alone.
- **D7.** `CurrencySelector` (`currency_selector.tsx:13-22`) passes neither `listClassName` nor `accessibilityLabel`; onboarding's inline control passes both (`index.tsx:133-134`); `SegmentedTabs` forwards `accessibilityLabel` to `Tabs.List` at `tabs.tsx:237`; `account_form.tsx:89` is the sole consumer. The a11y regression is real and the fix lands in the right place.
- **D8.** Default colour unchanged — `DEFAULT_ACCOUNT_COLOR` is `AcctTokens.midnight.rich` (`account_palette.ts:127`), the same value `add_account.hook.ts:63` uses today. `FormSectionLabel` (`form_section_label.tsx:10`) is the same weight, colour, padding, tracking and 12 px; its `uppercase` is a no-op against `strings.ts:25-35`. Credit block is field-for-field identical, read off `credit_card_fields.tsx:35-145` against `index.tsx:193-352`. Row 2's line-height caveat re-pointed at `text-xs` vs `text-[12px]`, which is the actual source of the difference.
- **Step 2.6.** `Strings.o4SectionColor` has exactly one consumer, `onboarding/.../index.tsx:171`. `o4ColorLabel` (`strings.ts:39`) is already unreferenced — correctly left alone.
- **Cases 9 and 12 fail on `main` for the stated reason.** `add_account.hook.ts:106` and `:131` both truthy-test the raw param, so `?isAddingMore=false` takes the add-more branch and skips the step write on save and resolves N3 instead of N1 on back.
- **Money mapping.** `toNewAccountInput` (`account_form.helpers.ts:74-93`) is MA-007's and is reused unmodified: `roundMoney` on every amount, credit-only fields `null` rather than `0` off the credit path, `interest_tracking` still `1|0`, `current_balance = opening_balance` still the repository's. Nothing in this task recomputes a displayed value.
- **`.claude/rules/review.md`.** Defect 1 (silent async failure) is the one this task lives inside and it is now covered in both channels; defect 2 (focus-reload churn) is what D6 deletes. Defects 3–5 do not apply — no new SQL, no derived state stored, no display recomputation.

#### Left alone deliberately

- **Sarah's two rulings.** D6's three call sites and the `n2SaveError` copy are untouched, and the plan is internally consistent with both — the copy still names a CTA MA-009 renames, and no step in the plan edits that string.
- **D7's `w-full` fallback.** R4 is unverifiable from this checkout too (`node_modules` absent). The fallback is decided in advance and the emulator settles it; that is the right shape for a plan-stage unknown.
- **The plan's file-level citations.** Spot-checked about twenty; two were off by one line (both inside D2, both corrected). The rest resolve exactly, which is unusual and worth saying.
- **The `use_account_form.hook.ts` change is not scope creep, and I did not treat it as one.** The hook's own JSDoc at `:27-34` already promises "the next `submit()` re-runs only this callback"; D9 makes the code deliver what MA-007 documented for this task.

**Changes required** — the plan ships to @dev as amended above.

---

## Emulator verification

**Not run.** This implementation session's container has no `adb`, no emulator and no `mqa` — plan step 5 (S1–S4) is not executable here. Nothing below was inferred or faked; this records what step 5 would have checked and what stands in for it at this step.

### S1 — the post-save checkpoint

Unverified: the on-device forced `setStep` failure, `mqa ui`/`mqa db` reads of the real SQLite file, and the "still 1" row count after a real retry tap.

Covered instead by `__tests__/screens/onboarding_add_account.hook.test.ts` case 7 ("a rejecting step write after a resolved insert retries into the next step without inserting again") and `__tests__/account_form.hook.test.ts` ("the retry does not re-validate against the row it just inserted" — the D9 case). Both were watched failing against the pre-change code before the fix landed (see report). They assert `addAccount` called exactly once across both taps, the second tap's `setStep`/`replace` landing, and — the assertion that specifically rules out the `errNameDuplicate` failure mode S1's on-device grep was checking for — `form.formState.errors.name` staying `undefined` after the retry. What they cannot assert: that a real SQLite write actually produced one row, or that the status track visually renders the error string.

### S2 — onboarding writes a correct row; colour sheet wired to the right owner

Unverified: the real `mqa db` column read after an onboarding save, opening the colour sheet on-device and confirming the trigger row's label updates, and the N2 screenshot comparison for section-label typography, field-error styling and the currency row's width (D7/D8 rows 2–3, R4, R5).

Covered instead: `toNewAccountInput`/`account_form.helpers.test.ts` pins `opening_balance = current_balance`, the credit columns `null` (not `0`) off the credit path, and `roundMoney` on every amount — but exercised directly, not through the onboarding hook's mapping call. The colour sheet's owner-keyed slot is pinned structurally by `account_color_sheet.state.test.ts`, not by driving the onboarding screen's trigger row. Typography, shadow and width deltas (D7 row 6, D8 row 2, R4, R5) have no unit-test equivalent — they stay unverified until step 7's independent emulator run.

### S3 — Settings still writes a correct row, on a credit card

Unverified: the real `mqa db` read on a Settings-created credit card account, and the currency row screenshot for the S2/S3 width comparison.

Covered instead: `__tests__/screens/accounts/add_account.hook.test.ts` (Settings' host, confirmed unmoved by this diff) and `account_form.helpers.test.ts`'s credit-card cases (populated credit columns, `current_balance = opening_balance`). This is the regression check on Step 3's shared `CurrencySelector` edit; the unit suites confirm the mapping and the Settings host are untouched, not that the screen renders correctly on a device.

### S4 — the add-more re-entry (MA-011 depends on this)

Unverified: the real two-row `mqa db` count and distinct `sort_order`, and the add-more N2's back chevron landing on N3 rather than N1 on a live remount.

Covered instead by `__tests__/screens/onboarding_add_account.hook.test.ts` cases 8 and 11 (`?isAddingMore=true` saves without a step write; `onBack` with the same param replaces to more_accounts without a step write) and the existing `more_accounts.hook.test.ts` `handleAddAnother` cases. The `inserted`-latch-resets-on-fresh-mount claim (R3) is pinned generically by `account_form.hook.test.ts`'s "a fresh mount starts clean" case, not by a real remount through the add-more route.

### Net

Every S1–S4 assertion that has a behavioural unit-test equivalent was written, watched failing against the pre-change code where the plan called for that, and passes now. What stays open until an environment with a device is available: the real SQLite state after a save (all four scenarios), the visual read of the colour sheet, section labels, field errors and the currency row's width (S2/S3, D7/D8/R4/R5), and the on-device back-chevron behaviour after an add-more remount (S4). None of this was walked, guessed at, or asserted as passing.

---

## Implementation review

### Round 1 — changes requested

Diff reviewed against `origin/main` (94eb46a): four implementation commits, 14 files, +769/−457. The plan is implemented step for step and nothing beyond it landed — I checked every deleted line against the plan's deletion list and every added line against a plan clause. The headline Done-when (post-save checkpoint) works, and it is pinned by tests I watched fail myself rather than taking on trust.

Two findings. One is a real behavioural regression on a screen the plan asserted was untouched; the other is a coverage claim in `## Emulator verification` that cites a suite asserting the opposite of what it is cited for.

#### What I verified by breaking it, not by reading it

The plan review's central worry was tests that pass for the wrong reason. I reverted each guard one at a time and re-ran the suites. Every one of them is load-bearing:

| Perturbation | Failing test | Failure |
|---|---|---|
| Remove the D9 bypass line (`use_account_form.hook.ts:87`) | `account_form.hook.test.ts` "the retry does not re-validate against the row it just inserted" **and** `onboarding_add_account.hook.test.ts` case 7 | `onSaved` called 1, expected 2 — the retry is eaten by `errNameDuplicate`, exactly as D9 predicted |
| `=== 'true'` → truthy (`add_account.hook.ts:20`) | cases 9 and 12 (`?isAddingMore=false`, save **and** back) | both fail |
| Remove `reset()` from `handleSave` (`:71`) | case 16 "a save failure replaces a stale back-failure message" | fails — D2's merge really does pin without it |
| Remove the `busy` re-check in `onSaved` (`:40`) | case 17 "a back that starts inside the save validation window" | fails — D4 is not decoration |
| Remove `onBack`'s `saving` guard (`:77`) | case 13 | fails |
| Remove `handleSave`'s `busy` guard (`:65`) | case 14 | fails |

The D9 test scaffolding is not vacuous: `republishOnAdd()` grows `mockAccounts` with `{ id: 'new', name: 'CIB Savings' }` — the same name `fillAndSubmit` types and `name` present — which is what makes the schema rebuild and the second validation actually attempt the duplicate check. Both stores are reset in `beforeEach` (`:78-79`). Every `mockRejectedValueOnce` in the suite is consumed within its own case, so nothing leaks past `clearAllMocks`.

I also traced the interlock rather than trusting the table. It genuinely closes the interleaving: a back landing during `addAccount`'s or `setStep`'s await hits `onBack`'s `saving` guard; a back landing during validation gets through both guards and is caught by `onSaved`'s `busy` re-read. The only residual is the one D4/R1 states in writing — the row is written where `main` would have written none.

And the money path is clean, which I expected to have to argue about and did not. The bypass calls `onValid(form.getValues())` with an unvalidated draft, but `onValid`'s `if (!inserted)` means `toNewAccountInput` is never reached on that branch, so an unvalidated draft can never produce a row. Separately, onboarding's `parseFloat` mapping is gone in favour of `roundMoney(parseNonNegativeDecimal(...))` — that is a genuine improvement in the money-drift class, not just de-duplication.

Done-when, item by item: both hosts render `AccountForm`; `ACCOUNT_COLORS`, the form body and the `parseFloat` mapping have zero occurrences in `src/` (grep); `o4SectionColor` removed, `o4ColorLabel` correctly left; `loadAccounts` has zero occurrences anywhere under `src/modules/onboarding/`; the param type is `'true'`; no temporary S1 repository edit survived (`git diff --stat origin/main -- src/modules/onboarding/repositories/` is empty).

---

#### Defects

**D-1 — `use_account_form.hook.ts:87`: the D9 bypass fires after a *successful* save, not only after a failed one, and re-runs the host's `onSaved`. On Settings that pops two screens.**

The plan and the plan review both justify the blast radius with "`inserted` is only ever true there after a save that already succeeded **and unmounted the screen**" (Plan D9, R8). That sentence is false against the code that shipped. `onSaved` for Settings is `() => router.back()` (`accounts/add_account/add_account.hook.ts:13`) — navigation is asynchronous, the screen stays mounted for the pop animation, and `finishSave()` runs on the next line so `state.saving` goes false and the CTA (`isDisabled={state.saving}`, `accounts/add_account/index.tsx:41`) re-enables immediately. `inserted` stays latched until the *next* mount's `useInit(reset)`.

Concrete scenario. Settings → Accounts → Add Account, fill a valid draft, double-tap **Save Account** fast enough that tap 2 lands after `finishSave()` and before the pop completes:

- On `main`: tap 2 runs `form.handleSubmit`, the schema has been rebuilt from the republished `accounts`, validation fails `errNameDuplicate`, `onValid` never runs, nothing navigates.
- On this branch: tap 2 takes the bypass, skips validation, skips the insert (`inserted`), and calls `onSaved()` again → a second `router.back()`. The Accounts list is popped too and the user lands on Settings root.

Same shape on the live first-run path: after a successful N2 save, a second CTA tap re-runs `onSaved`, writing `setStep(N3)` a second time and firing a second `router.replace` (`add_account.hook.ts:52,59`). Idempotent, so less harmful, but it is a duplicated write on the path this task exists to protect.

No second row is ever inserted — the `inserted` check holds — so this is not the money/data class. It is a navigation regression on a shared file, introduced by this diff, on a screen the plan claimed was behaviourally unchanged.

**Smallest fix, with the constraint that makes it non-obvious.** The distinction the bypass needs is "the previous attempt failed", not "a row exists". A terminal latch set by `finishSave()` and cleared by `reset()`, with `submit()` returning early on it, is the natural shape — **but it must not swallow D4's recovery**: `onSaved`'s early return on `busy` (`add_account.hook.ts:40`) also falls through to `finishSave()`, so a naive latch would leave N2 permanently unsaveable if the competing back transition then failed, and would break case 17's premise. Whatever @dev picks needs `onSaved` to be able to say "I declined" distinctly from "I completed" — a sentinel rejection, or a boolean return honoured by `useAccountForm`. Cases 5, 7, 13–17 and `account_form.hook.test.ts:65` all need to stay green; `:65` currently asserts `onSaved` twice on a sequential re-tap after success, and that assertion is the one that encodes the behaviour being changed, so it needs revisiting alongside.

If @tariq's view is that the double-tap window is not worth the interaction risk, that is a defensible call — but then it belongs in `Risks accepted` with the *correct* reason, because the reason currently recorded ("unmounted the screen") is not what the code does, and the next reader will rely on it.

**D-2 — `## Emulator verification`, S2 and S3: `current_balance = opening_balance` is attributed to a suite that asserts the opposite.**

S2 says "`toNewAccountInput`/`account_form.helpers.test.ts` pins `opening_balance = current_balance`" and S3 repeats it for the credit-card case. `account_form.helpers.test.ts:328-329` asserts the reverse — *"the returned object carries no `current_balance` key"* — because the mapper deliberately never produces that column; the repository does. The behaviour **is** unit-covered, by `__tests__/account.repository.test.ts:72` ("sets current_balance = opening_balance", against a real SQLite database), which is the citation the plan itself used at step 2.1. So this is a misattribution, not an uncovered gap — but it is the one class of error this section exists to prevent, and `@pr-reviewer` reads this section as the record of what was and was not checked.

Fix: re-cite `account.repository.test.ts:72` in both S2 and S3, and keep the honest caveat already there (exercised directly, not through the onboarding hook's call).

Everything else in that section checks out. Every S1–S4 assertion is accounted for as unit-covered or explicitly open; the "watched failing against the pre-change code" claim for S1 is true — I reproduced both failures independently. S4's "a fresh mount starts clean" case exists (`account_form.hook.test.ts:183`). The visual items (D7 currency-row width, D8 row 2 label leading, row 3 field-error styling, R4, R5) are correctly listed as open, and the decision not to walk the explicit-`false` case on device is correctly recorded as a decision.

#### Suggestions

- `src/modules/accounts/constants/account_palette.ts:11-13` — the header comment still reads "Three older colour lists still exist and are NOT touched here — the two `ACCOUNT_COLORS` arrays in the add-account hooks…". Both arrays are gone as of this commit. The comment is now false and this is the task that made it false; one line.

#### Nits

- `src/constants/strings.ts:54` — `accountCurrencyA11y` is filed under the `// Welcome — base currency` heading, but it is the *account* form's label and its only consumer is `CurrencySelector` inside `AccountForm`. Grouping only.
- `CurrencySelector` now hardcodes the accessible name for every future consumer. One consumer today, so no defect; if a second host ever renders it for a non-account currency the name will be wrong, and a prop with this as the default would age better.

---

#### CI parity evidence

Chain from `CLAUDE.md` § Commands, run in order in this worktree.

```
npm run format:check   → All matched files use the correct format. (761 files, 463ms)
npm run lint           → exit 0 (warnings only, all pre-existing in jest.setup.js / metro.config.js)
npm run typecheck      → exit 0
npm test -- --ci       → 236 suites passed, 2316 tests passed, 0 failed (17.2s)
npx expo-doctor@1.20.1 → exit 1 — 18/20 passed
npx expo prebuild --no-install --platform android → exit 0; android/ present
```

**The two `expo-doctor` failures are environmental, and I confirmed the diagnosis rather than accepting it.** Both failing checks make live HTTPS calls that this container's egress proxy rejects at CONNECT:

- *Check Expo config schema* → `api.expo.dev`. `node -e "fetch('https://api.expo.dev/v2/project/configuration/schema/54.0.0')"` returns the body `Host not in allowlist: api.expo.dev. Add this host to your network egress settings to allow access.` — which is verbatim the source of expo-doctor's `SyntaxError: Unexpected token 'H', "Host not i"... is not valid JSON`.
- *Validate packages against React Native Directory* → `reactnative.directory`. `curl` gets `HTTP/1.1 403 Forbidden` on the CONNECT tunnel, body `request rejected: host not permitted`; expo-doctor reports "Directory check failed with unexpected server response".

Neither touches project state, `app.json` is unmodified by this diff, and the other 18 checks pass. Treated as environmental. Working tree is clean (`git status --porcelain` empty) after every perturbation I applied and reverted, and after `prebuild`.

**Emulator verification (workflow step 7) was not run.** This container has no `adb`, no emulator and no `mqa`, so the independent walk that normally carries this gate does not exist for MA-008 — by me at step 7 any more than by @dev at step 6. S1's row count in real SQLite, S2/S3's `mqa db` column reads, the colour-sheet trigger on device, and every typography/width comparison (D7, D8 rows 2–3, R4, R5) remain unverified by anyone. @sarah should carry that forward explicitly: **gate 3 is the only remaining net under those**, and the device-QA checklist for this task needs S1's forced-failure walk and the S2/S3 database reads added to it, not just the visual pass.

**Changes required.**
