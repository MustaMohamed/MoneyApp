---
id: MA-008
scope: MA-onboarding-redesign
milestone: M2
issue: 210
verify: emulator
branch:
pr:
---

# MA-008 — Onboarding adopts the shared account form

## Summary

Delete onboarding's copy of the account form, host the shared one instead, and fix the two onboarding-side defects that only stop being duplicated once there is one form.

## Details

This is the second half of the de-duplication MA-007 began, and it is the one that touches the live first-run path. When it merges, the second onboarding step should look and behave as it does today — same fields, same order, same copy — but rendered by the Accounts module's form, with onboarding keeping only what is genuinely its own: the starting currency, its own copy, the back behaviour, the step it persists after a save, and where it goes next.

**Everything onboarding duplicated goes in this task**: its copy of the form body, its copy of the submission mapping, its own twelve-colour list, and its hand-rolled swatch row. The swatch row is replaced by the colour sheet MA-006 built — onboarding picks that up as a side effect of adopting the shared form, and it is the first visible change on this screen.

**The starting currency is onboarding's, not the form's.** The form takes a starting currency from its host; onboarding passes the base currency the user chose on the first step. Settings passes its own default. Neither knows about the other.

### Two live defects fixed here

- **The "adding another account" flag is a string tested for truthiness.** An explicit `false` therefore takes the add-more branch, and the stack's own parameter type claims the value is a boolean when it is not. It must be compared exactly, and the type must stop lying. MA-011 depends on this working — its whole add-another round trip runs through this flag.
- **The screen reloads the account list on mount** even though startup already loaded it and saving publishes its own refresh. The load goes.

### The post-save checkpoint

This is behaviour the mockup draws and no task owned. Frame A3's caption states it and C4/C6 are where a user meets it: **when the account insert succeeds but the step write fails, the retry repeats only the step write and never re-inserts the account.**

The failure the user sees is about the step, not the data. Their account is on disk. Tapping the primary action again must not produce a second account — and it must not be prevented from retrying either, because the step really does need writing. The error renders in the shell's status track, which MA-004 delivered; the form reports its failure and the host renders it.

This is the interaction between the guarded save path from MA-007 and the persist-before-navigate ordering from MA-005, and it is the single most valuable thing to force on the emulator in this task: make the step write fail with an account already inserted, tap retry, and count the rows.

**Behaviour that must be preserved exactly:** duplicate names still rejected case-insensitively after trimming; current balance still equals opening balance; credit-only values still absent rather than zero on non-credit accounts; and a successful save still advances to the more-accounts step, persisting before it navigates and replacing rather than pushing, per MA-005.

**Done when:** both entry points render from the same form, and the duplicated form body, submission mapping and colour list exist in exactly zero places; the add-more flag is compared exactly, its declared type matches what is actually passed, and a test covers the explicit-`false` case that is broken today; no onboarding route loads the account list itself; a forced step-write failure after a successful insert leaves exactly one account row, shows the error in the status track, and retries into the next step without inserting again; and creating an account from onboarding and from Settings on the emulator both produce a correct row, checked against the database.

---

## Plan

### Summary

- **Onboarding's N2 becomes a host, nothing more.** `add_account.hook.ts` drops its own RHF/Zod setup, its own `ACCOUNT_COLORS`, and its own `parseFloat` submission mapping, and calls `useAccountForm` with three things onboarding actually owns: the base currency, `Strings.n2SaveError`, and an `onSaved` that writes the step and replaces the route. `index.tsx` drops ~280 lines of duplicated field markup and renders `<AccountForm form={form} ownerId="onboarding/add_account" />` inside the shell it already has.
- **One re-entry guard and one busy flag on the save path — the form's.** `runOnboardingTransition` is *not* nested inside `onSaved`; the tail is written inline from `resolveOnboardingStep` + `ONBOARDING_STEP_HREF`, and it throws rather than swallowing, so `useAccountForm`'s `inserted` latch and `failSave` are what implement the post-save checkpoint. The transition state survives on this screen for `onBack` only, and the two are interlocked explicitly in both directions.
- **`isAddingMore` is compared with `=== 'true'`**, once, at the top of the hook, and `OnboardingStackParams` stops declaring it a boolean.
- **All three onboarding `loadAccounts()` call sites go** — N2, N3 and N4 — because `spec.md:502` names exactly those three and the Done-when is the plural one.
- **`CurrencySelector` gains the accessible name** onboarding is otherwise about to lose (flagged for this task at MA-007 step 5, `MA-007.md:108`).
- **Tests:** `__tests__/screens/onboarding_add_account.hook.test.ts` is rewritten to fifteen cases against the new hook shape, including the explicit-`false` case that is broken today (both on save and on back), the retry-writes-only-the-step case, and both halves of the interlock. Four other suites are cited rather than duplicated.
- **Emulator:** four scoped scenarios, one of which forces `setStep` to reject exactly once with an account already inserted and counts rows in SQLite.

### Detail

#### Step 0 — decisions made before any code, with the evidence

**D1 — how the two guarded paths compose. The form owns the save; the transition state owns the back.**

The two candidate designs, and why the second one loses:

| | A: `onSaved` calls `runOnboardingTransition` | **B (chosen): `onSaved` is the tail and it throws** |
|---|---|---|
| Re-entry guard on the CTA | two — `beginSave()` (`account_form.state.ts:45`) *and* `begin()` (`onboarding_transition.state.ts:41`) | one — `beginSave()` |
| Busy flag the CTA renders | two — `saving` and `busy` | one — `saving` |
| Error channel | `runOnboardingTransition` catches and calls `api.fail` (`onboarding_transition.ts:64`), so `onSaved` never rejects, `failSave` never runs, and `state.errorMessage` is permanently `undefined` | `failSave(Strings.n2SaveError)` — exactly the contract `use_account_form.hook.ts:26-34` documents *for this task* |
| Back-in-flight + Save | **silent orphan insert.** `beginSave()` passes, `addAccount` writes the row, then `begin()` returns `null` (`busy` is true from `onBack`), `runOnboardingTransition` never runs, `onSaved` resolves normally, `finishSave()` clears the flag. An account on disk, no step written, no error shown. Today this cannot happen because `begin()` runs *before* `addAccount` (`add_account.hook.ts:75`) | blocked at the CTA by the D3 interlock, before validation |

B is chosen. A is not merely redundant; its third row is a live data defect it would introduce.

**What B gives up, and what replaces it.** `runOnboardingTransition` contributes five things to today's save path. Four are kept:

| Contribution | Under B |
|---|---|
| `resolveOnboardingStep(desiredStep, count)` called **after** the insert (MA-005 Decision 2 row 2 — the first-account hard loop) | called directly in `onSaved`, which by construction runs after `markInserted()` (`use_account_form.hook.ts:67-72`) |
| persist before navigate | two statements in `onSaved`, in that order, with no `try` between them — a rejecting `setStep` cannot reach the `router.replace` line |
| `invalidate()` immediately before every navigate (MA-005 Decision 3) | `useAddAccountTransitionState.getState().invalidate()` on the line before `router.replace` |
| `ONBOARDING_STEP_HREF[resolved]`, `replace` not `push` | same map, same call |
| `isCurrent(session)` between the insert and the step write | **dropped — see D4** |

**D2 — one status track, two writers, stated precedence.** `OnboardingShell` takes a single `statusMessage` (`onboarding_shell/index.tsx:19`). After D1 the save path writes `useAccountFormState.errorMessage` and the back path writes `useAddAccountTransitionState.statusMessage`. The hook merges them as:

```ts
statusMessage: transitionStatusMessage || formErrorMessage
```

Transition wins because it is the later event in the only sequence where both are set: a failed save leaves `errorMessage` set, and if the user then taps back and *that* fails, the message the user needs is the one about the chevron. The reverse precedence would pin the stale save error. `begin()` clears `statusMessage` to `''` on every accepted back attempt (`onboarding_transition.state.ts:45`) and `beginSave()` clears `errorMessage` on every accepted save attempt (`account_form.state.ts:46`), so neither channel can hold a message from an attempt two operations ago. Pinned by test 15.

**D3 — the interlock, in both directions, because there are still two guards on this screen.**

- `onBack` returns early when `useAccountFormState.getState().saving` is true. This also makes `spec.md:81` ("Back disabled during the write") true for the first time, and it restores exactly the coverage today's single shared `busy` gave.
- `handleSave` returns early when `useAddAccountTransitionState.getState().busy` is true, **before** calling `submit()`, so nothing is validated and no state moves. Silent, matching `spec.md:492` ("CTA re-entry is ignored while an operation is active") and matching today's behaviour, where `begin()` returns `null` with no message.

Residual, stated rather than hidden: both checks read a flag that is set *inside* `form.handleSubmit`'s async `onValid`, so there is a sub-frame window after a tap where neither flag is set yet. That window is identical to `main`'s — today's `begin()` is also inside `onSubmit` (`add_account.hook.ts:75`) — so this is not a regression, and closing it is not this task's.

**D4 — what replaces `isCurrent()` on the save path.** The only producer of a competing session on this screen is `onBack`; nothing else calls `begin()` on `useAddAccountTransitionState` (grep: `add_account.hook.ts` is the file's sole consumer, and `add_account.state.ts` creates a per-screen instance, `onboarding_transition.state.ts:34`). D3 stops that producer at the source, and the `invalidate()` in D1 bumps the session before every navigate, so any back transition that somehow got in flight cannot navigate afterwards (`onboarding_transition.ts:68` re-checks `isCurrent` after `persist`). Prevention at the source instead of detection mid-flight. Checked by test 14 and emulator scenario S4.

**D5 — the flag and its type.**

- Runtime: `const isAddingMore = useLocalSearchParams<{ isAddingMore?: string }>().isAddingMore === 'true';` computed once at the top of the hook and used at all three sites (today `add_account.hook.ts:106`, `:131`, `:136`).
- Declared type: `src/app/(onboarding)/_layout.tsx:10` becomes `add_account: { isAddingMore?: 'true' }`. `'true'` and not `string`, because `more_accounts.hook.ts:52-55` passes that literal and nothing else ever does.
- **Honest about what this buys:** nothing at runtime. `OnboardingStackParams` has exactly one occurrence in the tree — its own declaration (grep over `src/` and `__tests__/`) — and `useLocalSearchParams<T>` does not validate. The runtime fix is the `=== 'true'`. The type fix exists so MA-011 plans its add-another round trip against what is actually on the wire. The *reader* keeps `string | undefined`, because a deep link can put anything there and the exact comparison is what makes that total.

**D6 — the mount load, and the widening from one call site to three.** The Details paragraph names N2's; the Done-when says "no onboarding route loads the account list itself", and `spec.md:502` names all three explicitly: `add_account.hook.ts:47`, `ready.hook.ts:27`, `more_accounts.hook.ts:24-28`. Doing all three here. Verified, not assumed:

- Startup loads it: `use_layout_init.hook.ts:38` awaits `loadAccounts()` inside `start`, and `src/app/_layout.tsx:78` renders the `Stack` **only** when `status === 'ready'`, which `resolveReady` sets after that await (`use_layout_init.hook.ts:38-41`). No onboarding route can mount before the snapshot exists.
- Saving republishes: `account.store.ts:85` — `addAccount` awaits its own `loadAccounts()` before resolving.

Three lines and two test assertions, all inside `src/modules/onboarding/`. No module boundary is crossed and `main` works after this alone.

**D7 — `CurrencySelector` loses onboarding's two extra props.** `MA-007.md:108` flagged this for this task. Onboarding's inline control passes `listClassName="w-full"` and `accessibilityLabel="Account currency"` (`onboarding/.../index.tsx:133-134`); `currency_selector.tsx:14-21` passes neither.

- **The accessible name is a real regression on the first-run path and is fixed here**, in `CurrencySelector` (its only consumer is `account_form.tsx:89` — grep), not with a form prop.
- **`w-full` is not added.** It is very likely a no-op: `Tabs.List` sits in a column parent, and React Native's default `alignItems: 'stretch'` already makes a width-less child fill it; Settings has rendered `CurrencySelector` without it since before this scope with no narrowing reported. I could not confirm HeroUI's `Tabs.List` base classes statically — `node_modules` is not installed in this checkout — so this is an observation, not a proof. Settled on the emulator in S2/S3, with the fallback decided in advance: if N2's currency row is visibly narrower than the pre-merge build, add `listClassName="w-full"` inside `CurrencySelector` and record it in the emulator section. Adding it speculatively would widen Settings' row, on a screen this task otherwise does not touch, for a row MA-009 rebuilds.

**D8 — every visible change, named. "No visible change" would be false.**

| # | Surface | Today (onboarding) | After | Verdict |
|---|---|---|---|---|
| 1 | Colour control | 12 `PressableFeedback` swatches, label `COLOR — 12 PRESETS` | `AccountColorField` trigger row + MA-006's 32-colour sheet, label `ACCOUNT COLOUR` | **Expected and task-stated.** Default colour is unchanged: `DEFAULT_ACCOUNT_COLOR` is `AcctTokens.midnight.rich` (`account_palette.ts:127`), which is what `add_account.hook.ts:63` uses today |
| 2 | Section labels | `Text variant="hint"` (`text.tsx:17` → `font-inter text-[12px] text-muted`) + `font-sora-bold text-gold-500 pt-2 pb-2 tracking-widest` | `FormSectionLabel` → `Typography` + `font-sora-bold text-gold-500 pt-2 pb-2 text-xs tracking-widest uppercase` | Same 12 px, same Sora bold, same gold, same padding and tracking. `uppercase` is a no-op — every label string is already upper (`strings.ts:25-35`). One unknown: `Typography`'s default line height vs RN `Text`'s. Screenshot in S2 |
| 3 | Field errors | `Animated.Text`, `text-negative font-inter text-[12px] mt-1`, FadeInDown 150 / FadeOutUp 100 | `FormErrorText` → HeroUI `FieldError` with its own colour token, size and animation | **Real delta.** Already shipped to Settings at MA-007; onboarding now matches it |
| 4 | Interest switch | React Native `Switch` with gold `trackColor` (`index.tsx:303-311`) | HeroUI `Switch` (`credit_card_fields.tsx:115-119`) | **Real delta**, credit-card path only. Already in Settings |
| 5 | Credit hints, interest label | `Text variant="caption"` / `variant="body"` + classes | `Typography` with `text-[11px]` / `text-[15px]` and the same colour classes | Same size and colour |
| 6 | Currency control | inline `SegmentedTabs`, `variant="solid-gold"`, `w-full`, a11y label | `CurrencySelector`, same primitive and variant, a11y label restored (D7) | Width: see D7 |
| 7 | CTA busy source | `formState.isSubmitting` (`index.tsx:64-65`) | `state.saving` | The busy label no longer flashes while an **invalid** draft is being validated. Matches Settings (`accounts/.../index.tsx:41-42`) |
| 8 | Credit block content and order | revolving · limit · min+hint · due day · switch · APR+hint | identical, from `credit_card_fields.tsx:35-145` | Field-for-field the same, read off both files |

Nothing else on this screen moves: `OnboardingShell` props, `n2HeaderTitle`, `n2Footnote`, `o4Cta`, `n2CtaBusy`, the `ScreenScroll` padding and `keyboardShouldPersistTaps`, and the button's press-scale animation all stay as they are.

---

#### Step 1 — the declared param type

**Files:** modify `src/app/(onboarding)/_layout.tsx:10`.

- [ ] **1.1** Replace the lying member and say why, so the next reader does not "fix" it back:

```ts
  // Expo Router params are strings on the wire. more_accounts.hook.ts:52-55
  // passes the literal 'true' and nothing else ever navigates here with a
  // param — so this is the type, and the reader compares with === 'true'.
  add_account: { isAddingMore?: 'true' };
```

- [ ] **1.2** `npm run typecheck` — expected green; `OnboardingStackParams` has no consumer (grep over `src/` and `__tests__/` returns only its own declaration), so nothing depends on the old shape.
- [ ] Commit: `fix(onboarding): the add-more param type matches what is passed (MA-008)`

---

#### Step 2 — the N2 host (test first)

**Files:**
- Test: rewrite `__tests__/screens/onboarding_add_account.hook.test.ts`
- Modify: `src/modules/onboarding/screens/onboarding/add_account/add_account.hook.ts`
- Modify: `src/modules/onboarding/screens/onboarding/add_account/index.tsx`
- Modify: `src/modules/onboarding/screens/onboarding/add_account/add_account.anim.ts`
- Modify: `src/constants/strings.ts` (remove one orphaned key)
- Unchanged: `add_account.state.ts` (still needed — `onBack` uses it)

- [ ] **2.1 Rewrite the suite.** Keep the existing mock scaffolding shape (`__tests__/screens/onboarding_add_account.hook.test.ts:11-48`): `jest.mock` for `expo-router`, `@/modules/accounts/store/account.store` and `@/modules/onboarding/store/onboarding.store`, with `attachMockSelectorStore` from `@/test_helpers/mock_zustand_selectors`. Two changes to the scaffolding:
  - `beforeEach` resets **both** stores: `useAddAccountTransitionState.getState().reset()` and `useAccountFormState.getState().reset()`. Missing the second leaks `inserted` between cases and turns case 7 green for the wrong reason.
  - `setup()` takes the raw param string rather than a boolean, so `'true'`, `'false'` and absent are all expressible: `setup(isAddingMore?: string)` → `useLocalSearchParams` returns `isAddingMore === undefined ? {} : { isAddingMore }`.
  - `attachMockSelectorStore` must supply `accounts`, `addAccount` **and** `loadAccounts` — the last only so case 4 can assert it was never called.

  The fifteen cases:

  | # | Name | Asserts |
  |---|---|---|
  | 1 | renders without throwing | `renderHook(() => useAddAccount())` resolves |
  | 2 | the draft starts on the onboarding base currency | store `baseCurrency: Currency.USD` → `form.getValues('currency') === Currency.USD`. The form never reads the onboarding store itself — no onboarding mock is needed by `use_account_form.hook.ts`, which is asserted structurally by `__tests__/account_form.hook.test.ts` having no such mock |
  | 3 | the draft's defaults are unchanged by the adoption | `selected_color === DEFAULT_ACCOUNT_COLOR`, `selected_type === AccountType.Bank` |
  | 4 | does not load the account list on mount | the `loadAccounts` mock has zero calls after `renderHook` |
  | 5 | a successful save writes the step **before** it navigates, and replaces | `addAccount` once · `setStep(OnboardingStep.N3)` · `replace('/(onboarding)/more_accounts')` · and the ordering explicitly: `mockSetStep.mock.invocationCallOrder[0] < mockReplace.mock.invocationCallOrder[0]` |
  | 6 | a rejecting insert writes no step, does not navigate, and shows `n2SaveError` | `setStep` not called · `replace` not called · `state.statusMessage === Strings.n2SaveError` · `state.saving === false` |
  | 7 | **a rejecting step write after a resolved insert retries into the next step without inserting again** | `mockSetStep.mockRejectedValueOnce`. After submit 1: `addAccount` 1 · `replace` 0 · `statusMessage === Strings.n2SaveError` · `saving === false`. After submit 2: `addAccount` still **1** · `setStep` 2 · `replace` 1, with `/(onboarding)/more_accounts` · `statusMessage` falsy |
  | 8 | `?isAddingMore=true` saves without writing a step | `addAccount` 1 · `setStep` not called · `replace('/(onboarding)/more_accounts')` |
  | 9 | **`?isAddingMore=false` saves like a first account** | `setStep(OnboardingStep.N3)` **was** called — this fails on `main`, where the truthy check takes the add-more branch and skips the step write |
  | 10 | onBack with no param writes N1 and replaces to welcome | as today (`:115`) |
  | 11 | onBack with `?isAddingMore=true` writes no step and replaces to more_accounts | as today (`:124`) |
  | 12 | **onBack with `?isAddingMore=false` writes N1 and replaces to welcome** | the second half of the same defect; fails on `main` |
  | 13 | onBack is ignored while a save is in flight | `useAccountFormState.setState({ saving: true })`, then `await onBack()` → `setStep` not called, `replace` not called |
  | 14 | the CTA is ignored while a back transition is in flight | `useAddAccountTransitionState.getState().begin()`, then fill a valid draft and `await handleSave()` → `addAccount` not called |
  | 15 | a back failure replaces a stale save-failure message | make the save's `setStep` reject once (leaving `errorMessage` set), then make the back's `setStep` reject → `state.statusMessage === Strings.onboardingBackSaveError`. Pins D2's precedence |

  Deliberately **not** re-asserted here, because they are already covered and re-testing them through this hook buys nothing:
  - case-insensitive trimmed duplicate rejection → `__tests__/add_account.schema.test.ts:69` and `:74`
  - `current_balance = opening_balance` → `__tests__/account.repository.test.ts:72`
  - credit-only values absent (`null`) rather than `0` on non-credit types → `__tests__/account_form.helpers.test.ts:205`, plus `:254`/`:263` for blank vs explicit zero
  - the guarded-save latch in the abstract → `__tests__/account_form.hook.test.ts:81` and `:154`, `__tests__/account_form.state.test.ts:53`

- [ ] **2.2 Run it and watch it fail.** `npm test -- onboarding_add_account` — expect failures across the board (the hook still returns `state.busy`, still exposes `ACCOUNT_COLORS`, still truthy-checks the flag). Cases 9 and 12 must fail *for the right reason*: on `main` case 9 fails because `setStep` was not called, not because the module is missing. If either passes before the change, stop — the premise is wrong.

- [ ] **2.3 Rewrite `add_account.hook.ts`.** Whole file, in this order:

  - imports: `useLocalSearchParams`, `useRouter` · `OnboardingStep` · `Strings` · `useAccountStore` · `useAccountForm` and `useAccountFormState` from `@/modules/accounts/components/account_form/…` · `ONBOARDING_STEP_HREF`, `resolveOnboardingStep` from `@/modules/onboarding/domain/onboarding_route` · `runOnboardingTransition` · `useOnboardingStore` · `useInit` · `useAddAccountTransitionState`.
  - **Deleted:** the `ACCOUNT_COLORS` array (`:19-34`), the `AcctTokens` import, `createAddAccountSchema`/`AddAccountFormData` from `@/utils/schemas/add_account.schema` (`:10-13` — a compatibility surface, `spec.md:465`), `useZodForm`, `useMemo`, `AccountType`, the `accounts`/`addAccount`/`loadAccounts` reads, `useInit(loadAccounts)` (`:47`), the whole `useZodForm` block (`:54-72`) and the whole `onSubmit` body (`:74-121`).
  - **Kept:** `useInit(() => useAddAccountTransitionState.getState().reset())` with its comment (`:48-52`) — it is `onBack`'s channel now, and the add-more re-entry still arrives by `replace`.
  - the flag, once: `const isAddingMore = useLocalSearchParams<{ isAddingMore?: string }>().isAddingMore === 'true';`
  - the form:

```ts
const { form, submit, state } = useAccountForm({
  initialCurrency: baseCurrency,
  saveErrorMessage: Strings.n2SaveError,
  onSaved: async () => {
    // resolve AFTER the insert — useAccountForm calls onSaved only past
    // markInserted(), so accounts.length already includes the new row.
    // Reading it earlier reproduces MA-005 Decision 2 row 2's hard loop.
    const resolved = isAddingMore
      ? OnboardingStep.N3
      : resolveOnboardingStep(OnboardingStep.N3, useAccountStore.getState().accounts.length);

    // Add-more: the persisted step never moved off N3, so there is nothing
    // to write and only the route changes — identical to add_account.hook.ts:106
    // before this task.
    if (!isAddingMore) await setStep(resolved);

    // No catch: a rejecting setStep propagates to useAccountForm's catch,
    // which calls failSave(Strings.n2SaveError) and leaves `inserted` latched.
    // Persist-before-navigate is structural — the replace below is unreachable
    // from a failed write.
    useAddAccountTransitionState.getState().invalidate(); // MA-005 Decision 3
    router.replace(ONBOARDING_STEP_HREF[resolved]);
  },
});
```

  - `handleSave` and `onBack` with the D3 interlock:

```ts
const handleSave = async () => {
  // D3: the back path's guard, checked before validation so nothing moves.
  if (useAddAccountTransitionState.getState().busy) return;
  await submit();
};

const onBack = async () => {
  // D3: spec.md:81 — back is inert during the write.
  if (useAccountFormState.getState().saving) return;
  const session = useAddAccountTransitionState.getState().begin();
  if (session === null) return;
  await runOnboardingTransition({ /* unchanged from :127-142, with the
    exact isAddingMore above in both places */ });
};
```

  - the return: `{ form, handleSave, onBack, state: { statusMessage: transitionStatusMessage || state.errorMessage, saving: state.saving } }`. `busy` is **not** returned — nothing renders it, and leaving it would be the second busy flag D1 exists to prevent.

- [ ] **2.4 Rewrite `index.tsx`.** The shell call is unchanged (`step`, `n2HeaderTitle`, `onBack`, `n2Footnote`, `statusMessage`, `cta`) and so is the `ScreenScroll`. Everything between them collapses to `<AccountForm form={form} ownerId="onboarding/add_account" />` — the owner id per `AccountFormProps.ownerId`'s JSDoc (`account_form.tsx:21-28`) and `account_color_sheet.state.ts:5-16`; Settings keeps `'accounts/add_account'`. The CTA's `isDisabled`/`isLoading` switch from `isSubmitting` to `state.saving`; `label`, `loadingLabel` and the `Animated.View` wrapper are untouched. Deleted imports: `cn`, `PressableFeedback`, `Controller`, `useWatch`, `Switch`, `TYPE_OPTIONS`/`TypePill` (this removes the last onboarding consumer of the `src/components/account_type_pill` compat shim), `Input`, `SegmentedTabs`, `Text`, `AccountType`, `Currency`, `CoreTokens`, `GoldTokens`, `ACCOUNT_COLORS`.

- [ ] **2.5 `add_account.anim.ts` keeps only `btnAnim` + `triggerBtnPress`.** The six other values (`ccEntering`/`ccExiting`/`aprEntering`/`aprExiting`/`errorEntering`/`errorExiting`, `:28-33`) are deleted: the first four now live in `account_form.anim.ts`, and the last two are `FieldError`'s. The file ends up identical in shape to `src/modules/accounts/screens/accounts/add_account/add_account.anim.ts`.

- [ ] **2.6 Remove the orphaned string.** `Strings.o4SectionColor` (`strings.ts:29`, `'COLOR — 12 PRESETS'`) has exactly one consumer today, the swatch row this step deletes. Grep to confirm zero references before removing it. Leave `o4ColorLabel` alone — it was already unreferenced before this task and is not this task's to clean.

- [ ] **2.7 Delete the other two `loadAccounts` call sites (D6).**
  - `src/modules/onboarding/screens/onboarding/ready/ready.hook.ts:23,27` — the `loadAccounts` read and `useInit(loadAccounts)`.
  - `src/modules/onboarding/screens/onboarding/more_accounts/more_accounts.hook.ts:16,24-28` — the `loadAccounts` read and the whole `useFocusEffect` block, plus the now-unused `useFocusEffect` and `useCallback` imports.
  - Add one assertion to each suite: `expect(mockLoadAccounts).not.toHaveBeenCalled()` in `__tests__/screens/onboarding_ready.hook.test.ts` (the mock already exists at `:73`) and the same in `__tests__/screens/onboarding_more_accounts.hook.test.ts` (promote the inline `jest.fn()` at `:50` to a named mock first). Leave the `useFocusEffect: jest.fn()` entry in the more-accounts `expo-router` mock — harmless, and removing it is churn.

- [ ] **2.8 Run.** `npm test -- --ci` — the fifteen new cases green, and these five suites green **untouched**: `account_form.hook.test.ts`, `account_form.state.test.ts`, `account_form.helpers.test.ts`, `add_account.schema.test.ts`, `screens/accounts/add_account.hook.test.ts`. If `screens/accounts/add_account.hook.test.ts` moves, something crossed into Settings that should not have.
- [ ] **2.9** `npm run typecheck`.
- [ ] Commit: `refactor(onboarding): N2 renders the shared account form (MA-008)`

---

#### Step 3 — the currency control's accessible name

**Files:** modify `src/constants/strings.ts`, `src/modules/currency/components/currency_selector.tsx`.

- [ ] **3.1** Add `accountCurrencyA11y: 'Account currency'` to `strings.ts`, beside the other onboarding form copy. Same wording onboarding uses today (`onboarding/.../index.tsx:134`), so this restores rather than invents.
- [ ] **3.2** Pass it: `accessibilityLabel={Strings.accountCurrencyA11y}` on the `SegmentedTabs` inside `CurrencySelector`. `SegmentedTabsProps.accessibilityLabel` already forwards to `Tabs.List` (`tabs.tsx:88-91`, `:237`). One consumer only — `account_form.tsx:89` — so both hosts gain it and nothing else in the app is touched.
- [ ] **3.3** `npm run typecheck && npm test -- --ci`.
- [ ] Commit: `fix(currency): the currency selector announces itself (MA-008)`

---

#### Step 4 — CI parity, then the emulator

- [ ] **4.1** Run the chain from `CLAUDE.md` § Commands, top to bottom, fixing and restarting from the top until green. It ends in `expo prebuild --no-install`, which regenerates `android/` — so it runs **before** any build, per the emulator skill.
- [ ] **4.2** `git status --porcelain` must be clean apart from the intended files. This matters at 5.1, which temporarily edits a source file.

---

#### Step 5 — emulator verification (workflow step 6)

`verify: emulator`. Setup per the `emulator-verify` skill: `npm install` in the worktree, `mqa needs-build` (**expect exit 1 — this diff is JS-only**: no `package*.json`, `app.json`, `eas.json`, `patches/`, `android/` or `ios/` change), a private Metro on 8082, `MQA_PORT=8082 mqa launch`, then `mqa park`. Confirm the branch's bundle is what loaded before trusting anything — the colour **trigger row** in place of the twelve swatches is visible on the first screen under test, which makes that check free.

Four scenarios, scripted through `mqa walk`.

- [ ] **5.1 — S1: the post-save checkpoint.** The scenario the task calls the single most valuable thing to force here.

  **Forcing the failure.** `setStep` writes to SecureStore (`onboarding.repository.ts:28-30`), so it cannot be broken from outside the app. Temporarily edit `src/modules/onboarding/repositories/onboarding.repository.ts` to fail the **first** call only, and no other file:

```ts
  let forcedFailureUsed = false; // MA-008 S1 — TEMPORARY, revert before commit
  async setStep(step: OnboardingStep): Promise<void> {
    if (!forcedFailureUsed) { forcedFailureUsed = true; throw new Error('forced MA-008 S1'); }
    await SecureStore.setItemAsync(SecureStoreKeys.OnboardingStep, step);
  }
```

  Fail-once rather than fail-always is deliberate: reverting the file between the two taps would fast-refresh the route, remount the hook, and `useInit` would `reset()` the latch (`use_account_form.hook.ts:60`) — which is precisely the state the retry is supposed to be testing. The module-level flag keeps the bundle still across both taps.

  Walk: `mqa reset` → launch → N1 Continue → N2 → type a name and a balance → **Save Account**. Then assert:
  - `mqa ui | grep -c "Couldn't save that"` ≥ 1 — the error is in the status track, red glyph, in the footer box.
  - `mqa ui | grep -c "Add your first account"` ≥ 1 — still on N2, no navigation.
  - `mqa db "select count(*) as c from accounts"` → **1**.

  Tap **Save Account** again (same bundle, no reload) and assert:
  - `mqa db "select count(*) as c from accounts"` → still **1**. This is the Done-when.
  - `mqa ui | grep -c "Anything else"` ≥ 1 — landed on N3.
  - `mqa db "select count(*) as c from accounts"` once more after the navigation settles, in case a second insert lands late.

  Then revert the repository file, `git diff --stat src/modules/onboarding/repositories/` must be empty, and re-run `npm test -- --ci` before committing.

- [ ] **5.2 — S2: onboarding writes a correct row, and the colour sheet is wired to the right owner.** `mqa reset` → N1 → N2. Fill name `MA008 Bank`, balance `5000`. Tap the colour trigger row (`testID="account-color-trigger"`, `account_color_field.tsx:37`) — the sheet must open, pick a non-default swatch, **Use this colour**, and the trigger row's family label must change. Save. Then:

```
mqa db "select name,type,currency,opening_balance,current_balance,color,credit_limit,minimum_payment,statement_due_day,apr,revolving_balance,sort_order from accounts"
```
  Assert `opening_balance = current_balance = 5000` (business rule 6, on the live path), all five credit columns **NULL and not 0** (the "absent, not zero" preservation), `color` equal to the hex picked in the sheet, `sort_order = 0`.

  One screenshot of N2 before saving, Read and compared against `main`'s: the section labels (D8 row 2), the field error styling with a deliberately blank name (D8 row 3), and the currency row's width (D7). Report any delta rather than assuming parity.

- [ ] **5.3 — S3: Settings still writes a correct row, on a credit card.** Finish onboarding through N3/N4, then Settings → Accounts → Add Account. Create a Credit Card with a limit, a minimum payment, a due day and interest tracking on with an APR. Save, then `mqa db` the same column list: credit columns **populated**, `current_balance = opening_balance`. This is the other half of the Done-when and the regression check on Step 3's shared-component edit. Screenshot the currency row here too, for the D7 comparison against S2's.

- [ ] **5.4 — S4: the add-more re-entry, which is what MA-011 depends on.** From N3, tap **Add another account** (this is the only navigation in the app that sets `isAddingMore`, `more_accounts.hook.ts:52-55`). Create a second account. Assert:
  - `mqa db "select count(*) as c from accounts"` → **2** — the latch reset on the fresh mount, so the second insert is not swallowed.
  - back on N3 (`mqa ui | grep -c "Anything else"`).
  - `mqa db` shows `sort_order` 0 and 1, distinct.
  - then the add-more **back** chevron from a re-entered N2 returns to N3, not to N1.

  The explicit-`false` case is **not** walked: no UI path passes `isAddingMore=false`, so it is only reachable from a deep link and is pinned by unit tests 9 and 12 instead. Saying so here so the omission is a decision, not a gap.

- [ ] **5.5** Record the walk in `## Emulator verification` on this file: the queried values (not screen readings), the screenshots Read, and what could not be checked here — typography weight, shadows, gesture feel and the sheet's spring, all of which stay gate 3's.

---

### Non-goals

- **The redesign.** MA-009 owns the type grid, the message rails, the credit slot, the balance+currency row, and the `Save and continue` CTA label. This task keeps `Strings.o4Cta` (`Save Account`) and every other string on the screen exactly as they are.
- **The `n2SaveError` copy.** It reads "Tap Save and continue to try again" while the button says `Save Account` — a mismatch that arrived with MA-004 and that MA-009 resolves by changing the label. Not fixed here: it is user-facing copy with a CTA rename attached, and fixing it in this task would put the two out of sync in the other direction. Flagged to @sarah.
- **`src/utils/schemas/add_account.schema.ts`.** Onboarding was its last consumer; after Step 2 it has zero. `spec.md:465` says leave it. Left, unimported. A later cleanup can delete it.
- **`runOnboardingTransition`, `resolveOnboardingStep`, `ONBOARDING_STEP_HREF`, `onboarding_transition.state.ts`** — read and reused, not modified. The other three onboarding screens' use of them is untouched except for the `loadAccounts` deletions in D6.
- **`listClassName="w-full"` on `CurrencySelector`** unless S2/S3 shows a narrowing (D7).
- **No new dependency, no native-surface change, no migration, no feature-flag flip.** `mqa needs-build` is expected to say so.

### Risks accepted, with the check that would catch each

| # | Risk | Check |
|---|---|---|
| R1 | Two guards remain on one screen, interlocked rather than unified, and both are set inside an async `onValid`, so a sub-frame double-tap across *different* controls is unguarded | Tests 13 and 14 pin the interlock. The residual window is identical to `main`'s (`add_account.hook.ts:75`), so this is not a regression — but it is not closed either |
| R2 | The `isCurrent()` stale guard is gone from the save path | D4's argument is that the only competing session producer is `onBack`, blocked at source, plus `invalidate()` before the replace. Test 14 and S4. If a future task adds a third async control to N2, this argument expires — say so in that task |
| R3 | `useAccountFormState` is a module singleton shared with Settings; a second mounted host would share `inserted` | Only one add-account host is mounted at a time, and `useInit(reset)` clears on every mount (`use_account_form.hook.ts:60`, pinned by `account_form.hook.test.ts:154`). S4 proves the add-more remount on device |
| R4 | `Tabs.List`'s default width is unverified — `node_modules` is not installed in this checkout, so D7's "`w-full` is a no-op" is an inference from RN's default `alignItems: 'stretch'`, not a reading of HeroUI's source | S2/S3 screenshots, with the fallback decided in advance |
| R5 | `Typography`'s default line height may differ from RN `Text`'s on the section labels, shifting the form vertically by a few points | S2 screenshot compared against `main` |
| R6 | S1's forced failure is a temporary source edit that could be committed | `git diff --stat src/modules/onboarding/repositories/` empty and `npm test -- --ci` re-run after the revert, before the commit (4.2, 5.1) |
| R7 | D6 widens the task from one `loadAccounts` call site to three, touching two screens MA-011 and MA-012 own | `spec.md:502` names all three; each is one line, all inside `src/modules/onboarding/`, and each has a `not.toHaveBeenCalled()` assertion in the suite that already exists for that screen |

---

## Sarah's rulings on the plan (step 4 → 5)

Both of @tariq's escalations resolved without waking the user; neither is a critical trigger.

**D6's widening stands — all three `loadAccounts()` call sites.** Not a scope balloon. The task's own Done-when is plural ("no onboarding route loads the account list itself"), `spec.md:502` names exactly those three, and the change is three lines plus two assertions entirely inside `src/modules/onboarding/`. Handing two of them to MA-011/MA-012 would leave this task's Done-when unmet by its own PR, which is worse than touching two lines in screens those tasks will rewrite anyway.

**The `n2SaveError` copy is not fixed here, and it is not the user's call.** CLAUDE.md's critical trigger 5 excludes error messages explicitly — they stay team-decided. @tariq's reasoning is accepted as written: the sentence names the CTA, MA-009 renames that CTA per `spec.md:81`, and correcting the string here would put the pair out of sync in the other direction for exactly one merge. It stays a Non-goal. **MA-009 must reconcile both halves in the same commit** — recorded here so the pairing is not rediscovered.

R4's unverified `Tabs.List` width is acceptable at plan stage precisely because D7 decides the fallback in advance rather than deferring it to discovery.
