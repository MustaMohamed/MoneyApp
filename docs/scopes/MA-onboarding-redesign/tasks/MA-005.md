---
id: MA-005
scope: MA-onboarding-redesign
milestone: M1
issue: 207
verify: emulator
branch:
pr:
---

# MA-005 — Onboarding resume: route resolver, persist-before-navigate, locked gestures

## Summary

Make the step the app has written down and the screen the user is looking at incapable of disagreeing — on relaunch, on a swipe back, and on a failed write.

## Details

This task was added at step 3. `spec.md` § *Architecture — Async and navigation ownership* specifies four behaviours that no task in the delivered breakdown owned, and two of them were being asserted by later tasks that had no way to satisfy them on their own. They are one concern, they are always reviewed together, and they are the mechanism behind business rule 2 — force-close during onboarding resumes from that step.

**None of this is a redesign.** The four onboarding routes still hold whatever bodies they hold when this lands. What changes is when the step is written, which route the app opens, and whether a gesture can get between the two.

### One — resuming lands on a step that is actually reachable

Today relaunch reads the persisted step and opens the matching route, full stop. It never asks whether that step makes sense given what is in the database. A session that was killed after the step advanced but before the account was written reopens on a screen that says an account was saved, above nothing.

The rule, from the spec: a persisted second step with at least one account resolves forward to the third; a persisted third or fourth step with no accounts resolves back to the second; anything else opens what was persisted. It is a pure function of the persisted step and the account count, and it is tested as one — every combination, including the ones that should pass straight through.

**The account snapshot is already loaded when this decision is made** — startup awaits it, and the router is not mounted until startup resolves. This has been checked; do not re-derive it, and do not add a load to make it true. What the plan *must* confirm is that the resolver cannot run against an empty snapshot in any startup path, including the retry after a startup failure, because a resolver that sees zero accounts sends a returning user back to the account form with their first account already saved. That is the one way this task can make things worse than they are today.

### Two — the step is persisted before the route changes, and the route is replaced

Every step transition writes the destination step first and then navigates, and it replaces the current route rather than stacking one. Today all three transitions push, so the back stack outlives the persisted step and a swipe can put the user on a screen the store no longer agrees with. This is a deliberate behaviour change, named as such in the spec.

**A failed step write is not silent.** If the persist rejects, the navigation does not happen, the user stays where they are with their input intact, the primary action returns to idle, and the failure appears in the footer's status track that MA-004 delivered. The button is the retry. This is defect class 1 in `.claude/rules/review.md` and it is the most repeated defect in this codebase — three of these transitions currently discard the rejection.

### Three — the onboarding stack does not accept gestures

Swipe-back is disabled for the whole onboarding stack. With routes being replaced there is usually nothing to go back to, but "usually" is what makes this the kind of thing that surfaces on someone's device and nowhere else. Back movement inside onboarding happens through the header's back affordance only.

### Four — a save cannot be undone by a route that has already gone

A save belongs to the form session that started it. A completion arriving after that session's route has been replaced must not navigate, must not close a route that replaced it, and must not write a step. Today nothing enforces this; with replace semantics and a slow write, it becomes reachable.

**Done when:** the resolver is a pure function with a test covering every persisted-step-and-account-count combination, including the pass-through cases; force-closing on each of the four steps and relaunching lands on the correct screen on the emulator, verified against what is actually in the database and including the killed-between-step-and-insert case; every step transition persists before navigating and replaces rather than pushes, under test; a forced failure of each step write leaves the user in place with the error in the status track and the action retryable, watched on the emulator at least once; swipe-back does nothing anywhere in onboarding on a real touch; a delayed completion arriving after its route was replaced changes nothing; **and the header's back affordance renders on N3 and N4 and performs the back movement this task defines, consistent with mockup frames E1 and F1.**

> **Step-3 gap, corrected at step 4 (2026-08-07, @tariq).** The final clause above is new. As delivered at step 3 this Done-when never required a back affordance to appear on N3 or N4 — only § *Three*'s prose mentions "the header's back affordance". MA-004 deliberately shipped both screens without one (`onboarding_header.tsx:50-52` renders a `w-9` spacer when `onBack` is absent) because deciding what that button *does* is this task's work; the hand-off was flagged three times inside MA-004 — plan decision 11, step-5 review finding 2 (`MA-004.md:480`), step-7 review (`MA-004.md:583`) — and recorded on issue #207. Mockup frames E1 (`assets/mockup.html:2004`) and F1 (`:2319`) both draw the chevron. Without this clause, MA-005 could be reviewed as complete with both screens silently missing it and nobody at fault.

---

## Plan

### Summary

- **One pure resolver, used in two places.** `resolveOnboardingStep(persistedStep, accountCount)` maps a persisted step plus an account count to the step that is actually reachable. It runs at the root redirect (resume) and again on every forward transition (so a destination can never be a step the data does not support). Twelve-row `test.each`, no router, no store.
- **Every navigation inside onboarding becomes persist-then-`replace`.** The three step transitions (N1→, N2→, N3→) and the three back actions (N2, N3, N4) all go through one runner that resolves the destination, awaits the persist, checks the session is still current, and only then replaces the route. `handleAddAnother` also becomes a `replace`, which is what makes "there is nothing on the stack" true rather than "usually true".
- **Every rejection becomes a rendered message in the status track MA-004 shipped.** Three handlers discard their rejection today (`welcome/index.tsx:31`, `add_account/index.tsx:57`, `more_accounts/index.tsx:33`); N4's is recorded in `useAsync`'s `isError` but rendered nowhere. After this task no onboarding handler can reject, and each one owns a `statusMessage` its shell renders. The CTA is the retry; no second button appears.
- **System back is consumed on N2, N3 and N4; N1 keeps today's exit.** One `BackHandler` subscription in `src/app/(onboarding)/_layout.tsx`, conditional on the active segment. `gestureEnabled: false` is added for iOS, where it is the only lever that exists; on Android the swipe *is* system back and the subscription is the lever. The N1 exemption was ruled at step 5 — see Decision 4.
- **N3 and N4 gain the back chevron the corrected Done-when requires.** N3 back persists N1 and replaces with Welcome; N4 back persists N3 and replaces with More accounts. Both are disabled while a write is in flight.
- **Session identity is a monotonic counter inside each screen's `.state.ts`.** Navigating away bumps it; a completion that lands on a stale session does not navigate, does not clear or set a message, and does not write a step.

### Detail

#### Decision 0 — what the resolver is, and the table it is tested against

`resolveOnboardingStep(step: OnboardingStep, accountCount: number): OnboardingStep`. Pure, total, imports only `@/constants/enums`.

| # | persisted step | accountCount | resolves to | why |
|---|---|---|---|---|
| 1 | N1 | 0 | **N1** | pass-through — the ordinary first launch |
| 2 | N1 | 1 | **N1** | pass-through — reached by N3's back action; rule 2 |
| 3 | N1 | 2 | **N1** | pass-through |
| 4 | N2 | 0 | **N2** | pass-through; business rule 3 — N2 needs ≥1 account before it may be left |
| 5 | N2 | 1 | **N3** | forward — killed after the insert, before the step write |
| 6 | N2 | 2 | **N3** | forward — same, from the add-more path |
| 7 | N3 | 0 | **N2** | back — a success screen above nothing; mockup E3 |
| 8 | N3 | 1 | **N3** | pass-through; business rule 4 — N3 is skippable once N2 wrote one |
| 9 | N3 | 2 | **N3** | pass-through |
| 10 | N4 | 0 | **N2** | back — no accounts, nothing to review |
| 11 | N4 | 1 | **N4** | pass-through |
| 12 | N4 | 2 | **N4** | pass-through |

**Business-rule check, explicitly.** Rule 2 (force-close resumes from that step) is rows 1–4, 8, 9, 11, 12 — eight of twelve pass straight through, which is the rule holding. Rows 5–7 and 10 are the four states rule 2 cannot describe because the persisted step is not reachable from the data; each resolves to the nearest step that is. Rule 3 (N2 requires ≥1 saved account before proceeding) is why row 4 stays and why rows 7 and 10 land on N2 rather than N3. Rule 4 (N3 skippable once N2 wrote one) is rows 8 and 9 — resolution never sends a user *back* into N2 once an account exists. Rule 1 (complete only on the N4 CTA) is upstream of the resolver entirely: `src/app/index.tsx:18` redirects to `/dashboard` on `complete` **before** the resolver is consulted, and `src/app/(onboarding)/_layout.tsx:14-15` repeats that guard for the group. Keep that ordering; inverting it would run the resolver for a user who finished onboarding months ago.

`accountCount` is the non-archived snapshot count, since `AccountRepository.getAll()` is what `loadAccounts` publishes. **Read it as `useAccountStore.getState().accounts.length` at call time inside a transition, and as `useAccountStore(s => s.accounts.length)` only at `src/app/index.tsx`, where the redirect is a render** — see the correction under Decision 2 row 2 for why the difference is not cosmetic.

**Why the resolver runs on forward transitions and not only on resume.** @tariq presents this as MA-010's "N1 Continue returns to N3" falling out for free. It is stronger than that: **this task's own corrected Done-when makes it mandatory.** MA-005 now ships N3's back chevron, which persists N1 and replaces to Welcome (`locked-design-2026-07-23.md:307-309`). A user who takes it has one account saved and is sitting on N1. If N1's Continue were an unresolved `push`/`replace` to N2, it would drop them into the first-account form with a saved account — and the very next tap fails `add_account.schema.ts:30-31` on name uniqueness, in a task that has just deferred the post-save checkpoint to MA-008. Forward resolution is what keeps MA-005's own new affordance from opening a dead end. MA-010 inheriting a passing case is the side effect, not the reason.

#### Decision 1 — the empty-snapshot hazard, traced

**Finding: the resolver cannot observe a pre-load snapshot on any startup path, including the retry after a startup failure.** The trace, which @plan-reviewer should re-walk rather than take on trust:

1. `src/app/_layout.tsx:78-86` renders the root `<Stack>` **only** when `status === 'ready'`. Otherwise it renders `<StartupError>` or `null`. No route mounts — `src/app/index.tsx` included — before that.
2. `status` becomes `'ready'` only at `src/utils/use_layout_init.hook.ts:41` (`resolveReady(generation)`), which is after `await Promise.all([initOnboarding(), loadAccounts(), loadRate()])` at `:38`.
3. `loadAccounts` (`src/modules/accounts/store/account.store.ts:47-61`) publishes `{ accounts, hasLoaded: true }` at `:54` before resolving, and **rethrows** at `:59`. A rethrow rejects the `Promise.all`, so `use_layout_init.hook.ts:45` calls `rejectFatal` and the `<Stack>` never mounts. There is no branch where a failed load resolves ready with an empty array.
4. **The retry path.** `_layout.tsx:57` wires `retry` → `use_layout_init.hook.ts:53-55` → `start()` → `useAppReadyStore.begin()` (`src/store/ready.store.ts:29-33`). `begin()` sets `status: 'initializing'` and bumps `generation` — it does **not** clear `error` (`:31` writes only those two fields). So `_layout.tsx:68`'s `showStartupError = status === 'fatalError' || (status === 'initializing' && error !== null)` stays **true** for the whole retry, the `<Stack>` stays unmounted, and it remounts only when `resolveReady` sets `status: 'ready'` **and** `error: null` (`ready.store.ts:36`) — again strictly after `loadAccounts()` resolved. **This is the path the task singled out, and it is closed by `begin()` not clearing `error`.**

   **@plan-reviewer, round 1 — verified, and it is not an accident.** `__tests__/ready.store.test.ts:57-68` (`keeps the failure context mounted while a retry initializes`) asserts exactly this: `rejectFatal` then `begin()`, then `toMatchObject({ status: 'initializing', error })`. The behaviour is a pinned contract, not an omission, so a future "tidy" of `ready.store.ts:29-33` goes red rather than silently reopening the hazard. That test is the explicit dependency the task asked for. Do not touch `ready.store.ts` or that test in this diff, and if a later task needs to change `begin()`, that test is the one that must be argued with.
5. No other caller. `grep -rn "useAppReadyStore" src/` returns exactly `ready.store.ts` and `use_layout_init.hook.ts`; `begin()`/`reset()` have no other invocation, so there is no third path that can remount the router.
6. No account-store reset. `grep -rn "useAccountStore.getState().reset" src/` returns nothing — nothing empties the snapshot while onboarding is mounted.

**Therefore: do not re-derive the snapshot and do not add a load.** The resolver reads `accounts.length` from the already-published snapshot. It does **not** read `hasLoaded` as a gate — adding one would be a guard against a state proven unreachable, and it would fail open on the one case that matters (a genuine zero-account user would be indistinguishable from an unloaded one).

Residual risk, stated: a future task that mounts an onboarding route outside the ready gate reopens this. The mitigation is the trace above living in this file and step 6.4's test asserting the redirect reads the store rather than a local default.

#### Decision 2 — the three transitions, by file and line

| Transition | Today | Must become |
|---|---|---|
| N1 Continue | `welcome.hook.ts:14-18` — `await setBaseCurrency` → `await setStep(N2)` → `router.push('/(onboarding)/add_account')` (`:17`). Rejection discarded by `void onContinue()` at `welcome/index.tsx:31`. No busy state. | resolve `N2` against `accountCount` → persist currency, then the **resolved** step → `router.replace(href)`. Rejection → `Strings.n1StepSaveError` in the track, selection intact, CTA back to idle. Busy label `Strings.n1CtaBusy`. |
| N2 Save and continue | `add_account.hook.ts:64-87` — `await addAccount(...)` → `await setStep(N3)` (`:84`) → `router.push` (`:85`); add-more branch `backOrReplace` (`:82`). Rejection discarded by `void handleSave()` at `add_account/index.tsx:57`. | after the insert, resolve `N3` against the **post-insert** count → persist → `router.replace`. Add-more branch: no step write, `router.replace('/(onboarding)/more_accounts')`. Any rejection → `Strings.n2SaveError` in the track, form values intact. |

> **@plan-reviewer, round 1 — corrected. "Post-insert" was load-bearing and the runner contract below contradicted it.** `useAddAccount` reads `accounts` at render (`add_account.hook.ts:36`), so the count captured in `onSubmit`'s closure is the **pre-insert** one — `0` on the first-account path. Decision 3's stated order resolves *before* `persist`, so a literal implementation computes `resolveOnboardingStep(N3, 0)` = **N2** (row 4), writes step N2 and replaces to `/(onboarding)/add_account`. N2's CTA would never leave N2 — a hard loop on the app's first-run screen, with a saved account and a form that now fails its own name-uniqueness rule. The count must be read **after** `addAccount` resolves, from `useAccountStore.getState().accounts.length` (`addAccount` awaits its own `loadAccounts()` at `account.store.ts:85`, so the snapshot is current the instant the save promise settles). The runner signature in Decision 3 is amended accordingly: `accountCount` is a thunk, and the resolve moves inside `persist`. This is the one change to @tariq's design, and it is to the parameter shape, not the approach.
| N3 Review setup | `more_accounts.hook.ts:31-34` — `await setStep(N4)` (`:32`) → `router.push('/(onboarding)/ready')` (`:33`). Rejection discarded by `void handleContinue()` at `more_accounts/index.tsx:33`. | resolve `N4` against `accountCount` → persist → `router.replace`. Rejection → `Strings.n3StepSaveError`, list and success chip untouched (they render from the account store, which this path never writes). Busy label `Strings.n3CtaBusy`. |

Two more navigations change, and both are named here rather than left to be found:

| Navigation | Today | Must become | Why it is in this task |
|---|---|---|---|
| N3 → add another | `more_accounts.hook.ts:23-28` — `router.push` with `{ isAddingMore: 'true' }` | `router.replace` with the same params, still **no step write** | It is the only remaining `push` inside the group. Left as a push, the add-more N2's back — which per this task must `replace` to more_accounts — produces a stack of `[more_accounts, more_accounts]`. Replacing here makes onboarding stack depth exactly 1 at all times, which is what § *Three*'s premise assumes. |
| N4 Open my dashboard | `ready.hook.ts:42-45` — `await complete()`, rejection reaches `void handleComplete()` (`ready/index.tsx:35`) and is rendered nowhere | handler catches; `complete.isError` (already set by `use_async.hook.ts:39`) renders `Strings.n4CompleteError` in the track | The corrected Done-when puts a back chevron on N4, so this task already owns `ready.hook.ts`/`ready/index.tsx`, and a back button that is live during the completion write is a new race. Disabling it needs the busy flag; rendering the error alongside is three lines and closes the last swallowed rejection in the group. Costs one string MA-012 will re-word against F9. |

**`void handler()` stays at all four call sites and that is correct.** `.claude/rules/review.md` defect 1 forbids "`void handler()` **swallowing a rejection**". After this task every handler catches its own failure and resolves; none can reject, so `void` discards nothing. Say so in the diff with a one-line comment at each site, because a reviewer scanning for the idiom will otherwise flag it.

#### Decision 3 — the shared runner and where session identity lives

Four new files, one deleted.

- **`src/modules/onboarding/domain/onboarding_route.ts`** (new) — `resolveOnboardingStep`, and `ONBOARDING_STEP_HREF: Record<OnboardingStep, Href>` moved verbatim from `src/app/index.tsx:7-12`. `Href` comes in as `import type`, so the file pulls no expo-router runtime and is importable from a plain `.ts` suite. Precedent for a `domain/` folder in a module: `src/modules/transactions/domain/transaction_amounts.ts`.
- **`src/modules/onboarding/domain/onboarding_transition.ts`** (new) — `runOnboardingTransition`, all dependencies injected so it is pure:

  ```text
  runOnboardingTransition({
    session,                                   // number returned by begin()
    api,                                       // { isCurrent, fail, settle, invalidate }
    navigate,                                  // (href: Href) => void — router.replace
    desiredStep,
    readAccountCount,                          // () => number — NOT a captured value
    persist,                                   // (resolve, isCurrent) => Promise<OnboardingStep>
    errorMessage,
  })
  ```
  Order, and it is the order the test asserts: `const resolved = await persist(resolve, isCurrent)` → on rejection `api.fail(session, errorMessage)` and **return without navigating** → on success `if (resolved === undefined || !api.isCurrent(session)) return;` → `api.invalidate()` → `navigate(ONBOARDING_STEP_HREF[resolved])`.

  **`resolve` is a thunk, not a precomputed step** (`@plan-reviewer, round 1`). `resolve()` is `resolveOnboardingStep(desiredStep, readAccountCount())`, and `readAccountCount` is `() => useAccountStore.getState().accounts.length` — read at call time, never captured at render. Three of the five transitions may call it before their first await, because nothing they do changes the count. **N2's save must call it after `addAccount` resolves**, which is why it is a thunk at all: see the correction under Decision 2 row 2.

  `persist` receives `isCurrent` because N2's persist contains **two** awaits and the guard has to sit between them, and it returns the step it actually wrote so the runner knows where to navigate. N2's body is exactly:

  ```text
  await addAccount(...)                        // count changes here
  if (!isCurrent()) return undefined;          // stale session: account stays, step is not written
  const resolved = resolve();                  // reads the POST-insert count → N3
  await setStep(resolved);
  return resolved;
  ```

  Returning `undefined` is the concrete meaning of "must not write a step" — a save whose route has gone leaves the account on disk and the step untouched, and the resolver's row 5 heals it on the next launch. The three back actions and N1's Continue have a single-await persist and return their `resolve()` directly; the add-more save branch writes no step and returns `OnboardingStep.N3` unchanged (its persisted step never moved).

- **`src/modules/onboarding/screens/onboarding/onboarding_transition.state.ts`** (new) — `createOnboardingTransitionState()`, shaped exactly like `src/modules/currency/screens/currency/currency.state.ts:27-38` (module factory + `createMoneyAppSelectors` + `create`). Reactive fields `{ statusMessage: string; busy: boolean }`; a closure `let session = 0` in the same position as `initGeneration` in `onboarding.store.ts:30` and `loadRequestId` in `account.store.ts:40`. Actions: `begin(): number | null` (returns `null` when `busy` — **this is the CTA re-entry guard, in one place**; otherwise `++session`, `set({ busy: true, statusMessage: '' })`), `isCurrent(s)`, `fail(s, msg)`, `settle(s)`, `invalidate()` (`++session`, `set(INITIAL_STATE)`), `reset()`.
- **`welcome/welcome.state.ts`, `add_account/add_account.state.ts`, `more_accounts/more_accounts.state.ts`** (new, one line each) — one instance per screen. **Not one shared instance**: a single slot written by three screens is audit L27 (`.claude/rules/state.md`, single-owner lookups), and during a `replace` the outgoing and incoming routes are briefly both mounted.
- **Deleted: `src/utils/onboarding_nav.ts` + `__tests__/onboarding_nav.test.ts`, and its entry in `jest.config.js` `collectCoverageFrom`.** `backOrReplace` has exactly two call sites (`add_account.hook.ts:82,90`), both rewritten here, and it is precisely the in-memory-history dependency `locked-design-2026-07-23.md:432` forbids. Leaving it in the tree invites re-adoption. Coverage is not in the pre-push chain, so this cannot break CI; `npm run test:coverage` sheds a file and its test together.

`statusMessage` is `''` when idle and is passed straight to `OnboardingShell`'s `statusMessage?: string` — `resolveStatusTrack` (`onboarding_shell.geometry.ts:79-93`) treats `''` as *no failure* and keeps the footnote, with `tone: 'idle'`, `accessibilityLiveRegion: 'polite'`. No `undefined` juggling, and no new prop: **the shell has `statusMessage` and no `statusTone`** — the tone is derived from message presence, so a failure is the only thing that can turn the track red. `spec.md:518`'s `statusTone` was never built; plan against the shipped signature (`onboarding_shell/index.tsx:11-22`).

> **@plan-reviewer, round 1 — verified, and the spec has been fixed rather than worked around.** The shipped props are `{ step, title?, onBack?, footnote, statusMessage?, cta, children }` (`onboarding_shell/index.tsx:11-22`); there is no `statusTone`, and `footnote` — which `spec.md:518` omitted entirely — is **required**, because the track is never empty. `resolveStatusTrack` derives the tone from message presence (`onboarding_shell.geometry.ts:79-93`), which is a stronger contract than a `statusTone` prop: a caller cannot paint the track red without a message, and cannot show a message without turning it red. Leaving the spec wrong would let MA-009/MA-011/MA-012 plan against a prop that does not exist, so `spec.md:518` has been corrected in place with a dated note. Nothing in this plan changes as a result.

Stale message on re-entry: `invalidate()` runs immediately before every `navigate`, so the store is clean when the screen next mounts, with no one-frame flash. `useInit(reset)` on mount as well — belt and braces for an entry path that does not go through the runner. Both are one line; keep both, and say why in the diff.

#### Decision 4 — locking the stack, and the back affordance

**Two levers, because one platform each.**

- `src/app/(onboarding)/_layout.tsx` `screenOptions` gains `gestureEnabled: false`. Verified iOS-only: `react-native-screens/src/types.tsx:203-208` marks it `@platform ios`, and nothing under `react-native-screens/src/` reads it on Android. It is not decoration — it is the whole iOS answer.
- **Android: one `BackHandler` subscription in the same layout.** `app.json` sets `android.predictiveBackGestureEnabled: false` (verified), so the edge swipe *is* legacy system back; there is no separate app-level swipe to disable. Returning `true` from the handler consumes it. Dispatch walks `_backPressSubscriptions` in reverse and returns on the first `true` (`react-native/Libraries/Utilities/BackHandler.android.js:30-34`), falling through to `exitApp()` (`:36`) when every handler returns `false`. Subscribe in a `useEffect` **before** the `if (complete) return <Redirect …/>` early return, and return `subscription.remove()`.

  Read `useBackButton` carefully, because it changes what "do nothing" means: with every route replaced the onboarding stack is depth 1, `canGoBack()` is `false`, that handler returns `false`, and `BackHandler` falls through to `exitApp()` (`BackHandler.android.js:36`). **So without this subscription, `replace` would silently turn back on N2/N3/N4 from "go to the previous step" into "quit the app".** Consuming it is what prevents that, not a nicety.

  > **@plan-reviewer, round 1 — the conclusion holds but the stated reason was backwards.** The claim was "ours is registered after expo-router's container-level one, so ours runs first". By tree position it is the opposite: `useBackButton` is called from `expo-router/build/fork/NavigationContainer.js:40`, an **ancestor** of the route layouts, and React flushes effects child-first — so on a single commit the container would register *last* and therefore run *first* under LIFO. What actually puts ours last in the array is commit sequencing: `src/app/_layout.tsx:78` mounts the root `<Stack>` only when `status === 'ready'`, and `useAppInit` cannot reach `'ready'` on the first commit (`getDb` and `runMigrations` are awaited), so `(onboarding)/_layout.tsx` always mounts in a **later** commit than the container. Ours is genuinely last, and genuinely first to run.
  >
  > **And it does not matter either way, which is the load-bearing part.** At stack depth 1 expo-router's handler returns `false` and the loop continues to ours regardless of order. The only ordering-sensitive case is depth > 1, which decision 2's `handleAddAnother` → `replace` is precisely what eliminates. Keep both facts in the diff comment; a reader who checks only the tree position will otherwise "correct" this into a bug report.

- **N1 is exempt: back on the welcome screen still exits the app.** `@plan-reviewer's ruling, round 1 — @tariq deferred this to gate 3 and offered a one-line exemption; the exemption is the right default and is taken now rather than shipped for the user to discover.* The reason to consume back on N2/N3/N4 is that `replace` turns "back" into "quit" — a regression **this task introduces**. N1 has no such regression: back on N1 exits today, that is correct Android behaviour on a launch screen, and business rule 2 makes exiting safe (the app resumes on N1). Consuming it would make the first screen a new user ever sees the one screen they cannot leave, and § *Three*'s premise is that a gesture cannot **desynchronise the persisted step** — exiting the app desynchronises nothing.

  So the handler is conditional on the active route, not unconditional:

  ```text
  const segments = useSegments();
  useEffect(() => {
    const sub = BackHandler.addEventListener('hardwareBackPress', () =>
      segments[segments.length - 1] !== 'welcome',
    );
    return () => sub.remove();
  }, [segments]);
  ```

  `useSegments` rather than `useOnboardingStore.currentStep`: with persist-before-navigate the two agree on four of the five mounted states, but the add-more N2 deliberately holds a persisted step of N3 while the route is `add_account`, and route identity is what this decision is about. Re-subscribing on segment change is fine — `remove()` runs first, so the array never holds two of ours.

  Still record it for gate 3: the user should meet "back on N2/N3/N4 no longer leaves onboarding" deliberately, and if @marcus wants N1 consumed as well it is the same one line inverted.

  Risk, and how it is settled: if `react-native-screens`' native stack were to consume back before JS, the subscription would never fire. `grep` says it does not — `BackHandler` appears in `react-native-screens/src/utils.ts` only inside `executeNativeBackPress`, used for the search-bar surface. This is nonetheless settled empirically by emulator step E.4 (`mqa key 4` on each route), and the fallback if it fails is a per-screen `navigation.addListener('beforeRemove', e => e.preventDefault())`. Do not reach for the fallback without recording why.

- **The back affordance.** `OnboardingHeader` (`onboarding_header.tsx:33-52`) already renders the chevron whenever `title !== undefined && onBack` and the `w-9` spacer otherwise, with `hitSlop={8}`, `Size.iconBack`, `Radius.sm` and `Strings.goBackAccessibility` — no header change is needed, only the prop. N1 passes no `title`, so it takes the brand branch and **cannot** grow a back button; that matches `spec.md:550` open question 3 (N1 has no back).

  | Screen | Back destination | Step written | Source |
  |---|---|---|---|
  | N2, first-account mode | `/(onboarding)/welcome` | **N1** | `locked-design-2026-07-23.md:307-309` |
  | N2, add-more mode | `/(onboarding)/more_accounts` | **none** — persisted step stays N3 | `locked-design-2026-07-23.md:310-312` |
  | N3 | `/(onboarding)/welcome` | **N1** | `locked-design-2026-07-23.md:307-309`, mockup E1 |
  | N4 | `/(onboarding)/more_accounts` | **N3** | symmetry with N3; mockup F1 draws the chevron and nothing specifies its target. **@tariq's call, recorded here** — the alternative (back to N1) skips a step the user just completed and would need its own resolver row. |

  All four go through `runOnboardingTransition` with the same persist-then-replace ordering and the same error handling: a back action whose step write fails stays put and shows the message. All four are `isDisabled` while `busy` — `spec.md:81` ("Back disabled during the write") generalised to every route, which is also what keeps the stale-completion window shut from the UI side.

  **Which "busy" each control reads, so @dev does not invent a third** (`@plan-reviewer, round 1`). N2 already has one: the CTA is `isDisabled={isSubmitting} isLoading={isSubmitting}` from RHF (`add_account/index.tsx:59-60`), and that stays. N4 already has one: `completing` from `useAsync` (`ready/index.tsx:37-39`), and that stays. The new `busy` in `onboarding_transition.state.ts` is the source for **N1's and N3's CTAs** (both new) and for **all four back chevrons**. Two flags on N2 and N4 is acceptable because they are set by the same tap through the same runner and clear together; three would not be, so do not add a `busy` read to a CTA that already has one.

  The N4 back resolves `N3` against `accountCount`, so a hypothetical zero-account N4 goes to N2 rather than to a broken N3. Costs nothing; it is the same call.

#### Decision 5 — copy

Add to `src/constants/strings.ts` beside the existing `n1Footnote`…`n4CtaBusy` block (`strings.ts:72-77`). Error messages and busy labels are team-decided (CLAUDE.md § *Critical triggers*, item 5's carve-out), so this is not a trigger.

| Key | Value | Source |
|---|---|---|
| `n1CtaBusy` | `Saving…` | mockup B3, `mockup.html:1196` |
| `n3CtaBusy` | `Opening…` | mockup E4, `mockup.html:2146` |
| `n1StepSaveError` | `Couldn't save your choice. Check you have free storage, then tap Continue again.` | mockup B4, `mockup.html:1253` — used verbatim |
| `n2SaveError` | `Couldn't save that. Tap Save and continue to try again.` | **deliberately weaker than the mockup.** C4 (`:1729`) says "Nothing was written" and A3 (`:979`) says "Your account is safe" — those are the two halves of the post-save checkpoint, which is **MA-008's**. Until it exists, MA-005 must not promise either. MA-009 owns C4's exact wording. |
| `n3StepSaveError` | `Couldn't move on. Your accounts are saved — tap Review setup to try again.` | mockup E5 (`:2189`) with the count removed. E5 reads "All three accounts are saved"; the count-and-plural form is **MA-011's**, whose Done-when requires matching E5. |
| `n4CompleteError` | `Couldn't finish setup. Your accounts are saved — tap Open my dashboard to try again.` | mockup F9 (`:2731`) with the count removed; **MA-012** owns matching F9. |

#### Steps

1. **`src/modules/onboarding/domain/onboarding_route.ts`** — `resolveOnboardingStep` + `ONBOARDING_STEP_HREF`, the latter moved out of `src/app/index.tsx:7-12`.
   Test: **`__tests__/onboarding_route.test.ts`** (new). `test.each` over all twelve rows of Decision 0's table; plus a parity assertion that `Object.keys(ONBOARDING_STEP_HREF)` equals `Object.values(OnboardingStep)`, so a fifth step cannot be added without a route.

2. **`src/modules/onboarding/domain/onboarding_transition.ts`** — `runOnboardingTransition` per Decision 3.
   **`src/modules/onboarding/screens/onboarding/onboarding_transition.state.ts`** — `createOnboardingTransitionState()`.
   Test: **`__tests__/onboarding_transition.test.ts`** (new), against a real store instance from the factory and a `jest.fn()` navigate:
   - persist is called before navigate — assert on a shared `calls: string[]` order array, not on two separate `toHaveBeenCalled`s;
   - navigate receives the **resolved** href (`desiredStep: N2`, `accountCount: 1` → `/(onboarding)/more_accounts`);
   - a rejecting persist → `navigate` not called, `statusMessage` set to the passed message, `busy` false;
   - `begin()` returns `null` while `busy` → a second concurrent call performs no persist and no navigate (re-entry guard);
   - `invalidate()` between the persist resolving and the runner continuing → no navigate, `statusMessage` still `''`, `busy` false (stale-completion guard);
   - a two-await persist whose session is invalidated between the awaits → the second write is never called;
   - **`readAccountCount` is called after the first await, not before** — a `readAccountCount` returning `0` on its first call and `1` on its second, with a `desiredStep` of `N3`, must navigate to `/(onboarding)/more_accounts`. This is the regression test for the pre-insert-count loop corrected under Decision 2 row 2; it fails against a runner that resolves before `persist`.

3. **`src/app/index.tsx`** — read `accountCount` from `useAccountStore`, keep the `complete → /dashboard` branch first (`:18`), then `<Redirect href={ONBOARDING_STEP_HREF[resolveOnboardingStep(currentStep, accountCount)]} />`. The local `STEP_HREF` map is deleted, not duplicated.

4. **`welcome.state.ts` / `welcome.hook.ts` / `welcome/index.tsx`** — Decision 2 row 1. The hook returns `{ state: { selected, statusMessage, busy }, setSelected, onContinue }`, keeping the existing `useState<Currency>` for the selection (screen-local form state, unchanged by this task). `index.tsx` passes `statusMessage` to the shell and `isLoading`/`isDisabled`/`loadingLabel` to the CTA.
   Test: **`__tests__/screens/onboarding_welcome.hook.test.ts`** (modify — it asserts `mockPush` at `:79` and must move to `replace`). Add: currency-write rejection → no navigate, message set; step-write rejection → same; `accountCount: 1` → replaces to `/(onboarding)/more_accounts` (this is the edge `spec.md:550` open question 3 and MA-010 both point here for).

5. **`add_account.state.ts` / `add_account.hook.ts` / `add_account/index.tsx`** — Decision 2 row 2 and the N2 back rows of Decision 4. `onSubmit` keeps its current mapping body untouched (`parseFloat`, the credit `null` rules and `sort_order` are MA-007/MA-009's); only the tail from `:81` changes. `onBack` (`:89-90`) becomes a runner call. `backOrReplace` import removed.
   Test: **`__tests__/screens/onboarding_add_account.hook.test.ts`** (modify — it mocks `backOrReplace` at `:13,29`, which is being deleted). Add: insert rejection → no step write, no navigate, message set; step-write rejection after a resolved insert → no navigate, message set, `addAccount` called exactly once; back in first-account mode writes N1 and replaces to welcome; back in add-more mode writes **no** step and replaces to more_accounts.

6. **`more_accounts.state.ts` / `more_accounts.hook.ts` / `more_accounts/index.tsx`** — Decision 2 row 3, `handleAddAnother` → `replace`, and the N3 back row.
   Test: **`__tests__/screens/onboarding_more_accounts.hook.test.ts`** (modify — `mockPush` at `:90` and `:98`). Add: step-write rejection → no navigate, message set, `accounts` untouched; `handleAddAnother` replaces with `{ isAddingMore: 'true' }` and writes no step; back writes N1 and replaces to welcome.

7. **`ready.hook.ts` / `ready/index.tsx`** — the N4 back row of Decision 4 and the completion-failure row of Decision 2. `handleComplete` catches so it cannot reject; `statusMessage` derives from `complete.isError` (`use_async.hook.ts:39` sets it, `:23` clears it on the next attempt — that is exactly retry semantics, so no new state). `useAsync` stays; removing it would orphan `src/utils/use_async.hook.ts`, whose only consumer is `ready.hook.ts:14`, and that deletion belongs with MA-012's rewrite.
   Test: **`__tests__/screens/onboarding_ready.hook.test.ts`** (modify). Add: a rejecting `completeOnboarding` leaves `handleComplete` resolved, sets the message, leaves `rows` unchanged; `onBack` writes N3 and replaces to more_accounts; `onBack` is inert while completing.

8. **`src/app/(onboarding)/_layout.tsx`** — `gestureEnabled: false` in `screenOptions`, and the `BackHandler` effect above the `complete` early return. Leave `animation: 'fade'` and `animationTypeForReplace: 'pop'` alone: with a cross-fade the replace direction is not visually distinguishable, so `animationTypeForReplace` is inert here — confirm that on the emulator rather than changing it blind.

9. **`src/constants/strings.ts`** — the six keys in Decision 5.

10. **Deletions** — `src/utils/onboarding_nav.ts`, `__tests__/onboarding_nav.test.ts`, and the `'src/utils/onboarding_nav.ts'` line in `jest.config.js` `collectCoverageFrom`.

#### What the tests can and cannot reach

**Reachable in `__tests__/**/*.ts`, logic-only, per `.claude/rules/tests.md`:** the whole resolver table; transition ordering (persist-before-navigate, asserted on a call-order array); the resolved-not-desired destination; every rejection path setting a message and suppressing the navigate; the re-entry guard; the stale-session guard including the between-awaits case; back destinations and which of them write a step. `renderHook` from `@testing-library/react-native` inside a `.ts` file is already the house pattern for these four hook suites (`onboarding_welcome.hook.test.ts:1`), so no new `.tsx` render suite is added and the "keep the files, don't add to them" policy is respected.

**Not reachable, and honestly assigned to the emulator/gate 3:** that `BackHandler` actually intercepts the platform event; that `gestureEnabled: false` reaches the iOS recogniser; that the fade transition reads as forward rather than backward after `push`→`replace`; that the status track's error state renders at the shipped geometry; that the app resumes on the right screen after a real `force-stop` with real SecureStore.

**Jest pins `Dimensions` at 750pt** (`jest-expo` preset), so `responsive.ts:17` computes `SCALE = clamp(750/390, 0.85, 1.15) = 1.15` in every suite. No test in this task asserts a scaled value and none may claim multi-scale coverage; this task adds no geometry.

#### Emulator verification — `verify: emulator`

Worktree setup per the `emulator-verify` skill: real `npm install`, `npx expo prebuild --platform android`, `./gradlew assembleDebug`, and a private Metro port (`MQA_PORT=8082`). `M=.claude/skills/emulator-verify/mqa.sh`. Run this **before** the CI-parity chain, which regenerates `android/` and takes the APK with it.

**Read this first: the persisted step is not in SQLite.** `OnboardingRepository.setStep` writes only `SecureStore` (`onboarding.repository.ts:28-30`, key `onboarding_step`), so `mqa db` cannot see it. Two consequences: the observable proxy for "which step is the app on" is the progress-rail label — `Step N of 4` from `resolveProgressRail` (`onboarding_shell.geometry.ts:50-58`), read with `$M ui`; and forcing a particular persisted step requires a temporary JS edit, which Metro fast-refresh applies with **no Gradle rebuild**. Every temporary edit below must be reverted before `git add`; `git status` must be clean of them at step 6's commit.

Settling queries:

```bash
$M db "select id, name, type, currency, opening_balance, current_balance, sort_order from accounts order by sort_order"
$M db "select key, value from app_settings where key in ('onboarding_complete','base_currency')"
```

**A · Resume from each of the four steps (Done-when clause 2, business rule 2).** From `$M reset`, walk forward one step at a time. At each of N1, N2, N3, N4: `adb shell am force-stop com.moneyapp.app`, `$M launch`, then `$M ui` and screenshot. Expect the same `Step N of 4` you left, and for N3/N4 expect the account list/summary to match the `accounts` query. Four force-close/relaunch cycles, four screenshots.

**B · Killed between the step and the insert (Done-when clause 2, resolver row 5).** Temporarily edit `onboarding.repository.ts:28-30` `setStep` to `throw new Error('forced')`. Save (fast refresh). On N2, fill the form and tap **Save and continue**. Expect: the insert lands, the CTA returns to idle, `Strings.n2SaveError` in the status track, still on `Step 2 of 4`. Confirm with the `accounts` query — exactly one row. Revert the edit, `force-stop`, `$M launch`. **Expect `Step 3 of 4`** — the resolver moving N2 forward against one account. Screenshot both states.

**C · Forced failure of each step write (Done-when clause 4).** One temporary throw at a time, fast-refreshed, reverted after each.

> **@plan-reviewer, round 1 — ordering, or three of these four cannot be reached.** Every recipe below throws from the same write the app needs to *arrive* at the screen under test. **Navigate to the target screen first, then apply the edit** — fast refresh does not navigate, so the app stays put while the edit lands. Applying the `setStep` throw before walking to N3 leaves you stuck on N2; the same trap catches scenario B. When a `$M ui` after an edit shows an unexpected screen, the module re-evaluation remounted the route — walk back to it and re-apply rather than recording a false failure.
- `onboarding.repository.ts:32-35` `setBaseCurrency` → N1 Continue. Expect `Strings.n1StepSaveError` in the track, the currency selection unchanged, CTA idle, still `Step 1 of 4`. Revert; tap Continue again; expect it to succeed — the CTA is the retry and no second button appeared.
- `setStep` → N3 **Review setup**. Expect `Strings.n3StepSaveError`, **the account list and the success chip still on screen** (`spec.md:99`, mockup E5), `Step 3 of 4`.
- `OnboardingRepository.complete` (`:37-40`) → N4 **Open my dashboard**. Expect `Strings.n4CompleteError`, the summary intact, `Step 4 of 4`, and `app_settings.onboarding_complete` still absent/not `'true'`.
- `AccountRepository.add` → N2. Expect `Strings.n2SaveError`, form values intact, and **zero** rows from the `accounts` query.

**D · Resolver's backward rows (rows 7 and 10), which need a forced input.** `$M reset`, `$M launch` (0 accounts). Temporarily edit `OnboardingRepository.load` (`:42-54`) to return `step: OnboardingStep.N3`; fast refresh; `force-stop`; `$M launch`. **Expect `Step 2 of 4`** — the E3 guard, satisfied at the root redirect before N3 can paint. Repeat with `N4`. Revert.

**E · Back and gestures (Done-when clauses 5 and 7).**
1. N3 back chevron → expect `Step 1 of 4` with the currency selection preserved; then Continue → **expect `Step 3 of 4`, not N2** (the resolver on the forward transition).
2. N4 back chevron → expect `Step 3 of 4`.
3. N3 → *Add another account* → N2 back chevron → expect `Step 3 of 4` with no step written and the list unchanged. **Also watch the list itself**: N3 is now remounted rather than revealed, so every row replays its staggered `FadeInRight` (behaviour change 8). Screenshot or describe it; it is MA-011's to settle, and this run is where it first becomes visible.
4. `$M key 4` (KEYCODE_BACK) on **N2, N3, N4 and the add-more N2**. Expect `$M ui` to return the identical screen each time and the app still foregrounded. **KEYCODE_BACK is the honest Android proxy for swipe-back**: `app.json` sets `android.predictiveBackGestureEnabled: false`, so the edge swipe dispatches the same event. Gesture *feel* and the iOS interactive pop are gate 3's, and this task's `gestureEnabled: false` is unverifiable on this emulator — say so in the report rather than implying otherwise.
5. **N1 is the opposite assertion, and it must be run.** `$M key 4` on N1 → expect the app to **leave the foreground** (`adb shell dumpsys activity activities | grep mResumedActivity` no longer names `com.moneyapp.app`, or `$M ui` returns the launcher). Then `$M launch` → expect `Step 1 of 4` with the currency selection as persisted. This is the exemption ruled at step 5 (Decision 4); a run that consumes back on N1 is a failure, not a pass. Record both halves for gate 3 alongside the N2/N3/N4 result, so the user sees the whole back story at once.

**F · Delayed completion (Done-when clause 6).**

> **@plan-reviewer, round 1 — this recipe did not execute as written.** Removing `isDisabled` on N2's back chevron is not enough: the back action goes through `runOnboardingTransition`, whose `begin()` returns `null` while `busy` (Decision 3), and the in-flight save set `busy: true`. The back tap would be inert, `$M ui` would still read `Step 2 of 4`, and the run would be recorded as a pass having exercised nothing. **In the shipped build this window is closed from the UI on every route** — back is disabled while busy, the CTA is disabled while busy, and system back is consumed on N2/N3/N4 — so the stale-completion guard is defence-in-depth with no reachable user path. It is verified by test (Decision 3's `invalidate()`-between-awaits case); scenario F verifies that the guard *works*, which needs the guard's own gate temporarily removed.

**Three** temporary edits, applied together while sitting on N2: a `await new Promise(r => setTimeout(r, 6000))` at the top of `AccountRepository.add`; remove the `isDisabled` on N2's back chevron; and make N2's back bypass the busy gate — call `invalidate()` and `router.replace('/(onboarding)/welcome')` directly instead of going through the runner. Fast refresh. Tap **Save and continue**, then tap the back chevron within the 6 s. Expect: the app lands on `Step 1 of 4`; when the save completes it does **not** jump to N3, does **not** replace the Welcome route, and no message appears; `$M ui` still reads `Step 1 of 4`. Then `$M db "select count(*) as n from accounts"` → **1**, and after `force-stop` + `$M launch` the resolver opens `Step 3 of 4` (row 5 again — the account exists, the step was never written). Revert all three edits and confirm with `git status` that nothing survives.

The bypass is the whole point of the scenario and it is also the thing that must not ship. State in the emulator report that the third edit was made and reverted, and quote the `git diff --stat` that shows it gone.

**G · Re-entry (`spec.md:492`).** With the 6 s delay still injected but F's other two edits **reverted** (back chevron restored, runner bypass removed), tap **Save and continue** five times. Expect the CTA to read `Saving…` with a spinner, be non-interactive, and `select count(*) from accounts` to be exactly 1. Note which gate you are actually observing: the visible one is `isDisabled={isSubmitting}` on the button (`add_account/index.tsx:59`), which pre-dates this task; `begin()` returning `null` is the second gate underneath it and is reachable only in the unit test. Revert the delay.

**H · The happy path, end to end.** `$M reset`, walk N1 → N2 → N3 → *Add another* → N3 → N4 → dashboard. Confirm the transitions cross-fade forward and none reads as a backward pop; confirm `onboarding_complete = 'true'` and `base_currency` in `app_settings`, and that back from the dashboard does not return into onboarding.

#### Verification command

```bash
npm test -- --ci --runTestsByPath \
  __tests__/onboarding_route.test.ts \
  __tests__/onboarding_transition.test.ts \
  __tests__/screens/onboarding_welcome.hook.test.ts \
  __tests__/screens/onboarding_add_account.hook.test.ts \
  __tests__/screens/onboarding_more_accounts.hook.test.ts \
  __tests__/screens/onboarding_ready.hook.test.ts
```

then, after the emulator run and before pushing, the full pre-push CI parity chain from CLAUDE.md § *Commands*, unmodified.

#### Non-goals — in this task's files, deliberately untouched

| Left alone | Owner | Why not here |
|---|---|---|
| `isAddingMore` truthy check (`add_account.hook.ts:81`) and the lying `boolean` type on `OnboardingStackParams` (`(onboarding)/_layout.tsx:9`) | **MA-008** | Its Done-when requires "a test covers the explicit-`false` case that is broken today". Fixing it here makes that clause unverifiable. The back and save branches written in this task read the flag exactly as it reads today. |
| The post-save checkpoint — retry after a failed step write must not re-insert | **MA-008** | Consequence to state plainly: after MA-005, a step-write failure on N2 leaves the account on disk and a retry re-runs the whole submit, which then fails the name-uniqueness rule. That is what happens **today** as well (`add_account/index.tsx:57` swallows the rejection), minus the silence — this task strictly improves it and does not close it. **Two details @plan-reviewer added at round 1, because "recoverable" and "dead end" are different bug reports.** (1) The retry's failure surfaces as a **field error under the name input** — `Strings.errNameDuplicate` from `add_account.schema.ts:30-31`, since `createAddAccountSchema(accounts)` re-memoises on the now-larger snapshot — **not** in the status track. So the user sees a duplicate-name complaint about a name they typed once, which is confusing but is at least pointed at. (2) The user is **not stuck**: N2's back writes N1 and replaces to Welcome, and Welcome's Continue then resolves `N2 + 1 account` → N3 (row 5, forward resolution). The escape exists only because this task resolves forward destinations too; without that decision, this deferral really would be a hole. Say both in the emulator report at scenario B. |
| `useInit(loadAccounts)` at `add_account.hook.ts:42` and `ready.hook.ts:18`, `useFocusEffect` at `more_accounts.hook.ts:17-21` | **MA-008** ("no onboarding route loads the account list itself") | Focus churn, defect class 2. None of the three can affect the resolver, which runs at `src/app/index.tsx` before any onboarding route mounts. |
| `parseFloat` in the submit mapping (`add_account.hook.ts:70,74-79`) | **MA-007** | Silent 1000× corruption; a much bigger fix with its own case table. |
| `computeTotalBalance` / the N4 number / the seven states | **MA-012** | |
| `ACCOUNT_COLORS` (`add_account.hook.ts:18-31`) | **MA-006** | |
| Any screen body, frame copy or motion | **MA-009 – MA-012** | This task changes when the step is written, which route opens, and whether a gesture can get between them. Nothing else. |

#### Behaviour changes, all of them

Named because @impl-reviewer will find them in the diff either way.

1. **`push` → `replace` on the three step transitions.** Intended and named in `spec.md:488`.
2. **`push` → `replace` on *add another account*** (`more_accounts.hook.ts:24`). Beyond the three the task names — justified in Decision 2; it is what makes onboarding's stack depth exactly 1.
3. **Back actions stop consulting history.** `backOrReplace` deleted; all four back paths persist their stated destination and replace. `locked-design-2026-07-23.md:432`.
4. **System back is consumed on N2, N3 and N4 — and *not* on N1.** On N2/N3/N4 this preserves today's "back goes somewhere sensible" that `replace` would otherwise turn into "back quits the app" (Decision 4). **N1 keeps today's exit-on-back**, so this task ships no delta a user meets on the first screen. Ruled at step 5; still shown at gate 3, where inverting it is one line.
5. **N1 and N3 CTAs gain a busy state** (`Saving…` / `Opening…`) — implied by "the primary action returns to idle". MA-004 hands N1's over explicitly at `MA-004.md:389`; N3's is not named there (`:391` leaves the N3 CTA "unchanged") and follows from this task's own Done-when clause 4, not from a hand-off.
6. **N3 and N4 gain a back chevron** — the corrected Done-when.
7. **N4's completion failure becomes visible** — see Decision 2's second table for why it is here and not in MA-012.
8. **N3's row entrance animations replay on every return from add-more.** `@plan-reviewer, round 1 — found in the diff's second-order effects and named here rather than left for @impl-reviewer.* `handleAddAnother` → `replace` **unmounts** N3, so `initialCountRef` (`more_accounts.hook.ts:14-15`) re-initialises to the new `accounts.length` on remount and `index < initialCount` is true for every row. `rowEntering` (`more_accounts.anim.ts:14-15`) is **not** gated by `useFirstMountEntering` — unlike `checkEntering`/`headlineEntering`/`subtitleEntering`, which correctly go `undefined` on remount because `seen` is module-scope (`use_first_mount_entering.hook.ts:3`). Net effect: today only the newly added row animates, un-staggered; after this task the whole list re-staggers each time. `initialCount` becomes dead logic. **Do not fix it here** — N3's motion budget is MA-011's (`tasks.md` step-3 note, "Mount animation and reduce-motion added to MA-011 and MA-012") and E1–E5 are its frames. Observe it on the emulator (step E.3), record what it looks like, and leave `initialCount` in place so MA-011 inherits a visible symptom rather than a deleted ref.

#### Risks

| Risk | Mitigation |
|---|---|
| `BackHandler` does not fire because something consumes back first. | Verified by reading (`BackHandler.android.js:30-34` reverse-order dispatch, `:36` `exitApp`; expo-router's own handler is JS at `fork/useBackButton.native.js`, registered from `fork/NavigationContainer.js:40`; `react-native-screens` touches `BackHandler` only in `executeNativeBackPress` at `src/utils.ts:13`). Ordering re-derived at step 5 — see the correction in Decision 4; it holds, and at depth 1 it is not ordering-sensitive at all. Settled empirically by emulator step E.4. Fallback recorded in Decision 4. |
| The resolver runs against an empty snapshot. | Traced closed in Decision 1, including the retry. The load-bearing line is `ready.store.ts:29-33` not clearing `error`, and it is pinned by `__tests__/ready.store.test.ts:57-68` — a tidy-up goes red rather than silently reopening it. |
| The resolver runs against a **stale** snapshot on N2's save and loops the user back into N2. | The real failure mode, found at step 5. Closed by making `accountCount` a thunk read after `addAccount` resolves (Decision 2 row 2, Decision 3) and by the runner test that fails against a resolve-before-persist implementation (step 2). |
| Four rewritten navigation paths on the live first-run screen set — a white screen here is a user who never reaches the dashboard. | `verify: emulator`, eight scenario groups covering resume, both failure directions and the add-more round trip; gate 3 unchanged. |
| `replace` makes the fade transition read as a backward pop. | `animationTypeForReplace: 'pop'` is left as-is and checked on the emulator (step H) rather than changed on a guess. |
| Three near-identical `.state.ts` singletons drift. | One factory, one test; the screens hold a one-line instantiation each. Same shape as `currency.state.ts:27-38`. |
| Deleting `src/utils/onboarding_nav.ts` breaks something unseen. | Two call sites, both in this diff; `grep -rn "backOrReplace" src/ __tests__/` is the whole inventory and is reproduced in Decision 3. Coverage is not in the pre-push chain. |

#### Critical triggers

**None fires.** No new dependency (`BackHandler` and `useSegments` are React Native / expo-router core, both already in the tree), no native code or `app.json` change, no migration, no schema change, no auth/secure-store surface change — `SecureStoreKeys.OnboardingStep` is written by the same `setStep` as today, just at a different moment. Copy added is error messages and busy labels, explicitly carved out of trigger 5.

**@plan-reviewer, round 1 — re-checked trigger 7 (auth / secure store / data-loss surface) specifically, and it does not fire.** No key is added, renamed or removed (`src/constants/secure_store_keys.ts` is untouched), no key is read or written from a new place, and no value is deleted. `OnboardingRepository` is not opened at all. What changes is *when* `setStep` is called relative to `router.replace`, which moves a write earlier and strictly reduces the window in which the stored step and the visible route disagree. The one new write pattern is a back action persisting a **lower** step (N3 → N1, N4 → N3); it overwrites one enum string with another in the same key and destroys no user data — the accounts it describes are in SQLite and are not touched by any path in this task. Trigger 3 (data-loss risk) likewise does not fire: the resolver only chooses a route, and the worst outcome of a wrong row is a user standing on a screen they can navigate away from.

Two items are called out above for the user at gate 3. Neither is a trigger; both are things a user notices. **N4's back destination is @tariq's call** where the mockup draws the affordance but names no target (Decision 4) — endorsed at step 5. **Back on N1 still exits the app** — @tariq flagged the opposite for the user to decide; it was ruled at step 5 in favour of keeping today's behaviour, so gate 3 sees a confirmation rather than a question.

---

## Plan review

### Round 1 — changes made

Verdict: **changes made** — approved for @dev with the edits below applied in place. The approach is right and is not rewritten: one pure resolver, one shared runner, persist-then-`replace`, a session counter per screen, two platform levers for the stack lock. One defect would have shipped a hard loop on the first-run screen; the rest are corrections to reasoning that reached the right answer for the wrong reason, plus two things the plan claimed were verifiable that were not.

Everything in the plan that cites a file and line was opened. Claims I checked and found **correct**, so nobody re-walks them: `welcome.hook.ts:14-18`/`:17`, `welcome/index.tsx:31`, `add_account.hook.ts:64-87`/`:81`/`:82`/`:84`/`:85`/`:89-90`, `add_account/index.tsx:57`/`:59-60`, `more_accounts.hook.ts:14-15`/`:17-21`/`:23-28`/`:31-34`, `more_accounts/index.tsx:74`, `ready.hook.ts:14`/`:18`/`:42-45`, `ready/index.tsx:35`/`:37-39`, `use_async.hook.ts:23`/`:39`, `src/app/index.tsx:7-12`/`:18`, `(onboarding)/_layout.tsx:9`/`:14-15`, `onboarding_header.tsx:33-52` (chevron gated on `title !== undefined && onBack`, `w-9` spacer otherwise, `hitSlop={8}`, `Size.iconBack`, `Radius.sm`, `Strings.goBackAccessibility` — no header change needed, only the prop), `onboarding_shell.geometry.ts:79-93`, `onboarding.repository.ts:28-30`/`:32-35`/`:37-40`/`:42-54`, `onboarding.store.ts:30`, `ready.store.ts:29-33`, `use_layout_init.hook.ts:38`/`:41`/`:45`/`:53-55`, `_layout.tsx:57`/`:68`/`:78-86`, `account.store.ts:47-61`/`:54`/`:59`/`:85`, `currency.state.ts:27-38`, `strings.ts:72-77`, `jest.config.js:63`, `locked-design-2026-07-23.md:307-312`/`:432`, `spec.md:81`/`:99`/`:488`/`:492`/`:550`, `MA-004.md:480`/`:583`, `react-native-screens/src/types.tsx:203-208` (`gestureEnabled` is `@platform ios`, exactly those lines), `app.json` `android.predictiveBackGestureEnabled: false`, and all nine mockup line citations (`979`, `1196`, `1253`, `1729`, `2004`, `2146`, `2189`, `2319`, `2731`). The two `grep` inventories reproduce: `backOrReplace` has exactly the two call sites named plus its own definition, its test and its coverage line; `useAppReadyStore` has no consumer outside `ready.store.ts` and `use_layout_init.hook.ts`; nothing in `src/` calls the account store's `reset()`. `resolveOnboardingStep`'s twelve rows match `locked-design-2026-07-23.md:440-443` and business rules 2, 3 and 4 exactly, including the eight pass-through rows.

#### Claims that were false, and the correction

1. **The runner resolves the destination before it persists, and N2's save must resolve after. Left as written, N2's CTA never leaves N2.** Decision 2 row 2 says "resolve `N3` against the **post-insert** count"; Decision 3's order says `resolve → await persist(...)`. `useAddAccount` reads `accounts` at render (`add_account.hook.ts:36`), so the count in `onSubmit`'s closure is `0` on the first-account path — `resolveOnboardingStep(N3, 0)` is row 4, **N2**. The literal implementation writes step N2 and replaces to `/(onboarding)/add_account`, with the account already saved and the form now failing its own uniqueness rule on every retry. This is the app's first-run screen and it is the one thing in the plan that would not have survived the emulator. **Corrected:** `accountCount` becomes `readAccountCount: () => number` reading `useAccountStore.getState().accounts.length`; `persist` receives a `resolve` thunk and returns the step it wrote; the runner navigates to what `persist` returns. N2's body is spelled out. A regression test that fails against the old order is added to step 2.

2. **The `BackHandler` ordering argument is backwards; the conclusion survives for a different reason.** "Ours is registered after expo-router's" is false by tree position — `useBackButton` is called from `fork/NavigationContainer.js:40`, an ancestor, and React flushes effects child-first, so on one commit the container registers last and runs first. What actually saves it is that `_layout.tsx:78` gates the root `<Stack>` on `status === 'ready'`, which `useAppInit` cannot reach on the first commit, so the onboarding layout mounts in a later commit. Recorded both facts, plus the one that makes it moot: at depth 1 expo-router's handler returns `false` and the loop falls through regardless of order — which is exactly what `handleAddAnother` → `replace` guarantees.

3. **Emulator scenario F could not execute.** Removing `isDisabled` on N2's back does not make the back reachable: it goes through the runner, `begin()` returns `null` while `busy`, and the in-flight save set `busy`. The tap would be inert and the run would be recorded as a pass having exercised nothing. Rewritten to three temporary edits including an explicit bypass of the busy gate, with the honest framing that in the shipped build this window has **no reachable user path** — the guard is defence-in-depth and the unit test is its real verification.

4. **Three of scenario C's four recipes, and scenario B, throw from the write needed to reach the screen under test.** Applying the `setStep` throw before walking to N3 strands you on N2. Added the ordering rule: navigate first, then edit.

5. **`spec.md:518` was wrong and is now fixed, not worked around.** There is no `statusTone`; there is a required `footnote` the spec omitted. Corrected in `spec.md` in place with a dated note, because MA-009, MA-011 and MA-012 all plan against that row. @tariq's reading of the shipped code is right in every particular: tone is derived inside `resolveStatusTrack` from a truthy `message`, and `''` deliberately preserves the footnote (`onboarding_shell.geometry.ts:79-93` and its comment at `:68-78`).

6. **A behaviour change was missing from the list that claims to be complete.** `handleAddAnother` → `replace` unmounts N3, so `initialCountRef` re-initialises on remount and every row satisfies `index < initialCount`; `rowEntering` is not gated by `useFirstMountEntering` the way the other three entrances are, so the whole list re-staggers on every return from add-more instead of only the new row animating. Added as behaviour change 8, handed to MA-011, with an emulator observation rather than a fix.

7. **Minor:** `BackHandler.android.js` — the dispatch loop is `:30-34`, `exitApp()` is `:36`; `:29-36` was close but the cited range starts on the `HardwareBackPressEvent` construction. `ready.store.ts`'s `resolveReady` is `:36`, not `:35`. `MA-004.md:389` hands over N1's busy CTA but not N3's — `:391` leaves the N3 CTA explicitly unchanged, so N3's busy label follows from this task's own Done-when, not from a hand-off. All three corrected in place.

#### Verdicts on the items flagged for independent judgement

- **The Done-when amendment is right, sufficient, and not an enlargement.** MA-004's own step-7 review escalated it to @sarah with two options (`MA-004.md:583`: "MA-005's plan must add it, or its Done-when must gain the line"); @tariq took the second, which is the one that makes the clause reviewable. It enlarges nothing: § *Three*'s body prose at `:39` already put back movement inside this task, `locked-design-2026-07-23.md:307-312` — an asset locked at gate 1 — already specifies N3's back destination and both N2 modes, and the header already renders the chevron on a bare prop. Gate 2 approved a task whose body contained this; the amendment only moves it where a reviewer checks.
- **N4 back → N3 is the right call.** Verified the target is genuinely unowned: `locked-design-2026-07-23.md` § *Screen 4: Ready* names no back action at all, and F0–F9 belong to MA-012, whose Done-when never mentions back. N3 is right because it is the symmetric inverse of N3 → N4, it skips no completed step, and it needs no new resolver row; back-to-N1 would jump two steps and is reachable anyway via N3's own chevron. Resolving `N3` against `accountCount` on the way costs nothing and covers the zero-account N4.
- **The empty-snapshot hazard is closed, and I could not find a path around it.** Walked it adversarially: `loadAccounts` publishes at `account.store.ts:54` before resolving and rethrows at `:59`, so a failed load rejects the `Promise.all` and never reaches ready; `begin()` preserves `error`, so `showStartupError` holds through the whole retry; `resolveReady` is the only writer of `status: 'ready'` and it runs strictly after the load; nothing in `src/` calls the account store's `reset()`; the post-mount `useInit(loadAccounts)`/`useFocusEffect` loaders this task leaves alone set `loadError` on failure but never clear `accounts`. The one thing @tariq called accidental is not: `__tests__/ready.store.test.ts:57-68` pins `begin()` preserving `error` by name. That is a better guarantee than a comment and it is now cited in Decision 1 — a future tidy-up goes red.
- **Both halves of the `gestureEnabled` claim hold**, and so does the `exitApp()` fall-through at depth 1. See correction 2 for the part that needed rewriting.
- **Back on N1: ruled, not deferred.** N1 stays exempt and keeps today's exit. Consuming back on N2/N3/N4 fixes a regression *this task creates* — `replace` turning "previous step" into "quit". N1 has no such regression: back exits there today, that is correct Android behaviour on a launch screen, business rule 2 makes it safe, and § *Three*'s premise is that a gesture cannot desynchronise the persisted step, which exiting does not. Consuming it would make the first screen a new user ever sees the only one they cannot leave. Implemented with `useSegments()` rather than a store read, because the add-more N2 deliberately holds a persisted step of N3 and route identity is what the decision is about. Still shown at gate 3, now as a confirmation.
- **`handleAddAnother` → `replace` is real and in scope.** Left as a push, N2's back — which this task requires to `replace` to more_accounts — turns `[more_accounts, add_account]` into `[more_accounts, more_accounts]`. That is not just untidy: depth 2 makes `canGoBack()` true, which is the one condition under which the `BackHandler` ordering above stops being moot. The fix is load-bearing for § *Three*, not scope creep.
- **Deleting `src/utils/onboarding_nav.ts` is correct and complete.** `locked-design-2026-07-23.md:432-433` reads "Explicit back actions persist their stated destination and use `router.replace`; they do not depend on an in-memory history stack" — the citation is exact. The whole inventory is the two call sites in this diff, the file, `__tests__/onboarding_nav.test.ts`, and `jest.config.js:63`; all four are in the plan. Coverage is not in the pre-push chain, and none of the new files falls under `collectCoverageFrom`, so the thresholds do not move.
- **MA-008's deferral is the right call but its consequence was under-stated**, and is now accurate in the non-goals table: the retry surfaces as a duplicate-name **field** error, not a status-track message, and the user is not stuck because back → N1 → Continue resolves forward to N3. The second half exists only because this task resolves forward destinations, which is worth saying out loud.
- **The resolver table is correct against rules 2, 3 and 4** including all eight pass-through rows, and the rule-1 ordering argument (`src/app/index.tsx:18` before the resolver) is right and worth keeping. Its forward application is not a favour to MA-010 — it is what stops MA-005's own new N3 back chevron from opening a dead end, and Decision 0 now says so.
- **The emulator plan is executable after corrections 3 and 4.** Every `$M` verb used exists in `.claude/skills/emulator-verify/mqa.sh`; `$M reset` is `pm clear`, which wipes SecureStore along with the database, so the SecureStore-only persisted step really does reset. The `Step N of 4` proxy works — `onboarding_progress_rail.tsx:60` renders it as visible `Typography` and the collapsed a11y node carries it too, so `$M ui` sees it either way. It is a proxy for **which route is mounted**, not for the persisted step, which is the correct thing to assert in every scenario that uses it. All temporary edits are now explicitly reverted, including F's new third one, with `git status`/`git diff --stat` evidence required in the report.

#### Deliberately left alone

- **N4's completion failure staying in this task.** MA-012's Done-when owns "matching F9", and on a strict reading this is three lines and a string that belong there. Kept: MA-005 already opens `ready.hook.ts` and `ready/index.tsx` for the back chevron, a live back button during the completion write is a race this task creates, and the plan already names MA-012 as the owner of the final wording. Not worth a round trip.
- **`onboarding_transition.state.ts` sitting at `screens/onboarding/` with no `index.tsx` beside it.** It is a factory, not a screen's state, and the alternative — putting a Zustand factory in `domain/` next to two pure functions — is worse. `.state.ts` is the right suffix by `.claude/rules/state.md`. Left as planned.
- **`welcome.hook.ts`'s `useState<Currency>`.** CLAUDE.md's screen anatomy says hooks hold no `useState`; this one pre-dates the task and moving it is MA-010's. The plan is right to keep it.
- **`n1CtaBusy`/`n3CtaBusy` duplicating the values of `n2CtaBusy`/`n4CtaBusy`.** One key per screen is the shipped pattern in that block and MA-010/MA-011 will re-point them at their own frames.
- **`animation: 'fade'` and `animationTypeForReplace: 'pop'`.** Checking on the emulator rather than changing on a guess is the right instinct; left untouched.

No critical trigger fires. Trigger 7 was re-checked specifically because this task moves a SecureStore write: no key is added, renamed, removed or read from a new place, no value is deleted, and the only new pattern is one enum string overwriting another in a key `setStep` already owns. Trigger 3 does not fire — the resolver picks a route and nothing else. Trigger 4 does not fire: `BackHandler` and `useSegments` are both already in the tree.
